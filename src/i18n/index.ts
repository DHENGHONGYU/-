/**
 * 国际化框架
 * v0.9.11 P2-DATA002
 * 当前阶段：建立框架骨架，UI_TEXT 复用 src/constants/uiText.ts
 * 下一阶段：支持 react-i18next
  * @doc []
*/
export { UI_TEXT } from '../constants/uiText'

// 未来扩展：
// export function t(key: string, params?: Record<string, string>): string { ... }
