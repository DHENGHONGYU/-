npx lint-staged

# ============================================================
# 踩坑规则门禁检查（v2.1 修复同步）
# 检查 4 条规则：正则动态构造 / 超时控制 / TTL+LRU 缓存 / 降级路径限制
# 详见：踩坑备忘录.md #11-#14
# ============================================================
python scripts/pitfall_check.py
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ 踩坑规则门禁检查未通过，已阻止提交"
  echo "   修复 ERROR 项后重新提交"
  echo "   或使用 git commit --no-verify 跳过检查（不推荐）"
  echo ""
  exit 1
fi
