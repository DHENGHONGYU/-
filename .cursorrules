# V9 智能投研复盘系统 Cursor 规则

## 系统提示词

在回答任何代码相关问题或生成代码前，请先阅读并遵循以下项目约束：

- `prompts/system-prompt-template.md`：项目分层、类型安全、颜色令牌、日志规范、质量门禁
- `prompts/component-prompt-template.md`：UI 组件与驾驶舱 Widget 生成规范
- `prompts/service-prompt-template.md`：Service 层与 DataBridge 使用规范
- `prompts/store-prompt-template.md`：Zustand Store 与 withBroadcast 规范
- `prompts/types-prompt-template.md`：类型定义零依赖原则

## 强制行为

1. 新增模块必须按 `types → store → service → component` 顺序生成。
2. 所有颜色必须引用 `src/constants/theme.tokens.ts` 中的令牌，禁止硬编码。
3. Service 层禁止直接访问 `db` 或 `dataLayer`，必须通过 `DataBridge.forward()`。
4. 生成代码后，必须假设会运行 `npm run audit:layers`、`npm run lint:colors`、`npx tsc --noEmit`。
5. 所有 `useEffect` 必须包含 cleanup 函数。
6. 公共函数必须包含 JSDoc。

## 输出习惯

1. 先生成文件清单，再输出代码。
2. 对复杂逻辑先给出伪代码说明。
3. 对不确定的业务语义主动提出澄清，而非假设。

