# Dependencies
node_modules/
.venv/

# Build artifacts
dist/
build/
release/
*.exe
*.msi
*.dmg
*.AppImage
*.snap
*.deb
*.rpm

# Electron
dist-electron/*.js.map

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Environment
.env
.env.local
.env.*.local

# Test artifacts
test_err.txt
test-output.txt
coverage/
*.lcov
test-results/

# Temporary
*.tmp
*.bak
*.bak-*
_tmp_*
.cache/
temp/

# External design review artifacts
finsight-v9-ui-review/

# Git backup / sandbox artifacts
.git.backup-*/

# HuggingFace model cache
.hf_cache/

# Test results & audit outputs
outputs/
e2e/test-results/
e2e/full-coverage/test-results-e2e/

# Reports (generated)
reports/
# 精准白名单：仅纳入 6 个治理产物，其余 docs/reports 内容保持忽略
!docs/reports/
docs/reports/**
!docs/reports/registry-doc-id-audit-2026-08-13.md
!docs/reports/registry-fill-checklist-V9-DOC-PROJ.md
!docs/reports/registry-fill-checklist-V9-DOC-QA.md
!docs/reports/registry-duplicate-docid-3-groups.md
!docs/reports/proj-195-vs-proj-260-diff.md
!docs/reports/pra-step1-step2-final-fix-report-2026-08-17.md

# Governance deliverables + analysis artifacts (explicitly tracked, do not ignore)
# These root directories hold governance reports, deep-diagnosis outputs and
# cross-project competitor comparisons. Keeping them here ensures reproducibility
# and prevents accidental future ignore-rules from excluding them.
!deliverables/
!timeline-competitor-compare/

# TSC / audit output files
tsc_*.txt
audit-out.txt

# Misc temp files
test_out*.txt

# Python data_service — 临时/调试文件
# _ 前缀下划线命名约定：正式代码另择其名
python/data_service/_chip_disasm.txt
python/data_service/_test_sw_*.py

# Secrets (LLM API Key from UI config page, never commit)
.secrets/
scripts/lib/ifindConfig.json

# Legacy token generation (removed 2026-08-15, prevent regeneration)
src/generated/

# Temporary artifacts (cleanup 2026-08-15) — 禁止提交的运行时/调试产物
dogfood-output/
test-screenshots/
test_screenshots/
test_output/
dispatch-round6.json
e2e-debug-homepage.png
finsight-v9-ui-review-delivery-v5.zip
hook-out*.txt
test_race_condition.py
test_screenshot_1.png
test_playwright.py
test_finsight.py
uc-error.txt
violation-code.txt
violation-details.txt

# Large binary tools (exceed GitHub 100MB limit)
tools/*.zip

# Audit temp outputs (generated during scan, not source)
tmp_audit_*.txt
test_result.json
test_screenshot_*.png

# P2 skill mirror — WorkBuddy 加载用副本（由 .agents/skills/ 拷贝，非源，避免重复提交）
.workbuddy/skills/

# Stray prototype — 单文件 HTML 原型（非构建物，勿入版本库）
stock-collector/

# User scratch / ad-hoc 临时产物（一次性调试脚本、快照、lint 输出文本）
# 避免 scope-guard rule-4 阻止合法工作提交；这些文件不在项目结构中。
_audit2.txt
cockpit_snap.txt
scripts/audit/lint-output.txt
scripts/audit/prod-warnings-detail.txt
# 2026-08-19 iteration-1 WIP 收尾：registry 清理临时脚本 / lint 扫描快照 / workbuddy 当日日记 / 矩阵快照
_cleanup_registry_22.cjs
_find_reg_entries.cjs
matrix_snap.txt
.workbuddy/memory/2026-08-19.md
.workbuddy/memory/MEMORY.md
scripts/audit/analyze-warnings.cjs
scripts/audit/fix-lint-warnings.cjs
scripts/audit/lint-check.txt
scripts/audit/lint-check2.txt
scripts/audit/lint-json-output.json
# dist 验证沙箱（一次性构建产物，非源码）
dist-verify/

# 2026-08-21 校对迭代本周产物：一次性分析归档 / tmp 调试脚本 / PWA E2E 运行快照 / 设计工作目录
# —— HTML/ZIP 分析快照（可重新生成，非源码）
.trae-html-share-packages/
ts-error-retrospective/
tsc-error-analysis/
# —— scripts 下的 tmp_ 前缀调试脚本/输出 + 临时覆盖率矩阵
scripts/audit/tmp_*.cjs
scripts/audit/tmp_*.json
scripts/calc-coverage.cjs
scripts/coverage_gap_matrix.py
# —— tests/e2e 运行时产物（截图、诊断输出文本）；保留 PWA 测试脚本（_pwa_*.py / pwa-cache-verify.py）
tests/e2e/artifacts/
# —— 一次性设计工作目录（.design 二进制工程文件，非源码交付物）
finsight-v9-ui-components-optimization/
# —— package-lock.json 同步提交；单文件交付报告保留在 deliverables/ 中

# 2026-08-21 文件整理契约：需要清理的文件/文档统一转移到 D:\转移文件清单V9
# （沙箱白名单已就绪，无需项目内暂存目录；任何整理均直接转移到契约终态目录）
