# 2026-08-17 工作日志

## RLES 回测标定引擎落地（06:10）
- 用户"继续"→ 选 P1-P2 收尾时的选项②（RLES 历史样本回测标定），而非①接真实市场宽度。理由：回测直接验证新因子预测力，且真实宽度源 V9 当前无（仅 mock）。
- 新增 `src/services/scoring/rles-engine/rlesBacktest.ts`：时点回测核心。`collectSamplesFromBars`(anchor 滑动切片算 secondWave+chip + 前向 fd 日收益)、`runBacktest`(注入)、`runLiveBacktest`(fetchKlineData live)、`aggregate`(byDetected/byStrengthQuartile/byTimingTier + Spearman IC)、`sortBarsAsc`(按日期归一化，消除后端正/倒序歧义)。
- **时点诚实性**：secondWave/chip 为 anchor 前切片真实因子；V6/板块/宽度为当前态，不纳入预测因子（避免未来函数）。完整时点回测需"历史 V6 服务"，列为生产化项。
- 新增 `rlesBacktest.test.ts`(6)：合成赢家股(强趋势+5倍放量→二波命中、前向正收益) vs 平淡股(横盘→不命中)，断言 detected 组 avgReturn > notDetected 组（验证预测方向正确）。
- 校验：tsc --noEmit 与 tsc:prod（RLES 相关）0 新增错误；vitest rles-engine 全量 18/18 passed。
- 交付：deliverables/rles-backtest-calibration.md（含标定方法论：IC 显著性 / 分位单调性 / tier 区分度 / 基准净增益）。
- 注意事项保留：本环境 npx vitest 会卡死交互安装，须用 ./node_modules/.bin/vitest。
- 后续生产化：真实宽度源 / 三条禁令黑名单源 / 历史 V6 服务 / 实盘权重标定。

## RLES 全链路状态（截至今日）
- 四维评分引擎 + 二波检测器 + 市场宽度 + 板块映射 + 八级筹码 + 三条禁令 + 回测标定 全部落地，代码层闭环。
- 仅剩数据生产化（真实源）与权重量化标定两项，属"运营增强"非"功能缺口"。

## 存储底层 P1 报告资产化 + DeduplicationService 接入（06:30）
- 用户"1、P1资产报告；2、数据采集调用，接入采集热路径"→ 推进两项（mandatory 门禁：v9-collection-pipeline-testing / v9-data-flow-integrity-audit / v9-module-sync-checklist 三件套）。
- **P1 报告资产化（v33，DB_VERSION 32→33）**：新增 `generated_reports`(keyPath reportId + by-symbol/by-generatedAt/by-template 索引) + `report_templates`(keyPath templateId + by-name 索引) 两个 first-class store。改动 9 处：dbConfig(ENVELOPE_ACTION×3 / STORE_NAME×2 / ACL 给 analyzer 加读写 / DB_VERSION+升级注释)、db-schema(ensureStore×2)、databridge(ACTION_TO_STORE_MAP×3)、databridgeHandlers(PutHandler×3)、dataLayerContentStores(GeneratedReport/ReportTemplate 接口 + generatedReportStore/reportTemplateStore 对象，写入归属 analyzer)、dataLayer(import×2+桶)、indexedDBProvider(import+桶)、validate-data-consistency(STORE_TO_TYPE_MAP×2)。
- **DeduplicationService 接入采集热路径**：dataSourceOrchestrator 行情(771)+K线(822)采集以 `dedupRecords` **进程内去重统计守卫**模式激活（不跳过写库——K线/行情为覆盖写语义，跳过会丢失 dataVersion 合并/历史 K 线更新）；修正 dailyQuotes keyGenerator 为 `symbol::latest.date`（原取扁平 `date` 字段，与真实 `DailyQuotes` 结构不符，原生成键恒为 `symbol::` 语义错误）。
- **门禁全绿**：tsc:prod 0 错误；audit:layers 0 违规；audit:acl-consistency 0/0；validate-data-consistency 75 通过/0 警告/0 错误；相关 vitest 271 通过；dataLayer.test.ts 桶计数断言 44→46 同步。
- **关键决策**：DeduplicationService 采用"统计守卫"而非"跳过写库"，因覆盖写语义下盲目跳过会丢更新；seenKeys 进程内内存、重启即清（已知限制，非阻断）。若未来要"真正跳过重复写库"，需 seenKeys 持久化 + 限定批量导入场景。
- 报告 deliverables/storage-bottom-layer-coverage-analysis.md §八 待办升级段已标记 P1 / DeduplicationService 已完成；缺口矩阵"已注册未接线"同步标注闭环。

## RLES 运营增强收尾（最后一公里，06:22）
- 补齐 RLES 最后两块运营增强：①市场宽度/三条禁令「真实源可切换骨架」②权重标定实证（因子有效性验证）。
- breadthFactor.ts：新增 fetchMarketBreadthLive（生产化扩展点，返null）+ fetchMarketBreadthResilient（优先live回退mock）+ RLES_USE_LIVE_BREADTH 开关；保留 mock 兜底。
- hardRiskDetector.ts：新增 isStByName（名称*ST/ST弱判断）+ detectHardRisksByName + fetchStockRiskFlagsLive（扩展点）+ detectHardRisksResilient 组合入口；FORBIDDEN_STOCKS 空占位保留。
- calibration/sampleBasket.ts：buildSampleBasket() 15只多形态确定性合成标的（6赢家/4横盘/3下跌/2ST），不依赖联网，刻意拉开区分度。
- calibration/rlesCalibration.ts：runCalibration()+buildCalibrationReport()+formatCalibrationReport()；输出 detectedEdge/tierEdge/IC/verdict/weightSuggestion。
- 真实标定结果：IC 0.874 / 二波增量+29.4%收益+68.3%胜率 / priority-cautious +29.7% / 四分位单调+25.4% → verdict STRONG。证明二波+筹码因子方向正确、具备强前向预测力。
- 测试：vitest rles-engine 全量 28/28 通过（calibration 11 + sampleBasket 4 + 既有13）；tsc --noEmit 与 tsc:prod 均 0 新增错误。
- 诚实性：样本为合成代理（刻意拉区分度），IC 高于实盘；正式标定需 runLiveBacktest（后端8000在线）替换样本篮。
- 交付：deliverables/rles-operational-enhancement.md。RLES 已从理论权重推进到因子方向被验证，仅差真实行情复核。
- 剩余生产化：①后端增 /api/collect/breadth ②增 /api/collect/risk ③live标定+历史V6时点重算。

## RLES 生产化接线（真实数据源落地，07:1x）
- 把 RLES 两个"生产化扩展点"从 mock/占位切换为真实后端源 + 补齐后端端点。
- **后端 `python/data_service/collect_endpoints.py`**：新增 `POST /api/collect/breadth`（东财 push2 qt/clist 全市场涨跌家数/涨跌停，60s 缓存 `_BREADTH_CACHE`，失败降级 success=False）+ `POST /api/collect/risk`（BACKEND_FORBIDDEN_STOCKS 空占位 + 腾讯名称推导 ST/退市）。模型：BreadthData / RiskCollectRequest / RiskCollectData。
- **前端**：`apiPaths.ts` 加 `API_COLLECT_BREADTH`/`API_COLLECT_RISK`；`breadthFactor.fetchMarketBreadthLive` 与 `hardRiskDetector.fetchStockRiskFlagsLive` 由 TODO 桩改为真实 `fetch`；`types.score.ts` 给 V6Score 补 `name?: string`；`reviewLaunchStore` 改接 `fetchMarketBreadthResilient` + `detectHardRisksResilient(target, v6Score?.name)`。启用宽度真实源：`.env` 置 `RLES_USE_LIVE_BREADTH=true`。
- **测试**：后端 `test_collect_endpoints.py` 加 TestCollectBreadth(3)+TestCollectRisk(4) 共 7/7（mock 网络）；前端 breadthFactor/hardRiskDetector 各加 4 个 fetch mock 用例，RLES 引擎套件 37/37。
- **门禁全绿**：py_compile OK / tsc --noEmit(--incremental false) 0 / tsc:prod 0 / vitest 37/37 / audit:layers 0 违规 / audit:acl-consistency 0-0 / 后端 pytest 7-7。
- **可复用教训（pytest 测 FastAPI + 本地 import 的 requests）**：① `monkeypatch.setattr("requests.get", fake)` 用字符串形式最稳；collect_endpoints 的 requests 是函数内 `import requests`，模块级无 `requests` 属性，但补丁仍作用于 sys.modules['requests']（已用独立脚本验证 patch 生效）。② 带模块级缓存的函数（如 `_BREADTH_CACHE`）跨用例会命中，测试须先 `collect_endpoints._BREADTH_CACHE = {"data":None,"ts":0.0}` 冷置缓存，否则后续用例命中缓存、不走网络。③ 受管 Python 缺 fastapi/pytest，已建 venv `C:/Users/DELL/.workbuddy/binaries/python/envs/default` 装 fastapi/pytest/requests/httpx 跑通。
- **仍可复用教训（tsc 缓存伪影）**：`tsc --noEmit` 偶发报 `researchPipelineOrchestrator.ts`/`disciplineStore.ts` 等未改文件的"假错误"，清缓存 `tsc --noEmit --incremental false` 即 0 错误；两文件分别为并发 Agent 未提交(`??`)/已改(`M`)产物，非本任务引入，勿擅改他人未提交代码。
- 交付：deliverables/rles-production-wiring.md。剩余：runLiveBacktest 实盘复核（函数已就绪，需后端在线+网络，沙箱未实跑）/ 历史 V6 时点重算（需历史 V6 服务，较大专项）/ BACKEND_FORBIDDEN_STOCKS 权威同步。

## 存储底层兜底·剩余缺口闭环（06:46 续推全面项目）
- **P0 筛选结果集持久化（v34）**：新增 `screening_results` first-class store（keyPath runId + by-symbol/by-createdAt 索引）；新增独立 `screening` 模块 ACL（read stocks/v6Scores/screeningResults，write screeningResults）；`multiFactorScreeningStore.runScreening` 成功后持久化结果集 + 新增 `loadSavedRuns` 历史回溯。改动 11 处（dbConfig/db-schema/databridge/databridgeHandlers/dataLayerContentStores/dataLayer/indexedDBProvider/multiFactorScreeningStore/validate-data-consistency/validate-data-blueprint/dataLayer.test.ts）。阶段③唯一未兜底短板现已闭环。
- **P1 复盘评分真实化（v34）**：`RealTradeReviewScoreCalculator.calculateDisciplineScore()` 由占位 throw 改为基于真实订单（useOrderStore 同步快照）的透明纪律分（胜率50%+执行完成度25%+仓位纪律25%）。**架构约束修复**：原直接 import useOrderStore 触发 audit:layers 1 处 services→store 跨层违规，改为注入式数据源 `setOrderDataSource`（由 disciplineStore 注入 `() => useOrderStore.getState().orders`），audit:layers 回归 0 违规。
- **门禁全绿**：tsc:prod 0 错误；audit:layers 0 违规；audit:acl-consistency 0/0；validate-data-consistency 76 通过/0警告/0错误；validate-data-blueprint ✅（53 stores）；相关 vitest 237 通过；dataLayer.test.ts 桶计数 46→47 同步。
- **文档计数漂移**：`validate-data-blueprint` 硬编码 50→53（实测 STORE_NAME=53，含 v33 报告资产化 2 项 + v34 筛选结果集 1 项 + 并发新增项），已对齐权威值。活动文档计数漂移（24→50）经核查仅历史存档与历史路线图，活动文档已写 50+，闭环。
- **五段链路存储兜底现已全覆盖**（采集/分析/筛选/复盘/报告均有 first-class 落库），早期三类隐性风险（已注册未接线/已设计未激活/文档漂移）全部闭环。报告 §八 待办与缺口矩阵已标记完成。
- 仍非存储阻断的残留：④ `holdings` 存储未实现（disciplineStore 已用 useOrderStore 聚合，非 store 缺失阻断）；用户选择态（conditionGroups）仍内存态（交互态，可接受）。

## 统一全链路编排器 · 策略三分类落地闭环（07:3x）
- 用户选择"统一全链路编排器"实现 spec（数据传递+数据跟踪）：采集→存放→V6评分→评分过低校对/回溯→策略三分类(核心/价值洼地/热门)→选股入研究池+纳入长期观察池→整合报告。
- 核心交付 `src/services/orchestration/researchPipelineOrchestrator.ts`（依赖注入；复用 runV6Score/runStrategy/transitionPoolItem + 注入真实采集器）。此前测试 20/20 全绿但策略三分类 0/0/0（全部 excluded）。
- **定位并修复 3 个真实缺陷**：
  1. **评分适配器估值分恒 null（生产级 bug）**：`scoringAdapter.ts` 读 `v6Score.factors['估值']`，但 V6 分层 ID 实为 `l3v`/`lMinus1`…，该键永不产生 → `valuationScore` 恒 null → 价值洼地分类在生产永远无法触发。改为 `factors['l3v'] ?? factors['lMinus1'] ?? 命中"估值"标签的分层`。
  2. **编排器 Stage6 重复计数**：观察池循环拼接 coreScarce+valueBargain+hotMomentum+rejected，而 R2 重分类/未进 TopN 的标的同时在分类数组与 rejected 中出现 → 研究+观察池合计 25(应20)。改为以 `input` 全集为基准按 symbol 去重（selected 进研究池，其余进观察池）。
  3. **0-5 尺度 bug**：`deriveBacktrackPlan` 用 `ls<50`（分层实为0-5）→ 回溯方案永不生成；`scoreCalibrationThreshold` 默认 55（应为0-5）。改为 `ls<2.5`、默认 3.0。
- 编排器新增注入点：`theme`/`ruleConfig`/`momentumMap`/`sectorResolver`（Stage2 updateStock 回填 sector/industryCode，UpdateStockHandler 全量合并故可持久化）。
- **测试确定性演示三分类**：`tests/research-pipeline.integration.test.ts` 注入宽松 theme(minCompositeScore=2.0)+ruleConfig；seed rotationScores(热门板块) 供 isHotSector；referenceValue 对 hot 候选给高PE(估值分低→不落价值洼地)、其余低PE(估值分高→价值洼地)；sectorResolver 给 hot 候选挂热门行业代码；momentumMap 注入动量。真实腾讯行情结果：采集/存放/评分 20/20，calibrationTriggered 20，coreScarce 3 / valueBargain 13 / hotMomentum 2，selected 13 + observation 7 = 20，referentialIntegrity true。
- **门禁全绿**：vitest 1/1 pass；tsc(--noEmit + test) 对改动文件 0 错误；audit:layers 0 违规（仅 1 处过渡期 db 直连 warning，编排器原已存在）。
- 交付物：`outputs/bulk-import-sample/integrated-pipeline-report.json` + `.html`（已随测试重生成）。
- 注意：spec 缺口②（观察池定期自动复盘调度）仍未实现，仅"纳入观察池"已落地；`hot-momentum` 在生产依赖实时 rotationScores（测试已 seed 演示）。

## 观察池定期自动复盘调度（缺口②闭环，08:2x）
- 用户"继续推进" → 选上一条目明确 flag 的 spec 缺口②（观察池沦为死列表），实现 `src/services/orchestration/observationPoolReviewer.ts`：`ObservationPoolReviewer` 类 + `run(deps)`。注入 `getWatchlist`(默认 `getWatchlistStocks`→`status=watching`) + `PipelineScorer`(默认 `runV6Score`)；计算评分漂移(scoreDelta)、`meetsResearchThreshold`(V6 0-5 尺度,默认 3.0)、`promotionEligible`(达门槛且 isInResearchPool=false)、`recommendation`(promote/hold/watch)；结果经 `eventBus` 广播 `OBSERVATION_REVIEW_COMPLETED`。
- 生产接线：`store-channels.constants.ts` 加事件；`orchestration/index.ts` 导出并在 `buildOrchestratorList`/`stopOrchestration` 注册（默认 `enabled:false`，已注入真实 deps），消除孤儿引擎（对齐 WeeklyReviewScheduler 启动清单治理）。
- 测试 `observationPoolReviewer.test.ts`(4)：晋升候选/首次无快照、二次漂移+广播、已入研究池排除、定时调度触发，全绿。
- **门禁**：`audit:layers`/`audit:acl-consistency`/`validate:dataConsistency`/`validate:blueprint` 均 0；`tsc:prod` **本任务代码零类型错误**（`index.ts`/`observationPoolReviewer.ts` 不在错误清单）。
- **重要边界（复用教训）**：`tsc:prod` 当前整体红线源于**并发 Agent 在途修改的未提交文件**（`shared.config.ts` 颜色令牌 SoT、`ConfigApp.tsx`/`OutputApp.tsx` 的 `LogContext` 日志签名等，多次运行错误集浮动 15→10），与本次无关，按约定未擅改他人未提交代码。验证时用 `tsc -p tsconfig.prod.json --noEmit --incremental false` 排除缓存伪影后，仅剩上述他人产物错误。
- 交付：`deliverables/observation-pool-reviewer.md`。观察池快照仍为内存态（重启即清，与 WeeklyReviewScheduler 一致），跨重启持久化列为后续增强（可落新 store 或复用 screening_results）。

## 缺口② 端到端真实数据演示 + 接线语义修正（08:2x 续推）
- **修正生产接线语义错配**：上条 `index.ts` 接线误用 `getWatchlistStocks()`（读 RESEARCH_STATUS.watching 研究池观察），而 spec「长期观察池」= 管线 Stage6 落地的 `intention.watchlist`。新增 `tradingService.getIntentionWatchlistStocks()`（by-status 索引查 INTENTION_STATUS.watchlist，MODULE_ID.trading 走 ACL），`index.ts` 接线改读它。理清两概念：研究池观察(research.watching) vs 长期观察池(intention.watchlist)。
- **端到端集成测试** `tests/observation-pool-review.integration.test.ts`（复用 20只真实腾讯 harness）：管线落地 研究池13+观察池7 → Observer 读 intention.watchlist 得 7 → 真实 V6 复盘 → 首次检出 7 晋升候选（composite~2.5 > 演示阈值2.2）→ 二次复盘（模拟 +0.3）漂移计算正确、7只全"评分上升"、晋升候选不减。产出 `observation-review-report.json/.html`。
- 顺手清理既有未用导入 `afterEach`（observationPoolReviewer.test.ts line7，TS6133）。
- **门禁全绿**：vitest 集成1/1 + 单测4/4；tsc(--noEmit + test) 对改动文件 0 错误；audit:layers 0 违规（1 过渡期 warning）。
- 交付物：`outputs/bulk-import-sample/observation-review-report.{json,html}`。缺口② 现已从"模块+单测"补全到"真实数据端到端演示+报告输出"（spec「所有相关内容输出」）。

## 复盘评分逻辑落地（占位清空，08:2x）
- 用户指向 `tradeReviewScoring.ts:39` 的 Mock 占位 → 核查发现 RealTradeReviewScoreCalculator(v34) 早已实现且 disciplineStore:404 已注入订单数据源，但 **从未被设为激活计算器**：`getTradeReviewScoreCalculator()` 默认仍返回 `MockTradeReviewScoreCalculator`（随机50-100），`setTradeReviewScoreCalculator` 在 src 内零调用 → 唯一消费方 `mockDataCollection.ts:553` 始终拿 Mock（空壳未真正落地）。
- **落地改动**：① `getTradeReviewScoreCalculator()` 默认改为 `new RealTradeReviewScoreCalculator()`（真实订单驱动纪律分成为全局默认）；② `resetTradeReviewScoreCalculator()` 同步改为 Real（保持默认一致）；③ `disciplineStore.ts:404-406` 显式 `setTradeReviewScoreCalculator(new RealTradeReviewScoreCalculator())` 激活 + 注释追溯（与既有 setOrderDataSource 注入并列）；④ Mock 类注释改为「仅测试/特殊场景显式注入」。
- **测试** `src/services/trading/tradeReviewScoring.test.ts`(5)：无订单→0、确定性算分(单标的买100卖120→98)、[0,100]边界、默认返回 Real 实例、Mock 仍可显式注入。全绿。
- **门禁全绿**：tsc:prod(--incremental false) 0 错误（本任务两文件不在错误清单，且并发伪影已清）；`audit:layers`/`audit:acl-consistency`/`validate:dataConsistency`/`validate:blueprint` 均 0；测试 5/5。
- **效果**：`mockDataCollection.ts:553` 生成 disciplineScore 时现走真实评分（无订单→0 而非随机假分），复盘评分从「占位/Mock」真正落地为真实订单驱动。
- 交付文档更新：deliverables/storage-bottom-layer-coverage-analysis.md ④行「🟡→🟢 已落地」并注明默认激活与 disciplineStore 激活点。
