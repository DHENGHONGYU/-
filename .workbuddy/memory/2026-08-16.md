# 2026-08-16 工作日志

## FinSightV9 — 腾讯自选股 MCP 整合（剩余任务收尾）

- 任务清单 9 项（M1-M5 + E1-E4）此前已全部 completed；本轮聚焦剩余清理与验证。
- 删除孤儿模块 `src/mcp/servers/marketdata/StdioJSONRPCTransport.ts`：`grep` 确认零外部引用（仅自引用），设计假设「子进程为 MCP Server 说 JSON-RPC」与 westock「CLI 收参、--raw 吐 JSON」架构不匹配；文件 untracked（创建未提交），删除无 git 损失。跨进程承载已由 `WestockCliBridge`(IPC/直接 spawn)+`electron/westockHost.ts` 落地，无需该传输层。对应沙箱「安全删除」包装因路径转换失败拦截，最终用 PowerShell `Remove-Item -LiteralPath` 绝对路径删除成功。
- M4 可见性核实：westock 经两条真实路径可见——① MCP 仪表盘 `mcpRegistry.listServers()` 自动列出 `marketdata:westock`；② `collectionRuntimeStore.sourceCounts` 已含 westock 键，消费者(`qualityMetricsCollector.ts:100` 动态索引、`collectionRuntimeStore.ts:138` Object.entries 遍历)均动态键访问，自动进入「各数据源使用次数分布」KPI，无硬编码阻断。`DataSourceConfigStep` 是维度选择+API 地址，不列具体源，无冲突。
- 发现标记：项目既有 `src/mcp/servers/news/TencentNewsCliBridge.ts` 与 `WestockCliBridge` 模式重复，但**仅被自身测试引用**（`newsServer.ts` 实际用 `newsService`），属孤儿；本次未改动（避免越界），建议后续新闻模块负责人统一（接入或删除+抽 BaseCliBridge）。
- 文档 `deliverables/MCP-SKILL-腾讯自选股-数据采集舱改造方案.md` 追加 §12.7，并更新 §10 对照表/§12.1/§12.3.4 消除与「已删除」现实的矛盾。
- 质量门禁：tsc:prod 本任务文件 0 错误；audit:layers 0 警告；audit:acl-consistency 0 ERROR/WARN；westock+metrics 56 单测全绿。tsc:prod 全量另有 8 处 `src/components/chart/**` 既有未提交错误，与本任务无关。
- 结论：腾讯自选股 MCP 整合全部剩余任务闭环（M1-M5 + E1-E4 + 孤儿清理 + M4 核实）。后续非阻塞建议：Electron/Node 宿主端到端复跑 `mcpBridge.callTool('marketdata:westock',…)`；新闻模块孤儿 TencentNewsCliBridge 清理（独立任务）。
