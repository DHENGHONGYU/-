# 2026-07-11 工作日志

## WorkBuddy 社区发文包（腾讯云开发者社区积分任务）
- 为腾讯云开发者社区撰写 WorkBuddy 相关发文，目标积分 3500（教程篇 3000 + 心得篇 500）。
- 主题方向：审计合伙人视角的 AI 工作流改造（贴合用户身份，差异化强于开发者视角）。
- 产出位置：`articles/` 目录
  - `01-tutorial-audit-workflow.md`：完整实操教程，约 2000 字，配 4 图（架构图/记忆层/专家配置/时间对比）。标题含 #WorkBuddy。
  - `02-experience-five-pitfalls.md`：使用心得/踩坑记录，约 1100 字，配 2 图（踩坑概览/决策流）。标题含 #WorkBuddy。
  - `images/`：6 张 SVG 示意图（fig1-fig6），采用 stone 暖灰系 + emerald 强调，亮色主题。
  - `index.html`：汇总预览页，渲染全部配图 + 发文过审清单 + 配图转 PNG 提示。
- 发文策略建议：先发教程篇冲精选 3000，3 天后发心得篇补 500；SVG 配图需在浏览器另存为 PNG 后上传。
- 注意：文章内容基于 WorkBuddy 真实能力（记忆层/Skills/Expert/MCP/自动化/Craft-Plan-Ask），未编造功能；审计场景数据为示意性说明。
- 续：新增 `articles/convert.html`（SVG→PNG 批量转换工具，本地 http.server 端口 8848 托管）+ `articles/publish-ready.md`（发文就绪卡：标题/摘要/标签/字数核验/配图上传对照/发布节奏/质检清单）。质检修正第二篇坑四时区段落逻辑矛盾。

## 原子组件体系阶段 3 — 业务目录有机体化（治理优先 + 试点先行）

### 步骤 0：构建 audit:atomic 层级边界审计脚本 ✅
- 新建 `scripts/audit-atomic.ts`，按 `_audit-pipeline` 契约（stdout=JSON / stderr=人类可读 / 退出码 0·1·2）。
- 校验内容：atom 不引 store/service/molecule/organism/template/page/app；molecule 不引 organism/template/store/service；template 不引 organism/store/service；ui/ shim 为纯 re-export；登记未注册业务组件。
- 注册 npm script `audit:atomic`（package.json 第 40 行）。
- 修复行注释正则语法错误（`//\/\/.*$/gm` 被源码注释截断 → `/\/\/.*$/gm`）。
- 修复注册表匹配逻辑：同时按 `sourcePath` 和 `targetPath` 匹配，已迁移组件不再误报 unregistered。
- 翻转 36 条已迁移条目的 registry status `migrating` → `active`（`fix-registry-status.cjs` 脚本）。
- 基线：0 阻断性违规、194 warning（168 stale-ui-import + 26 unregistered）。

### 步骤 1：input/ 试点物理迁移到 organisms/input/ ✅
- `src/components/input/` 18 个 .tsx（含 `wizard-steps/` 子目录）→ `src/components/organisms/input/`。
- 原 `input/X.tsx` 改写为纯 re-export shim，8 个消费者引用（pages/apps/pool）零改动。
- 翻转注册表对应条目 status → active。
- 迁移后结构：organisms/input/ 18 真实文件，input/ 14 顶层 shim + wizard-steps/ 子目录 shim。

### 收尾验证（全部门禁通过）
- `tsc:prod` ✅ 0 错误（resilienceChain.ts 预存错误已被用户在编辑器修正）。
- `build` ✅ exit 0（31.64s）。
- `audit:layers` ✅ 0 违规。
- `audit:atomic` ✅ 0 阻断违规；203 warning（173 stale-ui-import + 30 unregistered，过渡期预期）。
- `audit:docs` ✅ 0 违规。
- `audit:routes` ✅ 64 路由覆盖，exit 0。
- `audit:tokens` ✅ 0 硬编码。
- `lint:colors` ✅ exit 0。
- `audit:hardcode` ⚠️ 29 warning（基线债务，exit 0）。

### 后续待执行
- 步骤 2：低风险域逐域迁移（trading/output/news/strategy/agent/localDoc/system 共 ~22 文件）。
- 步骤 3：analysis/ 中风险域迁移（13 文件，含嵌套子目录）。
- chart/ 与 cockpit/cabin/widgets 仅 registry 标注不物理搬（Widget 注册表耦合）。

## 原子组件体系阶段 3 — 步骤 2+3：业务域全量物理迁移 ✅

### 步骤 2：低风险域逐域迁移 ✅
- 通用迁移脚本 `migrate-domains.cjs`（多域 + 子目录 + default 导出检测 + 测试文件无 shim）。
- 7 个域共 25 个文件（含 3 个 .ts）迁入 `organisms/` 对应子目录：
  - trading(3) / output(4 含 reviewArtifact.ts) / news(2) / strategy(2) / agent(2) / localDoc(1) / system(11 含 migration/ 子目录 5 文件)。
- shim 检测 default 导出：有则双行（`export *` + `export { default }`），无则单行。
- 翻转注册表 status → active；补登 9 个未登记条目（AgentTaskList/LogStreamPanel/SystemArchitectureDiagram/migration 子目录/reviewArtifact.ts）。

### 步骤 3：analysis/ 中风险域迁移 ✅
- 13 个文件（含 6 个嵌套子目录 hub/news/score/screening/sector/signal + 4 测试文件）迁入 `organisms/analysis/`，子目录结构保留。
- 子目录感知 shim 已生成。

### 收尾验证（全门禁通过）
- `tsc:prod` ✅ 0 错误（受管 Node 22 直跑，npm wrapper 报 "Could not determine Node.js install directory" 是环境问题非代码问题）。
- `build` ✅ exit 0（28.96s）。
- `audit:layers` ✅ 0 违规。
- `audit:atomic` ✅ 0 阻断违规；194 warning（173 stale-ui-import + 21 unregistered 含测试文件，过渡期预期）。
- `audit:docs` ✅ 0 违规。
- `audit:routes` ✅ exit 0。
- `audit:tokens` ✅ 0 硬编码。
- `lint:colors` ✅ exit 0。
- `audit:hardcode` ⚠️ 29 warning（基线债务，exit 0）。

### 阶段 3 总结
步骤 0–3 全部完成。11 个业务域共 63 个文件已物理迁移到 `organisms/`，原位置全部保留纯 re-export shim，全量消费者引用零改动、零类型错误、零阻断违规。chart/ 与 cockpit/cabin/widgets 按决策仅 registry 标注不物理搬。
