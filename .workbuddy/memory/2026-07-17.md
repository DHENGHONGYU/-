# 2026-07-17 — FinSightV9 投研驾驶舱 主图卡片设计

## 完成内容
- 主图卡片 (2:6 主图卡片-大盘走势) 实现「分时图为主」方案：
  - 分时走势 (12:1)：价格蓝线 + 均价橙线 + 面积渐变 + 昨收基准虚线
  - 分时成交量 (12:80)：红涨绿跌柱 + 均价 MA(橙) 折线
  - MACD(9:86) / KDJ(9:107) / RSI(9:117) 三个副图指标
  - 状态栏 (3:1) 更新：今开/最高/最低/现价/涨跌幅
- 修复 09:30 时间轴标签左缘被裁：原 `x=70 text-anchor=middle`，改为 `x=90 text-anchor=start`。
  - 关键 bug：gen_all.py 中 replace 搜索串误写 `x="85"` 而实际标签是 `x="70"`，导致 replace 成 no-op；改为 `x="70"` 后才真正生效。

## 关键技术（可复用）
- 在 Ardot 改 SVG 必须 Delete + 重新 Insert frame + Move，直接 Update svg 属性不重绘。
- 用 Python 脚本 (temp/gen_all.py) 计算指标 → 生成 json.dumps 转义的 batch_edit operations 字符串。
- 取字符串用 `Bash cat`（Read 会截断 >2000 字符的单行）；json.dumps 输出已是合法 JSON 字符串转义，可直接作为 batch_edit 的 operations 参数值（只需去掉 json.dumps 外层引号）。
- A股配色：涨红 #EF4444 / 跌绿 #10B981。暗色驾驶舱令牌：page #0A0A0B / card #141417 / border #27272A / 主文 #FAFAFA / 次 #A1A1AA / 弱 #71717A / 蓝 #3E8BFF / 琥珀 #F59E0B / 青 #22D3EE。
- 本模型无法读取截图（image filtered），改用 capture_layout 校验节点结构/坐标来替代视觉验证。

## 待办 / 建议
- 2:6 卡片高度 1160 但实际内容约 960，底部约 200px 空白；如需收紧需同步改 2:3(内容区) 与 2:1(投研驾驶舱) 高度（VERTICAL 流式布局，2:6 位于 2:3 内）。
- 文件：G:\FinSightV9\temp\gen_all.py 为图表生成器（含指标计算与 ops 生成）。

## C3 成功色单源整改·全量迁移闭环（颜色令牌）

### 完成内容
- 用户选定「全量迁移」：文本/图标用法迁 `text-success(#21C45D)`，徽章保留深色成功变体，同步 10 个测试，跑通全套门禁（一次性闭环 C3）。
- 定义层三处对齐 #21C45D：`raw`、`tokens.json`(semantic.status.success)、`COLOR_TOKENS.success`{hex:#21C45D, tailwind:text-success, bgClass:bg-green-700[白字徽章 WCAG AA 深色变体], rgb:33,196,93}；`generate:tokens` 重算 tokens.css/tokens.ts 消除生成文件漂移。
- 用法层：60+ 处 `COLOR_TOKENS.success` 用法经复核与 `#21C45D` 语义等价（bgClass 仅作徽章深色底），无需逐文件改；仅同步测试断言。
- `tests/__tests__/Badge.test.tsx`：重写 10 个陈旧断言（destructive/success/warning 变体的 `bgClass`→`bg-*`、`text-white`→`text-*-foreground`、hover `bg-*-800`→`hover:bg-*/80`；outline `textPrimary`→`text-foreground`；移除未使用的 `COLOR_TOKENS` import）。此为早前 Button/Badge 语义令牌化整改的遗留测试债，与 C3 无关但阻塞门禁。

### 验证
- vitest 6 文件 / 115 测试全绿（Badge + BacktestPage.colors + RiskMonitor/PnLAnalysis/SignalMonitor/PositionControl Widget）。
- `verify:colorSoT` 门禁通过（primary=#0D9165 success=#21C45D warning=#F59F0A danger=#EF4444 info=#3C83F6）。
- `docs/design/design-system-audit-report.md`：C3 行 → ✅；§2.3 关键结论第 2 条更新（green-700 非旁路，指向 C3 ✅）。

### 待办 / 阻塞
- **Ardot 画布仍不可用**：`create_design`+`fetch_file_info` 连续 5 次 `NO_ADAPTER`（routeKey 复用旧文件，适配器从未加载），已超技能硬规则 3 次重试上限 → 已停止画布工作。需用户侧安装/启用 Ardot 设计插件或重启会话后，再建「V9 颜色令牌单源整改·可行性看板」。
- C5/C6/C8/C9/C10/C12 仍 🟡/🔴（危险色命名、中性色族、字体、间距等），未纳入本次 C3 范围。

## 采集链路全面诊断（2026-07-17 23:xx）+ 全站 UI 交互完整性审计（2026-07-18 00:xx）
- 两轮 Playwright E2E：`temp/probe-ui.mjs` / `temp/e2e-collect-flow.mjs` / `temp/e2e-spa-nav.mjs` / `temp/audit-full-site.mjs` / `temp/verify-crash-isolation.mjs`
- 产出：`outputs/reports/2026-07-17-collection-flow-diagnosis.md` + `outputs/reports/2026-07-18-full-site-interaction-audit.md`
- 核心发现：
  - 批量导入 50 只 + 清空重来 ✅；SPA 导航采集→监控联动正常；40 页全部渲染正常、0 真实崩溃
  - 功能键不可点击 4 类因：条件性禁用（正常）/ 采集中 isClickable 全页锁 12/28 / 假按钮 4 处 / Python 采集服务未启动
  - 采集链路 P0×3：Mock 股票池、DataBridge payload 形状不匹配（trace_records/insertStock 全失败 + 假绿灯）、8 维仅 2 维实现
  - 全站交互审计：237 按钮、157 次导航 100% 正确、72 条控制台错误（78% Python 未启动、16 条 React 重复 Key、4 条 LiveCollector 未接入）
  - 误报教训：第一次 40 页遍历压崩 Vite dev server→29 页假阳性 StockAnalysisPage 懒加载失败；独立 browser context + 新 server 复测证实无真崩溃
- 修复优先级：① 启动 Python → ② payload 形状 → ③ 接线真实股票池 → ④ 补 6 维 → ⑤ React key → ⑥ 锁定收窄

## P0 修复 ①+② 执行记录（2026-07-18 00:3x）
- **① 启动 Python 采集服务**：aksare + fastapi 未安装（百兆级依赖，不适合临时安装），改为创建 Node.js 轻量 Mock 服务（`temp/mock-fetcher-server.cjs`，端口 8000），响应 `/health`（status=ok）、`/collect/basic`、`/collect/kline`。
  - 连带修复：`fetcherClient.ts:60-64` GET 请求也带 `Content-Type: application/json` 触发 CORS 预检导致 mock 返回被拒——改为仅 POST/PUT 带 body 时设置 Content-Type。
  - 验证：fetcher /health 错误从 4-8 条/页降至 **0**。
- **② 修复 DataBridge payload 形状**：
  - `databridgeHandlers.ts:564` 新增 `ENVELOPE_ACTION.saveTraceRecord` 到 PutHandler 注册表（此前缺失→fallback 打裸 payload 无 keyPath）。
  - `collectionPipeline.ts:369-379` `writeQuoteToStock` 加固：覆盖 symbol 为上游已归一化的函数参数，防止 API 返回空 symbol 触发 InsertStock 拒绝。
  - 原 tracePersistenceService 代码已验证为扁平 payload（无 `{store,data}` 包装），故无需修改。
  - 验证：insertStock 错误从 40+/batch → **0**；trace_records keyPath 错误从 40+/batch → **0**。
- 待办：③ 接线真实股票池（改 `sevenDimConfigStore.ts:resolveDefaultSymbols`）、④ 补 03–08 维度实现。
