# 2026-07-14 工作日志

## 25 只股票全流程 E2E 校对测试（三舱+总舱）

- 完成了端到端校对测试的「指标方案 → 测试运行 → 按舱分析报告」闭环。
- 产物：
  - `docs/reports/e2e-verify-25stocks-plan.md`（考核指标方案，Task#1）
  - `tests/e2e-verify-25stocks.integration.test.ts`（596 行，种子 20260714，25/29，强制 mock，`db.init()` 修复挂起）
  - `outputs/e2e-verify-25stocks.report.json`（机器可读，exit 0，134ms）
  - `docs/reports/e2e-verify-25stocks-report.md`（按舱人工解读，Task#3）
- 结果：综合 78 / 合格。舱分 input100/analysis100/trading67/output50/command75；维度 准确性75/完整性100/异常100/性能100/治理33。
- 关键根因：
  - **F2（高，真实缺陷）**：采集管线 `writeKlineToDailyQuotes` 发 `{store,data}` 信封；`PutHandler`（databridgeHandlers.ts:73）`db.put(store,payload)` 把整个对象落库，而 `daily_quotes` keyPath='symbol' 无 symbol 键 → IndexedDB DataError → K 线从未持久化。破坏半径贯穿三舱+汇聚+总控。修复：`db.put(store, payload.data)` 或信封直接发 DailyQuotes 对象。
  - **F3（原标高，实为测试桩误报）**：v6-engine 全量无 `Math.random`/`Crypto`/`nanoid`，数学确定性成立；双跑 `JSON.stringify` 不等价仅因 `aggregate()` 盖了 `timestamp:Date.now()`（engine.ts:262）。建议确定性比对排除 timestamp/性能字段，重判为低严重度。
  - 交易舱 orderOk=0 是 F2 级联：风控「新鲜度」门禁（riskEngine.ts:80）查不到 dailyQuotes → 全部买单被拒。
  - F1（导入落库状态 candidate 无合法出边）潜伏张力未触发（规则矩阵正确拒绝非法流转），建议纳入治理检查或对齐 INTENTION_STATUS.screening。
- 测试桩要点：`db` 单例需显式 `await db.init()` 否则 `db.ready()` 永不 resolve 挂起；用正确 DailyQuotes payload 落库才能跑通下游。

## 冗余设计专项考评补充（维度 F · 防任务漂移）

- 用户要求"软件的冗余设计，补充校对的考评指标，形成具体实施计划，按 todolist 执行防止任务漂移"。
- 在方案 v1.1 新增**维度 F：冗余设计**，5 子指标 R1–R5：配置层冗余 / 降级可用性 / 写入幂等性 / 故障隔离 / 采集器兜底；评分法升为 A–F 六维加权（冗余权重 0.15）；新增第八节实施计划（任务 #4–#8 驱动）。
- 新增测试 `tests/e2e-verify-redundancy.integration.test.ts`（~300 行，种子 20260714）：
  - `vi.mock('@/services/data-collector/directDataAPI')` 用 `importActual` 展开后**仅让 6 个真实源 getter 抛错**（tencentQuote/sinaQuote/akshareQuote/tencentBatchQuotes/sinaBatchQuotes/neteaseHistory），保留 `klinesToDailyQuotes` 真实实现 → 确定性强制降级。
  - R2 关键坑：`tryKlineSource` 仅支持 `netease`/`mock`，tencent/sina/akshare 直接 warn 跳过（不调真实 getter）→ K线强制失败须用 `['netease','mock']` 链 + mock `neteaseHistory`。
  - `getOrCreateCollector` 是 `TaskScheduler` 的 **private** 方法，R5 改为测"未知源 ID 安全跳过 + mock 兜底"（`getQuoteWithConfig(code,{sourcePriority:['unknownX','netease_bad','mock']})` → source='mock' 且不抛）。
  - `resolveQuoteChain`/`resolveKlineChain` 已 export，喂显式 `sourcePriority` 校验解析契约；不依赖 `BUSINESS_TO_QUOTE_SOURCE` 业务源命名。
- 结果：**冗余维度 100 / 优秀**，R1–R5 全绿，exit 0，93ms。
- 发现 **F4（中·冗余缺口）**：`resolveKlineChain` 取 quote 链中 `id==='mock'` 子集 + `buildDefaultSourcePriority` 永远注入 mock → K 线解析链**恒为 `['mock']`**，无真实源冗余/降级深度（单点 mock）。建议二选一：① 显式声明"K线仅 Mock 有意设计"；② 为 K 线接入真实源（AKShare/网易历史）成多源降级链。
- 交付物：`docs/reports/e2e-verify-redundancy-report.md`、`outputs/e2e-verify-redundancy.report.json`、测试脚本、方案 v1.1。
- 衔接：R3 证明存储层**同键 upsert 幂等契约健全**（同 symbol 采集/评分×3 记录数恒 1），故 F2 只是"适配器未解包信封"的独立缺陷，不影响冗余基座。

## 全链路 Mock 下「真实市场数据准确性」可行性分析 + 补充校验

- 用户命题：行情/LLM/AKShare 均不可达，全链路强制 Mock，故不校验真实市场数据准确性；要求补充校验 + 可行性分析 + 卡点 + 处理。
- 调研核实的关键问题：
  1. **溯源断裂（最严重）**：活动采集管线 `services/data-collector/collectionPipeline.ts`→`quoteToStock()`（`services/data-collector/directDataAPI.ts:373`）**不写 `dataProvenance`/`dataSource`**；带溯源的新写器 `services/fetcher/orchestrator/adapters/dataBridgeWriter.ts` 仅存于**未被接入的并行栈**。UI 降级徽章（`StockAnalysisPage.tsx:100` 读 `dataProvenance`）永不可见 → Mock 与真实不可区分。
  2. **双采集栈并行**：`services/fetcher/orchestrator/`（新版 ResilienceChain，类型 StockQuote）vs `services/data-collector/`（旧版 getQuoteWithConfig，类型 RealtimeQuote 无 source）各自 Mock/类型/写入器，漂移风险。
  3. **缺行情/K线/财务契约校验**：仅热点板块适配器有脏数据测试；StockQuote/KlineItem/CollectFinancialData 无不变式校验，且 Mock 绕过真实解析器（真实源解析 bug 在 Mock 模式隐藏）。
  4. **健康检查只查配置不查连通性**：`getApiHealthStatus`（fetcherConfig.ts:235）仅判 baseURL≠localhost；`testSourceConnectivity` 仅接 MCP Server，未暴露 UI → 全 Mock 对用户不可见。
  5. **LLM 无显式兜底**：`llmClient` 不可达即抛 `LlmConfigError`；"全链路 Mock"靠 DEV 标志 `USE_MOCK_DATA` 或上层吞错。
- **主要卡点（均为基建非代码）**：A. 直连源 CORS（需 VITE_API_BASE_URL 代理）；B. AKShare Python 后端未起（localhost:8000，python/ 目录疑似后端未随前端启动）；C. LLM key/网络未配置。`assertConfig` 直接抛错。
- **本次已落地（补充校验）**：
  - 新增 `src/services/fetcher/contractValidation.ts`：`validateStockQuote`/`validateKline`/`validateFinancial`/`checkMarketDataContract`（OHLC 一致性、量价非负、日期单调、财务有限+report_date 合法、涨跌幅区间、时间戳可信、source 合法）。
  - 新增 `src/services/fetcher/__market-data-contract.test.ts`：**14/14 通过**（Mock 一致性 / 植入脏数据必被捕获 / 回放真实解析形态必过）。
  - 修复 `src/services/fetcher/mockProvider.ts` `mockQuote` OHLC 不自洽（high 可能低于 open/price）→ 改 `high=max(open,price)+rand`、`low=max(0,min(open,price)-rand`，使 Mock 成为合格测试替身。本次即由测试暴露该缺陷。
  - 验证：受管 Node22 直驱 vitest 14 passed；相关文件 `tsc --noEmit` 无错。
- **处理建议（未落地，待排期）**：中期统一双栈、溯源透传进 quoteToStock、契约 warn-only 接入两写路径、Record/Replay 真实响应 fixture 回归；长期解决 CORS/后端/LLM key + 运行时连通性横幅。
- 报告：`outputs/全链路Mock_数据准确性_可行性分析与补充校验方案.md`。

## §七 改进建议 6 项落地（按 TODOLIST 执行防漂移）

- 用户指令："根据改进建议梳理 TODOLIST，根据任务清单执行修改任务，防止任务漂移"。
- 复盘发现：**磁盘实际状态与摘要所述不一致**——`tests/e2e-verify-25stocks.integration.test.ts`（679 行完整版）、`tests/e2e-verify-redundancy.integration.test.ts`、`package.json` 的 `test:e2e-verify` 脚本、`.husky/pre-push` 的 `[3.5]` 步骤**均已在磁盘**；但**源码修复有 2 处被回退**（疑似文件监视/格式化还原）：`inputService.ts:302` 仍 `RESEARCH_STATUS.candidate`、`dbConfig.ts:402` 的 `trading.read` 仍缺 `dailyQuotes`。其余 6 处（collectionPipeline/dataSourceOrchestrator 的 F2 信封、inputService:139 的 F1、riskEngine 的 P2 warn、riskEngine JSDoc、dataSourceOrchestrator F2）已落位。
- 重新落位被回退的 2 处：① `inputService.ts:302` `researchStatus` 初值改 `INTENTION_STATUS.screening`（并移除不再使用的 `RESEARCH_STATUS` 导入，防 lint no-unused）；② `dbConfig.ts:402` 为 `[MODULE_ID.trading].read` 增加 `STORE_NAME.dailyQuotes`（让 P2 风控新鲜度查询可达，此前恒走"无数据"分支、过期硬阻断不可达）。
- 六项改进落点（详见报告 §7.1）：
  - **P0 F2**：`writeKlineToDailyQuotes` + `collectAndSaveKline` 信封 `payload` 由 `{store,data}` 包裹改直接传 `DailyQuotes` 对象（对齐 `fetcherService.sendSaveDailyQuotes` 契约）。
  - **P0 纳入 CI**：`package.json` `test:e2e-verify` 脚本（三件套）+ `pre-push [3.5]` 已接入。
  - **P1 F3**：25 股 E2E 对 V6 `calculateAll` 以"同输入双跑、仅比对结构性字段（score+layerDetails）、排除 timestamp/性能元数据"判定确定性，根治全量 JSON.stringify 对 `timestamp:Date.now()` 过敏的误报。
  - **P1 F1**：导入初值 `researchStatus` 对齐 `INTENTION_STATUS.screening`，解除"刚导入股票无合法出边"死锁。
  - **P2 风控新鲜度**：`checkOrderRisk` "数据未到→warnings 降级放行；数据过期→维持 blocks 硬阻断"；配套 trading 读 ACL 扩权。
  - **P2 V6 区分度**：新增 `tests/v6-score-discrimination.integration.test.ts`（A 低估高盈利上升 / B 高估值亏损下降 / C 中性横盘，断言 A>B 且 spread>0.5）。
- **验证**：受管 Node22 直驱 `vitest run tests/e2e-verify-25stocks.integration.test.ts tests/e2e-verify-redundancy.integration.test.ts tests/v6-score-discrimination.integration.test.ts --no-coverage` → `Test Files 3 passed (3), Tests 3 passed (3)`，exit 0。
- 报告回填：`docs/reports/e2e-verify-25stocks-report.md` §7.1 实施结果表 + ACL 联动修复附记。
- ⚠️ 教训：本会话存在"源码编辑被静默回退、但测试/配置/CI 脚本保留"的现象（文件监视或格式化还原）。**每组源码修复后必须立即跑对应门禁验证**，不能仅依赖一次性编辑成功的返回。

## 策略合规补充测试 P0–P3（审计驱动）

- 基于 `outputs/strategy-compliance-audit-2026-07-14.md` 发现，按 P0→P3 优先级安排并实现了策略合规补充测试。
- 交付：`tests/strategy-compliance/` 下 5 个测试文件：
  - `strategy-config-contract.test.ts`（3 passed）— 阈值配置合同（证明"按策略执行"门槛已落地）
  - `p0-valuePit-screening.test.ts`（4 passed + 2 todo）— 价值洼地评分合同 + 筹码接线缺口 + 筛选器成长维度缺口
  - `p1-rotation-themeRegistry.test.ts`（4 passed + 5 todo）— 轮动信号真实引擎 + 主题注册 + 估值/股息代理缺口
  - `p2-hotMomentum.test.ts`（3 passed + 7 todo）— 热门板块五维合同 + 执行细节/热度闸门缺口
  - `p3-coreScarce-holdingPeriod.test.ts`（2 passed + 2 todo）— 持有期现状 + 配置化缺口
- 策略：✅ 通过测试守护**合同/真实引擎**；📌 `it.todo` 标记缺口（不阻塞 CI），修复后去掉 .todo 即激活回归。
- 验证：受管 Node22 直驱 `vitest run tests/strategy-compliance/` → `5 passed, 14 passed + 16 todo`，exit 0。
- 计划文档：`outputs/strategy-test-plan-2026-07-14.md`（编号 P0-01—P3-03 共 30 项，14✅ / 16📌)。
