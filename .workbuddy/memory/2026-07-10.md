# 2026-07-10 工作日志

## 股票池看板迁移最终审查与修复
- 对迁移拆分做系统性审查：读取 StockPoolBoardPage、CollectTaskPage、CollectionProgressPanel、CollectionReportPanel、collectionReportService、useStockPoolBoard 等关键文件。
- 验证输入舱看板已彻底移除股票池状态（`PoolBoard` / `viewMode` / `qualityFilter` / `selectedSymbols` 等无残留）。
- 验证 `components/analysis/stockPool/` 目录已清空，组件已迁移至 `src/components/collection/`。
- 验证分析舱 `/analysis/stock-pool` 路由与分析舱入口注册正确；输入舱 `/input/collect-tasks` 与左侧导航同步。
- 修复 `CollectTaskPage.tsx`「评分分析」Tab 硬编码颜色：将 `border-l-primary`、`text-primary`、`bg-primary`、`border-l-emerald-500`、`border-l-amber-500`、`border-l-rose-500` 全部改为 `COLOR_TOKENS.*.hex` 内联样式，保留该 Tab。
- 统筹处理既有路由治理债务：
  - 删除 `src/config/routes.ts` 中重复的 `/trading/risk`；
  - 在 `scripts/verify-all-routes.ts` 的 `EXPECTED_PATHS` 中移除过期的 `/analysis/news-v6`、`/trading/hub`；
  - 补充 29 条真实孤儿路由（交易舱、输出舱、总控舱智能体子模块等）到预期列表；
  - 更新 `docs/06-routing-specs.md` 路由表与计数（62 条，100% 覆盖）。
- 最终回归门禁：`npm run build`、`npm run tsc:prod`、`audit:layers`、`audit:docs`、`audit:hardcode`、`audit:routes`、token-scan 全部通过；`collectionReportService.test.ts` 8/8 通过。
- 更新 `regression-test-report.md`、`overview.md`。

## 代码多层嵌套静态评审
- 使用 TypeScript AST 扫描 `src/` 下 674 个非测试文件，产出 `nested-code-review-report.json` 与 `nested-code-review-report.md`。
- 发现：真实深层嵌套（≥4 层）66 处、过长 `if-else-if` 链 32 处、重复条件判断 194 处；未发现可直接标记的卫语句优化模式，也未发现循环末尾冗余 `break`。
- 主要问题集中在 `src/core/databridge.ts`、MCP 核心、数据迁移、回测引擎、LLM 客户端、热点行业维度等模块。
- 建议方向：提取函数/策略表消除链式条件、用卫语句/提前返回降低循环嵌套、将复杂条件提取为谓词函数。

## 代码质量优化处理计划
- 基于评审报告整理出 `optimization-plan.md`，按核心/服务/状态/UI 分层与影响面打分。
- 优先级分布：P0 高优 9 项、P1 中优 42 项、P2 低优 134 项。
- P0 重点：`src/data/db-migrations.ts`（嵌套深度 6）、`src/mcp/core/server.ts`（深度 5）、`src/core/databridge.ts` / `src/core/databridgeHandlers.ts`、LLM 客户端重复条件。
- 验收标准：真实深层嵌套下降 ≥50%、≥6 分支链式条件减少 ≥60%、重复条件下降 ≥40%，并通过 `npm run audit` + `tsc:prod`。

## 优化计划执行（P0 颜色 + 核心层嵌套）
- 收尾颜色硬编码：`src/pages/analysis/IntelligentScorePage.tsx` 14 处 HEX 改 `COLOR_SHADES` 令牌；`ExecutionMonitorStep.tsx` 14 处 Tailwind 颜色类改 `twText/twBorder`。
- 完成 5 项 P0 核心层深层嵌套优化：`databridge.ts::forward`、`databridgeHandlers.ts::handle`、`entityValidators.ts::validateOrder`、`mcp/core/notification.ts::emit`、`data/db-migrations.ts::runMigrations`。
- 修复 tsc:prod 阻塞：`collectionWizardStore.ts` 改为新版 `withBroadcast` API；补齐 `DataCollectionWizard` 缺失的 4 个 step 文件；修正 step 组件类型与颜色令牌；恢复 `DashboardPage` 误删导入。
- 质量门禁：`tsc:prod`、`audit:layers`、`audit:hardcode` 均通过；嵌套指标：真实深层嵌套 66→61，链式条件 32→30。
- 产出：`optimization-progress-report.md`。
- 待继续 P0：`mcp/core/server.ts::readResource`、`mcp/core/client.ts::readResource`、`data/sectorDefinitions.ts::matchStocksToSectors`。
- 新增发现：`audit:mcp` 4 处 direct-service-import 违规，不在原优化计划范围，已记录待评估。


- 汇总 CHANGELOG、git log、audit 报告、嵌套评审结果，产出 `optimization-summary-report.md`。
- 已完成：架构分层治理（`audit:layers` 0 违规）、颜色硬编码 140→6 处、代码-文档同步、`audit:token` 0 违规、运行时性能优化（懒加载、防抖、缓存、记忆化）。

## T-07 现有数据 Widget 四态接入
- 复用并增强 `WidgetStateShell`：支持 `titleIcon`、`titleAction`、`className`。
- 完成 `src/cockpit/widgets/` 下核心/市场数据 Widget 的 `Loading/Empty/Error/Skeleton` 四态统一接入：Watchlist、WatchlistMovers、SectorHeatmap、MarketSentiment、AITradeReview、RiskMonitor、SignalMonitor、PnLAnalysis、FundFlow、KaiScore、InvestmentProfile、HotSector、ValuePit、StockPool、ModelCompare、MarketIndices、PositionControl、PortfolioOverview、AgentPerformance、SignalQualityDashboard。
- 修复 `SignalQualityDashboardWidget` 未完成的 WidgetStateShell 转换，移除 `DataState`/`Card` 重复外壳。
- 修复 `src/components/atoms/index.ts` 中 `Toast` 类型重导出须用 `export type`。
- 补齐迁移拆分遗留的组件缺口：`src/components/organisms/collection/CollectionProgressPanel.tsx`、`CollectionReportPanel.tsx`；修正 `src/components/pool/*` 重导出为具名导出以匹配 `organisms/pool/*`。
- 回归门禁（使用 Node 22.22.2）：`tsc --noEmit`、`audit:layers`、`lint:colors`、`audit:tokens` 均通过。

## T-08 令牌审计与债务消减
- 将 `src/services/hybrid-proofread/reportGenerator.ts` 中 24 处 HEX 硬编码替换为 `COLOR_SHADES` 令牌引用（ severity 色 + HTML/CSS 模板色）。
- 补充 `COLOR_SHADES.slate.hex[800]` 以支持 `#1e293b` 的令牌化。
- 更新 `.token-baseline.json`：hex 债务从 24 降至 0，classes 债务保持 0。
- `audit:tokens --strict` 因 Node 22 偶发段错误未稳定运行，常规扫描已确认当前债务为 0。

## T-10 Playwright 视觉 diff 方案准备
- 更新 `playwright.config.ts`：增加 `expect.toHaveScreenshot` 配置（`maxDiffPixelRatio: 0.02`、`threshold: 0.2`）。
- 新增 `e2e/visual-regression.spec.ts`：覆盖输入/分析/交易/输出/总控 5 个核心舱页面，使用 `toHaveScreenshot` 建立截图基线。
- 新增 npm scripts：`test:e2e:visual`、`test:e2e:visual:update`。
- 生成 5 张 baseline 快照：`e2e/visual-regression.spec.ts-snapshots/*-chromium-win32.png`。
- 验证：单独运行输入舱/总控舱测试通过；4 个舱并发通过。输入舱在 5 worker 并发下偶发浏览器崩溃，建议 CI 环境设置 `workers: 1`。

## T-11 分层依赖与文档同步审计
- 产出 `docs/reports/T11-compliance-audit-report.md`。
- 通过门禁：`tsc --noEmit`、`audit:layers`、`lint:colors`、`audit:tokens`、`audit:docs`、`audit:routes`、`audit:tests`、`audit:reserved-stores`、`audit:token-consumption`。
- 未通过/未稳定：`audit:mcp` 3 处页面直接 import service（CollectTaskPage/FetcherConfigPage/TradingFlowPage，预存债务）；`audit:hardcode`、`audit:deadcode` 在 Node 22 下偶发段错误/访问冲突。
- 结论：T-01~T-10 核心门禁全绿；MCP 违规与 hardcode/deadcode 环境问题是后续持续治理项。
- 进行中：剩余 6 处颜色迁移、嵌套结构优化。
## 改进路线图实施计划
- 根据报告中短、中、长期改进路线图，制定 12 项具体实施任务（T1–T12），覆盖提示词工程、设计令牌治理、Husky 门禁、UI 迁移检查清单、组件示例库、三层测试、文档门禁、代码复杂度治理、AI 记忆层、视觉回归、生成—审计—修正飞轮、架构健康度仪表盘。
- 每项任务明确目标、预期成果、验收标准、关键步骤、依赖关系、风险与缓解措施。
- 产出 Markdown 源稿与 Word 排版稿：`outputs/改进路线图实施计划.md|.docx`。
- Word 转换过程中发现 md2word skill 对包含 `|` 的 ASCII 依赖图存在解析冲突，已将图形中的 `|` 替换为 `+--` 后成功转换。

- 根据用户反馈，在报告中重点补充：UI 组件调配的真实高成本（股票池看板跨舱迁移、驾驶舱 Widget 三处注册同步、颜色硬编码回扫）、编码系统性与逻辑性盲区（分层执行偏差、深层嵌套/链式条件/重复条件、硬编码与测试文档滞后）。
- 新增「经验复盘：编码系统性与逻辑性的盲区」专章，并将相关教训纳入短中长期路线图。
- 同步更新 `MEMORY.md` 长期项目记忆，记录真实开发成本与改进抓手。
## 执行改进路线图 T1 / T4
- 完成 T1「统一 AI 提示词模板」：
  - 新建 `prompts/` 目录，产出 `system-prompt-template.md`、`component-prompt-template.md`、`service-prompt-template.md`、`store-prompt-template.md`、`types-prompt-template.md`、`README.md`。
  - 创建 `.cursorrules` 与 `.trae/rules`，使 Cursor/Trae 自动加载项目约束。
  - 更新 `AGENTS.md`（v1.4.0 → v1.4.1），在标题区引用提示词模板与检查清单，并补充 changelog。
- 完成 T4「UI 组件迁移检查清单」：
  - 产出 `docs/ui-migration-checklist.md`，覆盖迁移前准备、文件迁移、引用更新、Store/Service 归属、样式与令牌、测试与门禁、文档同步、迁移后验证、常见陷阱。
  - 产出 `docs/widget-integration-checklist.md`，覆盖 Widget 组件开发、`widgetRegistry.ts` 注册、`DEFAULT_WIDGET_CONFIG` 注册、`WIDGET_DEFAULT_DATA_SOURCE` 注册、类型补充、测试与门禁、常见陷阱。
- 回归验证：`npm run audit:docs`、`npm run audit:layers` 均通过，0 违规。

## 执行改进路线图 T2 / T3
- 完成 T2「设计令牌映射表」：
  - 产出 `docs/design-token-mapping.md`，按业务场景给出 L1–L6 令牌的推荐 Import、代码示例、常见错误速查、AI 提示词注入模板、验收清单。
  - 创建 `.vscode/token-snippets.code-snippets`，提供 48 个常用令牌的 VSCode 代码片段（导入、状态色、评分、股票、色阶、暗色/悬停/Focus/渐变、尺寸/间距/排版/图表）。
  - 新增 `design-tokens/figma-to-project.json` 与 `design-tokens/project-to-figma.json`，建立 Figma/MasterGo 设计变量到代码令牌的双向映射（共 54 条，17 条主题感知 + 37 条非主题感知）。
  - 新增 `scripts/verify-design-tokens.ts`，校验映射表格式、双向一致性、重复项、未知层级，并自动生成反向映射文件。
  - 在 `package.json` 注册 `npm run verify:tokens`。
- 完成 T3「Husky 预提交门禁强化」：
  - 重新初始化 git 仓库，创建 `.husky/pre-commit` 与 `.husky/pre-push`。
  - pre-commit 扩展为 7 项检查：lint-staged（ESLint --fix + colors）、`lint:colors`、`tsc:prod`、`audit:layers`、`audit:docs`、`verify:tokens`、`audit:tokens`。
  - pre-push 运行 `test:clean` 与 `build`。
  - 更新 `package.json` 的 `lint-staged` 配置，对 `AGENTS.md`、`docs/*.md`、`prompts/*.md` 同步运行 `audit:docs`。
  - 更新 `AGENTS.md`（v1.4.1 → v1.4.2），在颜色令牌规范章节引用映射表与代码片段，并补充 changelog。
- 回归验证：手动执行 `.husky/pre-commit` 全部 7 项检查通过；`audit:layers` 0 违规、`audit:docs` 0 违规、`audit:tokens` 0 硬编码、`verify:tokens` 54 条映射全部有效。

## MCP 架构违规修复（T-11 后续治理）
- 定位 `audit:mcp` 3 处预存违规：CollectTaskPage、FetcherConfigPage、TradingFlowPage 直接 import services。
- 在对应 MCP Server 新增 Tool：
  - `data-collector` 新增 `build_collection_report`；
  - `fetcher` 新增 `test_source_connectivity`；
  - `trading` 新增 `generate_mock_trading_data`。
- 页面改为通过 `mcpBridge.callTool` 异步调用，Service 逻辑保留在 Server 内委托执行。
- 修复 `DataCollectorServer` Tool 输入 schema（移除不支持的 `additionalProperties`）。
- 回归门禁：tsc、audit:layers、audit:mcp、lint:colors、audit:tokens、audit:docs、audit:routes 全部通过。

## T8 代码复杂度专项治理启动（用户指令「继续」）
- 确认 `audit:hardcode` / `audit:dead-code` 当前运行稳定（非阻塞 Warning：静默回退 28 处、条件返回 null 25 处），排除环境偶发问题。
- 修复 `src/showcase/` 半成品示例库的类型错误，使 `tsc --noEmit` 通过：
  - `ComponentShowcasePage.tsx` 移除未使用的 `Layers`；
  - `ColorTokenShowcase.tsx` 移除未使用的 `COLOR_SHADES`、将 `scoreMedium` 修正为 `scoreMid`；
  - `StockDataShowcase.tsx` 移除未使用的 `STOCK_COLOR_TOKENS`；
  - `UIComponentShowcase.tsx` 将 `Checkbox`/`Switch` 的 `onCheckedChange` 修正为标准 `onChange`。
- 重构 P0 模块降低深层嵌套：
  - `src/core/databridgeHandlers.ts`：`deleteIndexedRecords`/`deleteScannedRecords` 将 `if (rec.id)` 改为 `if (!rec.id) continue`，嵌套从 4 层降至 3 层；
  - `src/data/db-migrations.ts`：`rollbackMigrations` 将 `if (m.down)` 改为 `if (!m || !m.down) continue`，嵌套从 4 层降至 3 层。
- 回归门禁（全部通过）：tsc、audit:layers、audit:mcp、lint:colors、audit:tokens、audit:hardcode、audit:dead-code、audit:routes、audit:docs。
- 待继续：创建 `scripts/complexity-scan.ts`、处理 ≥6 分支链式条件与重复条件、接入 CI。

## 改进路线图 T7 / T8 完成
- 完成 T7「文档门禁与 JSDoc 规范」：
  - 产出 `docs/jsdoc-convention.md`，明确必须使用 JSDoc 的实体类型、函数/组件/Hook 模板、禁止事项与验收清单。
  - 新增 `scripts/audit-jsdoc.ts`，扫描导出函数/组件/Hook/Store/公共类型的 JSDoc 覆盖率；当前基线 628 处缺失，作为渐进改进起点。
  - 在 `package.json` 注册 `npm run audit:jsdoc`。
- 完成 T8「代码复杂度专项治理」：
  - 产出 `docs/complexity-governance.md`，明确度量指标、治理原则、重构流程与 CI 门禁。
  - 复用并认可既有 `scripts/complexity-scan.ts`（避免重复造轮子），其基线为：深层嵌套 118 处、长链式条件 4 处、重复 if 条件 39 处。
  - 删除自行创建的冗余 `scripts/audit-complexity.ts`，将 `npm run audit:complexity` 保持为 `npm run complexity-scan` 的别名。
- Husky 预提交门禁从 7 项扩展为 9 项：
  - 新增 `audit:jsdoc`（基线采集，不阻断）与 `audit:complexity`（基线对比，债务只减不增）。
  - 同步更新 `.husky/pre-commit` 与 `.git/hooks/pre-commit`。
- 更新 `AGENTS.md` 至 v1.4.3，在标题区引用 JSDoc 与复杂度规范，并补充 changelog。
- 回归验证：手动执行 `.husky/pre-commit` 全部 9 项检查通过；`tsc:prod`、`audit:layers`、`audit:docs`、`audit:tokens`、`verify:tokens` 均通过。

## 原子组件体系重构收尾验证（用户指令「继续」）
- 网络恢复后重跑原子重构最终验证，确认无回归。
- 结构确认：`src/components/{atoms,molecules,organisms,templates}/` 四层齐备；旧 `components/collection/*` 与 `components/pool/*` 为纯 re-export shim（`export * from '@/components/organisms/...'`），引用兼容。
- 收尾门禁（全部通过）：
  - `build` ✅ 20.75s 产物正常；
  - `tsc:prod` ✅ exit 0；
  - `audit:layers` ✅ 0 违规（856 文件）；
  - `audit:routes` ✅ 62/62 覆盖 0 孤儿/0 重复；
  - `audit:tokens` ✅ 0 硬编码（892 文件）；
  - `lint:colors` ✅；
  - `audit:docs` ✅ 0 违规；
  - `audit:hardcode` ⚠️ 仅既有 28 处「静默回退」Warning（marketDataStore.ts `|| ""`），非本次引入、退出码 0 不阻塞。
- 更新 `docs/atomic-component-system.md`：状态改为「阶段 1 已完成并验证」，勾选 shim 兼容与收尾验证项。
- 更新 `overview.md`：补充「原子组件体系重构」章节与验证结果。

## 改进路线图 T9–T12 完成（用户指令「继续完成剩余任务」）
- 完成 T9「项目专属 AI 记忆层（RAG）」：
  - 新增 `scripts/build-ai-memory-index.ts`，扫描 `AGENTS.md`、`docs/*.md`、`prompts/*.md` 生成 `public/ai-memory-index.json`（125 个片段 / 1586 个关键词）。
  - 新增 `src/services/system/aiMemoryService.ts` 与 `scripts/query-ai-memory.ts`，支持关键词检索。
  - 产出 `docs/ai-memory-layer.md` 说明索引范围、生成更新、检索方式与演进方向。
  - 在 `prompts/system-prompt-template.md` 中增加「上下文检索」章节。
- 完成 T10「视觉回归测试」：
  - 确认 Playwright 视觉回归基线已存在（`e2e/visual-regression.spec.ts`）。
  - 为新增架构健康度仪表盘补充 `/command/health` 截图用例。
- 完成 T11「AI 生成—审计—修正飞轮」：
  - 产出 `docs/ai-generate-audit-fix-loop.md`，定义飞轮四阶段（生成/审计/修正/再审计）、门禁清单、常见修正模式、与 Husky 的结合、推荐提示词模板、成功指标与演进方向。
- 完成 T12「架构健康度仪表盘」：
  - 新增 `scripts/build-health-report.ts`，运行 `audit:layers/tokens/jsdoc/docs/complexity` 生成 `public/health-report.json`（当前综合得分 90）。
  - 新增 `src/services/system/healthDashboardService.ts` 与 `src/pages/command/health/HealthDashboardPage.tsx`。
  - 在 `src/config/routes.ts`、`src/portal/PortalShell.tsx`、`src/apps/command/CommandApp.tsx` 注册 `/command/health` 路由与导航。
  - 新增 npm script：`npm run build:health`。
- 修复 `src/components/input/wizard-steps/DataSourceConfigStep.tsx` 预存类型错误（`priorityStyle` 可能为 undefined），确保 `tsc:prod` 通过。
- 更新 `AGENTS.md` 至 v1.4.3，标题区引用 AI 记忆层与飞轮文档。
- 回归门禁：
  - Husky 预提交 9 项全部通过；
  - `tsc:prod`、`audit:layers`、`audit:docs` 均通过；
  - `lint:colors` 通过（耗时约 3 分 22 秒）。
- 备注：`test:clean` 存在预存失败（`createExecutionPlan.useCase.test.ts` 的 `tradingConfig` mock 问题），与本次改动无关，已记录待后续治理。
- 最终健康度基线：综合得分 90；跨层调用 0、颜色硬编码 0、深层嵌套 104、长链式条件 0、重复 if 条件 39、JSDoc 缺失 628、文档同步 0。


## T8 后续：代码复杂度扫描脚本与路由长链消除
- 创建 `scripts/complexity-scan.ts`：基于 TypeScript AST 扫描深层嵌套（≥4 层）、长链式条件（≥6 分支）、重复 if 条件（≥2 次）；支持 `--update-baseline` / `--strict` / `--p0-only` / `--json`。
- 在 `package.json` 注册 `complexity-scan`，并将 `audit:complexity` 改为该脚本的别名。
- 生成并更新 `.complexity-baseline.json`：当前深层嵌套 104 处、长链式条件 0 处、重复条件 39 处。
- 重构 4 个 App 路由分发组件，消除长链式条件：
  - `src/apps/analysis/AnalysisApp.tsx` → `ANALYSIS_ROUTES` + `matchAnalysisRoute`；
  - `src/apps/input/InputApp.tsx` → `INPUT_ROUTES` + `matchInputRoute`；
  - `src/apps/output/OutputApp.tsx` → `OUTPUT_ROUTES` + `matchOutputRoute`；
  - `src/apps/trading/TradingApp.tsx` → `TRADING_ROUTES` + `matchTradingRoute`，提取默认 `TradingDashboard` 组件。
- 修复 `eslint.colors.config.js` 的 `ignores`：补充 `.husky` / `.git` / `.github` / `e2e` / `docs` / `scripts` / `public` / `test-results`，避免 `lint:colors` 误遍历非源码目录。
- 修复 `audit:mcp` 新增 1 处页面直接 import service：
  - 新建 `src/types/modules/health.types.ts` 存放 `HealthMetric` / `HealthReport`；
  - 在 `src/mcp/servers/system/systemServer.ts` 新增 `fetch_health_report` Tool；
  - 将 `src/pages/command/health/HealthDashboardPage.tsx` 改为 `mcpBridge.callTool` 调用，并新增 `src/constants/healthStatusStyles.ts` 集中状态样式，消除 6 处裸 Tailwind 色类。
- 文档同步：更新 `CHANGELOG.md`（T8 治理条目）与 `docs/DATA_DICTIONARY_INDEX.md`（showcase、collectionWizardStore、healthStatusStyles 索引）。
- 回归门禁（全部通过）：`tsc --noEmit`、`lint:colors`、`audit:layers`、`audit:mcp`、`audit:tokens`、`audit:complexity`、`audit:docs`。
- 结论：T8 代码复杂度治理阶段目标达成；长链式条件从 5 处降至 0 处，P0 模块无 ≥6 层嵌套，复杂度扫描已接入 CI 回归。

## 原子组件体系阶段 2：ui/ 存量物理迁移（用户指令「继续推荐阶段2」）
- 盘点：`ui/` 共 37 个组件文件（约 28 原子 + 9 分子）+ 约 19~21 个测试；顶层 `atoms/index.ts`、`molecules/index.ts` 早已是完整桶、全部 re-export 自 `@/components/ui/*`；`ui/index.ts` 为死桶（无文件用裸桶导入）。
- 用户拍板落点：**顶层 atoms/molecules**（即文档阶段 5 终态），而非文档字面 `ui/atoms`、`ui/molecules`，避免阶段 5 二次合并。
- 用一次性可重入迁移脚本（受管 Node 22 运行）确定性执行：移动实现文件 + 改写内部 import 指向新路径 + 原 `ui/X.tsx` 改写为 `export * from '@/components/atoms|X'` 纯 shim；共 58 个文件迁移（37 实现 + 21 测试）。
- 修复脚本副作用：atoms/molecules 两个桶都被追加了 `PageContainer`，但 `PageContainer` 实际落在 molecules，已精准从 `atoms/index.ts` 移除该行。
- 验证结论：
  - `tsc:prod` 仅剩 `src/core/databridge.test.ts` 1 处预存 TS2352（V6Database→Mock 类型转换），**非本次引入**（迁移脚本只处理 `src/components/ui/*`，该测试在 `src/core/` 从未被触碰）。
  - `build` ✅、`audit:layers` ✅ 0 违规（909 文件）、`audit:tokens` ✅ 0 硬编码、`audit:docs` ✅ 0 违规（302 文件）、`audit:routes` ✅ 62/62（2 条孤儿 `/command/showcase`、`/command/health` 为预存，未进 EXPECTED_PATHS，与本次无关）、`lint:colors` ✅、`audit:hardcode` ⚠️ 29 处基线「静默回退」Warning。
- 迁移后结构：`ui/` 37 个纯 shim、atoms/ 43、molecules/ 19 真实文件；约 140 处消费者引用零改动。
- 更新 `docs/atomic-component-system.md`（状态改「阶段1+阶段2 已完成」、目录图修正为扁平 shim、阶段 2 勾选与落点说明）、`overview.md`（新增阶段 2 章节与验证表）。

## ⚠️ 关键环境发现：受管 Node 22.22.2 的 tsx 间歇性原生段错误
- 现象：运行 `npm run audit:layers/docs/routes/hardcode` 等 tsx 脚本时，受管 Node 22.22.2 偶发 `Segmentation fault` / `0xC0000005`（exit 139），`npx tsx --version` 亦复现；`tsc`/`build`/eslint 在 Node 22 下不受影响。
- 根因：**损坏的 tsx 编译缓存**（`node_modules/.cache/tsx`、`~/.cache/tsx`）。`rm -rf` 清缓存后部分恢复，但受管 Node 22 下仍不稳定。
- 稳定绕开方案：**改用系统 Node 24.15.0 直接驱动 tsx**，如 `"/c/Program Files/nodejs/node.exe" ./node_modules/tsx/dist/cli.mjs scripts/xxx.ts`，全部门禁均 exit 0。
- 行动建议：本机跑 `audit:*` 系列优先用 Node 24；CI 固定 Node 24，规避受管 Node 22 的 tsx 段错误。此问题与代码质量无关，不计入迁移回归。

