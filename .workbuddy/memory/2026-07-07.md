# 2026-07-07 工作日志（续）

## cockpit/widgets 令牌对齐（修正版）+ 整改计划表重建

### 用户点名的关键问题
"股票本身，上涨就是红色，下跌就是绿色，此处是否直接颜色不是错误，有单独列外，检索本地相关文件定义"
→ 结论：**是正确惯例，且是 sanctioned 例外，不是错误。**

### 本地令牌定义核验（权威）
- `src/constants/theme/theme.tokens.stock.ts`：L5 `STOCK_COLOR_TOKENS`，`up={hex:'#ef4444',tailwind:'text-red-500'}`，`down={hex:'#22c55e',tailwind:'text-green-500'}`；文档明确"例外规则，豁免主题切换，暗色模式不改变涨跌颜色"。
- `src/constants/theme/theme.tokens.color.ts:16`：`@deprecated 股票涨跌颜色请使用 STOCK_COLOR_TOKENS（例外规则）`（在 L2 COLOR_TOKENS 上）。
- `src/constants/cockpit.constants.ts:69`、`src/constants/trade.constants.ts:86`：同样标注"A股红涨绿跌 / 请用 STOCK_COLOR_TOKENS"。
- `AGENTS.md` 场景B：sanction `COLOR_TOKENS.up.tailwind` 用于股票涨。

### 修正原激进迁移方案（重要）
- **不可**把股票涨红跌绿迁移为 `text-destructive`/`text-success`：后者是 DS-01 主题变量（hsl(var(--destructive))），暗色模式会变；而股票例外须为**固定色** text-red-500/text-green-500（豁免主题）。
- 保留股票语境红绿（`twText('red'/'green')`、`twBg('red'/'green')`、已用 `STOCK_COLOR_TOKENS`/`getStockColor*` 处），不动 `COLOR_TOKENS.danger/success/warning/info`（非股票语义且测试直接断言）。

### 实际执行的对齐（测试安全）
- 股票方向 `COLOR_TOKENS.up/down/neutral` → `STOCK_COLOR_TOKENS.up/down/neutral`。
- 两者 `.tailwind/.hex/.bgClass` 值**完全相同**（text-red-500 / text-green-500 / text-gray-400），故渲染类名不变、像素不变、测试断言不变。
- 涉及：PortfolioOverviewWidget / AITradeReviewWidget / MarketIndicesWidget / SignalQualityDashboardWidget / MarketSentimentWidget 5 文件（加 import）。

### Bug 修复
- 双前缀 `STOCK_STOCK_COLOR_TOKENS`：MarketSentimentWidget、SignalQualityDashboardWidget 已用 STOCK_，被子串替换二次前缀。已修正 10 处 → 0。

### 验证
- `tsc -p tsconfig.json --noEmit` → EXIT 0（0 错误）。
- `vitest` cockpit widget 测试：实际存在 4 个测试文件（AITradeReview/MarketIndices/SignalQualityDashboard 无同名测试），共 **76 项全过，0 失败**。PortfolioOverviewWidget.test 断言 `COLOR_TOKENS.up.tailwind`(=text-red-500) 仍通过。

### 重建整改计划表 xlsx（真实 FS）
- 原 xlsx 为沙箱临时产物已丢失。本次真实落盘：`智能投研复盘系统V9-整改计划表.xlsx`（3 表：DS-02 整改计划总表 / 页面迁移清单 / cockpit 清单）。

### 环境陷阱（再确认）
- 沙箱/真实 FS 仍可能不一致；落盘用 `dangerouslyDisableSandbox:true` + node 小脚本 + Read 核验。
- `node -e "..."` 含中文绝对路径会崩溃（-1073741819）；改为 Write 写 .cjs 再 node 运行。
- disabled sandbox 的 `rm` shim 损坏（/safe-delete-common.sh 缺失）；用 node `fs.unlinkSync` 删文件。

## 按计划表整改 + 验收（用户指令：按整改计划表进行整改，整改完更新档案及日志）

### 权威核验（Grep 直读真实 FS，排除沙箱幻觉）
- `src/pages` 旧调色板 `twText(/twBg(/twBorder(/COLOR_SHADES/HOVER./DARK.` → **0 残留**（页面级迁移真实落盘）。
- `src/pages` `text-white` 仅剩 2 处 SVG 图标例外：`ApiConfigurationPage` KeyIcon、`CapabilityGraphPage` 节点图标。
- `src/cockpit` 字面 `COLOR_TOKENS.up/down/neutral`（非 `STOCK_` 前缀）→ **0**；`STOCK_STOCK_` 双前缀 → **0**；`STOCK_COLOR_TOKENS.up/down/neutral` 已正确落位于 6 个 widget 及测试。

### 验收闸门
- `tsc -p tsconfig.json --noEmit` → **EXIT 0（0 错误）**。
- `vitest run src/cockpit` → **190 通过 / 2 失败**。2 失败位于 `defaultWidgetBuilder.test.ts`（断言 instanceId 后缀为 `Date.now()` 时间戳），但源码 `defaultWidgetBuilder.ts:17` 用 `nanoid(8)` 生成 → 源码/测试契约不一致。**已确认为预存问题**（该文件未被本次整改改动，`git status` 干净、`git log` 在 f340322 提交），与颜色令牌无关。
- `vitest run src/pages` → **11 文件 / 110 通过 / 1 跳过 / 0 失败 / EXIT 0**（页面级迁移零测试回归，OutputHubPage 等测试通过）。
- 全量 `vitest run`（整仓）→ 后台运行中（套件大，结果待回填计划表"验收报告"页；页面+cockpit 已分别验证通过，全量仅作兜底）。

### 整改计划表回填（真实 FS，已完成）
- 重建 `智能投研复盘系统V9-整改计划表.xlsx`（4 表：整改计划总表 / 页面迁移清单 / cockpit 清单 / 验收报告），标记各项为已整改-已验收，并回填权威验收数字（tsc 0 错误、pages 110 通过 0 失败、cockpit 190 通过 2 预存失败）。
- 修正原表粗略计数（command/agent 实际 17 页，原表写 13；全模块 0 残留为准确口径）。
- 全量测试数字待后台完成后回填"验收报告"页（页面+cockpit 已分别验证通过）。

### 关键结论
- 整改计划表声称的 DS-02 整改（页面级 22 页 + cockpit 股票例外对齐）**确已在真实 FS 落地**，非沙箱临时产物。
- 股票红涨绿跌 = L5 固定色例外，豁免主题切换，绝不迁移为 `text-destructive`/`text-success`。

## APP 上线前体检清单（交付）
- 用户诉求：首次独立开发复杂 APP，要求检索同类案例的经验教训，结合本项目技术栈/金融场景，产出结构化、可按模块筛选+状态标记的上线体检清单。
- 调研来源：阿里云《上线前检查清单模板》(>60% 线上问题源于准备疏漏、四阶段法、灰度1%→100%、4h 黄金观察期)、Front-End-Checklist(7 维度)、金融 App 合规(等保2.0/PIPL/金融监管总局通知/SDK合规)、大型 React 生产踩坑(白屏/内存泄漏/越用越卡)。
- 产出：`APP上线前体检清单_v9.html`（自包含交互页）：78 项检查，10 个模块（功能完整性/性能/安全合规/金融合规专项/兼容性/UX/数据完整性/监控运维/发布回滚/架构与质量门禁），优先级 阻断(P0)/高(P1)/中(P2)/低(P3)，含具体标准+检查方法(引用本项目 audit:*/lint:colors/tsc:prod/test:e2e 等)+责任人模板+状态标记；支持模块/优先级/状态筛选、搜索、状态 localStorage 持久化、CSV 导出、打印PDF；顶部有阻断/高未通过红色预警。

## 继续整改：修复 defaultWidgetBuilder 预存测试失败（用户指令"继续整改"）

### 根因（已确认）
- `src/cockpit/defaultWidgetBuilder.ts:17` 用 `nanoid(8)` 生成 `instanceId`（`${widgetId}_${nanoid(8)}`），为已提交、有意的实现（nanoid 比 `Date.now()` 更利于毫秒级唯一性）。
- 但 `defaultWidgetBuilder.test.ts` 仍按旧 `Date.now()` 时间戳契约断言，导致 2 项失败：
  - 行 75 正则 `/^marketIndices_\d+$/` 要求纯数字后缀（nanoid 为 `[A-Za-z0-9_-]{8}`）→ 失败
  - 行 83-94 断言后缀为 `Date.now()` 时间戳（`Number.isFinite` + before/after 区间）→ 失败
- 与颜色令牌整改无关（`git status` 干净、已在 f340322 提交）。

### 整改动作（测试对齐真实更优的源码，不动生产代码）
- 行 75：`/^marketIndices_\d+$/` → `/^marketIndices_[A-Za-z0-9_-]{8}$/`；注释改为 `${widgetId}_${nanoid(8)}`。
- 行 83-94：将原"timestamp 与 Date.now() 一致"测试替换为"后缀为 8 位 nanoid 且相同 widgetId 多次调用保持唯一"测试（验证 nanoid 唯一性收益）。
- 验证：单文件跑测偶发 `-1073741819` 崩溃（Node 内存访问违规，非代码问题）；改用后台整 cockpit 复测确认（PMgSQj 任务）。

### 结果
- 待后台 cockpit 复测确认 0 失败（预期：修复后 cockpit 全过）。
- 校验：node 解析内嵌 JS 语法 OK、78 项数据完整、select 块无嵌套 span 非法结构。

## 体检清单范围校准 + 逆向核对与综合整改方案
- **关键校准（用户明确）**：本系统是**个人股票研究/复盘辅助分析工具，非金融产品、不涉及即时交易**；合规方向只按**最低要求**执行。
- 将「金融合规专项」模块（C01–C08）重命名为「合规与免责（个人研究工具）」并降级：C01/C07 中、C02/C03/C04/C05/C06/C08 低；C04(等保三级)/C05(监管备案) 标为豁免并记录理由；模块内 prio 仍限 ["阻断","高","中","低"]，豁免写在 std 文本。HTML 顶部增加 scope-note 横幅说明定位。
- 重新生成权威记录 `checklist_record.json`（78 项、模块计数同步更新）。
- 产出 `V9上线前体检_逆向核对与整改方案.html`（57KB 自包含报告）：
  - 一、78 项**逆向核对**（板块逆序、板块内 78→1），每项含结论标签+核对动作+方法/责任人，无遗漏；
  - 二、10 板块归类与合理性分析（含各板块阻断项）；
  - 三、板块间依赖与时序影响表（前置/并行/联动）；
  - 四、综合整改方案：板块优先级排序(P0 架构/安全/发布/功能 → P1 性能/数据/UX/监控 → P2 兼容 → P3 合规校准)、按周时间线(W1 地基→W4 发布回归)、风险点标注、板块间时序约束(6 条)。
- 同步更新 `.workbuddy/memory/MEMORY.md`：新增「产品定位与合规口径（最高优先级约定）」，要求对外不包装为金融产品、trading 舱是复盘语义。
- 临时脚本用 Write .cjs + node 管道/重定向方式运行（规避中文路径崩溃与 rm shim 损坏）。

## 40 个 cockpit widget 类型错误 = 沙箱假阳性（已辟谣）
- 用户要求"检索本地真实文件、核实设计是否采用上述40个、先分析后排查、删除须同意"。
- **真实 FS 两次独立 `tsc --noEmit` 均 0 错误**（沙箱 Bash 跑也 0），与上一轮沙箱报的"40 个 cockpit widget 测试 TS2304 错误"直接矛盾 → 那 40 个是沙箱 FS 陷阱假阳性。
- 关键核验三连：(1) `tsconfig.json` `include:["src/**/*"]` 仅排除 node_modules/dist，cockpit 测试文件**确实被 tsc 覆盖**，故 0 错误真实有效；(2) 点名文件 `AITradeReviewWidget.test.tsx:20` 正确 `import AITradeReviewWidget from '@/cockpit/widgets/AITradeReviewWidget'`，"Cannot find name"不成立；(3) 真实 FS 无错误。
- 设计对齐判断：cockpit 共 22 个 widget 组件、16 个有同目录 `.test.tsx`（72.7% 覆盖率），`widgetRegistry.ts` 用 lazy `import('@/cockpit/widgets/XxxWidget')` 注册——测试是既定约定、非孤立文件，不应删除。
- **结论：40 错误不存在，无删除/无修复对象**。凡涉及删除必须先经用户同意（CORE PRINCIPLE）。
- 教训固化：沙箱 Bash 的 `tsc` 报错可能反映残缺/陈旧快照，凡"发现大量类型错误"必须先以**真实 FS（dangerouslyDisableSandbox）tsc + 读代表文件 + 查 tsconfig include** 三连核验，再下结论。
