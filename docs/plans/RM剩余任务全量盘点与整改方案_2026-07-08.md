# V9 智能投研复盘系统 — RM 剩余任务全量盘点与整改方案

> 生成时间：2026-07-08 19:30 | 数据基线：全量门禁实跑 + git 工作树快照 + 任务清单交叉核实
> 分支：`refactor/pr-6-module-split` | HEAD：`b2ee5d8`

---

## 〇、数据基线（实跑快照）

### 门禁状态矩阵（2026-07-08 19:25-19:32 实跑）

| 门禁 | 退出码 | 状态 | 关键发现 |
|:-----|:------:|:----:|:---------|
| `audit:layers` | 0 | ✅ PASS | 756 文件，0 违规 / 0 警告 |
| `audit:deadcode` | 0 | ✅ PASS | 0 空壳 / 0 路由缺失 / 0 未注册页面 |
| `audit:mcp` | 0 | ✅ PASS | 605 文件，0 违规 / 0 警告 |
| `audit:reserved-stores` | 0 | ⚠️ 发现 | `analysisStore` 标 @reserved 但 `AnalysisApp.tsx` 有 6 处已验证引用 |
| `audit:hardcode` | 1 | ❌ FAIL | 0 Major / 0 Critical（已修），59 Warning（静默回退），退出码逻辑回归 |
| `tsc:prod` | 2 | ❌ FAIL | 2 error：`acl.test.ts` 未用导入 + `databridge.ts:436` 类型不匹配 |
| `lint:colors` | N/A | ❌ BROKEN | `package.json:13` 单引号 bug，脚本无法执行 |
| `audit:tokens` | N/A | ❌ MISSING | `package.json` 无 `audit:tokens` 脚本（RM-012 接线回退） |
| `audit:tests` | 未跑 | — | 纯正则扫描，上次 0 阻塞 |
| `audit:docs` | 未跑 | — | 待补跑 |

### 工作树状态

- **已修改跟踪文件**：16 个（并行会话 DataBridge/rbac/migration 整改 + 本会话 hardcode 修复）
- **未跟踪文件**：~20 个（2 份 docs + 14 份审计 JSON + 2 份集成测试 + migration 目录 + uiPlaceholders.ts + test-logs/）
- **已提交链**（本会话）：`e7c47f1` → `e74133a` → `4091d02` → `d8995d2` → `b2ee5d8`

### 数据采集模块实现核实

| 任务组 | 关键文件 | 状态 |
|:-------|:---------|:-----|
| A 批量导入 | `batchImportService.ts` / `BulkImportPanel.tsx` | ❌ 不存在（0/4 启动） |
| B 真实采集 | `directDataAPI.ts` / `dataSourceOrchestrator.ts` | ❌ 不存在（0/4 启动） |
| D 缺失框架 | `CollectTaskPage.tsx`(456L) / `FetcherConfigPage.tsx`(515L) | ✅ 已存在（2/2 完成） |

---

## 一、剩余任务清单及优先级排序

### P0 — 阻断级（阻塞构建/门禁，必须立即修复）

| ID | 任务 | 当前状态 | 阻塞影响 |
|:---|:-----|:---------|:---------|
| **P0-1** | `tsc:prod` 2 处类型错误修复 | ❌ 未修复 | 阻塞 `npm run build`、阻塞所有提交的 husky 门禁 |
| **P0-2** | `audit:hardcode` 退出码逻辑回归修复 | ❌ 未修复 | 0 阻断违规却 exit 1，阻断 `npm run audit` 全链 |
| **P0-3** | `lint:colors` 脚本单引号 bug 修复 | ❌ 未修复 | 颜色门禁完全不可用，无法验证令牌合规 |
| **P0-4** | `audit:tokens` 脚本接线恢复 | ❌ 未恢复 | 令牌消费审计缺失，`npm run audit` 链不完整 |

### P1 — 高优先级（影响完整性/一致性，应本会话完成）

| ID | 任务 | 当前状态 | 影响范围 |
|:---|:-----|:---------|:---------|
| **P1-1** | 本会话 hardcode 修复提交（5 文件 + uiPlaceholders.ts + regex 修复） | ⏳ 已改未提交 | 改动散落工作树，与并行会话改动混杂 |
| **P1-2** | `audit:reserved-stores` analysisStore 发现处置 | ⚠️ 发现未处置 | @reserved 标记与实际引用矛盾 |
| **P1-3** | `docs/templates/` 缺失（AGENTS.md §12.7 悬空引用） | ❌ 未创建 | D10 文档↔代码一致性缺陷 |
| **P1-4** | 未跟踪审计报告 JSON 归集（14 份） | ❌ 未纳入 | 审计产物散落，无版本追溯 |

### P2 — 中优先级（功能补全，可排期）

| ID | 任务 | 当前状态 | 影响范围 |
|:---|:-----|:---------|:---------|
| **P2-1** | 数据采集 A 组：批量导入增强（4 任务） | ❌ 0/4 启动 | CSV/Excel/JSON 导入能力缺失 |
| **P2-2** | 数据采集 B 组：真实数据采集（4 任务） | ❌ 0/4 启动 | 真实行情 API 接入缺失，仅 Mock |
| **P2-3** | 并行会话 DataBridge 整改提交归集 | ⏳ 进行中 | BulkEnvelope/QueryEnvelope/EventEnvelope/symbol 广播 + systemServer + migration UI |
| **P2-4** | 并行会话集成测试验证（mcp-servers 427L + stockpool-acl 505L） | ❌ 未跟踪未验证 | 932 行测试代码散落未跟踪 |

### P3 — 低优先级（规格完善，可延后）

| ID | 任务 | 当前状态 | 影响范围 |
|:---|:-----|:---------|:---------|
| **P3-1** | 数据采集 C 组：UI/UX 优化（4 任务） | ❓ 待核实 | 交互体验提升 |
| **P3-2** | 数据采集 E/F/G/H 组：测试/时序/数据传递/监控（10 任务） | ❓ 规格已写，实现待核实 | 数据完整性保障 |
| **P3-3** | git 提交者身份规范化 | ⚠️ 自动推断 | 仅 warning，不阻断 |

---

## 二、每项任务的可行性评估

### P0-1：tsc:prod 类型错误修复

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 低。2 处明确错误：(1) `acl.test.ts:2` 删除未用的 `AclCheckResult` 导入；(2) `databridge.ts:436` `string \| undefined` → `string` 类型收窄（加 `?? ''` 或非空断言） |
| **资源需求** | 无额外依赖，仅需 Edit 工具 |
| **风险评估** | 中。`databridge.ts` 是并行会话**活跃编辑中的文件**——直接改可能与其产生冲突。`acl.test.ts` 不在并行会话修改集内，安全 |
| **阻塞根因** | 依赖阻塞：databridge.ts 错误源自并行会话在途的 DataBridge 整改（BulkEnvelope 类型签名变更），须等其稳定后再修或协调 |

### P0-2：audit:hardcode 退出码逻辑回归

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 低。`_audit-pipeline.ts:283` 现为 `exitCode = totalViolations > 0 ? 1 : 0`，需改为仅阻断性违规（排除 Warning）才 exit 1 |
| **资源需求** | 无额外依赖 |
| **风险评估** | 低。`_audit-pipeline.ts` 不在并行会话修改集内，无冲突风险。改动仅 1 行逻辑 |
| **阻塞根因** | 优先级调整：RM-011/12 已决策"静默回退降级 Warning"，但退出码逻辑未同步更新。59 条 Warning 被计入 totalViolations 导致假阳性 FAIL |

### P0-3：lint:colors 脚本单引号 bug

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 低。`package.json:13` 的 `--rule 'no-hardcoded-colors/...: error'` 在 npm 传参时单引号被吞，需改用 `--rule "no-hardcoded-colors/no-hardcoded-tailwind-colors: error"` 或 `--rule=\"...\"` |
| **资源需求** | 无额外依赖 |
| **风险评估** | 低。`package.json` 不在并行会话修改集内。但需先 Read 再 Edit（工具约束） |
| **阻塞根因** | 脚本编写错误：npm scripts 中单引号在 Windows/跨平台场景下不被 shell 正确解析 |

### P0-4：audit:tokens 脚本接线

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 低。需在 `package.json` scripts 中添加 `"audit:tokens": "tsx scripts/token-scan.cjs"` 并将其加入 `audit` 聚合命令 |
| **资源需求** | `scripts/token-scan.cjs` 已存在（已验证不误报 shadow-*） |
| **风险评估** | 低。仅添加脚本条目，不改逻辑 |
| **阻塞根因** | 接线遗漏：RM-012 创建了 `token-scan.cjs` 但未在 package.json 注册脚本名 |

### P1-1：本会话 hardcode 修复提交

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 中。须精确 `git add` 仅本会话改动的文件，不吞入并行会话的 16 个在途修改 |
| **资源需求** | husky pre-commit 会 lint-staged 扫描 staged 文件 |
| **风险评估** | 中。提交前须 `git status` 核对待提交集（教训：`18:4x` 误吞事件） |
| **阻塞根因** | 并行执行冲突：同一工作树两个会话共存，须严格隔离提交集 |

### P1-2：reserved-stores analysisStore 发现

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 低。`analysisStore` 已被 `AnalysisApp.tsx` 实际使用（6 处已验证引用），@reserved 标记已过时 → 移除标记 |
| **资源需求** | 无 |
| **风险评估** | 低。`analysisStore.ts` 不在并行会话修改集内 |
| **阻塞根因** | 标记滞后：Store 在重构期间标记 @reserved，但 AnalysisApp 已接入使用，标记未同步移除 |

### P1-3：docs/templates/ 缺失

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 低。需创建 `docs/templates/task-graph-template.md` + `regression-suite.md` 两个模板文件（内容可参照 AGENTS.md §12.3-12.4 结构） |
| **资源需求** | 无 |
| **风险评估** | 低。纯新增文件，无冲突 |
| **阻塞根因** | 文档先行但模板未落地：AGENTS.md §12.7 声明了模板引用，但 `docs/templates/` 目录从未创建 |

### P1-4：审计报告 JSON 归集

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 低。14 份 JSON 直接 `git add` |
| **资源需求** | 无 |
| **风险评估** | 低。纯新增 |
| **阻塞根因** | 审计脚本每次运行生成带时间戳的 JSON，未纳入版本控制 |

### P2-1/P2-2：数据采集 A/B 组

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 中-高。A 组需 PapaParse/SheetJS 集成 + 拖拽 UI；B 组需 HTTP 请求腾讯/新浪/网易 API + 四层降级编排 |
| **资源需求** | 可能需新增 npm 依赖（papaparse, xlsx）；B 组需网络访问权限 |
| **风险评估** | 中。B 组涉及外部 API 调用，需处理 CORS/限流/数据格式差异；A 组需处理大文件解析性能 |
| **阻塞根因** | 优先级调整：当前焦点在 RM 整改与 DataBridge 架构改进，数据采集功能开发被延后 |

### P2-3：并行会话 DataBridge 整改

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 高。BulkEnvelope 批量写通道 + QueryEnvelope 路由 + EventEnvelope 事件广播 + symbol 级广播 + systemServer MCP + migration UI，涉及核心数据层重构 |
| **资源需求** | 并行会话正在执行 |
| **风险评估** | 高。核心数据层变更影响全局，须完整回归测试 |
| **阻塞根因** | 进行中：并行会话活跃编辑，本会话不应介入（避免冲突） |

### P2-4：并行会话集成测试

| 维度 | 评估 |
|:-----|:-----|
| **技术难度** | 中。427+505=932 行测试需验证可编译+可通过 |
| **资源需求** | vitest 运行环境 |
| **风险评估** | 中。测试依赖并行会话的 DataBridge 整改完成（mock 对象/信封结构须匹配） |
| **阻塞根因** | 依赖阻塞：测试依赖被测代码（systemServer/ACL）稳定后才能验证 |

---

## 三、逐项整改的具体措施与时间节点

### P0-1：tsc:prod 类型错误修复

**措施**：
1. `acl.test.ts:2`：删除未用的 `AclCheckResult` 类型导入（本会话可安全执行）
2. `databridge.ts:436`：**等待并行会话稳定后**，在 `string | undefined` 参数处加空值兜底 `?? ''` 或类型收窄。若并行会话已自行修复则跳过

**预期完成**：acl.test.ts 立即（5 分钟）；databridge.ts 待并行会话收口后（预计 30 分钟内）

**验证**：`tsc -p tsconfig.json --noEmit` 退出码 0

### P0-2：audit:hardcode 退出码逻辑修复

**措施**：
修改 `scripts/_audit-pipeline.ts:283`：
```typescript
// 现状（回归）：
const exitCode = report.summary.totalViolations > 0 ? 1 : 0
// 修正为（仅阻断性违规才 exit 1）：
const blockingCount = report.summary.totalViolations - (report.summary.totalWarnings ?? 0)
const exitCode = blockingCount > 0 ? 1 : 0
```

**预期完成**：立即（10 分钟）

**验证**：`npm run audit:hardcode` 在 0 Major/Critical + 59 Warning 时退出码 0

### P0-3：lint:colors 脚本修复

**措施**：
修改 `package.json:13`，将单引号改为双引号转义：
```json
"lint:colors": "eslint src/ --ext .ts,.tsx --rule \"no-hardcoded-colors/no-hardcoded-tailwind-colors: error\""
```
同时移除 `--max-warnings 0`（基础配置有 1434 条无关 warning 会误伤，RM-011 已决策去掉）。

**预期完成**：立即（5 分钟）

**验证**：`npm run lint:colors` 可正常执行且仅报颜色 error 级

### P0-4：audit:tokens 脚本接线

**措施**：
在 `package.json` scripts 中添加：
```json
"audit:tokens": "tsx scripts/token-scan.cjs",
```
并将其加入 `audit` 聚合命令链。

**预期完成**：立即（5 分钟）

**验证**：`npm run audit:tokens` 正常执行，`npm run audit` 链包含该步骤

### P1-1：本会话 hardcode 修复提交

**措施**：
1. `git status` 核对待提交集
2. 精确 `git add` 仅本会话改动文件：
   - `src/config/uiPlaceholders.ts`（新建）
   - `src/components/analysis/sector/SectorRotationHeatmap.tsx`（bg-neutral-50 → bg-card）
   - `src/pages/command/agent/ApiConfigurationPage.tsx`（URL 占位符 → 常量）
   - `src/pages/command/agent/LlmManagementPage.tsx`（URL 占位符 → 常量）
   - `src/services/data-collector/TaskScheduler.ts`（magic 60000 → 常量）
   - `scripts/audit-hardcode.ts`（regex 去除 shadow 误报）
3. `git commit` 提交（husky lint-staged 仅扫描 staged 文件，不受并行会话在途文件影响）

**预期完成**：P0-2/3/4 修复后一并提交（20 分钟）

**验证**：`git log --oneline -1` 确认提交；`git status` 确认并行会话文件未被吞入

### P1-2：reserved-stores analysisStore 处置

**措施**：
读取 `src/store/analysisStore.ts`，移除 `@reserved` JSDoc 标记（Store 已被 AnalysisApp 实际使用，标记过时）。

**预期完成**：立即（5 分钟）

**验证**：`npm run audit:reserved-stores` 报告 0 违规

### P1-3：docs/templates/ 创建

**措施**：
创建 `docs/templates/` 目录及两个模板文件：
- `task-graph-template.md`：参照 AGENTS.md §12.3 结构（rootTask/phases/contextAnchor/tokenBudget）
- `regression-suite.md`：参照 AGENTS.md §12.4 结构（L1/L2/L3 三级套件）

**预期完成**：立即（15 分钟）

**验证**：`ls docs/templates/` 显示两个文件；AGENTS.md §12.7 引用不再悬空

### P1-4：审计报告 JSON 归集

**措施**：
`git add docs/reports/audit/*.json` 纳入版本控制（14 份带时间戳 JSON）。

**预期完成**：与 P1-1 一并提交（5 分钟）

**验证**：`git status` 无未跟踪 JSON

### P2-1：数据采集 A 组（批量导入增强）

**措施**：
1. A-1：新建 `src/services/data-collector/batchImportService.ts`，集成 PapaParse(CSV) + SheetJS(Excel) + JSON 解析器
2. A-2：新建 `src/components/input/BulkImportPanel.tsx`，拖拽上传 + 预览
3. A-3：预览表格行级状态标签（valid/duplicate/invalid）
4. A-4：导入进度条 + 模板下载

**预期完成**：A-1/A-2 可在本周内启动（需确认是否新增 npm 依赖）；A-3/A-4 随后

**验证**：3 种格式解析正确；拖拽+点击上传可用

### P2-2：数据采集 B 组（真实数据采集）

**措施**：
1. B-1：新建 `directDataAPI.ts`，实现腾讯财经直连（qt.gtimg.cn）
2. B-2：新浪+网易备用 API
3. B-3：`dataSourceOrchestrator.ts` 四层降级编排
4. B-4：采集结果通过 DataBridge.forward() 写入 IndexedDB

**预期完成**：需 A 组完成后排期；B-1/B-2 可并行开发

**验证**：四层降级逻辑 Mock 模式测试通过；真实 API 连通性测试

### P2-3：并行会话 DataBridge 整改归集

**措施**：
**不介入**。等待并行会话完成 DataBridge 整改（BulkEnvelope/QueryEnvelope/EventEnvelope/systemServer/migration UI）后：
1. 核验 `tsc:prod` 退出码 0
2. 核验 `npm run audit` 全链通过
3. 协助提交归集

**预期完成**：依赖并行会话进度（预计本会话内可收口）

**验证**：tsc 0 error + audit 全链 exit 0 + build 成功

### P2-4：并行会话集成测试验证

**措施**：
1. 待并行会话 DataBridge 整改提交后
2. `vitest run tests/__tests__/integration/mcp-servers.integration.test.ts`
3. `vitest run tests/__tests__/integration/stockpool-acl.integration.test.ts`
4. 通过后 `git add` 纳入版本控制

**预期完成**：P2-3 完成后（预计 15 分钟）

**验证**：2 套集成测试全绿

---

## 四、各任务间的依赖关系与并行执行策略

### 依赖关系图

```
P0-1(tsc修复) ──────────────────────────────────┐
  ├─ acl.test.ts: 本会话可独立修 ──────────────→ P1-1(提交)
  └─ databridge.ts: 依赖 P2-3(并行会话稳定) ──→ P2-3(归集) → P2-4(集成测试)

P0-2(退出码) ──┐
P0-3(lint:colors) ──┤── 可并行，互不依赖 ──→ P1-1(一并提交)
P0-4(audit:tokens) ─┘

P1-1(提交) ──→ P1-4(JSON归集可并入同一提交)

P1-2(reserved-stores) ── 独立，可并行
P1-3(docs/templates) ── 独立，可并行

P2-1(A组) ←── 无依赖，可独立启动（但需确认 npm 依赖）
P2-2(B组) ←── 弱依赖 A组（共用 data-collector 目录）

P2-3(并行会话) ←── 独立于本会话所有任务
P2-4(集成测试) ←── 强依赖 P2-3
```

### 并行执行策略

| 执行轨 | 任务序列 | 执行方 |
|:-------|:---------|:-------|
| **轨 A（本会话-门禁修复）** | P0-2 → P0-3 → P0-4 → P1-2 → P1-3 → P1-1(提交) | 本会话 |
| **轨 B（本会话-类型修复）** | P0-1(acl.test.ts 部分) → 并入轨 A 提交 | 本会话 |
| **轨 C（并行会话）** | P2-3(DataBridge 整改) → P0-1(databridge.ts 部分) → P2-4(集成测试) | 并行会话 |
| **轨 D（功能开发-延后）** | P2-1(A组) → P2-2(B组) | 待排期 |

**并行规则**：
- 轨 A/B 可完全并行（不同文件集，无冲突）
- 轨 C 独立于轨 A/B（并行会话操作 DataBridge/rbac/migration，本会话操作 scripts/config/store/docs）
- 轨 A 提交时须 `git add` 精确指定文件，**绝不整仓 `git commit`**（防止吞入轨 C 的在途改动）
- 轨 D 在轨 A/B/C 收口后启动

---

## 五、验收标准与责任人

### 验收标准矩阵

| ID | 验收命令 | 预期结果 | 责任人 |
|:---|:---------|:---------|:-------|
| P0-1 | `tsc -p tsconfig.json --noEmit` | exit 0 | acl.test.ts→本会话；databridge.ts→并行会话 |
| P0-2 | `npm run audit:hardcode` | exit 0（0 Major/Critical + 59 Warning） | 本会话 |
| P0-3 | `npm run lint:colors` | 脚本可执行，仅报颜色 error | 本会话 |
| P0-4 | `npm run audit:tokens` | 脚本存在且可执行 | 本会话 |
| P1-1 | `git log --oneline -1` + `git status` | 提交存在，并行会话文件未被吞入 | 本会话 |
| P1-2 | `npm run audit:reserved-stores` | 0 违规 | 本会话 |
| P1-3 | `ls docs/templates/` | 2 个模板文件存在 | 本会话 |
| P1-4 | `git status --porcelain \| grep '??'` | 无审计 JSON 未跟踪 | 本会话 |
| P2-3 | `tsc` + `npm run audit` + `npm run build` | 全部 exit 0 | 并行会话 |
| P2-4 | `vitest run tests/__tests__/integration/` | 2 套件全绿 | 并行会话/本会话协助 |

### 最终验收门禁（全部完成后）

```bash
# L3 完整回归套件（AGENTS.md §12.4）
tsc -p tsconfig.json --noEmit          # 0 error
npm run audit:layers                   # exit 0
npm run audit:hardcode                 # exit 0
npm run audit:deadcode                 # exit 0
npm run audit:mcp                      # exit 0
npm run audit:reserved-stores          # exit 0
npm run lint:colors                    # 仅颜色 error 级
npm run audit:tokens                   # exit 0
npm run audit:tests                    # 0 阻塞
npm test -- --run                      # 全绿
npm run build                          # 成功
```

### 责任人分配原则

- **本会话**：scripts/、config/、docs/templates/、store/@reserved 标记、hardcode 修复提交 — 这些文件不在并行会话修改集内，无冲突风险
- **并行会话**：src/core/databridge*、src/data/db*、src/config/dbConfig.ts、src/mcp/、src/components/system/migration/、集成测试 — 这些是并行会话的活跃编辑域
- **用户确认**：新增 npm 依赖（papaparse/xlsx）、外部 API 接入决策、git 提交者身份规范化

---

## 附录：已完成任务回顾（本会话累计）

| 提交 | 内容 | 状态 |
|:-----|:-----|:-----|
| `e7c47f1` | 恢复整改提交（报告去重 + 双令牌闭合） | ✅ |
| `e74133a` | rbac 模块拆分 + databridge 整改（21 文件） | ✅ |
| `4091d02` | cascade 模块 + 2 文档（5 文件） | ✅ |
| `d8995d2` | permissionRevocationService 测试补全（6/6 绿） | ✅ |
| `b2ee5d8` | 5 真死文件删除（用户授权） | ✅ |

**未纳入版本控制的在途工作**（并行会话，本会话不触碰）：
- `src/core/databridge.ts`（+180 行：BulkEnvelope/QueryEnvelope/EventEnvelope/symbol 广播）
- `src/core/databridgeHandlers.ts`（+78 行）
- `src/data/db.ts` / `db-migrations.ts` / `dbConfig.ts`（RBAC + migration 支持）
- `src/mcp/servers/system/systemServer.ts`（+131 行，新 MCP 服务端）
- `src/components/system/MigrationPanel.tsx`（-275/+重构）+ `migration/` 目录（新组件）
- `src/services/rbac/permissionRevocationService.ts`（+8 行微调）
- `scripts/audit-mcp.ts`（+13 行）
- 2 份集成测试（mcp-servers 427L + stockpool-acl 505L）
- 3 份 docs（B6 测试报告 + RBAC 可行性分析 + DataBridge 改进报告/计划）
