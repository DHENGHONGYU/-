---
title: news-contract.md ?? ????????????????????
type: reference
domain: project
phase: design
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?????????? `news` ???????????????????????????????????????????? ??????`./services-catalog.md`??24 ????????????`../../AGENTS.md` ??????????????????"
tags: [project, news, contract]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-PROJ-226
referenced_by: [V9-DOC-META-000, V9-DOC-PROJ-176, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# news-contract.md ?? ????????????????????

> **????**?????? `news` ????????????????????????????????????????????  
> **????**??`./services-catalog.md`??24 ????????????`../../AGENTS.md` ??????????????????

---

## 1. ????????

### 1.1 ????????

1. **??????????????????**????????????????????`newsService.ts`??????????????DJB2 ?????????????????????????????????? `dataLayer.news` ?? `newsStockMap` ??????????????????????????????????????????????????????????????????
2. **??????????????**??`sentimentAnalyzer.ts`??????????????????????????/???????? + ???????? + ?????????????????????????????????????????????? `positive` / `negative` / `neutral` ???????????????????????????? `sentimentCache` ??????????????
3. **????-????????????**??`stockLinker.ts`???????????????????????????????????????????????????????????????????????????????????????????? `NewsStockMap` ??????????????????????????????
4. **????????????**??`sentimentTrendEngine.ts`????????????????????????????????????/LLM ?????????? `global` / `stock` / `industry` ??????????????????????????????????????????????????????????

### 1.2 ????????

| ???? | ???? |
|------|------|
| ?????? | `src/services/`?????????? |
| ???????? | ???????? `core/`??`data/`??`lib/`????????????`config/`??`types/`?????????????? |
| ???????? | ???????? IndexedDB?????? `DataBridge.forward()` ?? `dataLayer` ???????????????????? `store/`??`pages/`??`components/` |
| ???????? | `store/`??`analysisNewsStore` ???? `newsService` ?? `sentimentTrendEngine`????`mcp/servers/news/`??`NewsServer` ?????????? MCP ?????? |

### 1.3 ????????????????

| ???????? | ???? | ?????? |
|----------|------|--------|
| `fetcher` | ?????????????????????????? | `fetcher` ???? ?? `newsService.saveNewsArticle()` ???? |
| `data/` | ?????????????????? `dataLayer` ???? IndexedDB | `newsService` ? `dataLayer.news` / `dataLayer.newsStockMap` / `dataLayer.sentimentCache` |
| `core/` | ?????????????????? `DataBridge` ???????? | `newsService` ?? `EnvelopeFactory` ?? `dataBridge.forward()` ?? `store/` ?????? |
| `mcp/servers/news` | ?????????????????????? MCP ???? | `newsService` / `sentimentAnalyzer` / `sentimentTrendEngine` ?? `NewsServer` ???? |
| `store/analysisNewsStore` | ???????????????????????? | `analysisNewsStore` ?? `listNews` / `saveNewsArticles` / `aggregateSentimentTrend` |

---

## 2. ????????

### 2.1 ??????????TypeScript Interface??

```typescript
// ??????src/services/news/stockLinker.ts

export interface StockInfo {
  symbol: string
  name: string
  industry?: string
}

export interface StockLink {
  symbol: string
  name: string
  matchType: 'exact_code' | 'exact_name' | 'fuzzy_name' | 'industry'
  confidence: number
  source: 'title' | 'content'
  matchedKeyword: string
}

export interface LinkerConfig {
  enableExactCode: boolean
  enableExactName: boolean
  enableFuzzy: boolean
  enableIndustry: boolean
  minFuzzyLength: number
  confidenceThreshold: number
  maxLinks: number
  titleWeight: number
  contentWeight: number
}

// ??????src/services/news/sentimentAnalyzer.ts

export type SentimentLabel = 'positive' | 'negative' | 'neutral'

export interface SentimentResult {
  sentiment: SentimentLabel
  confidence: number
  score: number // normalized score in [-1, +1]
}

// ??????src/types/modules/news.types.ts

export type SentimentTrendDimension = 'global' | 'stock' | 'industry'
export type SentimentType = 'positive' | 'negative' | 'neutral'

export interface SentimentTrendPoint {
  date: string
  positive: number
  negative: number
  neutral: number
  total: number
  positiveRatio: number
  negativeRatio: number
  neutralRatio: number
}

export interface SentimentTrendSeries {
  dimension: SentimentTrendDimension
  value: string
  data: SentimentTrendPoint[]
  summary: {
    totalArticles: number
    positiveCount: number
    negativeCount: number
    neutralCount: number
    avgDailyArticles: number
  }
}

export interface SentimentTrendOptions {
  dimension: SentimentTrendDimension
  value?: string
  startDate?: string
  endDate?: string
  fillGaps?: boolean
}

export interface NewsFilterState {
  keyword: string
  category: string
  sentiment: '' | 'positive' | 'negative' | 'neutral'
  source: string
}

// ??????src/data/types/types.knowledge.ts

export interface NewsArticle {
  id: string
  title: string
  content: string
  url: string
  source: string
  category: string
  publishTime: string
  fetchTime: string
  sentiment: 'positive' | 'negative' | 'neutral'
  sentimentConfidence: number
  relatedStocks: string[]
  keywords: string[]
  hash: string
}

export interface NewsStockMap {
  id: string // {symbol}_{newsId}
  symbol: string
  newsId: string
  relevanceScore: number
  isTitleMatch: boolean
  isContentMatch: boolean
  industryMatch: boolean
}

export interface SentimentCache {
  id: string // sent_{contentHash}
  contentHash: string
  sentiment: 'positive' | 'negative' | 'neutral'
  confidence: number
  method: 'rule' | 'llm' | 'hybrid'
  analyzedAt: number
  llmModel?: string
}
```

### 2.2 ??????????

| ???? | ???? | ???? | ???????? |
|------|------|------|----------|
| `saveNewsArticle()` | `(article, options?) => Promise<DataLayerResult<NewsArticle>>` | ??????????????????????????????????????????????DataBridge ???????? | `logger.error` + `DataLayerResult` ???? |
| `saveNewsArticles()` | `(articles, options?) => Promise<DataLayerResult<NewsArticle[]>>` | ?????????????????????? `saveNewsArticle`???????????? | ???? |
| `listNews()` | `(options?) => Promise<DataLayerResult<NewsArticle[]>>` | ????????????????/????/????/????/??????/?????????? | ???? |
| `getNewsBySymbol()` | `(symbol) => Promise<DataLayerResult<NewsArticle[]>>` | ?????????????????????? | ???? |
| `getNewsByHash()` | `(hash) => Promise<DataLayerResult<NewsArticle\|undefined>>` | ?????????????????????? | ???? |
| `generateMockArticles()` | `(count=5) => NewsArticle[]` | ??????????????????/???????? | ???????????????? |
| `analyzeNewsArticle()` | `(article) => SentimentResult` | ????????+???????????????? | ?????? |
| `getOrAnalyzeSentiment()` | `(content, method?) => Promise<SentimentResult & {contentHash}>` | ???????????????????????????? | ???? |
| `analyzeText()` | `(text) => SentimentResult` | ???????????????? | ?????? |
| `classifySentiment()` | `(score, threshold?) => SentimentLabel` | ?????????????? | ?????? |
| `linkArticleToStocks()` | `(article, stocks, config?) => {article, links, maps}` | ?????????????????????? | ?????? |
| `buildStockMap()` | `(stocks) => Map<string, StockInfo>` | ???? symbol+6?????????????? | ?????? |
| `aggregateSentimentTrend()` | `(articles, options) => SentimentTrendSeries` | ??????????????global/stock/industry?? | `logger.info` ???? |
| `extractStockOptions()` | `(articles) => string[]` | ?????????????????????? | ?????? |
| `extractIndustryOptions()` | `(articles) => string[]` | ?????????????????? | ?????? |

### 2.3 ??????????DataBridge ??????

| ?????? | ?????? | ?????? | ???? |
|--------|--------|--------|------|
| `ENVELOPE_ACTION.saveNews` | `newsService` | `DataBridge` ?? `store/` | ???????????????????????????? `NewsArticle` ???? |
| `ENVELOPE_ACTION.newsArticleLoaded` | `newsService` | `DataBridge` ?? ???????? | `listNews` ?????????????????????????????? |

> **??**?????????????????? `EventBus.subscribe/publish`?????????????? `DataBridge.forward(envelope)` ??????????

---

## 3. ??????

```
[????????: fetcher / MCP Server / UI Mock ????]
    ??
newsService.saveNewsArticle()
    ?????? sentimentAnalyzer.analyzeNewsArticle()  ?? sentimentCache????????????
    ?????? stockLinker.linkArticleToStocks()        ?? newsStockMap????????????
    ??
dataLayer.news.save()  +  dataLayer.newsStockMap.save()
    ?? (DataBridge.forward)
Envelope ?? routeToDB() ?? ???? store ????????
    ??
analysisNewsStore (Zustand)
    ?????? articles[] ?? NewsCard / NewsFilterPanel / NewsDetail
    ?????? sentimentTrend ?? NewsSentimentTrend (Recharts ????)
```

**????????**??
- `sentimentTrendEngine` ????????????????????????????/LLM???????? `newsStore` ???????? `NewsArticle[]`???????? `SentimentTrendSeries` ????????????
- `newsService` ???? `DataBridge.forward()` ?????????? `try-catch`????????????????????????????????????

---

## 4. ??????????

### 4.1 ????????????lib/ ????????????

| ???? | ???? | ???? |
|------|------|------|
| `dataLayer` | `@/data/dataLayer` | IndexedDB ??????news / newsStockMap / sentimentCache?? |
| `generateId` | `@/data/db` | ?????????? ID |
| `DataLayerResult` ???? | `@/data/types` | ???????????? |
| `dataBridge` | `@/core/databridge` | ???????????? |
| `EnvelopeFactory` | `@/core/envelope` | ?????????????? |
| `logger` | `@/lib/logger` | ?????????????????? |
| `ENVELOPE_ACTION/ENVELOPE_TARGET/MODULE_ID` | `@/config/dbConfig` | ???????????? |
| `MOCK_NEWS_URL_PREFIX` | `@/config/dataSourceUrls` | ???????? URL ???? |
| `DJB2_HASH_INIT/DJB2_HASH_MULTIPLIER` | `@/config/mathConstants` | ???????????? |
| `nanoid` | `nanoid` (npm) | ???? traceId ???? |

### 4.2 ??????

| ?????? | ?????? | ???? | ???? |
|--------|--------|------|------|
| `DEFAULT_LINKER_CONFIG` | `enableExactCode: true`, `enableExactName: true`, `enableFuzzy: true`, `enableIndustry: true`, `minFuzzyLength: 3`, `confidenceThreshold: 0.3`, `maxLinks: 5`, `titleWeight: 1.5`, `contentWeight: 1.0` | ?????????????? | `src/services/news/stockLinker.ts` |
| `DEFAULT_INDUSTRY_KEYWORDS` | 10 ?????????? | ????????????????????/????/?????????? | `src/services/news/stockLinker.ts` |
| `DEFAULT_STOCK_LIBRARY` | 18 ?? A ?? | ???????????????????????? | `src/services/news/stockLinker.ts` |
| `POSITIVE_WORDS` | 24 ?? | ???????????? | `src/services/news/sentimentAnalyzer.ts` |
| `NEGATIVE_WORDS` | 23 ?? | ???????????? | `src/services/news/sentimentAnalyzer.ts` |
| `NEGATION_WORDS` | 9 ?? | ?????????? | `src/services/news/sentimentAnalyzer.ts` |
| `DEGREE_WORDS` | 10 ?? | ?????????????????????? | `src/services/news/sentimentAnalyzer.ts` |
| `TITLE_WEIGHT` | `1.5` | ???????????? | `src/services/news/sentimentAnalyzer.ts` |
| `CONTENT_WEIGHT` | `1.0` | ???????????? | `src/services/news/sentimentAnalyzer.ts` |
| `SENTIMENT_THRESHOLD` | `0.1` | ???????????? | `src/services/news/sentimentAnalyzer.ts` |

---

## 5. ????????

| ???????? | ???? | ???? |
|----------|------|------|
| ???????? | `src/services/news/__tests__/sentimentTrendEngine.test.ts` | ??????????????????global/stock/industry ??????fillGaps??????????????????????????????8 ?????? |
| ???????? | `src/services/news/stockLinker.test.ts` | ???????????????????????? |
| ???????? | `tests/services/news.integration.test.ts` | ??????????DataBridge ??????`newsStore` ??????????-???????? |
| Mock ???? | `generateMockArticles()` | ???????????????????????????????? |

---

## 6. ????????

| ???? | ???? | ???? | ???? |
|------|------|------|------|
| 2026-07-12 | v0.1.0 | ???????? | ?????? |

---

> **TODO[???? owner]**???????????????? ??1-??5???????? `services-catalog.md` ?????????????????????? `tsc --noEmit` + `audit:layers` ??????
