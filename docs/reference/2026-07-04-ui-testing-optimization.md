---
title: 2026-07-04-ui-testing-optimization
type: reference
domain: qa
phase: testing
tier: reference
status: active
maintainer: V9 Architecture Team
summary: "For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended)..."
tags: [qa, optimization, test, testing, component, reference]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-QA-027
referenced_by: [V9-DOC-PROJ-174, V9-DOC-META-000, V9-DOC-PROJ-176, V9-DOC-PROJ-182, docs/00-meta/deprecated-docs/old-versions/registry-index-v1.0.0-02-design.md, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# V9 ?????????????????????????????????????????????

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ??? V9 ?????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????

**Architecture:** ???????????????????????????(1) UI ?????????????????? ??? (2) ???????????????????????? ??? (3) ????????????????????? ??? (4) ?????????????????????????????????????????????????????????/??????/??????/??????/?????????????????????????????????theme.tokens.ts???????????????????????????

**Tech Stack:** React 19, TypeScript, Tailwind CSS, Recharts, Vitest, Playwright, shadcn/ui ?????????

---

## ??????????????????

### 1. ??????????????????

| ?????? | ???????????? | ???????????? |
|------|---------|---------|
| **?????????** | InputHubPage, LocalKnowledgePage, FetcherConfigPage, SevenDimConfigPage, CollectTaskPage | ?????????????????????????????????????????? |
| **?????????** | StockAnalysisPage, IndustryScorePage, IntelligentScorePage, SectorAnalysisPage, BacktestPage, ScoreDocPage, NewsPage, HotSectorPage, ValuePitPage, MultiFactorScreeningPage | ????????????????????????????????????????????? |
| **?????????** | TradingHubPage, HoldingsPage, StrategySnapshotPage, TradeModal | ?????????????????????????????????????????? |
| **?????????** | OutputHubPage, ResearchReportPage, TradeReviewPage | ??????????????????????????? |
| **?????????** | CommandHubPage, AgentHubPage, AgentRegistryPage, AgentTasksPage, AgentTriggerPage, AgentFeedbackPage, MCPServerDashboardPage, EngineMonitorPage, ArchitecturePage | ?????????????????????????????????MCP ?????? |

### 2. UI ????????????

| ???????????? | ???????????? | ???????????? |
|---------|---------|---------|
| **????????????** | Button, Input, Checkbox, Radio, Switch, Slider, Textarea, Select | ??????????????????????????????????????? |
| **????????????** | Card, Table, Badge, Progress, Skeleton, Tooltip | ????????????????????????????????????????????? |
| **????????????** | Dialog, Toast, Alert, ErrorState, LoadingState, EmptyState | ?????????????????????????????????????????? |
| **????????????** | Breadcrumb, Tabs, Menu, Pagination, Separator | ????????????????????????????????????????????? |
| **????????????** | LineChart, BarChart, AreaChart, CandlestickChart, ScoreRadar, FactorHeatmap | ??????????????????????????????????????? |

### 3. ?????????????????????

- [ ] ?????????????????????????????????????????? `theme.tokens.ts` ?????? COLOR_TOKENS
- [ ] ???????????????padding/margin/gap ???????????? SPACING_TOKENS ??? Tailwind ?????????
- [ ] ??????????????????????????????????????????????????????
- [ ] ???????????????border-radius ?????????????????? THEME_TOKENS.radius
- [ ] ???????????????box-shadow ????????????
- [ ] ???????????????transition/animation ????????????

---

## ????????????

### Phase 1: UI ?????????????????????P0 - ???????????????

#### Task 1.1: Button ??????????????????

**Files:**
- Test: `src/components/atoms/Button.test.tsx`
- Component: `src/components/atoms/Button.tsx`

- [ ] **Step 1: ?????? Button ??????????????????**

```typescript
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import { Button } from './Button'

describe('Button', () => {
  it('renders with primary variant by default', () => {
    render(<Button>????????????</Button>)
    const button = screen.getByRole('button', { name: /????????????/i })
    expect(button).toBeInTheDocument()
    expect(button).toHaveClass('bg-primary')
  })

  it('handles click events', () => {
    const handleClick = vi.fn()
    render(<Button onClick={handleClick}>?????????</Button>)
    fireEvent.click(screen.getByRole('button', { name: /?????????/i }))
    expect(handleClick).toHaveBeenCalledTimes(1)
  })

  it('shows loading state', () => {
    render(<Button isLoading>?????????</Button>)
    const button = screen.getByRole('button')
    expect(button).toBeDisabled()
  })

  it('renders all variants correctly', () => {
    const variants = ['primary', 'secondary', 'outline', 'ghost', 'danger', 'success'] as const
    variants.forEach((variant) => {
      const { container } = render(<Button variant={variant}>{variant}</Button>)
      expect(container.firstChild).toBeInTheDocument()
    })
  })

  it('renders all sizes correctly', () => {
    const sizes = ['sm', 'md', 'lg'] as const
    sizes.forEach((size) => {
      const { container } = render(<Button size={size}>{size}</Button>)
      expect(container.firstChild).toBeInTheDocument()
    })
  })

  it('supports asChild prop for composition', () => {
    render(
      <Button asChild>
        <a href="/test">????????????</a>
      </Button>
    )
    const link = screen.getByRole('link', { name: /????????????/i })
    expect(link).toBeInTheDocument()
  })

  it('is disabled when disabled prop is true', () => {
    render(<Button disabled>????????????</Button>)
    expect(screen.getByRole('button')).toBeDisabled()
  })

  it('has proper focus styles', () => {
    render(<Button>????????????</Button>)
    const button = screen.getByRole('button')
    button.focus()
    expect(button).toHaveFocus()
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/components/ui/Button.test.tsx
```

???????????????????????????

- [ ] **Step 3: ?????? Button ????????????????????????**

??????????????????
- ?????? variant ???????????????????????? theme.tokens.ts
- hover/active/focus ????????????????????????????????????
- disabled ?????????????????????????????????????????? 0.5???
- ?????? size ????????????????????? 4px ????????????

- [ ] **Step 4: ????????????**

```bash
git add src/components/ui/Button.test.tsx
git commit -m "test: add comprehensive Button component tests"
```

---

#### Task 1.2: Input ??????????????????

**Files:**
- Test: `src/components/atoms/Input.test.tsx`
- Component: `src/components/atoms/Input.tsx`

- [ ] **Step 1: ?????? Input ??????????????????**

```typescript
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import { Input } from './Input'

describe('Input', () => {
  it('renders correctly', () => {
    render(<Input placeholder="?????????" />)
    expect(screen.getByPlaceholderText('?????????')).toBeInTheDocument()
  })

  it('handles value changes', () => {
    const handleChange = vi.fn()
    render(<Input onChange={handleChange} />)
    const input = screen.getByRole('textbox')
    fireEvent.change(input, { target: { value: '?????????' } })
    expect(handleChange).toHaveBeenCalled()
  })

  it('supports different types', () => {
    const { rerender } = render(<Input type="text" />)
    expect(screen.getByRole('textbox')).toHaveAttribute('type', 'text')
    
    rerender(<Input type="password" />)
    expect(screen.getByLabelText('')).toHaveAttribute('type', 'password')
  })

  it('shows disabled state', () => {
    render(<Input disabled />)
    expect(screen.getByRole('textbox')).toBeDisabled()
  })

  it('has proper focus styles', () => {
    render(<Input />)
    const input = screen.getByRole('textbox')
    input.focus()
    expect(input).toHaveFocus()
  })

  it('supports file input', () => {
    render(<Input type="file" />)
    const input = screen.getByRole('textbox')
    expect(input).toHaveAttribute('type', 'file')
  })

  it('applies custom className', () => {
    render(<Input className="custom-class" />)
    expect(screen.getByRole('textbox')).toHaveClass('custom-class')
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/components/ui/Input.test.tsx
```

- [ ] **Step 3: ?????? Input ???????????????????????????**

????????????
- ???????????? required ??????
- ????????? pattern ??????
- ????????????????????????????????????????????????????????????
- placeholder ??????????????????

- [ ] **Step 4: ????????????**

```bash
git add src/components/ui/Input.test.tsx
git commit -m "test: add comprehensive Input component tests"
```

---

#### Task 1.3: Dialog ??????????????????

**Files:**
- Test: `src/components/molecules/Dialog.test.tsx`
- Component: `src/components/molecules/Dialog.tsx`

- [ ] **Step 1: ?????? Dialog ??????????????????**

```typescript
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription } from './Dialog'

describe('Dialog', () => {
  it('renders when open is true', () => {
    render(
      <Dialog open={true}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>????????????</DialogTitle>
            <DialogDescription>????????????</DialogDescription>
          </DialogHeader>
        </DialogContent>
      </Dialog>
    )
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })

  it('does not render when open is false', () => {
    render(
      <Dialog open={false}>
        <DialogContent>
          <DialogTitle>????????????</DialogTitle>
        </DialogContent>
      </Dialog>
    )
    expect(screen.queryByText('????????????')).not.toBeInTheDocument()
  })

  it('calls onOpenChange when close button is clicked', async () => {
    const handleOpenChange = vi.fn()
    render(
      <Dialog open={true} onOpenChange={handleOpenChange}>
        <DialogContent>
          <DialogTitle>??????????????????</DialogTitle>
        </DialogContent>
      </Dialog>
    )
    
    const closeButton = screen.getByRole('button', { name: /close/i })
    fireEvent.click(closeButton)
    
    await waitFor(() => {
      expect(handleOpenChange).toHaveBeenCalledWith(false)
    })
  })

  it('closes on Escape key press', async () => {
    const handleOpenChange = vi.fn()
    render(
      <Dialog open={true} onOpenChange={handleOpenChange}>
        <DialogContent>
          <DialogTitle>ESC ????????????</DialogTitle>
        </DialogContent>
      </Dialog>
    )
    
    fireEvent.keyDown(screen.getByText('ESC ????????????'), { key: 'Escape' })
    
    await waitFor(() => {
      expect(handleOpenChange).toHaveBeenCalledWith(false)
    })
  })

  it('renders DialogHeader, DialogTitle, DialogDescription correctly', () => {
    render(
      <Dialog open={true}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>???????????????</DialogTitle>
            <DialogDescription>??????????????????</DialogDescription>
          </DialogHeader>
          <div>???????????????</div>
        </DialogContent>
      </Dialog>
    )
    
    expect(screen.getByText('???????????????')).toBeInTheDocument()
    expect(screen.getByText('??????????????????')).toBeInTheDocument()
    expect(screen.getByText('???????????????')).toBeInTheDocument()
  })

  it('has proper accessibility attributes', () => {
    render(
      <Dialog open={true}>
        <DialogContent>
          <DialogTitle>???????????????</DialogTitle>
        </DialogContent>
      </Dialog>
    )
    
    const dialog = screen.getByRole('dialog')
    expect(dialog).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/components/ui/Dialog.test.tsx
```

- [ ] **Step 3: ?????? Dialog ???????????????**

????????????
- ???????????????????????????
- ????????????????????????????????????
- ????????????????????????????????????
- ESC ??????????????????
- ???????????????????????????????????????????????????????????????????????????

- [ ] **Step 4: ????????????**

```bash
git add src/components/ui/Dialog.test.tsx
git commit -m "test: add comprehensive Dialog component tests"
```

---

#### Task 1.4: Card ??????????????????

**Files:**
- Test: `src/components/atoms/Card.test.tsx`
- Component: `src/components/atoms/Card.tsx`

- [ ] **Step 1: ?????? Card ??????????????????**

```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from './Card'

describe('Card', () => {
  it('renders Card correctly', () => {
    render(<Card data-testid="card">????????????</Card>)
    expect(screen.getByTestId('card')).toBeInTheDocument()
  })

  it('renders CardHeader, CardTitle, CardDescription correctly', () => {
    render(
      <Card>
        <CardHeader>
          <CardTitle>????????????</CardTitle>
          <CardDescription>????????????</CardDescription>
        </CardHeader>
      </Card>
    )
    
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })

  it('renders CardContent correctly', () => {
    render(
      <Card>
        <CardContent>??????????????????</CardContent>
      </Card>
    )
    
    expect(screen.getByText('??????????????????')).toBeInTheDocument()
  })

  it('renders CardFooter correctly', () => {
    render(
      <Card>
        <CardFooter>???????????????</CardFooter>
      </Card>
    )
    
    expect(screen.getByText('???????????????')).toBeInTheDocument()
  })

  it('applies custom className to all subcomponents', () => {
    render(
      <Card className="custom-card">
        <CardHeader className="custom-header">
          <CardTitle className="custom-title">??????</CardTitle>
        </CardHeader>
        <CardContent className="custom-content">??????</CardContent>
        <CardFooter className="custom-footer">??????</CardFooter>
      </Card>
    )
    
    expect(screen.getByText('??????').closest('div')).toHaveClass('custom-header')
    expect(screen.getByText('??????')).toHaveClass('custom-content')
    expect(screen.getByText('??????').closest('div')).toHaveClass('custom-footer')
  })

  it('has proper visual hierarchy', () => {
    render(
      <Card>
        <CardHeader>
          <CardTitle>????????????</CardTitle>
        </CardHeader>
      </Card>
    )
    
    const title = screen.getByText('????????????')
    expect(title.tagName).toBe('H3')
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/components/ui/Card.test.tsx
```

- [ ] **Step 3: ?????? Card ??????????????????**

????????????
- ???????????????????????? theme.tokens.ts ?????? border token
- ????????????????????? bg-card token
- ??????????????????
- ????????????????????? SPACING_TOKENS

- [ ] **Step 4: ????????????**

```bash
git add src/components/ui/Card.test.tsx
git commit -m "test: add comprehensive Card component tests"
```

---

### Phase 2: ???????????????????????????P0 - ???????????????

#### Task 2.1: ????????? Hub ????????????

**Files:**
- Test: `src/apps/input/InputApp.tsx`
- Page: `src/apps/input/InputApp.tsx`

- [ ] **Step 1: ?????? InputHubPage ????????????**

```typescript
import { render, screen } from '@testing-library/react'
import { BrowserRouter } from 'react-router'
import { describe, it, expect } from 'vitest'
import InputHubPage from '../InputHubPage'

const renderWithRouter = (component: React.ReactElement) => {
  return render(
    <BrowserRouter>
      {component}
    </BrowserRouter>
  )
}

describe('InputHubPage', () => {
  it('renders page title and description', () => {
    renderWithRouter(<InputHubPage />)
    expect(screen.getByText('?????????????????????')).toBeInTheDocument()
    expect(screen.getByText(/????????? ?? ???????????????/)).toBeInTheDocument()
  })

  it('renders breadcrumb navigation', () => {
    renderWithRouter(<InputHubPage />)
    expect(screen.getByText('??????')).toBeInTheDocument()
    expect(screen.getByText('?????????')).toBeInTheDocument()
  })

  it('renders all core modules', () => {
    renderWithRouter(<InputHubPage />)
    
    const expectedModules = [
      '????????????',
      '????????????',
      '????????????',
      '????????????',
      '???????????????',
    ]
    
    expectedModules.forEach((moduleName) => {
      expect(screen.getByText(moduleName)).toBeInTheDocument()
    })
  })

  it('renders expandable modules section', () => {
    renderWithRouter(<InputHubPage />)
    expect(screen.getByText('???????????????????????? V6 Pro???')).toBeInTheDocument()
    expect(screen.getByText('???????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })

  it('has working navigation links', () => {
    renderWithRouter(<InputHubPage />)
    
    const dashboardLink = screen.getByRole('link', { name: /????????????/i })
    expect(dashboardLink).toHaveAttribute('href', '/input')
    
    const bulkImportLink = screen.getByRole('link', { name: /????????????/i })
    // ???????????????????????????????????? InputDashboard Tabs????????????????????? /input/bulk-import
    expect(bulkImportLink).toHaveAttribute('href', '/input')
  })

  it('shows loading state when loading is true', () => {
    // Mock the store to return loading state
    // This requires mocking useInputHubStore
    renderWithRouter(<InputHubPage />)
    // Verify loading UI is shown
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/pages/input/__tests__/InputHubPage.test.tsx
```

- [ ] **Step 3: ????????????????????????**

????????????
- ???????????????????????????????????????
- ??????????????????????????????
- ??????"??????"?????????????????????????????????
- Badge ????????????????????????
- ???????????????????????????????????????????????????

- [ ] **Step 4: ????????????**

```bash
git add src/pages/input/__tests__/InputHubPage.test.tsx
git commit -m "test: add InputHubPage integration tests"
```

---

#### Task 2.2: ????????? Hub ????????????

**Files:**
- Test: `src/apps/analysis/AnalysisApp.tsx`
- Page: `src/apps/analysis/AnalysisApp.tsx`

- [ ] **Step 1: ?????? AnalysisHubPage ????????????**

```typescript
import { render, screen } from '@testing-library/react'
import { BrowserRouter } from 'react-router'
import { describe, it, expect } from 'vitest'
import AnalysisHubPage from '../AnalysisHubPage'

const renderWithRouter = (component: React.ReactElement) => {
  return render(
    <BrowserRouter>
      {component}
    </BrowserRouter>
  )
}

describe('AnalysisHubPage', () => {
  it('renders page title and description', () => {
    renderWithRouter(<AnalysisHubPage />)
    expect(screen.getByText('??????????????????')).toBeInTheDocument()
  })

  it('renders all core analysis modules', () => {
    renderWithRouter(<AnalysisHubPage />)
    
    const expectedModules = [
      'V4 ????????????',
      'V6 ????????????',
      'V6 ??????????????????',
      '????????????',
      '????????????',
      '????????????',
      '????????????',
      '??????????????????',
      '??????????????????',
    ]
    
    expectedModules.forEach((moduleName) => {
      expect(screen.getByText(moduleName)).toBeInTheDocument()
    })
  })

  it('has correct navigation paths', () => {
    renderWithRouter(<AnalysisHubPage />)
    
    const industryScoreLink = screen.getByRole('link', { name: /V4 ????????????/i })
    expect(industryScoreLink).toHaveAttribute('href', '/analysis/industry-score')
    
    const stockScoreLink = screen.getByRole('link', { name: /V6 ????????????/i })
    expect(stockScoreLink).toHaveAttribute('href', '/analysis/stock-score')
  })

  it('renders future modules section', () => {
    renderWithRouter(<AnalysisHubPage />)
    expect(screen.getByText('???????????????????????? V6 Pro???')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/pages/analysis/__tests__/AnalysisHubPage.test.tsx
```

- [ ] **Step 3: ????????????????????????**

????????????
- 9 ?????????????????????????????????
- ???????????????????????????????????????????????????
- ????????????????????????
- "?????????" Badge ??????????????????

- [ ] **Step 4: ????????????**

```bash
git add src/pages/analysis/__tests__/AnalysisHubPage.test.tsx
git commit -m "test: add AnalysisHubPage integration tests"
```

---

#### Task 2.3: ????????? Hub ????????????

**Files:**
- Test: `src/apps/trading/TradingApp.tsx`
- Page: `src/apps/trading/TradingApp.tsx`

- [ ] **Step 1: ?????? TradingHubPage ????????????**

```typescript
import { render, screen } from '@testing-library/react'
import { BrowserRouter } from 'react-router'
import { describe, it, expect } from 'vitest'
import TradingHubPage from '../TradingHubPage'

const renderWithRouter = (component: React.ReactElement) => {
  return render(
    <BrowserRouter>
      {component}
    </BrowserRouter>
  )
}

describe('TradingHubPage', () => {
  it('renders page title and description', () => {
    renderWithRouter(<TradingHubPage />)
    expect(screen.getByText('???????????????')).toBeInTheDocument()
  })

  it('renders all core trading modules', () => {
    renderWithRouter(<TradingHubPage />)
    
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })

  it('has correct navigation paths', () => {
    renderWithRouter(<TradingHubPage />)
    
    const signalsLink = screen.getByRole('link', { name: /????????????/i })
    expect(signalsLink).toHaveAttribute('href', '/trading')
    
    const holdingsLink = screen.getByRole('link', { name: /????????????/i })
    expect(holdingsLink).toHaveAttribute('href', '/trading/holdings')
  })

  it('renders future modules with badges', () => {
    renderWithRouter(<TradingHubPage />)
    
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('???????????????')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/pages/trading/__tests__/TradingHubPage.test.tsx
```

- [ ] **Step 3: ????????????**

```bash
git add src/pages/trading/__tests__/TradingHubPage.test.tsx
git commit -m "test: add TradingHubPage integration tests"
```

---

#### Task 2.4: ????????? Hub ????????????

**Files:**
- Test: `src/pages/output/__tests__/OutputHubPage.test.tsx`
- Page: `src/pages/output/OutputHubPage.tsx`

- [ ] **Step 1: ?????? OutputHubPage ????????????**

```typescript
import { render, screen } from '@testing-library/react'
import { BrowserRouter } from 'react-router'
import { describe, it, expect } from 'vitest'
import OutputHubPage from '../OutputHubPage'

const renderWithRouter = (component: React.ReactElement) => {
  return render(
    <BrowserRouter>
      {component}
    </BrowserRouter>
  )
}

describe('OutputHubPage', () => {
  it('renders page title', () => {
    renderWithRouter(<OutputHubPage />)
    expect(screen.getByText('?????????')).toBeInTheDocument()
  })

  it('renders all output modules', () => {
    renderWithRouter(<OutputHubPage />)
    
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })

  it('shows badges for placeholder modules', () => {
    renderWithRouter(<OutputHubPage />)
    
    const badges = screen.getAllByText('?????????')
    expect(badges.length).toBeGreaterThan(0)
  })

  it('has correct navigation paths', () => {
    renderWithRouter(<OutputHubPage />)
    
    const researchLink = screen.getByRole('link', { name: /????????????/i })
    expect(researchLink).toHaveAttribute('href', '/output/research')
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/pages/output/__tests__/OutputHubPage.test.tsx
```

- [ ] **Step 3: ????????????**

```bash
git add src/pages/output/__tests__/OutputHubPage.test.tsx
git commit -m "test: add OutputHubPage integration tests"
```

---

#### Task 2.5: ????????? Hub ????????????

**Files:**
- Test: `src/apps/command/CommandApp.tsx`
- Page: `src/apps/command/CommandApp.tsx`

- [ ] **Step 1: ?????? CommandHubPage ????????????**

```typescript
import { render, screen } from '@testing-library/react'
import { BrowserRouter } from 'react-router'
import { describe, it, expect } from 'vitest'
import CommandHubPage from '../CommandHubPage'

const renderWithRouter = (component: React.ReactElement) => {
  return render(
    <BrowserRouter>
      {component}
    </BrowserRouter>
  )
}

describe('CommandHubPage', () => {
  it('renders page title', () => {
    renderWithRouter(<CommandHubPage />)
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })

  it('renders all command modules', () => {
    renderWithRouter(<CommandHubPage />)
    
    expect(screen.getByText('AI ?????????')).toBeInTheDocument()
    expect(screen.getByText('MCP Server ??????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })

  it('has correct navigation paths', () => {
    renderWithRouter(<CommandHubPage />)
    
    const agentLink = screen.getByRole('link', { name: /AI ?????????/i })
    expect(agentLink).toHaveAttribute('href', '/command/agents')
    
    const mcpLink = screen.getByRole('link', { name: /MCP Server ??????/i })
    expect(mcpLink).toHaveAttribute('href', '/command/mcp-servers')
  })

  it('renders future modules section', () => {
    renderWithRouter(<CommandHubPage />)
    
    expect(screen.getByText('???????????????????????? V6 Pro???')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
    expect(screen.getByText('????????????')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm test -- src/pages/command/__tests__/CommandHubPage.test.tsx
```

- [ ] **Step 3: ????????????**

```bash
git add src/pages/command/__tests__/CommandHubPage.test.tsx
git commit -m "test: add CommandHubPage integration tests"
```

---

### Phase 3: ????????????????????????P1 - ???????????????

#### Task 3.1: ???????????????????????????

**Files:**
- Audit Script: `scripts/audit-color-tokens.ts`
- Target: ?????? `src/**/*.tsx` ??????

- [ ] **Step 1: ????????????????????????**

```typescript
// scripts/audit-color-tokens.ts
import { glob } from 'glob'
import { readFile } from 'fs/promises'
import { COLOR_TOKENS } from '../src/constants/theme.tokens'

const VALID_COLORS = new Set([
  ...Object.values(COLOR_TOKENS).map((token) => token.hex),
  ...Object.values(COLOR_TOKENS).map((token) => token.tailwind),
  ...Object.values(COLOR_TOKENS).map((token) => token.bgClass),
])

const HEX_REGEX = /#[0-9a-fA-F]{6}/g
const TAILWIND_COLOR_REGEX = /(?:text|bg|border)-(?:red|blue|green|yellow|purple|orange|gray|slate|emerald|amber|cyan|teal|indigo|pink|violet|fuchsia|lime|rose|sky|zinc|neutral|stone)-\d{3}/g

async function auditColorTokens(): Promise<void> {
  const files = await glob('src/**/*.{tsx,ts}', { ignore: ['**/*.test.{tsx,ts}', '**/node_modules/**'] })
  
  let violations = 0
  
  for (const file of files) {
    const content = await readFile(file, 'utf-8')
    const lines = content.split('\n')
    
    lines.forEach((line, index) => {
      const hexMatches = line.match(HEX_REGEX)
      if (hexMatches) {
        hexMatches.forEach((hex) => {
          if (!VALID_COLORS.has(hex)) {
            console.error(`? ${file}:${index + 1} - ???????????????: ${hex}`)
            violations++
          }
        })
      }
    })
  }
  
  if (violations > 0) {
    console.error(`\n?????? ${violations} ???????????????`)
    process.exit(1)
  } else {
    console.log('? ??????????????????????????????')
  }
}

auditColorTokens()
```

- [ ] **Step 2: ??????????????????**

```bash
npx tsx scripts/audit-color-tokens.ts
```

- [ ] **Step 3: ???????????????????????????**

????????????????????????
1. ?????????????????????????????? token?????? `COLOR_TOKENS.textPrimary`???
2. ?????????????????????
3. ??????????????????????????????

- [ ] **Step 4: ????????????**

```bash
git add -A
git commit -m "fix: replace hardcoded colors with theme tokens"
```

---

#### Task 3.2: ???????????????????????????

**Files:**
- Audit Script: `scripts/audit-spacing.ts`
- Target: ?????? `src/**/*.tsx` ??????

- [ ] **Step 1: ????????????????????????**

```typescript
// scripts/audit-spacing.ts
import { glob } from 'glob'
import { readFile } from 'fs/promises'
import { SPACING_TOKENS } from '../src/constants/theme.tokens'

const VALID_SPACING_VALUES = new Set([
  '4px', '8px', '12px', '16px', '24px', '32px', '48px',
  '0.25rem', '0.5rem', '0.75rem', '1rem', '1.5rem', '2rem', '3rem',
])

const INLINE_SPACING_REGEX = /(?:padding|margin|gap|top|right|bottom|left):\s*(\d+px)/g

async function auditSpacing(): Promise<void> {
  const files = await glob('src/**/*.{tsx,ts}', { ignore: ['**/*.test.{tsx,ts}', '**/node_modules/**'] })
  
  let violations = 0
  
  for (const file of files) {
    const content = await readFile(file, 'utf-8')
    const lines = content.split('\n')
    
    lines.forEach((line, index) => {
      const matches = line.match(INLINE_SPACING_REGEX)
      if (matches) {
        matches.forEach((match) => {
          const value = match.split(':')[1].trim()
          if (!VALID_SPACING_VALUES.has(value)) {
            console.error(`? ${file}:${index + 1} - ???????????????: ${value}`)
            violations++
          }
        })
      }
    })
  }
  
  if (violations > 0) {
    console.error(`\n?????? ${violations} ???????????????`)
    process.exit(1)
  } else {
    console.log('? ??????????????????????????????')
  }
}

auditSpacing()
```

- [ ] **Step 2: ??????????????????**

```bash
npx tsx scripts/audit-spacing.ts
```

- [ ] **Step 3: ???????????????????????????**

- [ ] **Step 4: ????????????**

```bash
git add -A
git commit -m "fix: standardize spacing to 4px grid system"
```

---

#### Task 3.3: ??????????????????

**Files:**
- Manual Review Checklist

- [ ] **Step 1: ???????????????????????????**

????????????
- ?????????????????????????????????h1: 2xl, h2: xl, h3: lg???
- ?????????????????????????????????base, sm???
- ???????????????????????? muted-foreground ??????
- ???????????????????????????bold, semibold, normal???

- [ ] **Step 2: ????????????????????????**

????????????
- ???????????????????????????leading-none, leading-tight???
- ???????????????????????????leading-normal, leading-relaxed???
- ????????????????????????tracking-tight, tracking-normal???

- [ ] **Step 3: ??????????????????????????????**

????????????????????????????????????

---

### Phase 4: ?????????????????????P1 - ???????????????

#### Task 4.1: ??????????????????

**Files:**
- Component: `src/components/organisms/shared/PageSkeleton.tsx`
- Target: ??????????????????

- [ ] **Step 1: ?????????????????????????????????**

????????????
- ?????????????????????????????? Suspense + PageSkeleton
- PageSkeleton ???????????????????????????
- ???????????????????????????> 300ms ???????????????

- [ ] **Step 2: ?????? PageSkeleton ??????**

```typescript
// ?????? PageSkeleton ???????????????
export function PageSkeleton(): React.JSX.Element {
  return (
    <div className="space-y-4 p-4" data-testid="page-skeleton">
      {/* ???????????? */}
      <div className="h-8 w-48 animate-pulse rounded bg-muted" />
      
      {/* ???????????? - ??????????????? */}
      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
        {Array.from({ length: 6 }).map((_, i) => (
          <Card key={i}>
            <CardHeader>
              <div className="h-5 w-24 animate-pulse rounded bg-muted" />
            </CardHeader>
            <CardContent>
              <div className="space-y-2">
                <div className="h-4 w-full animate-pulse rounded bg-muted" />
                <div className="h-4 w-3/4 animate-pulse rounded bg-muted" />
              </div>
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: ?????????????????????????????????**

??????????????????????????? ErrorBoundary ?????????

- [ ] **Step 4: ????????????**

```bash
git add src/components/PageSkeleton.tsx
git commit -m "feat: enhance PageSkeleton visual feedback"
```

---

#### Task 4.2: ??????????????????

**Files:**
- Component: `src/components/molecules/ErrorState.tsx`
- Target: ????????????????????????

- [ ] **Step 1: ?????? ErrorState ??????**

```typescript
import { AlertCircle } from 'lucide-react'
import { Button } from './Button'

interface ErrorStateProps {
  title?: string
  description?: string
  onRetry?: () => void
}

export function ErrorState({ 
  title = '?????????', 
  description = '???????????????', 
  onRetry 
}: ErrorStateProps): React.JSX.Element {
  return (
    <div className="flex flex-col items-center justify-center p-8 text-center">
      <AlertCircle className="h-12 w-12 text-destructive mb-4" />
      <h3 className="text-lg font-semibold mb-2">{title}</h3>
      <p className="text-sm text-muted-foreground mb-4">{description}</p>
      {onRetry && (
        <Button onClick={onRetry} variant="outline">
          ??????
        </Button>
      )}
    </div>
  )
}
```

- [ ] **Step 2: ???????????????????????????????????????**

????????????
- ????????????????????????????????? ErrorState
- ????????????????????????
- ????????????????????????

- [ ] **Step 3: ????????????**

```bash
git add src/components/ui/ErrorState.tsx
git commit -m "feat: improve ErrorState component with retry action"
```

---

#### Task 4.3: ???????????????

**Files:**
- Component: `src/components/molecules/EmptyState.tsx`
- Target: ??????????????????

- [ ] **Step 1: ?????? EmptyState ??????**

```typescript
import { Inbox } from 'lucide-react'
import { Button } from './Button'

interface EmptyStateProps {
  icon?: React.ElementType
  title?: string
  description?: string
  action?: {
    label: string
    onClick: () => void
  }
}

export function EmptyState({
  icon: Icon = Inbox,
  title = '????????????',
  description = '????????????????????????',
  action,
}: EmptyStateProps): React.JSX.Element {
  return (
    <div className="flex flex-col items-center justify-center p-12 text-center">
      <div className="flex h-16 w-16 items-center justify-center rounded-full bg-muted mb-4">
        <Icon className="h-8 w-8 text-muted-foreground" />
      </div>
      <h3 className="text-lg font-semibold mb-2">{title}</h3>
      <p className="text-sm text-muted-foreground mb-4 max-w-sm">{description}</p>
      {action && (
        <Button onClick={action.onClick}>
          {action.label}
        </Button>
      )}
    </div>
  )
}
```

- [ ] **Step 2: ????????????????????????????????????**

????????????
- ??????????????????????????? EmptyState
- ??????????????????????????????"?????????????????????"???
- ???????????????????????????

- [ ] **Step 3: ????????????**

```bash
git add src/components/ui/EmptyState.tsx
git commit -m "feat: enhance EmptyState with action support"
```

---

### Phase 5: ????????????????????????P2 - ???????????????

#### Task 5.1: ?????????????????????

**Files:**
- Test: `tests/e2e/responsive.spec.ts`

- [ ] **Step 1: ???????????????????????????**

```typescript
// tests/e2e/responsive.spec.ts
import { test, expect } from '@playwright/test'

const VIEWPORTS = {
  mobile: { width: 375, height: 667 },
  tablet: { width: 768, height: 1024 },
  desktop: { width: 1920, height: 1080 },
}

test.describe('?????????????????????', () => {
  test('????????? - ???????????????', async ({ page }) => {
    await page.setViewportSize(VIEWPORTS.mobile)
    await page.goto('/input/hub')
    
    // ???????????????????????????
    const sidebar = page.locator('aside')
    await expect(sidebar).toBeHidden()
    
    // ??????????????????????????????
    const menuButton = page.getByRole('button', { name: /??????/i })
    await expect(menuButton).toBeVisible()
  })

  test('????????? - ??????????????????', async ({ page }) => {
    await page.setViewportSize(VIEWPORTS.tablet)
    await page.goto('/analysis/hub')
    
    // ??????????????????????????? 2 ???
    const grid = page.locator('.grid')
    await expect(grid).toHaveClass(/md:grid-cols-2/)
  })

  test('????????? - ????????????', async ({ page }) => {
    await page.setViewportSize(VIEWPORTS.desktop)
    await page.goto('/trading/hub')
    
    // ???????????????????????????
    const sidebar = page.locator('aside')
    await expect(sidebar).toBeVisible()
    
    // ??????????????????????????? 3 ???
    const grid = page.locator('.grid')
    await expect(grid).toHaveClass(/lg:grid-cols-3/)
  })
})
```

- [ ] **Step 2: ?????????????????????**

```bash
npm run test:e2e -- tests/e2e/responsive.spec.ts
```

- [ ] **Step 3: ?????????????????????**

????????????
- ??????????????????????????????
- ???????????????????????????
- ?????????????????????????????????
- ??????????????????????????????
- ??????????????????????????????

- [ ] **Step 4: ????????????**

```bash
git add -A
git commit -m "fix: improve responsive design across all viewports"
```

---

### Phase 6: ?????????????????????P2 - ???????????????

#### Task 6.1: ARIA ????????????

- [ ] **Step 1: ????????????????????????**

????????????
- ?????????????????????????????? aria-label
- ???????????????????????????????????????
- ???????????????????????????????????? label
- ???????????????????????? role="dialog"

- [ ] **Step 2: ??????????????????**

????????????
- ?????????????????? Tab ???????????????????????????
- ?????????????????? Enter/Space ????????????
- ?????????????????? ESC ???????????????
- ????????????????????????

- [ ] **Step 3: ?????????????????????**

?????????????????????
- ????????????????????????????????? >= 4.5:1
- ???????????????????????? >= 3:1
- ?????????????????????????????????

- [ ] **Step 4: ?????????????????????**

????????????????????????????????????????????????

---

## ??????????????????

### ????????????P0 - ????????????

1. ? Task 1.1: Button ????????????
2. ? Task 1.2: Input ????????????
3. ? Task 1.3: Dialog ????????????
4. ? Task 1.4: Card ????????????
5. ? Task 2.1: ????????? Hub ????????????
6. ? Task 2.2: ????????? Hub ????????????
7. ? Task 2.3: ????????? Hub ????????????
8. ? Task 2.4: ????????? Hub ????????????
9. ? Task 2.5: ????????? Hub ????????????

**????????????????????????????????????**

### ????????????P1 - ???????????????

10. Task 3.1: ???????????????????????????
11. Task 3.2: ???????????????????????????
12. Task 3.3: ??????????????????
13. Task 4.1: ??????????????????
14. Task 4.2: ??????????????????
15. Task 4.3: ???????????????

**????????????????????????????????????**

### ????????????P2 - ???????????????

16. Task 5.1: ?????????????????????
17. Task 6.1: ??????????????????

---

## ??????????????????

### ???????????????

- [ ] ??????????????????????????????????????????
- [ ] ??????????????????????????????
- [ ] ??????????????????????????????
- [ ] ??????????????????????????????

### ???????????????

- [ ] ?????????????????? theme.tokens.ts
- [ ] ?????????????????? 4px ????????????
- [ ] ???????????????????????????
- [ ] ?????????????????????

### ????????????

- [ ] ????????????????????????
- [ ] ????????????????????????
- [ ] ????????????????????????
- [ ] ?????????????????????

### ????????????

- [ ] ????????????????????? > 80%
- [ ] ??? TypeScript ????????????
- [ ] ??? ESLint ??????
- [ ] ??????????????????

---

## ????????????

- [shadcn/ui ?????????](https://ui.shadcn.com/)
- [Tailwind CSS ??????](https://tailwindcss.com/docs)
- [React ????????????](https://react.dev/learn)
- [Web ??????????????????](https://www.w3.org/WAI/tutorials/)

---

## ????????????

| ?????? | ?????? | ???????????? |
|------|------|---------|
| 2026-07-04 | v1.0.0 | ???????????????????????? UI ????????????????????? |
