---
title: input-contract.md ?? ????????????????????
type: reference
domain: project
phase: design
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?????????? `input` ???????????????????????????????????????????? ??????`./services-catalog.md`??24 ????????????`../../AGENTS.md` ??????????????????"
tags: [project, contract, reference]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-PROJ-228
referenced_by: [V9-DOC-META-000, V9-DOC-PROJ-176, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# input-contract.md ?? ????????????????????

> **????**?????? `input` ????????????????????????????????????????????  
> **????**??`./services-catalog.md`??24 ????????????`../../AGENTS.md` ??????????????????

---

## 1. ????????

### 1.1 ????????

1. **????????????????**?????? `addStock()` ??????????????????????????????????????????????????????`stocks` store?????????????????????????????? / K ????????
2. **??????????????????**????????????????????CSV/JSON/Excel ???????????????????? `importStocks()` / `importStocksWithProgress()` ??????????????????????????????????valid / duplicate / invalid????
3. **??????????????????????**?????????? mock ???????? `searchStocks()` ?????????????? `getHotSectors()` ????????????????????????????????????????????????
4. **??????????????**?????????????? JSON ????????????`exportPool`????????????????`importPool`??????????????/??????????????????

### 1.2 ????????

| ???? | ???? |
|------|------|
| ?????? | `src/services/`?????????? |
| ???????? | ???????? `core/`??`data/`??`lib/`?????????? |
| ???????? | ???????? IndexedDB?????? `DataBridge.forward()`?? |
| ???????? | `store/`????????????`pages/`?????????????????????????? |

### 1.3 ????????????????

| ???????? | ???? | ?????? |
|----------|------|--------|
| `fetcher` (useCase) | ??????????????/???????????? | `fetchBasicDataUseCase` / `fetchKlineDataUseCase` ?? `inputService`??`addStock` ???????????????? |
| `stockpool` (store/module) | ?????????????????? | `inputService` ?? `DataBridge.forward()` ?? `stocks` store ?? `stockpool` / `inputHubStore` |
| `scoring` / `analysis` | ?????????????????????????? | ????????????????????????????????/?????? |

---

## 2. ????????

### 2.1 ??????????TypeScript Interface??

```typescript
// ??????src/services/input/inputService.ts

export interface AddStockInput {
  symbol: string
  name: string
}

export interface AddStockOptions {
  /** ???????????????????????? AKShare ???????? */
  fetchBasicAfterAdd?: boolean
  /** ???????????????????????? AKShare K?????? */
  fetchKlineAfterAdd?: boolean
  /** ???????????????????????????????????? */
  group?: string
}

export type StockSearchResult = MockStock

export interface PoolExportPayload {
  version: 'v9-pool-export-1'
  exportedAt: number
  stocks: Stock[]
}

export interface PoolImportResult {
  success: number
  failed: number
  errors: string[]
}

// ??????src/services/input/batchImportParsers.ts

export type BulkImportRowStatus = 'valid' | 'duplicate' | 'invalid'

export interface BulkImportRow {
  code: string
  name: string
  symbol: string
  status: BulkImportRowStatus
  statusReason?: string
}

export interface BulkImportResult {
  total: number
  success: number
  failed: number
  errors: Array<{ row: number; raw: string; error: string }>
  stocks: Stock[]
}

// ??????src/services/input/batchImportExecutor.ts

export interface ImportStocksOptions extends AddStockOptions {
  /** ???????????????????????????? true?? */
  skipDuplicates?: boolean
}

// ??????src/services/input/hotSectorService.ts

export interface HotSectorStock {
  symbol: string
  name: string
}

export interface HotSector {
  code: string
  name: string
  score: number
  trend: 'up' | 'down' | 'neutral'
  factors: {
    momentum: number
    fundFlow: number
    valuation: number
    sentiment: number
  }
  stocks: HotSectorStock[]
}

export interface AddHotSectorStockResult {
  added: Stock[]
  failed: Array<{ symbol: string; error: string }>
}

export type AddHotSectorOptions = AddStockOptions

// ??????src/services/input/mockStockLibrary.ts

export interface MockStock {
  symbol: string
  name: string
  industry: string
  pe?: number
  pb?: number
  marketCap?: number
}
```

### 2.2 ??????????

| ???? | ???? | ???? | ???????? |
|------|------|------|----------|
| `addStock()` | `(input: AddStockInput, options?: AddStockOptions) => Promise<DataLayerResult<Stock>>` | ????????????????????????????????????/K?????? | `addStockInFlight` ???????? + logger ???? + ???????????? |
| `addStockFromSearch()` | `(result: StockSearchResult, options?: AddStockOptions) => Promise<DataLayerResult<Stock>>` | ?????????????????? | ???? `addStock()` |
| `searchStocks()` | `(query: string) => StockSearchResult[]` | ???? mock ????????????????/????/?????? | ?????????????????? |
| `exportPool()` | `(status?: ResearchStatus) => Promise<DataLayerResult<PoolExportPayload>>` | ???????????? JSON Payload | `DataBridge.query()` ???????? |
| `importPool()` | `(payload: PoolExportPayload) => Promise<DataLayerResult<PoolImportResult>>` | ?????????????????????????????????? | ???????? + ???? try/catch ???????? |
| `listStocks()` | `() => Promise<DataLayerResult<Stock[]>>` | ???????????????? | `DataBridge.query()` ???????? |
| `listStocksByStatus()` | `(status: ResearchStatus) => Promise<DataLayerResult<Stock[]>>` | ?????????????????? | `DataBridge.query()` ???????? |
| `parseBulkInput()` | `(text: string) => BulkImportRow[]` | ?????????????????????????????? | ???????????? `invalid` |
| `parseFile()` | `(file: File) => Promise<BulkImportRow[]>` | ??????????????????CSV/JSON/Excel?? | ?????????????????? |
| `detectDuplicates()` | `(rows: BulkImportRow[], existingSymbols: Set<string>) => BulkImportRow[]` | ?????????????????????? | ???????????????? |
| `importStocks()` | `(rows: BulkImportRow[], options?: ImportStocksOptions) => Promise<DataLayerResult<BulkImportResult>>` | ???????????????????? | ???????????? `errors`???????? |
| `importStocksWithProgress()` | `(rows: BulkImportRow[], options?: ImportStocksOptions, onProgress?: fn) => Promise<DataLayerResult<BulkImportResult>>` | ???????????????????? | ???????????????? |
| `downloadTemplate()` | `() => void` | ???? CSV ???????? | ?????? logger ???? |
| `getHotSectors()` | `() => HotSector[]` | ?????????????????????? | ?????????? |
| `getHotSectorByCode()` | `(code: string) => HotSector \| undefined` | ?????????????????? | ?????????? `undefined` |
| `addHotSectorStocks()` | `(sectorCode: string, options?: AddHotSectorOptions) => Promise<DataLayerResult<AddHotSectorStockResult>>` | ?????????????????????????? | ???????? `addStock()` ???????? |
| `addHotSectorStock()` | `(sectorCode: string, symbol: string, options?: AddHotSectorOptions) => Promise<DataLayerResult<Stock>>` | ?????????????????????? | ???? `addStock()` |

### 2.3 ????????

| ?????? | ?????? | ?????? | ???? |
|--------|--------|--------|------|
| `BATCH_IMPORT_COMPLETED` | `batchImportExecutor` | `poolStore` / `inputHubStore` ???????? | ???????????????????????? `{ total, success, failed }` |

---

## 3. ??????

```
[???????? / ???? / ????????]
    ??
inputService.addStock() / batchImportExecutor.importStocks()
    ?? (DataBridge.forward() ?? addStock ????????)
DataBridge ?? routeToDB() ?? dataLayer ?? IndexedDB
    ?? (eventBus.emit('BATCH_IMPORT_COMPLETED'))
inputHubStore / poolStore (Zustand + withBroadcast)
    ??
components/pages (???? Store ????)
```

**????**??
- ???????????? `inputService.addStock()` ???? `Envelope` ?????? `dataBridge.forward()` ??????
- ???????????? `batchImportExecutor` ???????? `addStock()`???????????? `DataBridge`??
- ?????????????? `eventBus.emit('BATCH_IMPORT_COMPLETED')` ?????????????? Store ?????? UI??

---

## 4. ??????????

### 4.1 ????????????lib/??

| ???? | ???? | ???? |
|------|------|------|
| logger | `@/lib/logger` | ????????????????`[inputService]` / `[batchImport]` ?????? |
| eventBus | `@/lib/eventBus` | ???????????????????? |

### 4.2 ????????????

| ???? | ???? | ???? |
|------|------|------|
| dataBridge | `@/core/databridge` | ??????????????????`forward()` / `query()`?? |
| EnvelopeFactory | `@/core/envelope` | ???????????? |
| dataLayer | `@/data/dataLayer` | **????**??`batchImportExecutor` ?? `hotSectorService` ?????????? `dataLayer.stocks.get()` ???????????????????????????????? `DataBridge.query()`?? |
| fetcherOrchestrator.useCase | `@/services/useCase/fetcherOrchestrator.useCase` | `addStock` ??????????????/K?????? |

### 4.3 ??????

| ?????? | ?????? | ???? | ???? |
|--------|--------|------|------|
| `INPUT_CONFIG.bulkImport.maxRows` | `500` | ???????????????????? | `src/config/inputConfig.ts` |
| `INPUT_CONFIG.bulkImport.batchSize` | `20` | ?????????????????? | `src/config/inputConfig.ts` |
| `INPUT_CONFIG.bulkImport.batchIntervalMs` | `500` | ????????????ms?? | `src/config/inputConfig.ts` |
| `INPUT_CONFIG.search.minQueryLength` | `1` | ?????????????????? | `src/config/inputConfig.ts` |
| `INPUT_CONFIG.search.maxResults` | `20` | ???????????????? | `src/config/inputConfig.ts` |

---

## 5. ????????

| ???????? | ???? | ???? |
|----------|------|------|
| ???????? | `src/services/input/` | **????????** ?? ???????? `inputService`??`batchImportParsers`??`batchImportExecutor` ?????????????????????? |
| Store ???? | `tests/__tests__/inputHubStore.enhanced.test.ts` | `inputHubStore` ?????????????????????? Store ???? |
| ???????? | `tests/services/input.integration.test.ts` | **????????** ?? ???????? DataBridge ???????????????????????? |
| Mock ???? | `src/services/input/mockStockLibrary.ts` | ???? mock ?????????????????????????? |

---

## 6. ????????

| ???? | ???? | ???? | ???? |
|------|------|------|------|
| 2026-07-12 | v0.1.0 | ???????? | ?????? |

---

> **TODO[???? owner]**??
> 1. ???? `src/services/input/` ??????????`batchImportParsers`??`inputService`????
> 2. ???? `batchImportExecutor.ts` ?? `hotSectorService.ts` ?????????? `dataLayer.stocks.get()` ???????????????? `DataBridge.query()` ????????????????
> 3. ?????????? `tsc --noEmit` + `audit:layers` ??????
