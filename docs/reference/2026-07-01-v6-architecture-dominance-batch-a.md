---
title: 2026-07-01-v6-architecture-dominance-batch-a
type: reference
domain: architecture
phase: design
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?# ???? A???? V6 ???????? V9?????????? + ????????????????"
tags: [architecture, batch, reference]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-ARCH-023
referenced_by: [V9-DOC-PROJ-174, V9-DOC-META-000, V9-DOC-PROJ-176, V9-DOC-PROJ-182, docs/00-meta/deprecated-docs/old-versions/registry-index-v1.0.0-02-design.md, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

?# ???? A???? V6 ???????? V9?????????? + ????????????????

> **Status**: ? ????????2026-07-01??  
> **????????**: Subagent-Driven + ??????????/??????????????????  
> **????????**: ???????? 97/97 ??????ESLint 0 errors??`vite build` ??????

**Goal:** ?????????????????????? `src/services/scoring/v6-engine` ?? L-1~L8 ?????????????? `PortalShell` ?? `apps/*App` ?? `pages/*HubPage` ??????????????????????????????????????

**Architecture:** ???? `v6ScoreService.ts` ???????????????????? `symbol`?????? `{ success, data, error }`?????????????????? `createV6Engine()`??`PortalShell` ???? `HUB_APPS`???? `CabinApp` ?????????????????????? Hub ?????????????????????? `PortalShell`??

**Tech Stack:** TypeScript, React, Zustand, React Router, Vite

---

## ????????

| ???? | ???? |
|:---|:---|
| `src/services/scoring/v6ScoreService.ts` | ?????????????????????? `v6-engine`?????????????????????????? |
| `src/services/scoring/v6-engine/types.ts` | ???? `LayerInput` ???? `Stock` / `DailyQuotes` ?????????????? |
| `src/services/scoring/v6-engine/engine.ts` | ???????????????????????????? |
| `src/store/stockAnalysisStore.ts` | ?? `runV6Score` ???????? `v6-engine` ???????? |
| `src/store/analysisStore.ts` | ???? |
| `src/store/strategySnapshotStore.ts` | ???? |
| `src/portal/PortalShell.tsx` | ???? `HUB_APPS`?????? `ActiveApp` ???????? |
| `src/apps/input/InputApp.tsx` | ???????? `/input` ?? `/input/hub` ???????????? `InputApp` ?????? `InputHubPage` |
| `src/apps/analysis/AnalysisApp.tsx` | ???? |
| `src/apps/trading/TradingApp.tsx` | ???? |
| `src/apps/output/OutputApp.tsx` | ???? |
| `src/apps/command/CommandApp.tsx` | ???? |
| `src/config/routes.ts` | ?????????????? `/hub` ?????????? |

---

## Task 1???? `v6-engine` ?????? `Stock` / `DailyQuotes` ?? `LayerInput` ????????????????

**Files:**
- Modify: `src/services/scoring/v6-engine/types.ts`
- Modify: `src/services/scoring/v6-engine/index.ts`
- Test: `src/services/scoring/v6-engine/v6-engine.test.ts`

- [x] **Step 1: ???? `StockBasicData` ???? `Stock`**

???? `StockBasicData` ?????? `Stock` ?????????????????????? `types.ts` ??????????????????????????????

```typescript
// src/services/scoring/v6-engine/types.ts
import type { Stock, DailyQuotes } from '@/data/types'

export interface StockBasicData {
  symbol: string
  name?: string
  price?: number
  pe?: number
  pb?: number
  roe?: number
  marketCap?: number
}

export function stockToBasicData(stock: Stock): StockBasicData {
  return {
    symbol: stock.symbol,
    name: stock.name,
    price: stock.price,
    pe: stock.pe,
    pb: stock.pb,
    roe: stock.roe,
    marketCap: stock.marketCap,
  }
}

export interface QuoteData {
  currentPrice: number
  history: Array<{ date: string; close: number; volume?: number }>
}

export function quotesToQuoteData(quotes: DailyQuotes): QuoteData {
  return {
    currentPrice: quotes.history[quotes.history.length - 1]?.close ?? 0,
    history: quotes.history.map((q) => ({
      date: q.date,
      close: q.close,
      volume: q.volume,
    })),
  }
}
```

- [x] **Step 2: ?? barrel ????????????**

```typescript
// src/services/scoring/v6-engine/index.ts
export { stockToBasicData, quotesToQuoteData } from './types'
```

- [x] **Step 3: ???? v6-engine ????????????????????**

Run: `npx vitest run src/services/scoring/v6-engine/v6-engine.test.ts`
Expected: PASS

---

## Task 2?????? `v6ScoreService.ts`?????????? `v6-engine`

**Files:**
- Modify: `src/services/scoring/v6ScoreService.ts`
- Test: `src/services/scoring/v6ScoreService.test.ts`

- [x] **Step 1: ?????????? v6-engine ????**

??????????????????

```typescript
export interface RunV6ScoreResult {
  success: boolean
  data?: V6Score
  error?: string
}

export async function runV6Score(
  symbol: string,
  options?: { quotes?: DailyQuotes; signal?: AbortSignal },
): Promise<RunV6ScoreResult>
```

??????????????

```typescript
import { createV6Engine, stockToBasicData, quotesToQuoteData } from './v6-engine'
import { loadStockForAnalysis, loadDailyQuotesForAnalysis } from '@/services/analysis/scorePageService'

export async function runV6Score(
  symbol: string,
  options?: { quotes?: DailyQuotes; signal?: AbortSignal },
): Promise<RunV6ScoreResult> {
  logger.info(`[v6ScoreService] runV6Score start: ${symbol}`)
  try {
    if (options?.signal?.aborted) {
      return { success: false, error: 'Aborted' }
    }

    const [stock, quotes] = await Promise.all([
      loadStockForAnalysis(symbol),
      options?.quotes ? Promise.resolve(options.quotes) : loadDailyQuotesForAnalysis(symbol),
    ])

    if (!stock) {
      return { success: false, error: `?????????? ${symbol}` }
    }

    const engine = createV6Engine()
    const input: V6ScoreInput = {
      symbol,
      stock: stockToBasicData(stock),
      quote: quotes ? quotesToQuoteData(quotes) : undefined,
      financial: undefined,
      industryScore: undefined,
    }

    const composite = await engine.calculateComposite(input)

    const v6Score: V6Score = {
      symbol,
      score: composite.totalScore,
      dimensions: composite.layerScores.map((layer) => ({
        name: layer.layerId,
        score: layer.score,
        weight: layer.weight,
        rationale: layer.rationale,
      })),
      updatedAt: Date.now(),
    }

    logger.info(`[v6ScoreService] runV6Score complete: ${symbol}, score=${v6Score.score}`)
    return { success: true, data: v6Score }
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err)
    logger.error(`[v6ScoreService] runV6Score error: ${message}`)
    return { success: false, error: message }
  }
}
```

> ??????`V6ScoreInput`??`calculateComposite` ?????????????? `v6-engine/types.ts` ?? `engine.ts` ????????????????????????????????????

- [x] **Step 2: ??????????????????????????????????????**

???? `v6-engine` ???????????????????????????????????? `calculateFactorFromBasicData` ???????? fallback????????????????

- [x] **Step 3: ???? `v6ScoreService.test.ts` ????**

????????????
1. ???? `success: true`
2. `data.score` ?? 1~5 ????
3. `data.dimensions` ????
4. ???????????? `success: false`

- [x] **Step 4: ????????**

Run: `npx vitest run src/services/scoring/v6ScoreService.test.ts`
Expected: PASS

---

## Task 3?????? Store ??????????????????????

**Files:**
- Modify: `src/store/stockAnalysisStore.ts`
- Modify: `src/store/analysisStore.ts`
- Modify: `src/store/strategySnapshotStore.ts`
- Test: ???? store ????

- [x] **Step 1: ???? `runV6Score` ????????**

???????????? `RunV6ScoreResult { success, data?, error? }`??Store ??????????????????

- [x] **Step 2: ?? `analysisStore.ts` ??????????????**

??????????

```typescript
const result = await runV6Score(symbol)
if (result.success) {
  await get().loadScores()
  set({ loading: false })
}
```

???????????????????????????????????? `loadScores` ????/IndexedDB ??????

```typescript
const result = await runV6Score(symbol)
if (result.success && result.data) {
  const newScores = get().scores.map((s) =>
    s.symbol === symbol ? result.data! : s,
  )
  if (!newScores.some((s) => s.symbol === symbol)) {
    newScores.push(result.data!)
  }
  set({ scores: newScores, loading: false })
  logger.info(`[analysisStore] handleScore ????: ${symbol}`)
} else {
  // ... error
}
```

- [x] **Step 3: ???? Store ????**

Run: `npx vitest run src/store/stockAnalysisStore.test.ts src/store/analysisStore.test.ts src/store/strategySnapshotStore.test.ts`
Expected: PASS

---

## Task 4??PortalShell ??????????????

**Files:**
- Modify: `src/portal/PortalShell.tsx`
- Modify: `src/config/routes.ts`
- Modify: `src/apps/input/InputApp.tsx` ?? 5 ?? CabinApp

- [x] **Step 1: ?? `PortalShell.tsx` ?????? `HUB_APPS` ?? `isHubView` ????**

?????? 33-37 ???? lazy import ???? 47-53 ???? `HUB_APPS` ??????

???? `ActiveApp` ????????????

```typescript
const ActiveApp = SubApp ?? CABIN_APPS[activeCabin]
```

- [x] **Step 2: ???? `routes.ts`???? `/input`??`/analysis` ?????????????? PortalShell?????? CabinApp ???????? Hub ????**

????????????

```typescript
{ path: '/input', component: React.lazy(() => import('@/portal/PortalShell')), category: 'input' }
{ path: '/input/hub', component: React.lazy(() => import('@/portal/PortalShell')), category: 'input' }
```

???? `/input` ???? `PortalShell` ??????`InputApp` ???????????????? `InputHubPage`??

- [x] **Step 3: ?? `InputApp.tsx` ?????????? Hub ????**

???? `InputApp.tsx` ?????? `Routes` ??????????????????????????

```typescript
import { Routes, Route, Navigate } from 'react-router'
import InputHubPage from '@/pages/input/InputHubPage'

// ?? Routes ????
<Route path="hub" element={<InputHubPage />} />
<Route path="*" element={<Navigate to="hub" replace />} />
```

???????? `InputApp` ?????????????????? `InputApp` ????????????

```typescript
export default function InputApp(): JSX.Element {
  return <InputHubPage />
}
```

?????? 4 ?? CabinApp ??????????????

- [x] **Step 4: ???? `CABINS` ????????**

???????? `CABINS` ?? `path` ?? `/input/hub`?????? `/input`?????????????????????????? CabinApp??CabinApp ???????????? Hub??

- [x] **Step 5: ????????????????**

Run: `npx tsc --noEmit`
Expected: 0 errors

---

## Task 5??????????

- [x] **Step 1: ????????????**

Run: `npx tsc --noEmit`
Expected: 0 errors

- [x] **Step 2: ????????????**

Run: `npx vitest run src/services/scoring src/store src/portal`
Expected: PASS??????????????????????????

- [x] **Step 3: ESLint ????????????**

Run: `npx eslint src/services/scoring/v6ScoreService.ts src/portal/PortalShell.tsx src/apps/input/InputApp.tsx src/apps/analysis/AnalysisApp.tsx src/apps/trading/TradingApp.tsx src/apps/output/OutputApp.tsx src/apps/command/CommandApp.tsx src/store/stockAnalysisStore.ts src/store/analysisStore.ts src/store/strategySnapshotStore.ts`
Expected: 0 errors

- [x] **Step 4: ????????????**

Run: `npx vite build`
Expected: ????????????

---

## Spec Coverage Check

| ???? | ???????? |
|:---|:---|
| V6 ?????????????????? | Task 1, Task 2 |
| ???????????????? | Task 2 |
| Store ?????????? | Task 3 |
| PortalShell ???????????? | Task 4 |
| ?????????????????? PortalShell | Task 4 |
| ?????????????????? | Task 5 |

## Placeholder Scan

- ?? TBD/TODO
- ??????????????????????????????????
- ???????????????????????? Task 2 ????????????????????????

---

## ??????????

### ????????????

| ???? | ???? |
|:---|:---|
| `src/services/scoring/v6ScoreService.ts` | ???????? `createV6Engine().calculateAll()`???? `ALL_LAYER_IDS` ???????????????? `AbortSignal` ???? |
| `src/apps/command/CommandApp.tsx` | ???? `React.memo` ?? `ErrorBoundary` ???? |
| `tests/InputApp.test.tsx` | ???????????? `/input/dashboard` |
| `tests/AnalysisApp.test.tsx` | ???? `MemoryRouter`?????????? `/analysis/score` |
| `tests/TradingApp.test.tsx` | ???? `MemoryRouter`?????????? `/trading/sim` |
| `tests/CommandApp.test.tsx` | ???????????? `/command/monitor` |

### ????????

| ?????? | ???? |
|:---|:---|
| ???????? | 97/97 ???? |
| ESLint | 0 errors???????? `no-magic-numbers` warnings?? |
| `npx vite build` | ???? |
| `npx tsc --noEmit` | ???? 8 ?????????????? A ???? |

### ?????????? / ??????????????????

- **??????????**???????? DataBridge ??????????????????????????????????????Sector ????????????????????????`avgTurnover20d` ???????????????????? B/C/D ??????????
- **??????????**??????????????????????????????????????????????????????????????????????????????????????AbortSignal ???????????????????????? A ????????????
