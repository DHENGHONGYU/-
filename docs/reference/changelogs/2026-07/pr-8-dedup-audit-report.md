---
title: pr-8-dedup-audit-report
type: reference
domain: qa
phase: testing
tier: reference
status: active
maintainer: V9 Architecture Team
summary: "????????????: audit-split-quality v1.1???AP-007 ???????????????????????????????0.85?> ????????????: src/ ?????????????13 ???????>..."
tags: [qa, audit, changelog, report, log]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-QA-025
related_docs: [V9-DOC-PROJ-083, V9-DOC-ARCH-004]
referenced_by: [V9-DOC-PROJ-174, V9-DOC-META-000, V9-DOC-PROJ-264, V9-DOC-PROJ-176, V9-DOC-PROJ-085, docs/00-meta/deprecated-docs/old-versions/registry-index-v1.0.0-02-design.md, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# PR-8 ??????????????????????????????

> **Version**: v1.0.0 | **??????**: 2026-07-08
> **????????????**: audit-split-quality v1.1???AP-007 ???????????????????????????????0.85?> **????????????**: src/ ?????????????13 ???????> **????????????**: 8 ?AP-007 ???????????????????243 ???????8 ??????

---

## ????????????????
### 1.1 ????????????

| ?????? | ???????| ???????????? | ?????? |
|------|--------|----------|------|
| AP-007 | 8 | major | ?????????????????????????????????????> 0.85?|

### 1.2 ????????????

| ?????? | ?????? | ???????????? | ???????????? |
|------|------|----------|----------|
| **A. ????????????????* | 5 | ??????????????????????????? | ???? ?|
| **B. ??????????????????** | 2 | ???????????????????????? | ???? ?|
| **C. ????????????????????????** | 1 | ?????????????????? | ???? ?|

### 1.3 ?????????????
| ?????? | ???????????? | ?????????????| ?????????????|
|------|----------|------------|------------|
| `now` | 3 ??????dataLayer?| ???????????? db.ts re-export?| ??????? |
| `formatIndustryDelta` | 0 ?import | ???????????????????????? | ??????? |
| `formatIntelligentDelta` | 0 ?import | ???????????????????????? | ??????? |
| `toSafeNumber` | 3 ??????marketDataAdapters?| ??????lib/safeCoerce?| ??????? |
| `toSafeBoolean` | 3 ??????marketDataAdapters?| ??????lib/safeCoerce?| ??????? |
| `generateId` | 1 ??????dataLayerTradingStores?| ???????????? db.ts?| ?????? ??????????|
| `bySector` | 2 ???????????????????| ???????????? store ???????| ?????? ??????????|
| `analyze` | 0 ?import | ?????????????????????????| ?????? ???????|

---

## ??????A ???????????????????????????5 ???????????????????
### 2.1 `now()` ??????????????
**??????**:
- ???? `src/data/db-utils.ts:25-27`
- ???? `src/lib/format.ts`

**???????*: 1.00??????????????????

**?????????????*:

```typescript
// src/data/db-utils.ts:25-27
export function now(): number {
  return Date.now()
}

// src/utils/timeUtils.ts:31-33
export function now(): number {
  return Date.now()
}
```

**????????????**: ?????????????????????????????????????
**???????*:
- `src/data/db.ts:7` ?`export { generateId, now } from './db-utils'`???re-export?- `src/data/dataLayerContentStores.ts:14` ?`import { now } from './db'`
- `src/data/dataLayerTradingStores.ts:13` ?`import { generateId, now } from './db'`
- `src/data/dataLayerStockStores.ts:17` ?`import { now } from './db'`

**????????????**:
- ?????? `src/data/db-utils.ts` ???????????????dataLayer ???????????????????- ?????? `src/lib/format.ts` ?`now()` ??????
- `timeUtils.ts` ?????? re-export???`export { now } from '@/data/db-utils'`????????????????????????

**????????????**:

```typescript
// src/utils/timeUtils.ts?????????????// ?????????????????????????@/data/db-utils???????re-export ??????????????????
export { now } from '@/data/db-utils'
```

---

### 2.2 `formatIndustryDelta()` ??????????????????????????
**??????**:
- ???? `src/hooks/cabin/useIndustryScorePage.ts:37-43`
- ???? `src/store/industryScoreStore.ts:64-70`

**???????*: 1.00??????????????????

**?????????????*:

```typescript
// src/hooks/cabin/useIndustryScorePage.ts:37-43
export function formatIndustryDelta(current: number | null, previous: number | null): string {
  if (current === null || previous === null) return ''
  const delta = current - previous
  if (delta > 0) return `(+${delta.toFixed(2)})`
  if (delta < 0) return `(${delta.toFixed(2)})`
  return '(0.00)'
}

// src/store/industryScoreStore.ts:64-70
export function formatIndustryDelta(current: number | null, previous: number | null): string {
  if (current === null || previous === null) return ''
  const delta = current - previous
  if (delta > 0) return `(+${delta.toFixed(2)})`
  if (delta < 0) return `(${delta.toFixed(2)})`
  return '(0.00)'
}
```

**????????????**: ?????????????????????????????????????
**???????*: ???????????? import???????????????????????????????
---

### 2.3 `formatIntelligentDelta()` ??????????????????????????
**??????**:
- ???? `src/hooks/cabin/useIntelligentScorePage.ts:38-44`
- ???? `src/store/intelligentScoreStore.ts:68-74`

**???????*: 1.00??????????????????

**?????????????*:

```typescript
// src/hooks/cabin/useIntelligentScorePage.ts:38-44
export function formatIntelligentDelta(current: number | null, previous: number | null): string {
  if (current === null || previous === null) return ''
  const delta = current - previous
  if (delta > 0) return `(+${delta.toFixed(2)})`
  if (delta < 0) return `(${delta.toFixed(2)})`
  return '(0.00)'
}

// src/store/intelligentScoreStore.ts:68-74
export function formatIntelligentDelta(current: number | null, previous: number | null): string {
  if (current === null || previous === null) return ''
  const delta = current - previous
  if (delta > 0) return `(+${delta.toFixed(2)})`
  if (delta < 0) return `(${delta.toFixed(2)})`
  return '(0.00)'
}
```

**????????????**: ??????????????? `formatIndustryDelta` ?`formatIntelligentDelta` ????????????????????????????????????????????? `formatScoreDelta`?
**????????????**: ????????????????????? `formatScoreDelta` ?`src/lib/format.ts`

**????????????**:

```typescript
// src/lib/format.ts????????????
/**
 * ?????????????????????? * @param current ????????????
 * @param previous ????????????
 * @returns ???????????????????????????????????? (+1.23) / (-0.45) / (0.00)
 */
export function formatScoreDelta(current: number | null, previous: number | null): string {
  if (current === null || previous === null) return ''
  const delta = current - previous
  if (delta > 0) return `(+${delta.toFixed(2)})`
  if (delta < 0) return `(${delta.toFixed(2)})`
  return '(0.00)'
}
```

```typescript
// src/hooks/cabin/useIndustryScorePage.ts?????????????import { formatScoreDelta } from '@/lib/format'
// ???????????? formatIndustryDelta ??????
// ?????????????formatScoreDelta

// src/store/industryScoreStore.ts?????????????import { formatScoreDelta } from '@/lib/format'
// ???????????? formatIndustryDelta ??????
// ?????? re-export ?????????????????????export { formatScoreDelta as formatIndustryDelta } from '@/lib/format'

// src/hooks/cabin/useIntelligentScorePage.ts?????????????import { formatScoreDelta } from '@/lib/format'
// ???????????? formatIntelligentDelta ??????

// src/store/intelligentScoreStore.ts?????????????import { formatScoreDelta } from '@/lib/format'
// ???????????? formatIntelligentDelta ??????
// ?????? re-export ?????????????????????export { formatScoreDelta as formatIntelligentDelta } from '@/lib/format'
```

---

### 2.4 `toSafeNumber()` ???????????????????

**??????**:
- ???? `src/lib/safeCoerce.ts:26-30`??????????????????
- ???? `src/services/fetcher/strategyDataAdapter.ts:76-80`??????????????????

**???????*: 1.00??????????????????

**?????????????*:

```typescript
// src/lib/safeCoerce.ts:26-30??????????????????
export function toSafeNumber(value: unknown, defaultValue = 0): number {
  if (value === null || value === undefined || value === '') return defaultValue
  const num = Number(value)
  return Number.isFinite(num) ? num : defaultValue
}

// src/services/fetcher/strategyDataAdapter.ts:76-80??????????????????
export function toSafeNumber(value: unknown, defaultValue = 0): number {
  if (value === null || value === undefined || value === '') return defaultValue
  const num = Number(value)
  return Number.isFinite(num) ? num : defaultValue
}
```

**????????????**: ?????????????????????????????????????
**???????*:
- `strategyDataAdapter.ts` ?????? 13 ?????????????114-155?- `lib/safeCoerce.ts` ?3 ?marketDataAdapters ?????? import

**????????????**:
- ?????? `strategyDataAdapter.ts:76-80` ???????`toSafeNumber` ??????
- ?`strategyDataAdapter.ts` ???????????? `import { toSafeNumber, toSafeBoolean } from '@/lib/safeCoerce'`

**????????????**:

```typescript
// src/services/fetcher/strategyDataAdapter.ts???????????????????import { toSafeNumber, toSafeBoolean } from '@/lib/safeCoerce'
// ???????????? toSafeNumber ?toSafeBoolean ???????????? 76-80 ?97-102?```

---

### 2.5 `toSafeBoolean()` ???????????????????

**??????**:
- ???? `src/lib/safeCoerce.ts:108-113`??????????????????
- ???? `src/services/fetcher/strategyDataAdapter.ts:97-102`??????????????????

**???????*: 1.00??????????????????

**?????????????*:

```typescript
// src/lib/safeCoerce.ts:108-113??????????????????
export function toSafeBoolean(value: unknown, defaultValue = false): boolean {
  if (typeof value === 'boolean') return value
  if (value === 1 || value === 'true' || value === 1) return true
  if (value === 0 || value === 'false' || value === 0) return false
  return defaultValue
}

// src/services/fetcher/strategyDataAdapter.ts:97-102??????????????????
export function toSafeBoolean(value: unknown, defaultValue = false): boolean {
  if (typeof value === 'boolean') return value
  if (value === 1 || value === 'true' || value === 1) return true
  if (value === 0 || value === 'false' || value === 0) return false
  return defaultValue
}
```

**????????????**: ?????????????????????????????????????
**????????????**: ?2.4 `toSafeNumber` ??????????????????????`@/lib/safeCoerce` ???????
**??????**: ???????????????????`value === 1 || value === 'true' || value === 1` ??????????????????`value === 1` ?????????????????????????????????????bug???????????????????????? `value === 1 || value === 'true'`?
**????????????????????????**:

```typescript
// src/lib/safeCoerce.ts???????bug ??????
export function toSafeBoolean(value: unknown, defaultValue = false): boolean {
  if (typeof value === 'boolean') return value
  if (value === 1 || value === 'true') return true
  if (value === 0 || value === 'false') return false
  return defaultValue
}
```

---

## ??????B ????????????????????????? ????????????????????????

### 3.1 `generateId()` ?ID ???????
**??????**:
- ???? `src/data/db-utils.ts:17-19`???nanoid ???????- ???? `src/lib/validation.ts`?????????????????????????
**???????*: 0.00???????????????????????????????????????????
**?????????????*:

```typescript
// src/data/db-utils.ts:17-19
export function generateId(): string {
  return nanoid(16)  // 16 ???????????????????}

// src/utils/a11y.ts:11-13
let idCounter = 0
export function generateId(prefix = 'a11y'): string {
  return `${prefix}-${++idCounter}`  // ????????????????ID
}
```

**????????????**: ????????????????????????????????????
- `db-utils.ts`: ?????? 16 ???????????? ID?????????????????????????- `a11y.ts`: ??????????????????????ID???????aria-labelledby/aria-describedby?
**???????*:
- `db-utils.ts` ?`generateId` ??????? `db.ts` re-export ?`dataLayerTradingStores.ts` ??????
- `a11y.ts` ?`generateId` ?**????????????**???a11y.ts ???????????????????????????????????? import?
**????????????**:
- `a11y.ts` ???????????? `generateA11yId`??????????????????
- ?????? `a11y.ts` ????????????????????????????????? PR ??????????????????

**????????????**:

```typescript
// src/utils/a11y.ts?????????????let idCounter = 0
/** ?????????????ID???????aria-labelledby/aria-describedby?*/
export function generateA11yId(prefix = 'a11y'): string {
  return `${prefix}-${++idCounter}`
}
```

---

### 3.2 `bySector()` ?????????????

**??????**:
- ???? `src/store/hotSectorStore.ts:221-223`?????? symbol ???????- ???? `src/store/rotationSignalStore.derived.ts:131-133`?????? sectorId ???????
**???????*: 0.65???????????????????????????????store???????????????????????????????????????

**?????????????*:

```typescript
// src/store/hotSectorStore.ts:221-223
export function bySector(symbol: string): HotSectorScore | undefined {
  return useHotSectorStore.getState().scores.find((s) => s.symbol === symbol)
}

// src/store/rotationSignalStore.derived.ts:131-133
export function bySector(sectorId: string): RotationSignal | undefined {
  return useRotationSignalStore.getState().signals.find(s => s.sectorId === sectorId)
}
```

**????????????**:
- `hotSectorStore.ts`: ???????????? `symbol`?????????????????? `s.symbol`???????`HotSectorScore`
- `rotationSignalStore.derived.ts`: ???????????? `sectorId`?????????????????? `s.sectorId`???????`RotationSignal`

**??????**: `hotSectorStore.ts` ???????????? `bySector` ????????????????????????????`symbol` ????????????????`sector` ???????
**???????*:
- `hotSectorStore.ts` ?`bySector` ????????????????????- `rotationSignalStore.derived.ts` ?`bySector` ????????????? + ?????? 2 ?????????????275?00?
**????????????**:
- `hotSectorStore.ts` ???????????? `bySymbol`?????????????????????????????????
- `rotationSignalStore.derived.ts` ?????? `bySector`??????????????????

**????????????**:

```typescript
// src/store/hotSectorStore.ts?????????????/** ?symbol ???????????? */
export function bySymbol(symbol: string): HotSectorScore | undefined {
  return useHotSectorStore.getState().scores.find((s) => s.symbol === symbol)
}

// src/store/hotSectorStore.test.ts?????????????import { useHotSectorStore, topScores, buySignals, bySymbol } from './hotSectorStore'
```

---

## ??????C ??????????????????????????????? ?????????????????????????
### 4.1 `analyze()` ???????????????????

**??????**:
- ???? `src/services/scoring/hotSectorDimensions.ts:365-418`
- ???? `src/services/scoring/valuePitAnalyzer.ts:328-372`

**???????*: 0.87?????????????????????????????????????????????????????????????????????

**?????????????*:

```typescript
// src/services/scoring/hotSectorDimensions.ts:365-418
export function analyze(input: HotSectorAnalyzerInput): HotSectorScore {
  const momentum = calculateMomentum(input.momentum)
  const sentiment = calculateSentiment(input.sentiment)
  const breakout = calculateBreakout(input.breakout)
  const valuationRisk = calculateValuationRisk(input.valuationRisk)
  const marketEnv = calculateMarketEnv(input.marketEnv)

  const overallScore =
    momentum * WEIGHTS.momentum +
    sentiment * WEIGHTS.sentiment +
    breakout * WEIGHTS.breakout +
    valuationRisk * WEIGHTS.valuationRisk +
    marketEnv * WEIGHTS.marketEnv

  const rounded =
    Math.round(overallScore * HOT_SECTOR_THRESHOLDS.SCORE_ROUNDING_PRECISION) /
    HOT_SECTOR_THRESHOLDS.SCORE_ROUNDING_PRECISION

  let action: HotSectorScore['action']
  if (rounded >= HOT_SECTOR_THRESHOLDS.ACTION_IMMEDIATE_THRESHOLD) {
    action = 'immediate'
  } else if (rounded >= HOT_SECTOR_THRESHOLDS.ACTION_PROBE_THRESHOLD) {
    action = 'probe'
  } else {
    action = 'ignore'
  }
  // ... ?????? HotSectorScore
}

// src/services/scoring/valuePitAnalyzer.ts:328-372
export function analyze(input: ValuePitAnalyzerInput): ValuePitScore {
  const catalyst = calculateCatalyst(input.catalyst)
  const valuationMargin = calculateValuationMargin(input.valuationMargin)
  const chipStructure = calculateChipStructure(input.chipStructure)
  const rotationPosition = calculateRotationPosition(input.rotationPosition)
  const liquidity = calculateLiquidity(input.liquidity)

  const overallScore =
    catalyst * WEIGHTS.catalyst +
    valuationMargin * WEIGHTS.valuationMargin +
    chipStructure * WEIGHTS.chipStructure +
    rotationPosition * WEIGHTS.rotationPosition +
    liquidity * WEIGHTS.liquidity

  const rounded = Math.round(overallScore * 100) / 100  // ???????100

  let action: ValuePitScore['action']
  if (rounded >= 4.0) {        // ?????????????    action = 'immediate'
  } else if (rounded >= 3.5) { // ?????????????    action = 'probe'
  } else if (rounded >= 3.0) { // ?????????????    action = 'wait'            // ?????????????  } else {
    action = 'ignore'
  }
  // ... ?????? ValuePitScore??????????rotationSignal ???????}
```

**????????????**:

| ?????? | hotSectorDimensions | valuePitAnalyzer |
|------|---------------------|------------------|
| ???????| momentum/sentiment/breakout/valuationRisk/marketEnv | catalyst/valuationMargin/chipStructure/rotationPosition/liquidity |
| ???????????? | `WEIGHTS` ?????? | `WEIGHTS` ?????? |
| ???????????? | `HOT_SECTOR_THRESHOLDS.SCORE_ROUNDING_PRECISION` | ???????`100` |
| ??????????| `HOT_SECTOR_THRESHOLDS.ACTION_*` | ???????`4.0/3.5/3.0` |
| action ????| 3 ??????immediate/probe/ignore?| 4 ??????immediate/probe/wait/ignore?|
| ???????????? | `HotSectorScore` | `ValuePitScore`?????? `rotationSignal` ???????|
| ?????????????| momentum/sentiment/technical/valuation/composite/marketEnv | catalyst/valuation/chip/rotation/liquidity/composite |

**??????**:
1. `valuePitAnalyzer.ts` ?????????????????????????????????????????????????2. ????????????????????????????????????????????? ??????? ?action ???????????????????????????????
**????????????**: ???????????????????????? `calculateWeightedScore` ?`src/services/scoring/_shared/`

**????????????**:

```typescript
// src/services/scoring/_shared/weightedScore.ts????????????
/**
 * ??????????????????????????????
 * @param dimensions ??????????????????
 * @param weights ????????????
 * @param config ?????????????????????????????????
 * @returns ????????????????????????? */
export interface WeightedScoreConfig {
  /** ?????????????????? 100 ???????????? 2 ???????????? */
  roundingPrecision: number
}

export function calculateWeightedScore(
  dimensions: Record<string, number>,
  weights: Record<string, number>,
  config: WeightedScoreConfig,
): number {
  let overallScore = 0
  for (const [key, value] of Object.entries(dimensions)) {
    overallScore += value * (weights[key] ?? 0)
  }
  return Math.round(overallScore * config.roundingPrecision) / config.roundingPrecision
}

/**
 * ?????? action ?????????? * @param score ??????
 * @param thresholds ???????????????????????????? * @returns action ??????? */
export function classifyAction<T extends string>(
  score: number,
  thresholds: Array<{ threshold: number; action: T }>,
  defaultAction: T,
): T {
  for (const { threshold, action } of thresholds) {
    if (score >= threshold) return action
  }
  return defaultAction
}
```

```typescript
// src/services/scoring/hotSectorDimensions.ts?????????????import { calculateWeightedScore, classifyAction } from './_shared/weightedScore'

export function analyze(input: HotSectorAnalyzerInput): HotSectorScore {
  const dimensions = {
    momentum: calculateMomentum(input.momentum),
    sentiment: calculateSentiment(input.sentiment),
    breakout: calculateBreakout(input.breakout),
    valuationRisk: calculateValuationRisk(input.valuationRisk),
    marketEnv: calculateMarketEnv(input.marketEnv),
  }

  const rounded = calculateWeightedScore(dimensions, WEIGHTS, {
    roundingPrecision: HOT_SECTOR_THRESHOLDS.SCORE_ROUNDING_PRECISION,
  })

  const action = classifyAction(rounded, [
    { threshold: HOT_SECTOR_THRESHOLDS.ACTION_IMMEDIATE_THRESHOLD, action: 'immediate' },
    { threshold: HOT_SECTOR_THRESHOLDS.ACTION_PROBE_THRESHOLD, action: 'probe' },
  ], 'ignore' as HotSectorScore['action'])

  // ... ?????? HotSectorScore????????????????????????????????????
}
```

```typescript
// src/services/scoring/valuePitAnalyzer.ts???????????????????????????????import { calculateWeightedScore, classifyAction } from './_shared/weightedScore'

// ?????????????????????????????????????const VALUE_PIT_THRESHOLDS = {
  SCORE_ROUNDING_PRECISION: 100,
  ACTION_IMMEDIATE_THRESHOLD: 4.0,
  ACTION_PROBE_THRESHOLD: 3.5,
  ACTION_WAIT_THRESHOLD: 3.0,
} as const

export function analyze(input: ValuePitAnalyzerInput): ValuePitScore {
  const dimensions = {
    catalyst: calculateCatalyst(input.catalyst),
    valuationMargin: calculateValuationMargin(input.valuationMargin),
    chipStructure: calculateChipStructure(input.chipStructure),
    rotationPosition: calculateRotationPosition(input.rotationPosition),
    liquidity: calculateLiquidity(input.liquidity),
  }

  const rounded = calculateWeightedScore(dimensions, WEIGHTS, {
    roundingPrecision: VALUE_PIT_THRESHOLDS.SCORE_ROUNDING_PRECISION,
  })

  const action = classifyAction(rounded, [
    { threshold: VALUE_PIT_THRESHOLDS.ACTION_IMMEDIATE_THRESHOLD, action: 'immediate' },
    { threshold: VALUE_PIT_THRESHOLDS.ACTION_PROBE_THRESHOLD, action: 'probe' },
    { threshold: VALUE_PIT_THRESHOLDS.ACTION_WAIT_THRESHOLD, action: 'wait' },
  ], 'ignore' as ValuePitScore['action'])

  // ... ?????? ValuePitScore
}
```

---

## ????????????????
| ?????? | ???????????? | ???????????? | ???????????? | ???????|
|------|----------|----------|----------|--------|
| `now` | ???? ?| 4 ?????? | ???? ?| P0 |
| `toSafeNumber` + `toSafeBoolean` | ???? ?| 2 ?????? | ???? ?| P0 |
| `formatIndustryDelta` + `formatIntelligentDelta` | ???? ?| 4 ?????? | ???? ?| P0 |
| `generateId`?????????????| ???? ?| 1 ?????????????????? | ???? ?| P1 |
| `bySector`?????????????| ???? ?| 2 ?????? + 2 ?????? | ???? ?| P1 |
| `analyze`?????????????????? | ???? ?| 2 ?????? + ?????? 1 | ???? ?| P2 |

---

## ????????????????
### 6.1 ?????????????
```powershell
# ??????????npx tsc --noEmit

# ????????????
npm test -- --run

# ????????????
npm run audit:layers
npm run audit:split-quality
```

### 6.2 ?????????????
```powershell
# ????????????????????? re-export ???????npx tsc --noEmit

# ???????????????????????????????????????????npm test -- --run

# ?????????????????????????AP-007 ?????????????npm run audit:split-quality
# ?????????AP-007 ???????8 ?????? 0???A ?5 ?????????????
# ??????????????????
npm run audit:layers
# ??????? violations
```

### 6.3 ????????????

- ?????? batch ???????????????????`npx tsc --noEmit` ?`npm test -- --run`
- ???????????????????????? `git checkout -- <??????>` ??????
- ???????????????????`npm run audit:split-quality` ??????????????????

---

## ????????????????
- [PR-7 ????????????](../../CHANGELOG.md)
- ????????????????????????
- [???????????? ??3.16 ????????????????????????](../../03-architecture-standards.md)
- [audit-split-quality ??????](../../../../scripts/audit/audit-split-quality.ts)
- [AP-007 ????????????](../../reports/audit/audit-split-quality-optimized.json)
