---
title: V9 数据宪法（Data Constitution?
type: reference
domain: project
phase: design
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "文档版本：v1.0 编制日期?026-06-28 编制?*：V9 数据治理架构? 法律效力：本文件?V9..."
tags: [project, spec, data]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---ence
domain: project
tier: standard
status: active
maintainer: V9 Architecture Team
tags: [project, spec, data]
phase: design
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
---

# V9 数据宪法（Data Constitution?
> **文档版本**：v1.0  
> **编制日期**?026-06-28  
> **编制?*：V9 数据治理架构? 
> **法律效力**：本文件?V9 系统数据层的最高权威文档，所有新?修改数据定义必须以此为准并经过数据治理流程审? 
> **维护原则**：单一事实来源（Single Source of Truth）—?每个数据实体有且仅有一个权威定义位?
---

## 目录

1. [总则](#一总则)
2. [数据资产全景图](#二数据资产全景图)
3. [核心数据实体字典](#三核心数据实体字?
4. [数据交互协议](#四数据交互协?
5. [数据存储规范](#五数据存储规?
6. [数据流规范](#六数据流规范)
7. [常量与枚举规范](#七常量与枚举规范)
8. [数据治理流程](#八数据治理流?
9. [版本与变更管理](#九版本与变更管理)
10. [附录](#十附?

---

## 一、总则

### 1.1 目的

本文档定?V9 智能投研复盘系统所有核心数据实体的权威定义、存储规范、交互协议和治理流程，作为系统数据层?宪法"，确保：

- **单一事实来源**：每个数据实体有且仅有一个权威定?- **跨层一致?*：从 UI 层到数据层，所有数据模型保持一?- **可追溯?*：所有数据变更有完整的版本记录和审批流程
- **可审计?*：数据流动路径清晰，支持全链路审?
### 1.2 适用范围

本宪法适用?V9 系统所有模块的数据定义，包括：

- 数据存储层（IndexedDB / localStorage / MemoryCache?- 数据交互层（DataBridge / DataFlow / EventBus?- 服务层（所?Service 模块的输入输出类型）
- 展示层（Widget / Page 组件的数据Props?- 测试层（所有测试用例的数据结构?
### 1.3 权威定义源层?
当出现定义冲突时，按以下优先级取最高级作为权威来源?
| 优先?| 来源类型 | 说明 |
|:---|:---|:---|
| P0（最高） | `src/types/modules/*.types.ts` | 代码中的 TypeScript 类型定义 ?运行时强制执?|
| P1 | 本文件（数据宪法?| 人工维护的权威文档，与代码类型同?|
| P2 | `docs/implementation/*.md` 实施文档 | 设计规格文档，可能滞后于代码 |
| P3 | `./03-architecture-standards.md` 等规格文?| 高层架构描述，不包含详细字段 |

> **重要**：所?P2/P3 文档中的数据定义均为参考，最终以 P0 代码类型?P1 本宪法为准?
### 1.4 数据命名规范

| 分类 | 命名风格 | 示例 |
|:---|:---|:---|
| 接口（Interface?| PascalCase，名?| `Stock`, `Order`, `HotSectorScore` |
| 类型别名（Type?| PascalCase，名?| `ModuleId`, `DataChannel` |
| 枚举（Enum?| PascalCase，名?| `TradeErrorType`, `ResearchStatus` |
| 枚举?| SCREAMING_SNAKE_CASE | `PENDING`, `COMPLETED` |
| Store ?| snake_case（DB?| `hot_sector_scores`, `v6_scores` |
| Store 访问?| camelCase（代码） | `hotSectorScores`, `v6Scores` |
| 状态字?| `is*` / `has*` / `loading` / `error` | `isLoading`, `hasError` |
| 时间字段 | 驼峰 + `At` 后缀（ms时间戳） | `calculatedAt`, `updatedAt` |
| ID 字段 | `id` ?`*Id` | `docId`, `symbol` |

---

## 二、数据资产全景图

### 2.1 数据实体总量统计

| 分类 | 数量 | 权威定义位置 |
|:---|:---|:---|
| 核心数据接口（Interface?| 80+ | `src/data/types.ts` + `src/types/modules/*.types.ts` |
| 枚举/类型别名（Enum/Type?| 40+ | `src/config/dbConfig.ts` + `src/constants/*.ts` |
| IndexedDB Store | 20 | `src/config/dbConfig.ts` ?`STORE_NAMES` |
| DataFlow 通道 | 12 | `src/core/dataflow/dataflowTypes.ts` ?`DataChannel` |
| Envelope 动作 | 30+ | `src/config/dbConfig.ts` ?`ENVELOPE_ACTION` |
| Store（Zustand?| 16 | `src/store/*.ts` |
| 配置常量文件 | 15 | `src/config/*.ts` |
| 常量文件 | 5 | `src/constants/*.ts` |

### 2.2 按业务域分类总览

```
V9 数据资产全景
├── 🔷 基础设施域（Infrastructure??  ├── StandardEnvelope        ?标准信封协议
?  ├── EnvelopeMeta            ?信封元数??  ├── ModuleId                ?模块标识枚举?6个）
?  ├── EnvelopeTarget          ?信封目标枚举?个）
?  ├── EnvelopeAction          ?信封动作枚举?2个）
?  ├── AclPermission           ?ACL权限定义
?  ├── DataChannel             ?数据流通道枚举?2个）
?  ├── DataPacket<T>           ?数据包泛??  ├── ChannelMeta             ?通道元数??  └── MemoryCache             ?LRU缓存配置
?├── 🟢 股票池域（Stock Pool??  ├── Stock                   ?股票核心实体
?  ├── StockDataQuality        ?数据质量状??  ├── ResearchStatus          ?五态研究状??  ├── DataSource              ?数据来源枚举
?  ├── Watchlist               ?观察列表
?  ├── ResearchLog             ?研究审计日志
?  ├── PoolGroupMeta           ?分组元数??  └── StrategyClassification  ?策略分类标签
?├── 🔴 评分引擎域（Scoring Engine??  ├── V6 Score体系
?  ?  ├── V6Score             ?V6综合评分
?  ?  ├── V6LayerScore        ?单层评分
?  ?  ├── CompositeScore      ?综合评分+评级
?  ?  └── LayerId             ?层级ID枚举（lMinus1~l8??  ├── 双策略体??  ?  ├── HotSectorScore      ?热门板块五维评分
?  ?  ├── HotSectorDimensionScores ?五维分数字段
?  ?  ├── ValuePitScore       ?价值洼地五维评??  ?  ├── ValuePitDimensionScores ?五维分数字段
?  ?  ├── RotationSignal      ?轮动信号检测结??  ?  ├── DualStrategyResult  ?双策略编排结??  ?  └── DualStrategyRuleConfig ?双策略规则配??  ├── 行业评分体系
?  ?  ├── IndustryScore       ?行业智能评分
?  ?  └── IndustryDimensionScore ?行业评分子维??  ├── 智能评分体系
?  ?  ├── IntelligentScore    ?AI智能评分
?  ?  └── DimensionScore      ?评分子维??  ├── 板块轮动体系
?  ?  ├── RotationSectorScore ?板块轮动五因子评??  ?  ├── RotationFactor      ?轮动因子
?  ?  ├── RotationSubFactor   ?轮动子指??  ?  ├── RotationSignalGrade ?信号分级
?  ?  ├── MarketStyle         ?市场风格周期
?  ?  └── RotationScoreBucket ?综合得分分档
?  └── 文档版本??      └── ScoreDocVersion     ?评分文档版本
?├── 🟠 交易与复盘域（Trading & Review??  ├── Order                   ?订单实体
?  ├── OrderDirection          ?订单方向枚举
?  ├── OrderStatus             ?订单状态枚??  ├── AccountType             ?账户类型枚举
?  ├── Signal                  ?交易信号
?  ├── SignalSnapshot          ?技术指标快??  ├── StrategySnapshot        ?策略组合快照
?  ├── 持仓管理
?  ?  ├── HoldingItem         ?持仓明细
?  ?  ├── HoldingsQueryParams ?查询参数
?  ?  ├── HoldingsListData    ?分页响应
?  ?  └── Portfolio           ?投资组合
?  ├── 风控引擎
?  ?  ├── RiskCheckResult     ?风控检查结??  ?  ├── PositionSizingResult ?仓位计算结果
?  ?  └── RiskConfig          ?风控配置
?  └── 交易复盘
?      ├── TradeErrorType      ?12类交易错??      ├── ErrorSeverity       ?错误严重??      ├── PsychProfile        ?6种心理画??      ├── ReviewDimension     ?复盘单维??      ├── ReviewReport        ?六维复盘报告
?      └── TradeReviewReport   ?AI复盘六维模型
?├── 🟣 AI智能体域（AI Agent??  ├── AgentItem               ?AI智能体条??  ├── AgentOverviewMetrics    ?顶部总览指标
?  ├── AgentListData           ?Agent列表数据
?  ├── AgentStatus             ?运行状态枚??  ├── AgentTag                ?标签枚举
?  ├── AgentDefinition         ?Agent定义
?  ├── AgentInstance           ?运行实例
?  ├── AgentTask               ?Agent任务记录
?  ├── AgentTaskStatus         ?任务状态枚??  ├── 健康监控
?  ?  ├── HealthMetricItem    ?健康指标条目
?  ?  ├── HealthMetricsData   ?健康监控数据
?  ?  ├── HealthStatus        ?健康状态枚??  ?  └── HealthModuleCategory ?模块分类枚举
?  └── 诊断分析
?      ├── DiagnosticReportItem ?诊断报告条目
?      ├── DiagnosticReportsData ?诊断分析数据
?      └── DiagnosticLevel     ?诊断等级枚举
?├── 🟡 新闻资讯域（News & Info??  ├── NewsArticle             ?资讯文章
?  ├── NewsStockMap            ?股票-资讯关联
?  ├── SentimentCache          ?情感分析缓存
?  ├── NewsCategory            ?新闻分类枚举
?  ├── SentimentLabel          ?情感标签枚举
?  ├── NewsFilter              ?筛选条??  └── NewsBookmark            ?资讯收藏
?├── 🔵 驾驶舱Widget域（Cockpit??  ├── MarketData              ?标准化市场数??  ├── MarketIndexData         ?大盘指数数据
?  ├── SectorHeatmapData       ?板块热力图数??  ├── FundFlowData            ?资金流向数据
?  ├── SentimentData           ?市场情绪数据
?  ├── WatchlistData           ?自选股数据
?  ├── PortfolioData           ?持仓概览数据
?  ├── KaiScore                ?KAI选股评分
?  ├── WidgetConfig            ?Widget实例配置
?  ├── WidgetMeta              ?Widget模板元数??  └── WidgetRuntimeState      ?Widget运行时状??└── ?配置与常量域（Config & Constants?    ├── V6FactorThresholds      ?V6因子阈?    ├── DataQualityThresholds   ?数据质量阈?    ├── AgentThresholds         ?Agent阈?    ├── HEALTH_SCORE_THRESHOLDS ?健康评分阈?    ├── STRATEGY_RISK_CONFIGS   ?差异化止盈止?    ├── SCREENING_CONFIG        ?选股晋升阈?    └── THEME_REGISTRY          ?主题注册?```

---

## 三、核心数据实体字?
### 3.1 基础设施?
#### 3.1.1 StandardEnvelope ?标准信封

> **权威定义**：[src/config/dbConfig.ts](../../src/config/dbConfig.ts) + [src/core/envelope.ts](../../src/core/envelope.ts)  
> **用?*：所有跨层写操作的统一载体，支持链路追踪和ACL校验

```typescript
interface StandardEnvelope {
  meta: EnvelopeMeta      // 元数据头
  payload: unknown        // 业务载荷（不能为undefined，可以为null?}

interface EnvelopeMeta {
  source: ModuleId        // 来源模块?6种）
  target: EnvelopeTarget  // 目标?种）
  action: EnvelopeAction  // 动作类型?2种）
  traceId: string         // 链路追踪ID
  timestamp: number       // 时间戳（正整数，ms?}
```

**校验规则**?1. meta 必须是对象，source/target/action/traceId/timestamp 均不可为?2. target 必须?`ENVELOPE_TARGET` 枚举有效?3. timestamp 必须是正整数
4. payload 不能?undefined（可以为 null?
#### 3.1.2 ModuleId ?模块标识

> **权威定义**：[src/config/dbConfig.ts](../../src/config/dbConfig.ts)

| 枚举?| 说明 |
|:---|:---|
| `fetcher` | 数据采集模块 |
| `stockpool` | 股票池模?|
| `analyzer` | 分析引擎模块 |
| `tradinghub` | 交易中枢模块 |
| `system` | 系统管理模块（超级权限） |
| `user` | 用户操作模块 |
| `rotation` | 板块轮动模块 |
| `sector` | 板块评分模块 |
| `news` | 新闻资讯模块 |
| `trading` | 交易执行模块 |
| `strategy` | 策略引擎模块 |
| `orderstore` | 订单存储模块（v15 新增?|
| `backtest` | 回测模块（v15 新增，只读） |
| `execution` | 执行计划模块（v16 新增?|
| `portfolio` | 投资组合模块（v16 新增?|
| `dataCollector` | 数据采集器模块（v15 新增?|

#### 3.1.3 EnvelopeTarget ?信封目标

> **权威定义**：[src/config/dbConfig.ts](../../src/config/dbConfig.ts)

| 枚举?| 说明 |
|:---|:---|
| `db` | 数据库操作（默认?|
| `analyzer` | 分析?|
| `ui` | 界面?|
| `tradinghub` | 交易中枢 |
| `system` | 系统模块 |
| `strategy:hotSector` | 热门板块策略数据?|
| `strategy:valuePit` | 价值洼地策略数据流 |
| `strategy:rotationSignal` | 轮动信号策略数据?|

#### 3.1.4 AclPermission ?ACL权限

> **权威定义**：[src/config/dbConfig.ts](../../src/config/dbConfig.ts)

```typescript
interface AclPermission {
  readonly read: readonly StoreName[]     // 可读的Store列表
  readonly write: readonly StoreName[]    // 可写的Store列表
  readonly actions: readonly DbOperation[] // 允许的操作类?}

type DbOperation = 'SELECT' | 'INSERT' | 'UPDATE' | 'DELETE'
```

> **安全注意**：`system` 模块拥有全部权限，需在生产环境中收紧?
### 3.2 股票池域

#### 3.2.1 Stock ?股票核心实体

> **权威定义**：[src/data/types.ts](src/data/types.ts)  
> **对应Store**：`stocks`，keyPath: `symbol`

```typescript
interface Stock {
  symbol: string              // 股票代码（主键，?600519.SH"?  name: string                // 股票名称
  price?: number              // 当前价格
  pe?: number                 // 市盈?  pb?: number                 // 市净?  roe?: number                // 净资产收益?  marketCap?: number          // 市值（亿元?  researchStatus: ResearchStatus  // 研究状态（五态）
  source: DataSource          // 数据来源
  dataVersion: number         // 数据版本号（number 类型，非 string?  dataQuality?: StockDataQuality  // 数据质量状?  industryCode?: string       // 行业代码
  theme?: string[]            // 主题标签
  sector?: string             // 所属板?  group?: string              // 所属分?  ingestedAt?: number         // 入库时间?  updatedAt?: number          // 最后更新时间戳（替代旧字段 lastUpdated?}
```

#### 3.2.2 ResearchStatus ?五态研究状?
> **权威定义**：[src/config/dbConfig.ts](../../src/config/dbConfig.ts)

| 状?| 说明 | 流转条件 |
|:---|:---|:---|
| `candidate` | 候选池 | 新录入标的初始状?|
| `screened` | 初筛?| V6综合??3.0 |
| `deepDive` | 深研?| V6 ?4.0 ?LLM智能评分 ?70 |
| `watching` | 观察?| 深研后进入观察，等待建仓信号 |
| `archived` | 归档?| 止盈止损或研究终?|

#### 3.2.3 StockDataQuality ?数据质量状?
> **权威定义**：[src/data/types.ts](src/data/types.ts)

```typescript
interface StockDataQuality {
  basic: boolean          // 基础信息是否完整
  kline: boolean          // K线数据是否完?  finance: boolean        // 财务数据是否完整
  lastChecked?: number    // 最后检查时间戳
}
```

### 3.3 双策略评分域

#### 3.3.1 HotSectorScore ?热门板块策略评分

> **权威定义**：[src/data/types.ts](src/data/types.ts)  
> **对应Store**：`hot_sector_scores`，keyPath: `symbol`  
> **配置来源**：[src/config/dualStrategyRules.ts](../../src/config/dualStrategyRules.ts) ?`hotSectorScoring`

```typescript
interface HotSectorScore {
  symbol: string                      // 标的代码
  name: string                        // 标的名称
  score: number                       // 综合得分?-5?  dimensions: HotSectorDimensionScores // 五维分数字段
  action: 'immediate' | 'probe' | 'ignore'  // 动作建议
  calculatedAt: number                // 计算时间?  dataVersion: string                 // 数据版本
}

interface HotSectorDimensionScores {
  momentum: number        // 动量强度（权?5%?  sentiment: number       // 情绪热度（权?5%?  technical: number       // 技术突破（权重20%?  valuation: number       // 估值风险（权重15%?  marketEnv: number       // 大盘环境（权?%?}
```

**动作阈?*（来?`dualStrategyRules.ts`）：

| 动作 | 阈?| 仓位建议 |
|:---|:---|:---|
| `immediate` | score ?4.0 | 5-8%，持?-15?|
| `probe` | 3.5 ?score < 4.0 | 2-3%试探 |
| `ignore` | score < 3.5 | 不跟?|

#### 3.3.2 ValuePitScore ?价值洼地策略评?
> **权威定义**：[src/data/types.ts](src/data/types.ts)  
> **对应Store**：`value_pit_scores`，keyPath: `symbol`

```typescript
interface ValuePitScore {
  symbol: string                      // 标的代码
  name: string                        // 标的名称
  score: number                       // 综合得分?-5?  dimensions: ValuePitDimensionScores // 五维分数字段
  rotationSignal: boolean             // 轮动信号是否触发
  action: 'immediate' | 'probe' | 'wait' | 'ignore'  // 动作建议
  calculatedAt: number                // 计算时间?  dataVersion: string                 // 数据版本
}

interface ValuePitDimensionScores {
  catalyst: number         // 催化确定性（权重30%?  valuation: number        // 估值安全垫（权?5%?  chip: number             // 筹码结构（权?0%?  rotation: number         // 轮动位置（权?5%?  liquidity: number        // 流动性（权重10%?}
```

**动作阈?*?
| 动作 | 阈?| 仓位建议 |
|:---|:---|:---|
| `immediate` | score ?4.0 | 5%?0%?5%分步建仓 |
| `probe` | 3.5 ?score < 4.0 | 2-3%试探 |
| `wait` | 3.0 ?score < 3.5 | 等待轮动信号 |
| `ignore` | score < 3.0 | 不操?|

#### 3.3.3 RotationSignal ?轮动信号检?
> **权威定义**：[src/data/types.ts](src/data/types.ts)  
> **检测器**：[src/services/scoring/rotationSignalDetector.ts](../../src/services/scoring/rotationSignalDetector.ts)

```typescript
interface RotationSignal {
  sectorId: string                    // 板块ID
  triggered: boolean                  // 是否触发（三条件全满足）
  conditions: {
    volumeBreakthrough: boolean       // 成交量突?    capitalInflow: boolean            // 资金连续净流入
    goldenCross: boolean              // 技术金?  }
  strength: 'weak' | 'medium' | 'strong'  // 信号强度
  detectedAt: number                  // 检测时间戳
}
```

**三条件触发阈?*（来?`dualStrategyRules.ts` ?`rotationSignal`）：

| 条件 | 阈?| 说明 |
|:---|:---|:---|
| 成交量突?| `volumeSurgeRatio = 1.5` | 近期均量 ?历史均量 × 1.5 |
| 资金净流入 | `fundFlowConsecutiveDays = 3` | 连续3日资金净流入 |
| 技术金?| `priceToMA20Threshold = 1.03` | 价格站上MA20?% |

#### 3.3.4 V6 综合评分体系

> **权威定义**：[src/services/scoring/v6-engine/types.ts](src/services/scoring/v6-engine/types.ts)  
> **对应Store**：`v6_scores`，keyPath: `symbol`

**九层漏斗权重**?
| 层ID | 层名?| 权重 | 核心输入 |
|:---|:---|:---|:---|
| `lMinus1` | L-1 行业评分估?| 10% | 行业C/N评分、相关性、配置偏?|
| `l0` | L0 STEEP宏观扫描 | 8% | 宏观/社会/技?环境/政治 |
| `l1` | L1 护城河分?| 15% | 品牌/技?资源/规模/网络效应 |
| `l2` | L2 竞品格局 | 10% | 市场份额、竞争壁垒、替代品 |
| `l3f` | L3a 财务健康 | 10% | 营收/利润/现金?负债率 |
| `l3v` | L3b 估值水?| 8% | PE/PB/PEG行业分位 |
| `l4` | L4 情景推演 | 8% | 乐观/中?悲观三情?|
| `l5` | L5 T-M矩阵 | 5% | 技术成熟度 × 市场渗?|
| `l6` | L6 Hype周期 | 7% | 技术炒作周期定?|
| `l7` | L7 第二曲线 | 15% | IPC业绩兑现临界点分?|
| `l8` | L8 技术筹?| 4% | 筹码分布、技术形?|

**综合输出**?
```typescript
interface CompositeScore {
  score: number                                   // 0-5 综合得分
  rating: 'strong_buy' | 'buy' | 'hold' | 'sell' | 'strong_sell'
  layers: Record<LayerId, LayerScore>
  allRisks: string[]
  recommendation: string
  timestamp: number
  engineVersion: string
  confidence?: {
    ess: number                                   // 证据充分?    level: string
    dataSources: Record<string, string>
  }
}
```

**评级阈?*：strongBuy ?4.0 / buy ?3.0 / hold ?2.0 / sell ?1.0 / strong_sell < 1.0

### 3.4 交易与复盘域

#### 3.4.1 Order ?订单实体

> **权威定义**：[src/data/types.ts](src/data/types.ts)  
> **对应Store**：`orders`，keyPath: `id`

```typescript
interface Order {
  id: string                    // 订单ID
  symbol: string                // 标的代码
  symbolName: string            // 标的名称
  direction: OrderDirection     // 买卖方向
  orderType: 'market' | 'limit' // 订单类型
  quantity: number              // 数量（股?  price?: number                // 委托价（限价单必填）
  filledPrice?: number          // 成交均价
  status: OrderStatus           // 订单状?  accountType: AccountType      // 账户类型
  strategy?: string             // 关联策略标识
  reason?: string               // 下单理由
  stopLossPrice?: number        // 计划止损?  takeProfitPrice?: number      // 计划止盈?  reviewErrors?: TradeErrorType[]  // 复盘错误标签
  disciplineScore?: number      // 纪律评分
  createdAt: number             // 创建时间
  filledAt?: number             // 成交时间
  closedAt?: number             // 平仓时间
  pnl?: number                  // 盈亏金额
  pnlPercent?: number           // 盈亏比例
}
```

#### 3.4.2 TradeErrorType ?12类交易错?
> **权威定义**：[src/types/modules/tradeReview.types.ts](../../src/types/modules/tradeReview.types.ts)  
> **分类器实?*：[src/services/trading/tradeErrorClassifier.ts](../../src/services/trading/tradeErrorClassifier.ts)

| 错误类型 | 中文?| 严重?| 扣分 | 心理根源 |
|:---|:---|:---|:---|:---|
| `chase_high_sell_low` | 追涨杀?| critical | 15 | 贪婪与恐惧交?|
| `no_stop_loss` | 扛单不止?| critical | 15 | 损失厌恶，赌徒心?|
| `against_trend_adding` | 逆势加仓 | critical | 15 | 过度自信，摊平成?|
| `plan_violation` | 违反计划 | critical | 15 | 缺乏纪律，临场冲?|
| `heavy_gambling` | 重仓豪赌 | critical | 15 | 急功近利，风险意识不?|
| `ignore_stop_loss` | 忽视止损 | critical | 15 | 侥幸心理，过度自?|
| `early_profit_taking` | 提前止盈 | major | 8 | 对利润的恐惧 |
| `greedy_tail_chasing` | 贪鱼?| major | 8 | 贪婪，追求完?|
| `revenge_trading` | 报复性交?| major | 8 | 愤怒、不甘心 |
| `fomo_entry` | FOMO入场 | major | 8 | 害怕错过，跟风心理 |
| `hesitation_miss` | 犹豫错过 | minor | 3 | 过度谨慎，完美主?|
| `overtrading` | 过度交易 | minor | 3 | 交易成瘾，寻求刺激 |

**纪律评分公式**：`100 - critical×15 - major×8 - minor×3`

#### 3.4.3 TradeReviewReport ?AI复盘六维模型

> **权威定义**：[src/services/trading/tradeReviewAI.ts](../../src/services/trading/tradeReviewAI.ts)

```typescript
interface TradeReviewReport {
  summary: TradeSummary              // 交易摘要
  errorAnalysis: ErrorAnalysis       // 错误分析
  disciplineAnalysis: DisciplineAnalysis  // 纪律分析
  skillDevelopment: SkillDevelopment // 技能发?  actionPlan: ActionPlan             // 行动计划
  aiDeepInsight: AIDeepInsight       // AI深度洞察
  generatedAt: number
}
```

### 3.5 AI智能体域

#### 3.5.1 AgentItem ?智能体条?
> **权威定义**：[src/types/modules/ai-center.types.ts](../../src/types/modules/ai-center.types.ts)  
> **常量映射**：[src/constants/ai-center.constants.ts](../../src/constants/ai-center.constants.ts)

```typescript
interface AgentItem {
  id: string
  type: AgentType
  name: string
  tags: AgentTag[]
  description: string
  callCount: number
  status: AgentStatus
  lastCallAt?: number
  avgResponseTime?: number
  successRate?: number
}
```

**AgentStatus 枚举**：`NORMAL` / `WARNING` / `ERROR` / `PAUSED`  
**AgentTag 枚举**：`LLM` / `KNOWLEDGE` / `TOOL` / `STRATEGY`

#### 3.5.2 健康监控体系

> **权威定义**：[src/types/modules/ai-center.types.ts](../../src/types/modules/ai-center.types.ts)  
> **常量映射**：[src/constants/health.constants.ts](../../src/constants/health.constants.ts)

| 实体 | 说明 | 核心字段 |
|:---|:---|:---|
| `HealthMetricItem` | 健康指标条目 | id, name, category, value, status, trend |
| `HealthMetricsData` | 健康监控数据 | metrics[], overallScore, lastCheckedAt |
| `HealthStatus` | 健康状态枚?| HEALTHY / WARNING / CRITICAL / UNKNOWN |
| `HealthModuleCategory` | 模块分类 | CORE / SYSTEM / AGENT / DATA |

### 3.6 新闻资讯?
#### 3.6.1 NewsArticle ?资讯文章

> **权威定义**：[src/data/types.ts](src/data/types.ts)  
> **对应Store**：`news`，keyPath: `id`

```typescript
interface NewsArticle {
  id: string
  title: string
  content: string
  url?: string
  source: string
  category: NewsCategory
  publishTime: number
  sentiment: 'positive' | 'negative' | 'neutral'
  sentimentConfidence?: number
  relatedSymbols?: string[]
  hash?: string              // 内容去重哈希
  isBookmarked?: boolean
  fetchedAt: number
}
```

---

## 四、数据交互协?
### 4.1 写操作：DataBridge + Envelope

所有跨模块写操?**必须** 通过 DataBridge 转发，禁止直接操?IndexedDB?
**调用路径**?
```
UI组件 ?Store Action ?sendWriteEnvelope() ?DataBridge.forward()
                                              ?ACL校验
                                              ?路由分发
                                              ?DB写入
                                              ?频道广播
```

**策略专用频道**?
| 频道?| 触发Action | 数据载荷 | 用?|
|:---|:---|:---|:---|
| `strategy:hotSector` | `STRATEGY_HOT_SECTOR_REFRESH` | `HotSectorScore[]` | 热门板块策略实时结果 |
| `strategy:valuePit` | `STRATEGY_VALUE_PIT_REFRESH` | `ValuePitScore[]` | 价值洼地策略实时结?|
| `strategy:rotationSignal` | `STRATEGY_ROTATION_SIGNAL_DETECT` | `RotationSignal[]` | 轮动信号检测结?|

### 4.2 读操作：DataFlow 订阅

实时数据通过 DataFlowEngine 订阅获取，支?SSE 推送和轮询降级?
**12个标准通道**?
| 通道?| 数据类型 | 刷新间隔 | 持久?| 优先?|
|:---|:---|:---|:---|:---|
| `market:index` | 大盘指数 | 5s | ?| high |
| `market:sector` | 板块涨跌 | 10s | ?| high |
| `market:fundflow` | 资金流向 | 15s | ?| normal |
| `market:emotion` | 情绪指标 | 30s | ?| low |
| `portfolio:summary` | 持仓总览 | 事件驱动 | ?| high |
| `portfolio:holding` | 持仓明细 | 30s | ?| normal |
| `portfolio:risk` | 风险监控 | 60s | ?| high |
| `strategy:signal` | 买卖信号 | 事件驱动 | ?| high |
| `strategy:score` | 股票评分 | 60s | ?| normal |
| `agent:status` | Agent状?| 10s | ?| normal |
| `agent:logs` | Agent日志 | 事件驱动 | ?| low |
| `system:health` | 系统健康 | 30s | ?| low |

### 4.3 事件广播：EventBus

?Widget / 跨模块的轻量级事件通过 EventBus 发布订阅?
**核心事件**?
| 事件?| 载荷类型 | 触发时机 |
|:---|:---|:---|
| `strategy:hotSectorChanged` | `HotSectorScore[]` | 热门板块评分更新 |
| `strategy:valuePitChanged` | `ValuePitScore[]` | 价值洼地评分更?|
| `strategy:rotationSignalTriggered` | `RotationSignal[]` | 轮动信号触发 |
| `widget:loaded` | `{ instanceId }` | Widget加载完成 |
| `widget:error` | `{ instanceId, error }` | Widget出错 |

---

## 五、数据存储规?
### 5.1 IndexedDB Store 命名规范

| Store 名（snake_case?| 代码访问器（camelCase?| keyPath | 对应实体 |
|:---|:---|:---|:---|
| `stocks` | `stocks` | `symbol` | Stock |
| `v6_scores` | `v6Scores` | `symbol` | V6Score |
| `intelligent_scores` | `intelligentScores` | `id` (auto) | IntelligentScore |
| `industry_scores` | `industryScores` | `id` (auto) | IndustryScore |
| `orders` | `orders` | `id` | Order |
| `watchlists` | `watchlists` | `id` | Watchlist |
| `signals` | `signals` | `id` | Signal |
| `research_logs` | `researchLogs` | `id` (auto) | ResearchLog |
| `daily_quotes` | `dailyQuotes` | `symbol` | DailyQuotes |
| `rotation_scores` | `rotationScores` | `id` | RotationSectorScore |
| `sector_scores` | `sectorScores` | `id` | SectorScoreRecord |
| `score_docs` | `scoreDocs` | `docId` | ScoreDocVersion |
| `strategy_snapshots` | `strategySnapshots` | `id` | StrategySnapshot |
| `local_docs` | `localDocs` | `id` | LocalDoc |
| `news` | `news` | `id` | NewsArticle |
| `news_stock_map` | `newsStockMap` | `id` | NewsStockMap |
| `sentiment_cache` | `sentimentCache` | `id` | SentimentCache |
| `news_bookmarks` | `newsBookmarks` | `id` | NewsBookmark |
| `hot_sector_scores` | `hotSectorScores` | `symbol` | HotSectorScore |
| `value_pit_scores` | `valuePitScores` | `symbol` | ValuePitScore |

**数据库名**：`V6ProDB`  
**当前版本**：`16`（v16 变更：新?`executionPlans`、`portfolios` Store；v15 变更：composite→marketEnv 字段重命名，value_pit_scores移除composite?
### 5.2 localStorage 使用规范

| Key | 用?| 类型 | 管理模块 |
|:---|:---|:---|:---|
| `v9_cockpit_layout` | 驾驶舱布局 | string (JSON) | CockpitShell |
| `v9_theme` | 主题偏好 | string | themeRegistry |
| `v9_last_route` | 最后访问路?| string | pageStore |
| `v9_llm_config` | LLM配置 | string (JSON) | llmConfig |

> **规范**：localStorage 仅用?UI 状态持久化?*禁止**存储业务数据。业务数据必须存?IndexedDB?
### 5.3 MemoryCache 缓存规范

> **权威定义**：[src/core/memoryCache.ts](../../src/core/memoryCache.ts)

| 配置?| 默认?| 说明 |
|:---|:---|:---|
| `defaultTTL` | 10,000 ms | 默认过期时间?0秒） |
| `maxSize` | 200 ?| 最大缓存条目数 |
| `cleanupInterval` | 30,000 ms | 自动清理间隔 |
| 淘汰策略 | LRU | 最久未访问优先淘汰 |

---

## 六、数据流规范

### 6.1 DataPacket 数据包格?
```typescript
interface DataPacket<T = unknown> {
  channel: DataChannel   // 频道?  data: T                // 载荷数据
  timestamp: number      // 数据产生时间?  seq: number            // 全局递增序列?}
```

### 6.2 数据新鲜度要?
| 数据类型 | 新鲜度要?| 过期时间 |
|:---|:---|:---|
| 大盘指数 | ?5s | 30s |
| 板块涨跌 | ?10s | 60s |
| 资金流向 | ?15s | 120s |
| 持仓数据 | 事件驱动 | 交易后立即更?|
| 评分数据 | ?60s | 300s |
| 新闻资讯 | ?5min | 1h |

---

## 七、常量与枚举规范

### 7.1 常量文件分布

| 文件 | 内容分类 | 数量 |
|:---|:---|:---|
| `src/constants/ai-center.constants.ts` | Agent状?标签/颜色映射 | ~199?|
| `src/constants/cockpit.constants.ts` | 驾驶舱布局/评分/颜色映射 | ~465?|
| `src/constants/health.constants.ts` | 健康状?诊断等级/阈?| ~100?|
| `src/constants/newsColorTokens.ts` | 新闻颜色令牌 | ~60?|
| `src/constants/trade.constants.ts` | 交易方向/分页/颜色映射 | ~250?|

### 7.2 配置文件分布

| 文件 | 内容分类 |
|:---|:---|
| `src/config/dualStrategyRules.ts` | 双策略评?动作阈值（核心?|
| `src/config/tradingConfig.ts` | 交易信号/止损止盈/风控阈?|
| `src/config/strategyRules.ts` | 四分类选股阈?|
| `src/config/thresholds.ts` | V6因子/数据质量/Agent阈?|
| `src/config/screeningConfig.ts` | 股票池晋升阈?|
| `src/config/rotationConfig.ts` | 轮动因子/信号分级 |
| `src/config/dbConfig.ts` | DB版本/Store?ACL/Envelope |
| `src/config/scoreFactors.ts` | V6九维评分因子 |
| `src/config/dataDimensions.ts` | 八维数据架构定义 |
| `src/config/symbols.ts` | 核心资源标的白名?|
| `src/config/themeRegistry.ts` | 主题注册?|
| `src/config/llmConfig.ts` | LLM模型预设 |
| `src/config/routes.ts` | 路由注册?|
| `src/config/fetcherConfig.ts` | 采集器配?|
| `src/config/inputConfig.ts` | 输入舱配?|

### 7.3 颜色使用规范

**禁止**在组件中硬编码颜色值。所有颜色必须来自：

1. CSS 变量（`var(--color-*)`）?用于UI样式
2. 常量文件（`src/constants/*.ts`）?用于动态计算颜?3. Tailwind 工具??用于布局和间?
> 审计命令：`npm run audit:hardcode` 检查硬编码颜色

---

## 八、数据治理流?
### 8.1 新增数据实体流程

```
提出需??设计评审 ?类型定义 ?文档更新 ?代码实现 ?测试验证 ?发布
   ?          ?          ?         ?         ?         ?   ?          ?          ?         ?         ?         ? 业务?   架构?       数据治理   数据治理   开发团?  测试团队
```

**新增 Checklist**?- [ ] ?`src/types/modules/` ?`src/data/types.ts` 定义 TypeScript 类型
- [ ] 如需持久化，?`dbConfig.ts` 新增 Store 并升?DB_VERSION
- [ ] 如需配置，在 `src/config/` 新增配置文件
- [ ] 更新本宪法（数据宪法）对应章?- [ ] 更新 `data-dictionary-index.md` 索引
- [ ] 添加单元测试
- [ ] 通过 `npm run audit:hardcode` 硬编码审?
### 8.2 修改数据实体流程

**破坏性变更（Breaking Change?*?1. 必须升级 DB_VERSION
2. 编写 `onupgradeneeded` 迁移脚本
3. 更新所有引用该类型的文?4. 更新文档和测?5. ?CHANGELOG 中记?
**非破坏性变更（新增可选字段）**?1. 添加 `?` 可选标记或提供默认?2. 保持向后兼容
3. 更新文档

### 8.3 数据质量门禁

| 门禁?| 检查命?| 阈?|
|:---|:---|:---|
| TypeScript 类型检?| `tsc --noEmit` | 0 errors |
| 跨层调用审计 | `npm run audit:layers` | 0 违规 |
| 硬编码审?| `npm run audit:hardcode` | 逐步收敛? |
| 死代码审?| `npm run audit:deadcode` | 0 空壳/0 漂移 |
| 测试覆盖?| `npm run coverage` | core ?85%, services ?70% |

---

## 九、版本与变更管理

### 9.1 文档版本历史

| 版本 | 日期 | 变更说明 | 编制?|
|:---|:---|:---|:---|
| v1.0 | 2026-06-28 | 初始版本，整合全部数据资产定?| 数据治理架构?|

### 9.2 与代码类型同步机?
本文件（P1）必须与 `src/types/` 下的代码类型（P0）保持同步。同步检查通过以下方式保证?
1. **代码是最终权?*：运行时?TypeScript 类型为准
2. **文档定期校对**：每?sprint 结束后校对一?3. **变更触发更新**：P0 类型变更必须同步更新 P1 文档
4. **自动化校?*：通过 `./09-quality-gates.md` 中的 doc-sync 审计脚本检?
---

## 十、附?
### 10.1 权威定义索引?
| 数据实体 | 权威定义文件 | 行号范围 |
|:---|:---|:---|
| StandardEnvelope | [src/config/dbConfig.ts](../../src/config/dbConfig.ts) | EnvelopeMeta接口 |
| HotSectorScore | [src/data/types.ts](src/data/types.ts) | HotSectorScore接口 |
| ValuePitScore | [src/data/types.ts](src/data/types.ts) | ValuePitScore接口 |
| RotationSignal | [src/data/types.ts](src/data/types.ts) | RotationSignal接口 |
| V6 CompositeScore | [src/services/scoring/v6-engine/types.ts](src/services/scoring/v6-engine/types.ts) | CompositeScore接口 |
| AgentItem | [src/types/modules/ai-center.types.ts](../../src/types/modules/ai-center.types.ts) | AgentItem接口 |
| TradeErrorType | [src/types/modules/tradeReview.types.ts](../../src/types/modules/tradeReview.types.ts) | TradeErrorType枚举 |
| NewsArticle | [src/data/types.ts](src/data/types.ts) | NewsArticle接口 |
| DataPacket | [src/core/dataflow/dataflowTypes.ts](../../src/core/dataflow/dataflowTypes.ts) | DataPacket接口 |
| MemoryCache | [src/core/memoryCache.ts](../../src/core/memoryCache.ts) | CacheEntry/CacheStats |

### 10.2 相关文档

- ~~[V9现有数据资产清单](../explanation/design/v9现有数据资产清单.md)~~ ?全量资产扫描结果（已删除，参见v9现有数据资产清单.md v2.2?- [data-dictionary-index.md](../explanation/design/data-dictionary-index.md) ?数据字典总索?- [03-architecture-standards.md](./03-architecture-standards.md) ?架构标准
- [data-interaction-protocols.md](../reference/data-interaction-protocols.md) ?数据交互协议

---

**本文档为 V9 系统数据治理的最高权威文件。任何数据定义的变更必须遵循本宪法规定的流程?*