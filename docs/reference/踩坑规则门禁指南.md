---
title: ????????
type: reference
domain: project
phase: development
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?? V9 ???????????? #11-#14????????? CI/CD ???? ?????????????????"
tags: [project, guide, reference]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-PROJ-191
related_docs: [V9-DOC-QA-093]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# ????????

> **Status**: Current
> **Version**: v1.0.0
> **Last Updated**: 2026-07-03
>
> ?? V9 ???????????? #11-#14????????? CI/CD ????
> ?????????????????

---

## 1. 

??????????????????? 4 ???????????** + ????**??

| ?? | ?? | ??? | ?? |
|--------|----------|----------|----------|
| ** husky pre-commit** | `git commit` | `scripts/monitor/pitfall_check.py` | exit 1 ????? |
| **?? CI/CD ??** | `push` / `PR`  main/develop | `scripts/monitor/pitfall_check.py` | exit 1 ???? |

????**???????????????????**

---

## 2. ??????

### 2.1 ??husky 9.x

```
git commit
   core.hooksPath = .husky/_
   .husky/_/pre-commit (husky ??)
   husky h ??
   .husky/pre-commit (????)
      npx lint-staged          (ESLint ?? .ts/.tsx ??)
      python scripts/pitfall_check.py   (????)
          exit 1 if ERROR       (?????)
```

### 2.2 ????GitHub Actions

```
git push origin main
   GitHub Actions  quality-check.yml
   Checkout + Setup Node.js + Setup Python 3.11
   TypeScript compilation
   JSON validation
   ESLint check
   Pitfall rules check           (????)
      python scripts/pitfall_check.py python/ scripts/
      exit 1 if ERROR            (????)
   Unit tests
   Data blueprint validation
```

### 2.3 ?????

| ?? | ????? | ?? |
|--------|----------|------|
|  husky | git ???? | ?????????? |
| ?? CI/CD | `python/` + `scripts/` ???? |  Python  |

---

## 3. 

###  #11: ??

|  | ?? |
|------|-----|
| **??** | ERRORJS/TS/ WARNPython|
| **????** | V9 2026-07-03 |
| **** | `check_regex_dynamic_construction` |

****JavaScript  `/v_${var}=/` ?? `${var}` ???????????????

****JS  `/.../` ???????? `new RegExp()` ???

****?? `new RegExp()` ??????? `${var}`

**????JS/TS**

```javascript
// ? ????? ${tencentCode} ??
const match = data.match(/v_${tencentCode}="([^"]+)"/);
```

**????JS/TS**

```javascript
// ? ?? new RegExp() ?????
const match = data.match(new RegExp(`v_${tencentCode}="([^"]+)"`));
```

**Python ??**

```python
# ? ?????re.compile ?? ${} ???????
pattern = re.compile("v_${var}=")

# ? ???? f-string
pattern = re.compile(f"v_{var}=")
```

---

###  #12: ????

|  | ?? |
|------|-----|
| **??** | ERRORrequests/ WARNAKShare|
| **????** | V9 2026-07-03 |
| **** | `check_timeout_control` |

****`requests.get()`  `timeout`  AKShare ??????????????????? TIMEOUT

****`requests` ??????AKShare ???? `timeout` 

****
- `requests` ?? `timeout` 
- AKShare ?? `call_with_timeout` ??

**????**

```python
# ? ?????requests.get  timeout
response = requests.get("https://example.com")

# ? ?????AKShare ??????????
df = ak.stock_zh_a_spot_em()
```

**????**

```python
# ? ??requests  timeout
response = requests.get("https://example.com", timeout=8.0)

# ? ??AKShare  call_with_timeout ??
from lib.timeout_utils import call_with_timeout

df = call_with_timeout(ak.stock_zh_a_spot_em, timeout=8.0)
```

**??????????** `scripts/monitor/pitfall_check.py`

```
stock_zh_a_spot_em, stock_zh_a_daily, stock_individual_info_em,
stock_financial_em, requests.get, requests.post, requests.put,
requests.delete, urlopen
```

---

###  #13: TTL+LRU 

|  | ?? |
|------|-----|
| **??** | WARN |
| **????** | V9 2026-07-03 |
| **** | `check_cache_strategy` |

****??????? API?????????????????

****??????????

****???? API  TTL+LRU ???

**????**

```python
# ? ????????? stock_zh_a_spot_em ????
def fetch_quote(symbol):
    df = ak.stock_zh_a_spot_em()  # ??????????
    return df[df[''] == symbol]
```

**????**

```python
# ? ???? TTL_LRUCache  @cached ??
from lib.cache_utils import TTL_LRUCache

_spot_cache = TTL_LRUCache(capacity=100, ttl=300.0)  # 5 TTL

def fetch_quote(symbol):
    df = _spot_cache.get('spot_em', lambda: ak.stock_zh_a_spot_em())
    return df[df[''] == symbol]
```

**??**?????????????

```
@cached, TTL_LRUCache, _KLINE_CACHE, _SPOT_CACHE, call_with_cache, CachedAPIClient
```

**??**

|  | TTL | LRU  |
|----------|-----|----------|
| K | 10 ??600s | 100  |
| ???? | 5 ??300s | 100  |
| ?? | 30 ??1800s | 50  |

---

###  #14: ??

|  | ?? |
|------|-----|
| **??** | WARN |
| **????** | V9 2026-07-03 |
| **** | `check_degradation_depth` |

****??? + ?? = ???? 3 ???  2   ???? = ??? 35 ???

****??????????????????????

****??  2 ?????????

**????**

```python
# ? ?????3 ????? 2 ????? 35 
def fetch_data(code):
    try:
        return ak.stock_individual_info_em(code)   # 1
    except:
        try:
            return ak.stock_zh_a_spot_em()          # 2
        except:
            try:
                return ak.stock_financial_em(code)  # 3  
            except:
                return None
```

**????**

```python
# ? ??2 ???????
def fetch_data(code):
    try:
        return ak.stock_zh_a_spot_em()  # ?????? 5min ???
    except Exception as e:
        logger.warning("????: %s", e)
        return {'price': None, 'pe': None}  #  None
```

**??** WARN

- ???? `3` / `4` / `3???` ????
- ?? `try:`   3  **** `except`   4 

---

## 4. ??husky pre-commit

### 4.1 ??

| ?? |  |
|------|------|
| [.husky/pre-commit](.husky/pre-commit) | ???? lint-staged + pitfall_check.py |
| [.husky/_/pre-commit](.husky/_/pre-commit) | husky ????????????|
| [package.json](package.json) | `"prepare": "husky"` ?? husky |

### 4.2 pre-commit ??

```bash
npx lint-staged

# ?????????v2.1 ????
python scripts/pitfall_check.py
if [ $? -ne 0 ]; then
  echo "? ???????????????"
  exit 1
fi
```

### 4.3 ????

```bash
git commit --no-verify -m "..."
```

> ?? ****??CI/CD ??????????????

---

## 5. ????CI/CD ??

### 5.1 ??

[.github/workflows/quality-check.yml](../../.github/workflows/quality-check.yml)

### 5.2 

```yaml
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:       # ??
```

### 5.3 ?????

```yaml
- name: Setup Python
  uses: actions/setup-python@v5
  with:
    python-version: '3.11'

- name: Pitfall rules check
  run: |
    python scripts/pitfall_check.py python/ scripts/ || {
      echo "? ?????????? CI/CD ??"
      exit 1
    }
```

---

## 6. ??????

### 6.1 ???????

```bash
git add .
git commit -m "feat: ??"
```

???????????? commit ???????????

### 6.2 ???????????????

```bash
# ??????
python scripts/pitfall_check.py python/ scripts/

# ????
python scripts/pitfall_check.py path/to/file.py

#  git ???? husky ??????
python scripts/pitfall_check.py
```

### 6.3 ????

| ?? |  |
|--------|------|
| 0 | ???????|
| 1 |  ERROR ??????????|

> WARN ???????

---

## 7. ??????

### 7.1  #11 ????

```bash
# ????
# ? app.ts:42  ${var} ??

# ???? new RegExp()
# ????: data.match(/v_${code}=/)
# ????: data.match(new RegExp(`v_${code}=`))
```

### 7.2  #12 ????

```bash
# ????
# ? collect_endpoints.py:120 requests ?? timeout 

# ?? A timeout 
requests.get(url, timeout=8.0)

# ?? B call_with_timeout ?? AKShare 
from lib.timeout_utils import call_with_timeout
result = call_with_timeout(ak.stock_zh_a_spot_em, timeout=8.0)
```

### 7.3  #13 ????

```bash
# ????
# ?? collect_endpoints.py ???? stock_zh_a_spot_em ??

# ?? TTL_LRUCache  @cached ??
from lib.cache_utils import TTL_LRUCache, cached

#  ATTL_LRUCache
_cache = TTL_LRUCache(capacity=100, ttl=300.0)
df = _cache.get('spot', lambda: ak.stock_zh_a_spot_em())

#  B@cached ??
@cached(ttl=300.0)
def fetch_spot():
    return ak.stock_zh_a_spot_em()
```

### 7.4  #14 ????

```bash
# ????
# ?? collect_endpoints.py ?? 9  try  + 9  except 

# ?????? try/except ??
# ??2 ??????????
```

---

## 8. 

### Q1: ??????????????

1. ???????? docstring ???????????
2. ??????`scripts/monitor/pitfall_check.py`  157-349 
3. ?????????? `git commit --no-verify` ??

### Q2: husky ????commit ??????

 husky ??????

```bash
git config core.hooksPath
# : .husky/_

ls .husky/_/pre-commit
# ??
```

???? `npx husky` ????

### Q3: CI/CD ??????????

???? GitHub ?????????

```bash
git remote add origin https://github.com/<user>/<repo>.git
git push -u origin main
```

### Q4: ????????

1.  `scripts/monitor/pitfall_check.py` ????
2.  `CHECKERS` ??????? 356-361 ??
3.  ?????? ??
4. ????

### Q5: pitfall_check.py ??????

???? `filter_code_lines` ?????? `marker in line`  docstring?????? `"""`  docstring `"""`????????`check_file`  369-374 ????

---

## 9. ??

| ?? | ?? |
|------|------|
| [scripts/pitfall_check.py](../../scripts/monitor/pitfall_check.py) | ????4 |
| [.husky/pre-commit](.husky/pre-commit) |  git hook |
| [.github/workflows/quality-check.yml](../../.github/workflows/quality-check.yml) | CI/CD ?? |
| [python/data_service/lib/timeout_utils.py](python/data_service/lib/timeout_utils.py) | ??????call_with_timeout|
| [python/data_service/lib/cache_utils.py](python/data_service/lib/cache_utils.py) | TTL+LRU ??? |
| [python/data_service/lib/dynamic_match.py](python/data_service/lib/dynamic_match.py) | ??????? |
| [docs/09-quality-gates.md](../explanation/design/09-quality-gates.md) | V9 ??12 ??|

---

## 10. ??

| ??? |  | ???? |
|------|------|----------|
| v1.0.0 | 2026-07-03 | ?????4 ???husky + CI/CD ?????????????????? |
