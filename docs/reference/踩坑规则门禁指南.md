---
title: ȿӹŽָ
type: reference
domain: project
phase: development
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "ĵ V9 ĿĲȿӹŽϵ #11-#14塢ػơԶ CI/CD ػơ ĿߣύĿߣرŶˡ"
tags: [project, guide, reference]
version: v1.0.0
last_updated: 2026-07-17
code_version: "2.0.0-rc.1"
doc_id: V9-DOC-PROJ-191
related_docs: [V9-DOC-QA-093]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# ȿӹŽָ

> **Status**: Current
> **Version**: v1.0.0
> **Last Updated**: 2026-07-03
>
> ĵ V9 ĿĲȿӹŽϵ #11-#14塢ػơԶ CI/CD ػơ
> ĿߣύĿߣرŶˡ

---

## 1. 

ȿӹŽһԶƣڴύ׶ 4 Ƶȿ⡣Žϵ** + Զ˫**ܹ

| ز | ʱ | 鹤 | Ϊ |
|--------|----------|----------|----------|
| ** husky pre-commit** | `git commit` | `scripts/monitor/pitfall_check.py` | exit 1 ֹύ |
| **Զ CI/CD ˮ** | `push` / `PR`  main/develop | `scripts/monitor/pitfall_check.py` | exit 1 ֹˮ |

ֻס**ύǰŽԶ飻Υ޷ύ**

---

## 2. Žϵܹ

### 2.1 ·husky 9.x

```
git commit
   core.hooksPath = .husky/_
   .husky/_/pre-commit (husky װ)
   husky h ű
   .husky/pre-commit (ûű)
      npx lint-staged          (ESLint ޸ .ts/.tsx ļ)
      python scripts/pitfall_check.py   (ȿӹ)
          exit 1 if ERROR       (ֹύ)
```

### 2.2 Զ·GitHub Actions

```
git push origin main
   GitHub Actions  quality-check.yml
   Checkout + Setup Node.js + Setup Python 3.11
   TypeScript compilation
   JSON validation
   ESLint check
   Pitfall rules check           (ȿӹ)
      python scripts/pitfall_check.py python/ scripts/
      exit 1 if ERROR            (ֹˮ)
   Unit tests
   Data blueprint validation
```

### 2.3 鷶Χ

| ز | 鷶Χ | ˵ |
|--------|----------|------|
|  husky | git ݴļ | ֻ鱾ύļ |
| Զ CI/CD | `python/` + `scripts/` Ŀ¼ |  Python  |

---

## 3. 

###  #11: ̬

|  | ֵ |
|------|-----|
| **ؼ** | ERRORJS/TS/ WARNPython|
| **״η** | V9 2026-07-03 |
| **** | `check_regex_dynamic_construction` |

****JavaScript  `/v_${var}=/` е `${var}` ֵַƥ侲Ĭʧܡ

****JS  `/.../` ֧ģֵַ `new RegExp()` 졣

****̬ `new RegExp()` 죬ֹʹ `${var}`

**ΥʾJS/TS**

```javascript
// ? Υ棺 ${tencentCode} ֵ
const match = data.match(/v_${tencentCode}="([^"]+)"/);
```

**޸ʾJS/TS**

```javascript
// ? ȷ new RegExp() 춯̬
const match = data.match(new RegExp(`v_${tencentCode}="([^"]+)"`));
```

**Python ע**

```python
# ? Υ棺re.compile е ${} ǲֵ﷨
pattern = re.compile("v_${var}=")

# ? ȷʹ f-string
pattern = re.compile(f"v_{var}=")
```

---

###  #12: óʱ

|  | ֵ |
|------|-----|
| **ؼ** | ERRORrequests/ WARNAKShare|
| **״η** | V9 2026-07-03 |
| **** | `check_timeout_control` |

****`requests.get()`  `timeout`  AKShare ֱӵ޳ʱװᵼ³޵ȴ TIMEOUT

****`requests` Ĭ޳ʱAKShare ڲ֧ `timeout` 

****
- `requests` ñ `timeout` 
- AKShare ñ `call_with_timeout` װ

**Υʾ**

```python
# ? Υ棺requests.get  timeout
response = requests.get("https://example.com")

# ? Υ棺AKShare ֱӵ޳ʱװ
df = ak.stock_zh_a_spot_em()
```

**޸ʾ**

```python
# ? ȷrequests  timeout
response = requests.get("https://example.com", timeout=8.0)

# ? ȷAKShare  call_with_timeout װ
from lib.timeout_utils import call_with_timeout

df = call_with_timeout(ak.stock_zh_a_spot_em, timeout=8.0)
```

**ҪʱƵĺб** `scripts/monitor/pitfall_check.py`

```
stock_zh_a_spot_em, stock_zh_a_daily, stock_individual_info_em,
stock_financial_em, requests.get, requests.post, requests.put,
requests.delete, urlopen
```

---

###  #13: TTL+LRU 

|  | ֵ |
|------|-----|
| **ؼ** | WARN |
| **״η** | V9 2026-07-03 |
| **** | `check_cache_strategy` |

****Ƶõⲿ APIȫг飩޻ƣظӳۻ

****޻ԣÿεö

****Ƶõ API  TTL+LRU 档

**Υʾ**

```python
# ? Υ棺ļе stock_zh_a_spot_em κλ
def fetch_quote(symbol):
    df = ak.stock_zh_a_spot_em()  # ÿζȡȫг
    return df[df[''] == symbol]
```

**޸ʾ**

```python
# ? ȷʹ TTL_LRUCache  @cached װ
from lib.cache_utils import TTL_LRUCache

_spot_cache = TTL_LRUCache(capacity=100, ttl=300.0)  # 5 TTL

def fetch_quote(symbol):
    df = _spot_cache.get('spot_em', lambda: ak.stock_zh_a_spot_em())
    return df[df[''] == symbol]
```

**ʶ**ʶ±ʶʾѻ棩

```
@cached, TTL_LRUCache, _KLINE_CACHE, _SPOT_CACHE, call_with_cache, CachedAPIClient
```

**Ƽ**

|  | TTL | LRU  |
|----------|-----|----------|
| K | 10 ӣ600s | 100  |
| ʵʱ | 5 ӣ300s | 100  |
| ָ | 30 ӣ1800s | 50  |

---

###  #14: ·

|  | ֵ |
|------|-----|
| **ؼ** | WARN |
| **״η** | V9 2026-07-03 |
| **** | `check_degradation_depth` |

****㽵 + ÿ = ӳۻ 3 㽵  2   ָ˱ =  35 롣

****·ÿ㶼Ժ˱ܣӳԵӡ

****·  2 㣬ֿȫá

**Υʾ**

```python
# ? Υ棺3 㽵ÿ 2 Σ 35 
def fetch_data(code):
    try:
        return ak.stock_individual_info_em(code)   # 1
    except:
        try:
            return ak.stock_zh_a_spot_em()          # 2
        except:
            try:
                return ak.stock_financial_em(code)  # 3  
            except:
                return None
```

**޸ʾ**

```python
# ? ȷ2 㽵ֿȫ
def fetch_data(code):
    try:
        return ak.stock_zh_a_spot_em()  # ΨһԴ 5min 棩
    except Exception as e:
        logger.warning("ȡʧ: %s", e)
        return {'price': None, 'pe': None}  #  None
```

**ֵ** WARN

- ļг `3` / `4` / `3㽵` ȹؼ
- ļ `try:`   3  **** `except`   4 

---

## 4. أhusky pre-commit

### 4.1 ļ

| ļ |  |
|------|------|
| [.husky/pre-commit](.husky/pre-commit) | ûű lint-staged + pitfall_check.py |
| [.husky/_/pre-commit](.husky/_/pre-commit) | husky װԶɣֶ޸ģ|
| [package.json](package.json) | `"prepare": "husky"` ʼ husky |

### 4.2 pre-commit ű

```bash
npx lint-staged

# ȿӹŽ飨v2.1 ޸ͬ
python scripts/pitfall_check.py
if [ $? -ne 0 ]; then
  echo "? ȿӹŽδֹͨύ"
  exit 1
fi
```

### 4.3 ŽƼ

```bash
git commit --no-verify -m "..."
```

> ?? ****ŽCI/CD ԻԶءҪŽϰߡ

---

## 5. ԶأCI/CD ˮ

### 5.1 ļ

[.github/workflows/quality-check.yml](../../.github/workflows/quality-check.yml)

### 5.2 

```yaml
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:       # ֶ
```

### 5.3 Ž鲽

```yaml
- name: Setup Python
  uses: actions/setup-python@v5
  with:
    python-version: '3.11'

- name: Pitfall rules check
  run: |
    python scripts/pitfall_check.py python/ scripts/ || {
      echo "? ȿӹδֹͨ CI/CD ˮ"
      exit 1
    }
```

---

## 6. ʹ÷ʽ

### 6.1 ճύԶ

```bash
git add .
git commit -m "feat: ¹"
```

ŽԶִУΥʱ commit ʧܡʾ޸ύ

### 6.2 ֶ飨ύǰԤ죩

```bash
# ָĿ¼
python scripts/pitfall_check.py python/ scripts/

# ָļ
python scripts/pitfall_check.py path/to/file.py

#  git ݴļ husky Ϊһ£
python scripts/pitfall_check.py
```

### 6.3 ˳˵

| ˳ |  |
|--------|------|
| 0 | ȫͨύ|
| 1 |  ERROR Υ棨ֹύ|

> WARN ֹύʾ

---

## 7. Υ޸ָ

### 7.1  #11 Υ޸

```bash
# Ϣʾ
# ? app.ts:42  ${var} ֵ

# ޸Ϊ new RegExp()
# ޸ǰ: data.match(/v_${code}=/)
# ޸ĺ: data.match(new RegExp(`v_${code}=`))
```

### 7.2  #12 Υ޸

```bash
# Ϣʾ
# ? collect_endpoints.py:120 requests ñ timeout 

# ޸ A timeout 
requests.get(url, timeout=8.0)

# ޸ B call_with_timeout װ AKShare 
from lib.timeout_utils import call_with_timeout
result = call_with_timeout(ak.stock_zh_a_spot_em, timeout=8.0)
```

### 7.3  #13 Υ޸

```bash
# Ϣʾ
# ?? collect_endpoints.py ļе stock_zh_a_spot_em ޻

# ޸ TTL_LRUCache  @cached װ
from lib.cache_utils import TTL_LRUCache, cached

#  ATTL_LRUCache
_cache = TTL_LRUCache(capacity=100, ttl=300.0)
df = _cache.get('spot', lambda: ak.stock_zh_a_spot_em())

#  B@cached װ
@cached(ttl=300.0)
def fetch_spot():
    return ak.stock_zh_a_spot_em()
```

### 7.4  #14 Υ޸

```bash
# Ϣʾ
# ?? collect_endpoints.py ļ 9  try  + 9  except 

# ޸ϲ· try/except Ƕ
# ԭ2 㽵㹻ֿȫ
```

---

## 8. 

### Q1: ŽΪΥ棬ô죿

1. ǷΪ󱨣 docstring ڵĴƬα⣩
2. ȷϼ߼`scripts/monitor/pitfall_check.py`  157-349 
3. ȷΪ󱨣ʹ `git commit --no-verify` Ƽ

### Q2: husky ûдcommit ֱӳɹ

 husky Ƿȷװ

```bash
git config core.hooksPath
# : .husky/_

ls .husky/_/pre-commit
# ļ
```

δװ `npx husky` ³ʼ

### Q3: CI/CD ˮߴδִУ

ĿҪ GitHub Զֿ̲⣺

```bash
git remote add origin https://github.com/<user>/<repo>.git
git push -u origin main
```

### Q4: µĲȿӹ

1.  `scripts/monitor/pitfall_check.py` µļ
2.  `CHECKERS` бעᣨ 356-361 У
3.  ȿӱ¼ ӹ
4. ±ĵ

### Q5: pitfall_check.py Ϊʲô

űڲ `filter_code_lines` ü򵥵 `marker in line`  docstring޷ַе `"""`  docstring `"""`Ϊָ󱨣`check_file`  369-374 Уļ

---

## 9. ļ

| ļ | ˵ |
|------|------|
| [scripts/pitfall_check.py](../../scripts/monitor/pitfall_check.py) | Žű4 |
| [.husky/pre-commit](.husky/pre-commit) |  git hook |
| [.github/workflows/quality-check.yml](../../.github/workflows/quality-check.yml) | CI/CD ˮ |
| [python/data_service/lib/timeout_utils.py](python/data_service/lib/timeout_utils.py) | ʱƹߣcall_with_timeout|
| [python/data_service/lib/cache_utils.py](python/data_service/lib/cache_utils.py) | TTL+LRU 湤 |
| [python/data_service/lib/dynamic_match.py](python/data_service/lib/dynamic_match.py) | ̬ƥ乤 |
| [docs/09-quality-gates.md](../09-quality-gates.md) | V9 质量门禁规范 |

---

## 10. ־

| 汾 |  | ժҪ |
|------|------|----------|
| v1.0.0 | 2026-07-03 | ʼ汾4 ⡢husky + CI/CD ˫ػơʹ÷ʽΥ޸ָ |
