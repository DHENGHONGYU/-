---
doc_id: V9-DOC-REF-971
title: v9-数据血缘追踪
tier: important
code_version: "2.0.0-rc.2"
version: v1.4.0
last_updated: 2026-08-17
change_log:
  - version: v1.4.0
    changes: "iFinD MCP 全面补充：新增 1.3 iFinD MCP 数据采集架构、2.22 14维度数据映射、3.7 MCP 采集链路序列图、5.5 MCP 服务器清单；补充 5.4 采集管线文件索引"
    date: 2026-08-17
  - version: v1.3.0
    changes: "新增 2.21 交易策略复盘(ChipStrategyReview)数据实体、3.6 交易策略复盘序列图、5.1 映射矩阵补充、5.4 关键文件索引补充"
    date: 2026-08-17
  - version: v1.0.0
    changes: "C 类版本闭环(2026-08-11)：补全 change_log 初始条目"
    date: 2026-08-11
---


# V9 数据血缘追踪与数据流全景图

> **文档版本**: v1.4
> **更新日期**: 2026-08-17
> **创建日期**: 2026-06-28
> **适用范围**: V9 智能投研复盘系统全栈数据链路
> **数据模型版本**: DB_VERSION = 34（与 `src/config/dbConfig.ts` 导出值一致）

---

## 目录

1. [数据流全景图](#1-数据流全景图)
2. [数据血缘表](#2-数据血缘表)
3. [关键数据链路](#3-关键数据链路)
4. [数据变更传播路径](#4-数据变更传播路径)
5. [附录](#5-附录)

---

## 1. 数据流全景图

### 1.1 整体架构总览

V9 系统采用纯前端架构，数据在浏览器内完成从采集、标准化、持久化到订阅消费的完整生命周期。核心数据流转路径如下：

```mermaid
flowchart TD
    subgraph COL["数据采集层 (src/services/data-collector/)"]
        direction LR
        MC["MockCollector<br/>(本地模拟)"]
        RC["RestCollector<br/>(REST API)"]
        WSC["WebSocketCollector<br/>(流式推送)"]
        TS["TaskScheduler<br/>(任务调度)"]
        MC --> TS
        RC --> TS
        WSC --> TS
    end

    subgraph STD["数据标准化层"]
        MDA["MarketDataAdapter<br/>(市场数据适配器)"]
        RAW["RawMarketData<br/>(原始数据)"]
        STD_DATA["MarketData<br/>(标准化数据)"]
        RAW --> MDA --> STD_DATA
    end

    subgraph WRITE["写入层 (DataBridge)"]
        direction TB
        DBA["DataBridgeAdapter<br/>(模块适配层)"]
        DB_CORE["DataBridge<br/>(核心路由)"]
        ACL["ACL 权限校验<br/>(aclEngine)"]
        AUDIT["审计日志<br/>(researchLogs)"]
        FBQ["FallbackQueue<br/>(失败重试队列)"]
        
        DBA --> DB_CORE
        DB_CORE --> ACL
        ACL --> AUDIT
        ACL -- 失败 --> FBQ
    end

    subgraph STORAGE["持久化层 (IndexedDB)"]
        direction TB
        IDB["V6Database<br/>(IndexedDB 封装)"]
        STORES["53 个 ObjectStore<br/>(stocks, v6_scores, orders, news, ...)"]
        IDB --> STORES
    end

    subgraph BROADCAST["广播层 (DataFlowEngine)"]
        direction TB
        DFE["DataFlowEngine<br/>(订阅引擎)"]
        CHANNELS["12 个数据通道<br/>(market:*, portfolio:*, strategy:*, ...)"]
        CACHE["内存缓存<br/>(channel -> data)"]
        DFE --> CHANNELS
        DFE --> CACHE
    end

    subgraph CONSUME["UI 消费层 (src/cockpit/)"]
        direction TB
        MDP["MarketDataProvider<br/>(React Context)"]
        WE["WidgetEngine<br/>(Widget 生命周期)"]
        WIDGETS["18+ Widget<br/>(KaiScoreWidget, HotSectorWidget, ...)"]
        PAGES["多页面<br/>(NewsPage, TradingPage, ...)"]
        
        MDP --> WE --> WIDGETS
        MDP --> PAGES
    end

    subgraph SERVICE["业务服务层 (计算/交易/新闻)"]
        direction TB
        SCORE["评分服务<br/>(v6ScoreService, hotSectorAnalyzer, valuePitAnalyzer)"]
        TRADE["交易服务<br/>(tradingService, riskEngine, signalGenerator)"]
        NEWS["新闻服务<br/>(newsService, sentimentAnalyzer, stockLinker)"]
        ROTATION["轮动服务<br/>(rotationScoreService)"]
    end

    TS --> RAW
    STD_DATA --> MDP
    
    SCORE --> DBA
    TRADE --> DBA
    NEWS --> DBA
    ROTATION --> DBA
    
    DB_CORE --> IDB
    IDB --> DFE
    
    DFE --> MDP
    DFE -.-> WIDGETS
    
    IDB --> SCORE
    IDB --> TRADE
    IDB --> NEWS
    IDB --> ROTATION
    
    style COL fill:#e1f5fe,stroke:#01579b
    style STD fill:#f3e5f5,stroke:#4a148c
    style WRITE fill:#fff3e0,stroke:#e65100
    style STORAGE fill:#e8f5e9,stroke:#1b5e20
    style BROADCAST fill:#fce4ec,stroke:#880e4f
    style CONSUME fill:#e0f7fa,stroke:#006064
    style SERVICE fill:#fbe9e7,stroke:#bf360c
```

### 1.2 数据流转分层详解

| 层级 | 核心模块 | 主要职责 | 关键文件 |
|------|---------|---------|---------|
| **数据采集层** | MockCollector / RestCollector / WebSocketCollector / TaskScheduler | 按配置定时采集原始市场数据，支持三种数据源模式 | `src/services/data-collector/` |
| **数据标准化层** | MarketDataAdapter | 将不同来源的 RawMarketData 统一映射为标准 MarketData 接口 | `src/services/data-collector/MarketDataAdapter.ts` |
| **写入层** | DataBridge / DataBridgeAdapter / ACL Engine | 统一写入入口，权限校验，审计日志，失败重试 | `src/core/databridge.ts`, `../../src/showcase/index.ts` |
| **持久化层** | V6Database (IndexedDB) | 53 个 ObjectStore 存储各类业务数据 | `src/data/db.ts` |
| **广播层** | DataFlowEngine | 基于通道的发布订阅模式，内存缓存，SSE/轮询双模式 | `src/core/dataflow/dataflowEngine.ts` |
| **业务服务层** | v6ScoreService / tradingService / newsService / rotationScoreService | 各类业务计算、风控、情绪分析等 | `src/services/scoring/`, `src/services/trading/`, `src/services/news/` |
| **UI 消费层** | MarketDataProvider / WidgetEngine / 各 Widget | React Context 注入数据，Widget 订阅渲染 | `src/cockpit/` |

### 1.3 iFinD MCP 数据采集架构

V9 系统通过 **同花顺 iFinD MCP 插件** 接入专业金融数据源，替代传统 Python ETL Bridge 方案，实现零额外进程、TypeScript 全链路类型安全的 MCP 协议采集。

#### 1.3.1 架构概览

```mermaid
flowchart TD
    subgraph UI["前端 UI 层"]
        CSP["采集策略配置面板<br/>(sevenDimConfigStore)"]
        CW["采集向导<br/>(CollectionWizard)"]
        DTP["数据测试面板<br/>(ApiTestDialog)"]
    end

    subgraph ORCH["采集编排层"]
        PL["collectionPipeline<br/>(采集流水线)"]
        MSF["multiSourceFetcher<br/>(多源拉取器)"]
        ASO["adaptiveSourceOrchestrator<br/>(自适应熔断)"]
        MCP_ADAPTER["mcpCollector<br/>(MCP 适配层)"]
    end

    subgraph SOURCES["数据源层"]
        subgraph MCP["iFinD MCP Servers"]
            S_STOCK["stock-mcp<br/>(get_stock_info, get_stock_financials,<br/>get_stock_performance, stock_highfreq_quotes,<br/>get_stock_shareholders, get_stock_events,<br/>get_stock_summary, search_stocks)"]
            S_INDEX["index-mcp<br/>(index_data, sector_data,<br/>index_highfreq_quotes)"]
            S_NEWS["news-mcp<br/>(search_notice, search_news)"]
            S_FUND["fund-mcp<br/>(get_fund_profile, get_fund_portfolio,<br/>fund_highfreq_quotes)"]
            S_GLOBAL["global-stock-mcp<br/>(global_stock_profile, global_stock_quotes)"]
        end
        subgraph LEGACY["传统数据源（降级链）"]
            TS["Tushare"]
            EM["东财爬虫"]
            TX["腾讯/新浪代理"]
        end
    end

    subgraph STORAGE["持久化 & 消费"]
        DB["DataBridge → IndexedDB"]
        SCORE["V6 评分 / 板块轮动 / 策略引擎"]
        PAGE["各舱室页面"]
    end

    CSP -->|"触发采集 (16维度)"| PL
    PL -->|"维度 01-02 (行情/K线)"| MSF
    PL -->|"维度 04-05, 08-09, 13-14 (MCP 优先)"| MCP_ADAPTER
    PL -->|"维度 03, 06-07, 15-16 (多源降级)"| MSF
    MSF -->|"Tushare → 东财 → 新浪"| LEGACY
    MCP_ADAPTER -->|"自然语言查询 → MCP"| MCP
    ASO -.->|"熔断监控"| MSF
    ASO -.->|"熔断监控"| MCP_ADAPTER
    LEGACY --> DB
    MCP --> DB
    DB --> SCORE
    DB --> PAGE

    style UI fill:#e1f5fe,stroke:#01579b
    style ORCH fill:#fff3e0,stroke:#e65100
    style MCP fill:#e8f5e9,stroke:#1b5e20
    style LEGACY fill:#fce4ec,stroke:#880e4f
    style STORAGE fill:#e0f7fa,stroke:#006064
```

#### 1.3.2 16维度数据源分工

| 维度代码 | 维度名称 | 主数据源 | 降级链 | 采集方式 |
|---------|---------|---------|--------|---------|
| 01 | 基本信息 | AKShare | tencent → sina → akshare | 行情API直连 |
| 02 | K线数据 | AKShare | tencent → sina → akshare | 行情API直连 |
| 03 | 筹码分布 | Tushare | 东财爬虫 → 新浪代理 | multiSourceFetcher |
| 04 | 重大事项 | **iFinD MCP** | westock → Tushare → 东财 → 新浪 | mcpCollector 优先 |
| 05 | 热点新闻 | **iFinD MCP** | westock+腾讯 → Tushare → 东财 → 新浪 | mcpCollector 优先, KIMI AI增强 |
| 06 | 行业竞品 | Tushare | 东财爬虫 → 腾讯代理 | multiSourceFetcher |
| 07 | 关联指数 | Tushare | 腾讯代理 | multiSourceFetcher (Pearson 计算) |
| 08 | 研报中心 | **iFinD MCP** | westock → Tushare → 东财 | mcpCollector 优先, KIMI AI增强 |
| 09 | 财务数据 | **iFinD MCP** | fetcherService | mcpCollector 优先 / fetcherService |
| 10 | 热门板块 | **iFinD MCP** | 无 | mcpCollector (sector_data) |
| 11 | 技术指标 | **iFinD MCP** | AKShare | mcpCollector (stock_highfreq_quotes) |
| 12 | 资金流向 | **iFinD MCP** | AKShare | mcpCollector (get_stock_performance) |
| 13 | 机构持仓 | **iFinD MCP** | 无 | mcpCollector (get_stock_shareholders) |
| 14 | 估值分析 | **iFinD MCP** | 无 | mcpCollector (get_stock_financials) |
| 15 | 分红股本 | Tushare | 东财爬虫 | multiSourceFetcher (三 API 并行) |
| 16 | 一致预期 | 东财爬虫 | 无 | multiSourceFetcher (并行预期+评级) |

#### 1.3.3 采集流水线核心流程

```
用户触发采集 (sevenDimConfigStore.runCollection)
  → 按策略模板解析启用的维度 (16维)
  → 按维度并发执行 runBatchTrace (每批 5 只股票)
    → 单次 runSingleTrace:
      ├─ 行情/ K线 (dim 01-02): dataSourceOrchestrator → 腾讯/新浪/ AKShare
      ├─ MCP 维度 (dim 04-05, 08-14): mcpCollector → iFinD MCP → 自然语言查询
      ├─ 多源维度 (dim 03, 06-07, 15-16): multiSourceFetcher → Tushare → 东财 → 新浪
      └─ 财务数据 (dim 09): fetcherService.fetchFinancial → DataBridge → financial_reports
    → 数据写入 DataBridge → IndexedDB
    → emit COLLECTION_EVENTS (进度/完成/失败)
  → 采集完成 → KIMI AI 增强 (dim 05 新闻摘要, dim 08 研报解读)
```

---

## 2. 数据血缘表

按数据实体分组，追踪每条数据从来源到最终消费的完整路径。

### 2.1 股票基础数据 (Stock)

| 维度 | 详情 |
|------|------|
| **数据实体** | `Stock` (symbol, name, price, pe, pb, roe, marketCap, sector, researchStatus, group, ...) |
| **来源** | 手动录入 / 批量导入 / Fetcher 数据采集 |
| **采集器** | `inputService` (手动), `batchImportService` (批量), `fetcherService` (API) |
| **处理节点** | `dataLayer.stocks.add()` → `sendWriteEnvelope('insertStock')` → `dataBridge.forward()` → ACL 校验 → `db.put('stocks')` |
| **Store** | `stocks` (keyPath: `symbol`, 索引: `by-status`, `by-group`) |
| **消费方** | `v6ScoreService` (评分输入), `tradingService` (交易标的), `newsService` (股票关联), `hotSectorAnalyzer`, `valuePitAnalyzer`, `StockPoolWidget`, `WatchlistWidget` |

### 2.2 K线行情数据 (DailyQuotes)

| 维度 | 详情 |
|------|------|
| **数据实体** | `DailyQuotes` (symbol, latest, history[]) |
| **来源** | Fetcher 行情接口 / 模拟数据 |
| **采集器** | `fetcherService`, `MockCollector` |
| **处理节点** | `fetcherService` → `dataBridge.forward(SAVE_DAILY_QUOTES)` → ACL 校验 → `db.put('daily_quotes')` |
| **Store** | `dailyQuotes` (keyPath: `symbol`) |
| **消费方** | `v6ScoreService` (动量/波动/流动性因子), `hotSectorAnalyzer` (技术突破维度), `valuePitAnalyzer` (轮动位置维度), `rotationScoreService` (市场风格判断) |

### 2.3 V6 评分数据 (V6Score)

| 维度 | 详情 |
|------|------|
| **数据实体** | `V6Score` (symbol, score, factors, algorithmVersion, calculatedAt, dataVersion, qualityWarning?) |
| **来源** | 规则引擎计算 + LLM 增强（可选） |
| **计算引擎** | `v6ScoreService.runV6Score()` / `runV6ScoreV2()` (L-1~L8 漏斗模型) |
| **处理节点** | 读取 `stocks` + `dailyQuotes` → 规则引擎计算 9 因子 → LLM 补充缺失因子 → 合成总分 → `dataBridge.forward(SAVE_V6_SCORE)` → `db.put('v6_scores')` |
| **Store** | `v6Scores` (keyPath: `symbol`) |
| **消费方** | `hotSectorAnalyzer` (输入前置过滤), `valuePitAnalyzer` (输入前置过滤), `KaiScoreWidget`, `StockPoolWidget`, `tradingService` (信号生成) |

### 2.4 热门板块评分 (HotSectorScore)

| 维度 | 详情 |
|------|------|
| **数据实体** | `HotSectorScore` (symbol, name, score, dimensions{momentum,sentiment,technical,valuation,marketEnv}, action, calculatedAt) |
| **来源** | 双策略体系 - 热门板块策略五维评分 |
| **计算引擎** | `hotSectorAnalyzer.analyzeHotSectors()` |
| **处理节点** | 读取 `stocks` + `dailyQuotes` + `v6Scores` → 五维评分（动量/情绪/技术/估值/大盘）→ 合成 action → `dataLayer.hotSectorScores.save()` → `db.put('hot_sector_scores')` |
| **Store** | `hotSectorScores` (keyPath: `symbol`, 索引: `by-calculated-at`) |
| **消费方** | `HotSectorWidget`, `tradingService` (信号生成), `dualStrategyEngine` |

### 2.5 价值洼地评分 (ValuePitScore)

| 维度 | 详情 |
|------|------|
| **数据实体** | `ValuePitScore` (symbol, name, score, dimensions{catalyst,valuation,chip,rotation,liquidity}, rotationSignal, action, calculatedAt) |
| **来源** | 双策略体系 - 价值洼地策略五维评分 |
| **计算引擎** | `valuePitAnalyzer.analyzeValuePits()` |
| **处理节点** | 读取 `stocks` + `dailyQuotes` + `v6Scores` → 五维评分（催化/估值/筹码/轮动/流动性）→ 合成 action → `dataLayer.valuePitScores.save()` → `db.put('value_pit_scores')` |
| **Store** | `valuePitScores` (keyPath: `symbol`, 索引: `by-calculated-at`) |
| **消费方** | `ValuePitWidget`, `tradingService` (信号生成), `dualStrategyEngine` |

### 2.6 交易订单 (Order)

| 维度 | 详情 |
|------|------|
| **数据实体** | `Order` (id, symbol, direction, quantity, price, amount, status, accountType, createdAt, planStopLoss?, planTakeProfit?, ...) |
| **来源** | 用户下单 / 策略自动下单 |
| **服务** | `tradingService.createBuyOrder()` / `createSellOrder()` |
| **处理节点** | 信号生成 → 仓位计算 → 风控校验 (`riskEngine.checkOrderRisk`) → 构建订单 → `dataBridge.forward(INSERT_ORDER)` → ACL 校验 → `db.put('orders')` |
| **Store** | `orders` (keyPath: `id`) |
| **消费方** | `tradingService` (持仓计算), `tradeReviewAI` (AI复盘), `PnLAnalysisWidget`, `PortfolioOverviewWidget`, `AITradeReviewWidget` |

### 2.7 交易信号 (Signal)

| 维度 | 详情 |
|------|------|
| **数据实体** | `Signal` (id, symbol, direction, confidence, reason, source, createdAt) |
| **来源** | 信号生成器 / 策略引擎 |
| **服务** | `signalGenerator.generateSignalsForSymbol()`, `tradingService.scanWatchingSignals()` |
| **处理节点** | 读取 `stocks` + `v6Scores` + 技术指标 → 生成多维度信号 → 选取最强信号 → `dataBridge.forward(INSERT_SIGNAL)` → ACL 校验 → `db.put('signals')` |
| **Store** | `signals` (keyPath: `id`) |
| **消费方** | `SignalMonitorWidget`, `tradingService` (交易建议), `positionSizer` (仓位计算) |

### 2.8 资讯文章 (NewsArticle)

| 维度 | 详情 |
|------|------|
| **数据实体** | `NewsArticle` (id, title, content, url, source, category, publishTime, fetchTime, sentiment, sentimentConfidence, relatedStocks[], keywords, hash) |
| **来源** | 资讯 API / 手动导入 / 模拟数据 |
| **服务** | `newsService.saveNewsArticle()` / `saveNewsArticles()` |
| **处理节点** | 原始文章 → 哈希去重 → 情绪分析 (`sentimentAnalyzer`) → 股票关联 (`stockLinker`) → 保存关联映射 → `dataLayer.news.save()` → `dataBridge.forward(SAVE_NEWS)` → `db.put('news')` |
| **Store** | `news` (keyPath: `id`, 索引: `by-source`, `by-category`, `by-publish-time`, `by-hash`) |
| **消费方** | `NewsPage`, `MarketSentimentWidget` (情绪聚合), `newsService.getNewsBySymbol()`, `StockChatWidget` |

### 2.9 情绪分析缓存 (SentimentCache)

| 维度 | 详情 |
|------|------|
| **数据实体** | `SentimentCache` (id, contentHash, sentiment, confidence, analyzedAt, model?) |
| **来源** | 情绪分析器计算结果 |
| **计算引擎** | `sentimentAnalyzer.analyzeNewsArticle()` / `getOrAnalyzeSentiment()` |
| **处理节点** | 文本输入 → 查找缓存 (by-content-hash) → 命中则直接返回 → 未命中则分析 → 写入缓存 → 返回结果 |
| **Store** | `sentimentCache` (keyPath: `id`, 索引: `by-content-hash`, `by-analyzed-at`) |
| **消费方** | `newsService` (文章情绪标注), 其他需要情绪分析的模块 |

### 2.10 股票-资讯关联 (NewsStockMap)

| 维度 | 详情 |
|------|------|
| **数据实体** | `NewsStockMap` (id, newsId, symbol, relevanceScore, matchType) |
| **来源** | 股票关联器自动匹配 |
| **计算引擎** | `stockLinker.linkArticleToStocks()` |
| **处理节点** | 文章标题+内容 → 关键词匹配股票库 → 生成关联映射 → `dataLayer.newsStockMap.save()` → `db.put('news_stock_map')` |
| **Store** | `newsStockMap` (keyPath: `id`, 索引: `by-symbol`, `by-news`) |
| **消费方** | `newsService.getNewsBySymbol()`, `NewsPage` (按股票筛选) |

### 2.11 板块轮动评分 (RotationSectorScore)

| 维度 | 详情 |
|------|------|
| **数据实体** | `RotationSectorScore` (id, sectorCode, sectorName, scoreDate, subScores, f1~f5, total, resonance, signal, alert, decline, poolStocks?, analysisReport?, modelUsed?) |
| **来源** | 板块轮动量化引擎（五因子模型） |
| **计算引擎** | `rotationScoreService.saveRotationScore()`, `rotationCalculator` |
| **处理节点** | 子分数输入 → 验证 → 五因子合成 → 共振度计算 → 信号分级 → `dataBridge.forward(SAVE_ROTATION_SCORES)` → `db.put('rotation_scores')` |
| **Store** | `rotationScores` (keyPath: `id`, 索引: `by-sector-date`(唯一), `by-sector`, `by-total`, `by-resonance`) |
| **消费方** | `rotationScoreService.getRotationScores()`, 策略引擎, `SectorHeatmapWidget` |

### 2.12 板块评分 (SectorScoreRecord)

| 维度 | 详情 |
|------|------|
| **数据实体** | `SectorScoreRecord` (id, sectorCode, sectorName, composite, dimensions[], isCore, keyStocks[], recommendation, ...) |
| **来源** | 行业分析 / SKILL 评分 |
| **服务** | `sectorScoreService` |
| **处理节点** | 行业数据 → 多维度评分 → 合成综合分 → `dataBridge.forward(SAVE_SECTOR_SCORES)` → `db.put('sector_scores')` |
| **Store** | `sectorScores` (keyPath: `id`, 索引: `by-sector`, `by-composite`, `by-is-core`) |
| **消费方** | `SectorHeatmapWidget`, 选股策略, `rotationScoreService` (兜底数据) |

### 2.13 智能评分 (IntelligentScore)

| 维度 | 详情 |
|------|------|
| **数据实体** | `IntelligentScore` (id?, symbol, overallScore, dimensionScores[], summary, basis, missingFields, sourceSnapshot, configSnapshot, modelResponse, scoredAt) |
| **来源** | LLM 驱动的智能评分（SKILL） |
| **服务** | `intelligentScoreService` |
| **处理节点** | 读取股票数据 + 本地文档 → LLM 评分 → `dataBridge.forward(SAVE_INTELLIGENT_SCORES)` → `db.put('intelligent_scores')` |
| **Store** | `intelligentScores` (keyPath: `id` autoIncrement, 索引: `by-symbol`) |
| **消费方** | 个股分析页, `KaiScoreWidget` (数据来源之一) |

### 2.14 行业评分 (IndustryScore)

| 维度 | 详情 |
|------|------|
| **数据实体** | `IndustryScore` (id?, code, name, overallScore, dimensionScores[], summary, basis, sectorSnapshot, configSnapshot, modelResponse, scoredAt) |
| **来源** | LLM 驱动的行业评分（SKILL） |
| **服务** | `industryScoreService` |
| **处理节点** | 行业数据 → LLM 评分 → `dataBridge.forward(SAVE_INDUSTRY_SCORES)` → `db.put('industry_scores')` |
| **Store** | `industryScores` (keyPath: `id` autoIncrement, 索引: `by-code`) |
| **消费方** | 行业分析页, 板块轮动参考 |

### 2.15 评分文档 (ScoreDocVersion)

| 维度 | 详情 |
|------|------|
| **数据实体** | `ScoreDocVersion` (docId, symbol, version, composite, dimensions[], content, createdAt) |
| **来源** | 评分文档生成器 |
| **服务** | `scoreDocService` |
| **处理节点** | 评分结果 → 生成文档 → 版本化存储 → `dataBridge.forward(SAVE_SCORE_DOCS)` → `db.put('score_docs')` |
| **Store** | `scoreDocs` (keyPath: `docId`, 索引: `by-symbol`, `by-symbol-version`(唯一), `by-composite`) |
| **消费方** | 个股深度分析页, 文档对比功能 |

### 2.16 策略快照 (StrategySnapshot)

| 维度 | 详情 |
|------|------|
| **数据实体** | `StrategySnapshot` (id, version, date, timestamp, holdings, scores, signals, performance) |
| **来源** | 策略运行时快照 |
| **服务** | `strategySnapshotService` |
| **处理节点** | 策略运行 → 定时/触发式快照 → `dataBridge.forward(SAVE_STRATEGY_SNAPSHOTS)` → `db.put('strategy_snapshots')` |
| **Store** | `strategySnapshots` (keyPath: `id`, 索引: `by-version`(唯一), `by-date`, `by-timestamp`) |
| **消费方** | 策略回测, 复盘分析, `dualStrategyEngine` |

### 2.17 本地知识库 (LocalDoc)

| 维度 | 详情 |
|------|------|
| **数据实体** | `LocalDoc` (id, symbol, title, content, category, source, addedAt) |
| **来源** | 用户上传 / 导入的研究资料 |
| **服务** | `localDocService` |
| **处理节点** | 文档上传 → 解析 → 分类 → `dataBridge.forward(SAVE_LOCAL_DOCS)` → `db.put('local_docs')` |
| **Store** | `localDocs` (keyPath: `id`, 索引: `by-symbol`, `by-category`, `by-added-at`) |
| **消费方** | `intelligentScoreService` (评分输入), 研究笔记功能 |

### 2.18 自选股 (Watchlist)

| 维度 | 详情 |
|------|------|
| **数据实体** | `Watchlist` (id, name, items[], createdAt, updatedAt) |
| **来源** | 用户创建 |
| **服务** | `stockpoolService` |
| **处理节点** | 用户操作 → `dataBridge.forward()` → ACL 校验 → `db.put('watchlists')` |
| **Store** | `watchlists` (keyPath: `id`) |
| **消费方** | `WatchlistWidget`, `tradingService` (观察池扫描) |

### 2.19 研究日志 (ResearchLog)

| 维度 | 详情 |
|------|------|
| **数据实体** | `ResearchLog` (id, action, module, symbol?, detail, timestamp) |
| **来源** | DataBridge 审计自动生成 |
| **服务** | DataBridge 内部 `writeAuditLog()` |
| **处理节点** | DataBridge.forward() → ACL 通过 → 异步写入审计日志 → `db.put('research_logs')` |
| **Store** | `researchLogs` (keyPath: `id` autoIncrement) |
| **消费方** | 审计追溯, 操作历史, 系统健康监控 |

### 2.20 资讯收藏 (NewsBookmark)

| 维度 | 详情 |
|------|------|
| **数据实体** | `NewsBookmark` (id, newsId, bookmarkedAt, note?) |
| **来源** | 用户收藏操作 |
| **服务** | `newsService` |
| **处理节点** | 用户点击收藏 → `dataBridge.forward(NEWS_ARTICLE_BOOKMARKED)` → `db.put('news_bookmarks')` |
| **Store** | `newsBookmarks` (keyPath: `id`, 索引: `by-bookmarked-at`) |
| **消费方** | `NewsPage` (收藏夹视图) |

### 2.21 交易策略复盘 (ChipStrategyReview)

| 维度 | 详情 |
|------|------|
| **数据实体** | `ChipAnalysisResult` (symbol, name, price, pe, position, energy, overlap, divergence, action, signal, confidence, reason, scoredAt) |
| **来源** | 筹码分布数据 + 换手率/量比 + 前60日涨跌幅 → 多维度交叉分析 |
| **计算引擎** | `analyzeStockChips()` / `computeActualMatch()` / `computeTurnoverVolumeEnergy()` |
| **处理节点** | 读取意向池/研究池/持仓池 → 获取个股基础数据 → 筹码分布分析 → 换手率量能评估 → 灰色地带决策 → 生成交易信号(买入/卖出/持有) → 前端渲染 |
| **Store** | 无独立 Store（计算结果内存态，支持导出 Excel） |
| **消费方** | `ChipStrategyReviewPage` (筹码与交易策略复盘页面) |
| **数据依赖** | `intentionPoolStore` (意向池), `researchPoolStore` (研究池), `positionPoolStore` (持仓池), `stocks` (基础数据) |
| **关键逻辑** | 筹码信号分为：主力吸筹/主力拉升/主力出货/主力撤退/散户追涨/散户割肉；结合换手率量能(高/中/低)和乖离率，生成综合交易建议 |

### 2.22 iFinD MCP 14维度数据映射 (McpDimensionMapping)

| 维度 | 详情 |
|------|------|
| **数据实体** | `McpDimensionMapping` (dimensionCode, dimensionName, queries[{name, serverName, toolName, buildArgs, parseResponse}]) |
| **来源** | 同花顺 iFinD MCP 插件（5 个 MCP Server，18 个 Tool） |
| **采集器** | `mcpCollector` (MCP 适配层) |
| **处理节点** | UI 触发采集 → `collectionPipeline.runSingleTrace` → `mcpCollector.buildMcpRequests(ctx)` → `run_mcp` 调用 iFinD Tool → `mcpCollector.aggregateMcpResults()` → DataBridge 写入 IndexedDB |
| **Store** | 按维度分别写入对应 Store（news / sectorScores / researchLogs / financial_reports / localDocs 等） |
| **消费方** | 各舱室页面（NewsPage / SectorHeatmapWidget / 研报分析 / 估值分析 / 技术指标 Widget） |
| **14维度→MCP Tool 映射** | 见下表 |

#### 14维度 → iFinD MCP Tool 映射表

| 维度 | 名称 | iFinD MCP Server | MCP Tool | 查询内容 |
|------|------|-----------------|----------|---------|
| 01 | 基本信息 | stock-mcp | `get_stock_info` + `get_stock_summary` | 上市时间、申万行业、主营业务、PE/PB/ROE/市值 |
| 02 | K线数据 | stock-mcp | `get_stock_performance` + `stock_highfreq_quotes` | 日K线(开高低收量额)、实时行情、换手率 |
| 03 | 筹码分布 | stock-mcp | `get_stock_shareholders` | 股东户数、户均持股、前十大股东占比 |
| 04 | 重大事项 | news-mcp + stock-mcp | `search_notice` + `get_stock_events` | 近一年公告、增减持、股权激励、回购 |
| 05 | 热点新闻 | news-mcp | `search_news` | 近30天最新动态 |
| 06 | 行业竞品 | stock-mcp + index-mcp | `search_stocks` + `sector_data` | 行业市值排名、竞品公司、申万板块行情估值 |
| 07 | 关联指数 | index-mcp | `index_data` | 沪深300/中证500/行业指数涨跌幅 |
| 08 | 研报中心 | news-mcp | `search_news` (研报类) | 近180天研报、评级、目标价 |
| 09 | 财务数据 | stock-mcp | `get_stock_financials` | 营收/净利润/ROE/毛利率/净利率/经营现金流/EPS |
| 10 | 热门板块 | index-mcp | `sector_data` | 所属板块近7天行情涨跌幅、成交额排名 |
| 11 | 技术指标 | stock-mcp | `stock_highfreq_quotes` | MA5/10/20/60、KDJ、MACD、RSI (5min高频) |
| 12 | 资金流向 | stock-mcp | `get_stock_performance` | 融资融券余额、龙虎榜、主力资金流向 |
| 13 | 机构持仓 | stock-mcp | `get_stock_shareholders` | 机构持股比例、基金/北向/社保/QFII持仓 |
| 14 | 估值分析 | stock-mcp | `get_stock_financials` | PE/PB/PS/EV_EBITDA/PEG/股息率历史分位 |

> **架构优势**：iFinD MCP 采用自然语言查询（`query` 参数），无需硬编码 API 参数，维度扩展只需新增 `DIMENSION_MCP_MAPPING` 条目，零代码改动。

---

## 3. 关键数据链路

### 3.1 股票基础数据链路（行情 → 存储 → UI展示）

```mermaid
sequenceDiagram
    participant U as 用户/导入
    participant IS as inputService / batchImportService
    participant DL as dataLayer.stocks
    participant DB as DataBridge
    participant IDB as IndexedDB (stocks)
    participant DFE as DataFlowEngine
    participant MDP as MarketDataProvider
    participant W as StockPoolWidget

    U->>IS: 录入/导入股票 (symbol, name, price, pe, ...)
    IS->>DL: stockStore.add(stockData)
    DL->>DB: forward(INSERT_STOCK, payload=stock)
    Note over DB: 1. 验证 Envelope<br/>2. ACL 权限校验<br/>3. 推断目标 Store: stocks<br/>4. 写入审计日志
    DB->>IDB: put('stocks', stock)
    IDB-->>DB: 写入成功
    DB->>DB: broadcast('stocks', envelope)
    Note over DB: 通知所有订阅 stocks 通道的订阅者
    
    Note over DFE: 通道模式:<br/>DataFlowEngine 管理 12 个独立通道
    DFE->>MDP: market:index / market:sector 等通道推送
    MDP->>W: setData(updated marketData)
    W->>W: 重新渲染股票列表
```

**链路要点：**

1. **入口**: `inputService.addStock()` / `batchImportService.importStocks()`
2. **写入**: `dataLayer.stocks.add()` → `sendWriteEnvelope('insertStock')` → `dataBridge.forward()`
3. **校验**: Envelope 格式验证 → ACL 权限检查 (`stockpool` 模块 → `stocks` Store INSERT)
4. **存储**: `db.put('stocks', stock)` (keyPath: `symbol`)
5. **广播**: DataBridge 写入完成后 `broadcast(targetStore, envelope)`
6. **消费**: MarketDataProvider 订阅相关通道 → Widget 通过 `useMarketData()` 获取数据

### 3.2 评分数据链路（采集 → 计算 → 存储 → 订阅 → 展示）

```mermaid
sequenceDiagram
    participant FS as fetcherService
    participant IDB as IndexedDB
    participant VS as v6ScoreService
    participant V6E as V6 Engine (L0~L8)
    participant LLM as LLM Client (可选)
    participant DB as DataBridge
    participant DFE as DataFlowEngine
    participant KW as KaiScoreWidget

    FS->>IDB: 写入 stocks + dailyQuotes
    Note over VS: 触发评分: runV6ScoreV2(symbol)
    
    VS->>IDB: 读取 stock 基础数据
    VS->>IDB: 读取 dailyQuotes K线数据
    IDB-->>VS: 返回数据
    
    VS->>V6E: createV6Engine().calculateAll(input)
    Note over V6E: L-1: 行业评分<br/>L0: 宏观扫描<br/>L1-L2: 护城河定性<br/>L3: 财务量化<br/>L4-L6: 估值情景<br/>L7-L8: 技术筹码
    
    alt LLM 增强模式
        V6E->>LLM: 请求补充定性因子
        LLM-->>V6E: 返回因子评分
    end
    
    V6E-->>VS: CompositeScore (score, rating, layers, risks)
    
    VS->>DB: forward(SAVE_V6_SCORE, V6Score)
    Note over DB: ACL: analyzer → v6_scores INSERT
    
    DB->>IDB: put('v6_scores', score)
    IDB-->>DB: 写入成功
    DB->>DB: broadcast('v6_scores', envelope)
    
    Note over DFE: strategy:score 通道
    DFE->>KW: 推送评分更新
    KW->>KW: 渲染评分图谱
```

**链路要点：**

1. **触发**: 用户主动触发 / 策略定时触发
2. **数据输入**: 从 `stocks` + `dailyQuotes` 读取基础数据
3. **计算引擎**: V6 v2.0 引擎（11 层漏斗模型），规则引擎为主，LLM 可选增强
4. **写入**: 通过 `dataBridge.forward(SAVE_V6_SCORE)` 持久化
5. **权限**: `analyzer` 模块对 `v6_scores` 有 INSERT/UPDATE 权限
6. **下游消费**: HotSectorAnalyzer / ValuePitAnalyzer 作为前置过滤输入，KaiScoreWidget 展示

### 3.3 交易订单链路（下单 → 风控 → 存储 → 复盘）

```mermaid
sequenceDiagram
    participant U as 用户
    participant TS as tradingService
    participant SG as signalGenerator
    participant PS as positionSizer
    participant RE as riskEngine
    participant DB as DataBridge
    participant IDB as IndexedDB
    participant TRA as tradeReviewAI
    participant PNL as PnLAnalysisWidget

    U->>TS: createBuyOrder(stock, quantity)
    TS->>TS: createOrderWithRiskCheck(input)
    
    TS->>RE: checkOrderRisk({symbol, direction, quantity, price})
    Note over RE: 风控检查项:<br/>- 单票仓位上限<br/>- 总仓位上限<br/>- 止损/止盈合理性<br/>- 流动性检查
    
    alt 风控未通过
        RE-->>TS: { ok: false, blocks: [...] }
        TS-->>U: 返回错误
    else 风控通过
        RE-->>TS: { ok: true, warnings: [...] }
        
        TS->>TS: buildOrder(input) → Order
        TS->>DB: forward(INSERT_ORDER, order)
        Note over DB: ACL: tradinghub → orders INSERT
        
        DB->>IDB: put('orders', order)
        IDB-->>DB: 写入成功
        DB->>DB: broadcast('orders', envelope)
        
        TS-->>U: 返回成功订单
    end
    
    Note over TRA: 复盘链路 (异步/按需)
    TRA->>IDB: 读取全部订单
    TRA->>TRA: AI 复盘分析 (胜率/纪律/错误分类)
    TRA-->>PNL: 推送复盘结果
    PNL->>PNL: 渲染盈亏分析
```

**链路要点：**

1. **入口**: `tradingService.createBuyOrder()` / `createSellOrder()`
2. **风控**: `riskEngine.checkOrderRisk()` 检查仓位、止损、流动性等
3. **构建**: `buildOrder()` 生成标准 Order 对象
4. **写入**: `dataBridge.forward(INSERT_ORDER)` → ACL 校验 → IndexedDB
5. **权限**: `tradinghub` 模块对 `orders` 有 INSERT/UPDATE/DELETE 权限
6. **下游**: 持仓计算、AI复盘、盈亏分析 Widget 均从 `orders` Store 读取

### 3.4 新闻情绪链路（采集 → 分析 → 存储 → 展示）

```mermaid
sequenceDiagram
    participant SRC as 资讯源
    participant NS as newsService
    participant SA as sentimentAnalyzer
    participant SL as stockLinker
    participant DL as dataLayer
    participant DB as DataBridge
    participant IDB as IndexedDB
    participant NP as NewsPage
    participant MS as MarketSentimentWidget

    SRC->>NS: 原始文章列表
    NS->>NS: saveNewsArticle(article)
    
    Note over NS: 步骤 1: 去重
    NS->>DL: news.getByHash(hash)
    DL->>IDB: get('news', by-hash index)
    
    alt 文章已存在
        IDB-->>NS: 返回已有文章
        NS-->>SRC: 去重跳过
    else 新文章
        IDB-->>NS: 未找到
        
        Note over NS: 步骤 2: 情绪分析
        NS->>SA: getOrAnalyzeSentiment(text)
        SA->>DL: sentimentCache.getByHash(contentHash)
        DL->>IDB: get('sentiment_cache', by-content-hash)
        
        alt 缓存命中
            IDB-->>SA: 返回缓存情绪
        else 缓存未命中
            IDB-->>SA: 未命中
            SA->>SA: analyzeNewsArticle(text)
            SA->>DL: sentimentCache.save(result)
            DL->>IDB: put('sentiment_cache', result)
        end
        SA-->>NS: {sentiment, confidence}
        
        Note over NS: 步骤 3: 股票关联
        NS->>SL: linkArticleToStocks(article, stockLibrary)
        SL->>DL: stocks.list()
        DL->>IDB: getAll('stocks')
        IDB-->>SL: 返回股票列表
        SL->>SL: 关键词匹配 + 相关度评分
        SL-->>NS: {links[], maps[]}
        
        Note over NS: 步骤 4: 保存关联映射
        loop 每个关联映射
            NS->>DL: newsStockMap.save(map)
            DL->>IDB: put('news_stock_map', map)
        end
        
        Note over NS: 步骤 5: 保存文章 + 广播
        NS->>DL: news.save(fullArticle)
        DL->>DB: forward(SAVE_NEWS, article)
        Note over DB: ACL: news → news INSERT
        DB->>IDB: put('news', article)
        IDB-->>DB: 写入成功
        DB->>DB: broadcast('news', envelope)
        
        NS-->>SRC: 返回保存结果
    end
    
    Note over NP: 展示链路
    NP->>DL: newsService.listNews(options)
    DL->>IDB: getAll('news') + 过滤排序
    IDB-->>DL: 返回文章列表
    DL-->>NP: 渲染新闻列表
    
    Note over MS: 情绪聚合
    MS->>DL: 读取近期新闻情绪
    DL->>IDB: getAll('news')
    IDB-->>DL: 返回数据
    DL-->>MS: 聚合市场情绪指标
```

**链路要点：**

1. **去重**: 基于内容哈希 (`by-hash` 索引) 防止重复存储
2. **情绪分析**: `sentimentAnalyzer` 支持缓存，避免重复计算
3. **股票关联**: `stockLinker` 通过关键词匹配关联股票，生成 `news_stock_map` 映射
4. **写入顺序**: 先存关联映射 → 再存文章 → DataBridge 广播
5. **权限**: `news` 模块对 `news`/`newsStockMap`/`sentimentCache` 有完整读写权限
6. **下游消费**: NewsPage 列表展示、MarketSentimentWidget 情绪聚合、个股新闻查询

### 3.5 板块轮动链路（采集 → 策略 → 信号 → 展示）

```mermaid
sequenceDiagram
    participant FS as fetcherService
    participant IDB as IndexedDB
    participant RS as rotationScoreService
    participant RC as rotationCalculator
    participant RSG as rotationSignalGrader
    participant DB as DataBridge
    participant DFE as DataFlowEngine
    participant SH as SectorHeatmapWidget
    participant DS as dualStrategyEngine

    FS->>IDB: 写入板块行情/资金/估值数据
    
    Note over RS: 触发轮动评分
    RS->>IDB: 读取当前市场风格 (沪深300 vs 创业板指)
    RS->>IDB: 读取板块定义 + 历史评分
    
    RS->>RS: getCurrentMarketStyle()
    Note over RS: 判断 growth / value / balanced
    
    RS->>RS: filterByMarketStyle(sectors, style)
    
    loop 每个板块
        RS->>RC: calculateRotationScore(input)
        Note over RC: 五因子计算:<br/>F1 景气度 (F1A~F1E)<br/>F2 资金面 (F2A~F2D)<br/>F3 估值面 (F3A~F3C)<br/>F4 β偏离 (F4A~F4B)<br/>F5 量能 (F5A~F5B)
        
        RC->>RC: calculateResonance(subScores)
        RC->>RSG: getSignalGrade(total, resonance)
        RSG-->>RC: signal + alert + decline
        RC-->>RS: RotationSectorScore
    end
    
    RS->>DB: forward(SAVE_ROTATION_SCORES, score)
    Note over DB: ACL: rotation → rotation_scores INSERT
    DB->>IDB: put('rotation_scores', score)
    IDB-->>DB: 写入成功
    DB->>DB: broadcast('rotation_scores', envelope)
    
    Note over DFE: strategy:signal 通道
    DFE->>SH: 推送板块热度更新
    SH->>SH: 渲染热力图
    
    Note over DS: 下游策略联动
    DS->>IDB: 读取轮动评分
    DS->>DS: 双策略信号融合
```

**链路要点：**

1. **前置过滤**: 市场风格周期过滤器（growth/value/balanced），优先匹配当前风格板块
2. **计算引擎**: `rotationCalculator` 五因子合成 + 共振度计算 + 信号分级
3. **写入**: `saveRotationScoreViaDataBridge()` → `dataBridge.forward(SAVE_ROTATION_SCORES)`
4. **权限**: `rotation` 模块对 `rotationScores` 有完整 CRUD 权限
5. **索引**: `by-sector-date` 唯一索引确保同一板块同日只有一条记录
6. **下游**: 双策略引擎信号融合、SectorHeatmapWidget 热力图展示

### 3.6 交易策略复盘链路（筹码分析 → 信号生成 → 导出）

```mermaid
sequenceDiagram
    participant U as 用户
    participant CSP as ChipStrategyReviewPage
    participant IPS as intentionPoolStore
    participant RPS as researchPoolStore
    participant PPS as positionPoolStore
    participant IDB as IndexedDB (stocks)
    participant CA as chipAnalysis (domain)
    participant TE as computeTurnoverVolumeEnergy
    participant UI as K线图 + 筹码图

    U->>CSP: 打开筹码策略复盘页 (/output/chip-strategy)
    
    Note over CSP: 步骤 1: 加载三池数据
    CSP->>IPS: 读取意向池股票列表
    CSP->>RPS: 读取研究池股票列表
    CSP->>PPS: 读取持仓池股票列表
    IPS-->>CSP: 返回意向池 PoolItem[]
    RPS-->>CSP: 返回研究池 PoolItem[]
    PPS-->>CSP: 返回持仓池 PoolItem[]
    
    Note over CSP: 步骤 2: 读取个股基础数据
    CSP->>IDB: get('stocks', symbol)
    IDB-->>CSP: 返回 stock (price, pe, pb, ...)
    
    Note over CSP: 步骤 3: 用户输入筹码分析参数
    U->>CSP: 输入换手率 / 量比 / 60日涨跌幅
    CSP->>CSP: 构建 StockChipInput
    
    Note over CSP: 步骤 4: 筹码分析核心
    CSP->>CA: analyzeStockChips(input)
    CA->>CA: 计算筹码重叠度 (overlap)
    CA->>CA: 计算筹码乖离率 (divergence)
    CA->>CA: 判定持仓位置 (position)
    CA->>TE: computeTurnoverVolumeEnergy(turnover, volumeRatio)
    TE-->>CA: 返回量能等级 (高/中/低)
    CA->>CA: 综合判定交易信号 (主力吸筹/出货/...)
    CA->>CA: 灰色地带决策 (logGrayZoneDecision)
    CA-->>CSP: ChipAnalysisResult
    
    Note over CSP: 步骤 5: 渲染结果
    CSP->>UI: 渲染 K 线图 + 筹码分布图
    CSP->>CSP: 展示交易信号 / 综合建议
    
    alt 用户导出
        U->>CSP: 点击导出 Excel
        CSP->>CA: exportChipStrategyExcel()
        CA-->>CSP: 生成 Excel 文件
        CSP-->>U: 下载文件
    end
```

**链路要点：**

1. **数据入口**: 三池(意向池/研究池/持仓池) → `stocks` Store 基础数据
2. **用户输入**: 换手率、量比、前60日涨跌幅（手动输入，非自动采集）
3. **计算引擎**: `analyzeStockChips()` 六维分析 → 筹码重叠度 + 乖离率 + 持仓位置 + 量能等级 + 筹码信号 + 灰色地带
4. **信号类型**: 主力吸筹、主力拉升、主力出货、主力撤退、散户追涨、散户割肉
5. **无持久化**: 分析结果内存态，不写入 IndexedDB（可选导出 Excel）
6. **灰色地带**: 当筹码信号模糊时触发 `logGrayZoneDecision()`，记录调试日志供回溯

### 3.7 iFinD MCP 采集链路（配置 → 编排 → MCP调用 → 写入 → 消费）

```mermaid
sequenceDiagram
    participant U as 用户
    participant SC as sevenDimConfigStore
    participant PL as collectionPipeline
    participant MC as mcpCollector
    participant MCP as iFinD MCP Server
    participant DB as DataBridge
    participant IDB as IndexedDB
    participant KI as KIMI AI (可选)
    participant PG as 各舱室页面

    U->>SC: 选择策略模板 / 触发采集
    SC->>SC: 解析启用的维度 (16维)
    SC->>SC: 从意向池读取标的列表
    SC->>PL: runBatchTrace(symbols, dimCode, config)

    Note over PL: 按维度并发执行 (每批5只)
    
    loop 每个维度
        PL->>PL: resolveDimensionMode(dimCode)
        
        alt 行情/ K线 (dim 01-02)
            PL->>PL: dataSourceOrchestrator → 腾讯/新浪/ AKShare
        else MCP 维度 (dim 04-05, 08-14)
            PL->>MC: buildMcpRequests(ctx)
            MC->>MC: getMcpMapping(dimCode)
            MC->>MC: 构建 McpToolRequest[] (1-2 子查询)
            
            par 并行 MCP 子查询
                MC->>MCP: run_mcp(stock-mcp, get_stock_info, {query})
                MCP-->>MC: 股票基本信息
                MC->>MCP: run_mcp(stock-mcp, get_stock_summary, {query})
                MCP-->>MC: 估值摘要
            end
            
            MC->>MC: aggregateMcpResults()
            MC-->>PL: McpCollectResult {data, errors, queryCount, successCount}
            
        else 多源维度 (dim 03, 06-07, 15-16)
            PL->>PL: multiSourceFetcher → Tushare → 东财 → 新浪
        end
        
        PL->>DB: forward(ENVELOPE_ACTION, payload)
        Note over DB: ACL 校验 → 推断目标 Store
        DB->>IDB: put(storeName, data)
        IDB-->>DB: 写入成功
        DB->>DB: broadcast(storeName, envelope)
        
        PL->>PL: emit(COLLECTION_EVENTS.COMPLETE)
    end
    
    Note over KI: AI 增强 (dim 05 新闻摘要, dim 08 研报解读)
    KI-->>DB: 补充 _kimiSummary / _kimiDigest
    
    DB-->>PG: DataFlow 推送更新
    PG->>PG: 渲染最新数据
```

**链路要点：**

1. **触发入口**: `sevenDimConfigStore.runCollection()` → 按策略模板解析 16 维度 → 从意向池读取标的列表
2. **维度路由**: `collectionPipeline.resolveDimensionMode()` 根据 `DIMENSION_TO_MODE` 映射决定采集路径
3. **MCP 调用**: `mcpCollector.buildMcpRequests()` 将维度上下文转为自然语言 MCP 查询，支持 1-2 个子查询并行
4. **结果聚合**: `mcpCollector.aggregateMcpResults()` 合并子查询结果，统计成功/失败数
5. **写入**: 通过 DataBridge.forward() 写入对应 IndexedDB Store（news/sectorScores/researchLogs 等）
6. **AI 增强**: Dim 05 热点新闻 → KIMI 摘要; Dim 08 研报中心 → KIMI 解读（非阻塞，失败不影响主流程）
7. **熔断保护**: `adaptiveSourceOrchestrator.canExecute()` 在采集前检查熔断器状态，跳过已知死源

---

## 4. 数据变更传播路径

### 4.1 写入触发 DataFlow 通知机制

V9 系统存在**两套并行**的数据变更通知机制：

#### 4.1.1 DataBridge 广播机制（Store 级）

```
写入流程:
  dataBridge.forward(envelope)
    → 验证 Envelope 格式
    → ACL 权限校验
    → routeToDB() 写入 IndexedDB
    → broadcast(targetStore, envelope)  ← 通知订阅者
```

- **触发时机**: 每次 `forward()` 成功写入后立即广播
- **广播频道**: 以 Store 名称为频道名（如 `stocks`, `v6_scores`, `orders`, `news`）
- **订阅方式**: `dataBridge.subscribe(channel, callback)`
- **适用场景**: 业务模块间的实时数据同步

#### 4.1.2 DataFlowEngine 发布订阅（通道级）

```
发布流程:
  dataFlowEngine.publish(channel, data)
    → 更新内存 cache（若 persist=true）
    → eventBus.emit('DATAFLOW_PACKET_PUBLISHED')
    → _distribute(packet)  ← 通知所有订阅者
    → 每个回调通过 queueMicrotask 异步执行
```

- **触发时机**: 主动调用 `publish()` 或 `registerRefresh()` 定时触发
- **广播频道**: 12 个业务通道（`market:index`, `portfolio:summary`, `strategy:score` 等）
- **订阅方式**: `dataFlowEngine.subscribe(channel, callback)`
- **适用场景**: Widget 层的实时数据刷新

### 4.2 订阅者接收更新流程

```mermaid
flowchart LR
    subgraph PUB["发布端"]
        A1["dataBridge.forward()<br/>写入 + 广播"]
        A2["dataFlowEngine.publish()<br/>发布到通道"]
    end
    
    subgraph SUB["订阅端"]
        B1["dataBridge.subscribe()<br/>Store 级订阅"]
        B2["dataFlowEngine.subscribe()<br/>通道级订阅"]
        B3["useMarketData()<br/>React Hook"]
    end
    
    A1 --> B1
    A2 --> B2
    B2 --> B3
    
    style PUB fill:#fff3e0,stroke:#e65100
    style SUB fill:#e0f7fa,stroke:#006064
```

**接收细节：**

| 机制 | 订阅接口 | 首次订阅是否返回缓存 | 分发方式 | 性能监控 |
|------|---------|---------------------|---------|---------|
| DataBridge 广播 | `dataBridge.subscribe(channel, cb)` | 否 | 同步回调 | 无 |
| DataFlowEngine | `dataFlowEngine.subscribe(channel, cb)` | **是**（如有缓存，通过 queueMicrotask 异步投递） | queueMicrotask 异步分发 | 慢订阅警告 (>16ms) |
| MarketDataProvider | `useMarketData()` Hook | 是（Context 初始值） | React setState 重渲染 | loading/error 状态 |

### 4.3 缓存失效机制

#### 4.3.1 DataFlowEngine 内存缓存

```typescript
// 缓存结构
private cache = new Map<string, { data: unknown; timestamp: number; seq: number }>()

// 缓存策略
- 存储键: channel 名称
- 存储值: { data, timestamp, seq }
- 有效期: 永久（直到新的 publish 覆盖）
- 持久化控制: 由 ChannelMeta.persist 决定
  - persist = true: 发布时更新缓存，新订阅者立即收到缓存数据
  - persist = false: 不缓存，仅实时推送（如 agent:logs）
```

#### 4.3.2 IndexedDB 持久化缓存

| Store | 缓存策略 | 失效触发 |
|-------|---------|---------|
| `stocks` | 全量持久化 | 用户更新/删除时直接覆盖 |
| `dailyQuotes` | 全量持久化 | Fetcher 新数据写入时覆盖 |
| `v6Scores` | 全量持久化 | 重新评分时按 symbol 覆盖 |
| `hotSectorScores` | 全量持久化 | 重新计算时按 symbol 覆盖 |
| `valuePitScores` | 全量持久化 | 重新计算时按 symbol 覆盖 |
| `rotationScores` | 按板块+日期存储 | 同板块同日覆盖（by-sector-date 唯一索引） |
| `sentimentCache` | 按内容哈希缓存 | 缓存永久有效，哈希相同直接复用 |
| `news` | 按哈希去重 | 相同哈希文章跳过写入 |

#### 4.3.3 缓存失效场景

1. **写入即失效**: DataBridge 写入成功后广播，订阅者收到通知后应重新读取
2. **定时刷新**: DataFlowEngine `registerRefresh()` 按 interval 定时拉取新数据
3. **手动刷新**: Widget 重试按钮 → `refreshWidget(instanceId)` → 重新执行采集任务
4. **组件卸载**: Widget 卸载时，WidgetEngine 调用 `unmountInstance()` → TaskScheduler 停止对应采集任务 → 清理定时器

---

## 5. 附录

### 5.1 数据实体与模块的映射矩阵

表中标识各模块对各数据实体的操作权限（R=读, W=写）。

| 数据实体 (Store) | fetcher | stockpool | analyzer | rotation | sector | news | tradinghub | trading | strategy | system | user |
|-----------------|:-------:|:---------:|:--------:|:--------:|:------:|:----:|:----------:|:-------:|:--------:|:------:|:----:|
| **stocks** | RW | RW | R | R | R | R | R | R | R | RW | RW |
| **dailyQuotes** | RW | - | R | R | - | - | - | - | R | RW | - |
| **v6Scores** | - | R | RW | - | - | - | R | - | R | RW | R |
| **intelligentScores** | - | - | RW | - | - | - | - | - | - | RW | - |
| **industryScores** | - | - | RW | - | - | - | - | - | - | RW | - |
| **rotationScores** | - | - | - | RW | - | - | - | - | R | RW | - |
| **sectorScores** | - | - | - | - | RW | - | - | - | - | RW | - |
| **hotSectorScores** | - | - | RW | - | - | - | RW | - | RW | RW | - |
| **valuePitScores** | - | - | RW | - | - | - | RW | - | RW | RW | - |
| **scoreDocs** | - | - | RW | - | - | - | - | - | - | RW | - |
| **strategySnapshots** | - | - | - | - | - | - | RW | R | - | RW | - |
| **orders** | - | - | - | - | - | - | RW | RW | - | RW | RW |
| **signals** | - | - | - | - | - | - | RW | RW | - | RW | - |
| **watchlists** | - | RW | - | - | - | - | - | - | - | RW | - |
| **news** | - | - | - | - | - | RW | - | - | - | RW | - |
| **newsStockMap** | - | - | - | - | - | RW | - | - | - | RW | - |
| **sentimentCache** | - | - | - | - | - | RW | - | - | - | RW | - |
| **newsBookmarks** | - | - | - | - | - | RW | - | - | - | RW | - |
| **localDocs** | - | - | R | - | - | - | - | - | - | RW | - |
| **researchLogs** | - | - | - | - | - | - | - | - | - | RW | - |
| **ChipStrategyReview** ⚡ | - | R | - | - | - | - | - | - | - | - | - |

> ⚡ **ChipStrategyReview** 为内存态分析实体，无独立 IndexedDB Store。数据依赖 `intentionPoolStore` / `researchPoolStore` / `positionPoolStore`（三池 Zusand Store）和 `stocks` Store。分析结果仅在前端渲染，支持 Excel 导出。

### 5.2 读写权限表（ACL Matrix）

基于 `src/config/dbConfig.ts` 中的 `ACL_MATRIX`：

| 模块 ID | 可读 Store | 可写 Store | 允许操作 |
|---------|-----------|-----------|---------|
| **fetcher** | - | stocks, dailyQuotes | INSERT, UPDATE |
| **stockpool** | stocks, v6Scores | stocks | INSERT, UPDATE, DELETE |
| **analyzer** | stocks, v6Scores, intelligentScores, industryScores, scoreDocs, hotSectorScores, valuePitScores | v6Scores, intelligentScores, industryScores, scoreDocs, hotSectorScores, valuePitScores | SELECT, INSERT, UPDATE |
| **rotation** | stocks, rotationScores, dailyQuotes | rotationScores | SELECT, INSERT, UPDATE, DELETE |
| **sector** | stocks, sectorScores | sectorScores | SELECT, INSERT, UPDATE, DELETE |
| **news** | stocks, news, newsStockMap, sentimentCache, newsBookmarks | news, newsStockMap, sentimentCache, newsBookmarks | SELECT, INSERT, UPDATE, DELETE |
| **tradinghub** | stocks, v6Scores, orders, signals, strategySnapshots, hotSectorScores, valuePitScores | orders, signals, strategySnapshots, hotSectorScores, valuePitScores | INSERT, UPDATE, DELETE |
| **trading** | stocks, orders, signals, strategySnapshots | orders, signals | SELECT, INSERT, UPDATE |
| **strategy** | stocks, v6Scores, dailyQuotes, hotSectorScores, valuePitScores, rotationScores, signals | hotSectorScores, valuePitScores | SELECT, INSERT, UPDATE |
| **system** | 全部 (24个) | 全部 (24个) | 全部 |
| **user** | stocks, v6Scores, orders | stocks, orders | INSERT, UPDATE, DELETE |

### 5.3 DataFlow 通道清单

| 通道名 | 描述 | 刷新间隔 | 持久化 | 优先级 |
|--------|------|---------|--------|--------|
| `market:index` | 大盘指数实时数据 | 5000ms | 是 | high |
| `market:sector` | 板块涨跌排行 | 10000ms | 是 | high |
| `market:fundflow` | 资金流向统计 | 15000ms | 是 | normal |
| `market:emotion` | 市场情绪指标 | 30000ms | 否 | low |
| `portfolio:summary` | 持仓总览（事件驱动） | 0 | 是 | high |
| `portfolio:holding` | 持仓明细 | 30000ms | 是 | normal |
| `portfolio:risk` | 持仓风险监控 | 60000ms | 是 | high |
| `strategy:signal` | 买卖信号（事件驱动） | 0 | 否 | high |
| `strategy:score` | 股票评分 | 60000ms | 是 | normal |
| `agent:status` | Agent状态 | 10000ms | 否 | normal |
| `agent:logs` | Agent日志（事件驱动） | 0 | 否 | low |
| `system:health` | 系统健康 | 30000ms | 否 | low |

> 注：`refreshInterval = 0` 表示事件驱动模式，不启动轮询定时器。

### 5.4 关键文件索引

| 模块 | 核心文件路径 |
|------|-------------|
| DataBridge 核心 | `src/core/databridge.ts` |
| DataBridge 适配层 | `../../src/showcase/index.ts` |
| DataFlowEngine | `src/core/dataflow/dataflowEngine.ts` |
| DataFlow 类型 | `src/core/dataflow/dataflowTypes.ts` |
| IndexedDB 封装 | `src/data/db.ts` |
| 数据访问层 | `src/data/dataLayer.ts` |
| 数据类型定义 | `src/data/types.ts` |
| DB 配置与 ACL | `src/config/dbConfig.ts` |
| 任务调度器 | `src/services/data-collector/TaskScheduler.ts` |
| 市场数据适配器 | `src/services/data-collector/MarketDataAdapter.ts` |
| 采集器基类 | `src/services/data-collector/collectors/BaseCollector.ts` |
| V6 评分服务 | `src/services/scoring/v6ScoreService.ts` |
| 热门板块分析 | `src/services/scoring/hotSectorAnalyzer.ts` |
| 价值洼地分析 | `src/services/scoring/valuePitAnalyzer.ts` |
| 板块轮动服务 | `src/services/analysis/rotationScoreService.ts` |
| 交易服务 | `src/services/trading/tradingService.ts` |
| 风控引擎 | `src/services/trading/riskEngine.ts` |
| 信号生成器 | `src/services/trading/signalGenerator.ts` |
| 新闻服务 | `src/services/news/newsService.ts` |
| 情绪分析器 | `src/services/news/sentimentAnalyzer.ts` |
| 股票关联器 | `src/services/news/stockLinker.ts` |
| 市场数据 Provider | `src/cockpit/providers/MarketDataProvider.tsx` |
| Widget 引擎 | `src/cockpit/core/widgetEngine.ts` |
| Cockpit 外壳 | `src/cockpit/CockpitShell.tsx` |
| 交易策略复盘页 | `src/pages/output/ChipStrategyReviewPage.tsx` |
| 筹码分析领域 | `src/domain/chip/analysis.ts` |
| 筹码分析类型 | `src/domain/chip/types.ts` |
| 筹码分析辅助 | `src/domain/chip/helpers.ts` |
| 筹码调试日志 | `src/domain/chip/debugLog.ts` |
| 筹码策略图表 Hook | `src/hooks/chip/useChipStrategyCharts.ts` |
| 筹码分布图表 | `src/components/chart/ChipDistributionChart.tsx` |
| **iFinD MCP 适配层** | `src/services/data-collector/mcpCollector.ts` |
| **采集流水线** | `src/services/data-collector/collectionPipeline.ts` |
| **多源拉取器** | `src/services/data-collector/multiSourceFetcher.ts` |
| **自适应熔断器** | `src/services/data-collector/adaptiveSourceOrchestrator.ts` |
| **采集配置常亮** | `src/config/collectConfig.ts` |
| **七维配置 Store** | `src/store/sevenDimConfigStore.ts` |
| **采集类型定义** | `src/types/modules/collection.types.ts` |
| **采集流水线 Domain** | `src/domain/collection/pipeline.ts` |
| **字段合并器** | `src/services/data-sync/fieldMerger.ts` |
| **KIMI AI 增强** | `src/services/data-collector/kimiAIService.ts` |

### 5.5 iFinD MCP 服务器清单

V9 通过 iFinD 插件注册了 5 个 MCP Server，覆盖 A股/指数/新闻/基金/全球股票 五大领域。

| MCP Server ID | 简称 | 覆盖领域 | 核心 Tool | Tool 数量 |
|--------------|------|---------|----------|----------|
| `mcp_plugin_iFinD_hexin-ifind-ds-stock-mcp` | stock-mcp | A股数据 | get_stock_info, get_stock_summary, get_stock_performance, get_stock_financials, get_stock_shareholders, get_stock_events, search_stocks, stock_highfreq_quotes, get_risk_indicators, get_esg_data | 10 |
| `mcp_plugin_iFinD_hexin-ifind-ds-index-mcp` | index-mcp | 指数/板块 | index_data, sector_data, index_highfreq_quotes | 3 |
| `mcp_plugin_iFinD_hexin-ifind-ds-news-mcp` | news-mcp | 新闻/公告 | search_notice, search_news | 2 |
| `mcp_plugin_iFinD_hexin-ifind-ds-fund-mcp` | fund-mcp | 基金数据 | get_fund_profile, get_fund_market_performance, get_fund_portfolio, get_fund_financials, fund_highfreq_quotes | 5 |
| `mcp_plugin_iFinD_hexin-ifind-ds-global-stock-mcp` | global-mcp | 全球股票 | global_stock_profile, global_stock_quotes, global_stock_financial, global_stock_events | 4 |

#### MCP Server → 维度覆盖矩阵

| MCP Server | Dim 01 | Dim 02 | Dim 03 | Dim 04 | Dim 05 | Dim 06 | Dim 07 | Dim 08 | Dim 09 | Dim 10 | Dim 11 | Dim 12 | Dim 13 | Dim 14 |
|-----------|:------:|:------:|:------:|:------:|:------:|:------:|:------:|:------:|:------:|:------:|:------:|:------:|:------:|:------:|
| stock-mcp | ✓ | ✓ | ✓ | ✓ | - | ✓ | - | - | ✓ | - | ✓ | ✓ | ✓ | ✓ |
| index-mcp | - | - | - | - | - | ✓ | ✓ | - | - | ✓ | - | - | - | - |
| news-mcp | - | - | - | ✓ | ✓ | - | - | ✓ | - | - | - | - | - | - |
| fund-mcp | - | - | - | - | - | - | - | - | - | - | - | - | - | - |
| global-mcp | - | - | - | - | - | - | - | - | - | - | - | - | - | - |

> **注**：fund-mcp 和 global-mcp 当前未直接映射到 14 维度，作为扩展能力储备。fund-mcp 可用于基金分析场景，global-mcp 可用于港美股扩展。

#### MCP 调用方式

所有 iFinD MCP 调用均通过 `run_mcp` 工具执行，参数结构如下：

```typescript
// 自然语言查询模式（推荐）
run_mcp({
  serverName: 'mcp_plugin_iFinD_hexin-ifind-ds-stock-mcp',
  toolName: 'get_stock_info',
  args: {
    query: '000001.SZ 平安银行 的上市时间、所属申万行业、主营业务'
  }
})

// 结构化参数模式（高频行情）
run_mcp({
  serverName: 'mcp_plugin_iFinD_hexin-ifind-ds-stock-mcp',
  toolName: 'stock_highfreq_quotes',
  args: {
    symbols: '000001.SZ',
    indicators: '最新价,开盘价,最高价,最低价,涨跌幅,成交额,成交量,换手率',
    data_mode: 'real_time'
  }
})
```

---

> **文档结束** — 本文档基于 V9 DB_VERSION 34 架构生成，数据链路追踪覆盖 53 个 IndexedDB Store 和 12 个 DataFlow 通道。
