---
title: ???????? ?? useState ?? Zustand ????????
type: explanation
domain: project
phase: planning
tier: important
status: active
maintainer: Quality Auditor
summary: "???? Zustand store?????? 13 ???????? + 14 ?? action??"
tags: [project, news, store, migration, governance, documentation, strategy, explanation, state]
version: v1.0.0
last_updated: 2026-06-27
code_version: 2.0.0
doc_id: V9-DOC-PROJ-062
referenced_by: [V9-DOC-PROJ-174, V9-DOC-META-000, V9-DOC-PROJ-176, V9-DOC-PROJ-182, docs/00-meta/deprecated-docs/old-versions/registry-index-v1.0.0-02-design.md, V9-DOC-PROJ-149]
change_log: 
---

# ???????? ?? useState ?? Zustand ????????

## 1. ????????

| ???? | ?????? (useState) | ?????? (Zustand) |
|:---|:---|:---|
| ???????????? | ?????????????????????? | `src/store/analysisNewsStore.ts` |
| NewsPage ???????? | 7 ?? `useState` + 2 ?????????? | 1 ?? `useNewsStore()` ???? |
| NewsFeed ???????? | 4 ?? `useState` + 1 ?? `useRef` | 1 ?? `useNewsStore()` ???? |
| NewsPage ?? NewsFeed props | 10 ?? | 4 ???????????? |
| ?????????? | ?????? | ???????????? `useNewsStore()` ???? |
| ?????????? | ?? NewsPage ?? | ?? store ???????????????? |
| TypeScript ???? | ???? | ???? (`tsc --noEmit` exit 0) |

## 2. ????????

### `src/store/analysisNewsStore.ts`

???? Zustand store?????? 13 ???????? + 14 ?? action??

```typescript
// ????????5 ??????articles / loading / error / hasMore / currentOffset
// UI ????8 ??????selectedArticle / bookmarkedIds / filter / searchInput / showFilter / displayCount
// Actions??14 ??????setArticles / appendArticles / setLoading / setError / setHasMore /
//                  setCurrentOffset / selectArticle / toggleBookmark / setFilter /
//                  setSearchInput / setShowFilter / setDisplayCount / resetDisplay
```

??????????????
- `bookmarkedIds` ???? `Set<string>` ???????? `toggleBookmark` action ?????????? localStorage
- `appendArticles` ???? `set((state) => ...)` ??????????????????
- `setFilter` ???????? `displayCount`??????????????????????????????
- ???? action ?????????? `logger.info` ????

## 3. ????????

### `src/pages/analysis/NewsPage.tsx`

**??????????**

| ???? | ???????? | ???? |
|:---|:---|:---|
| L4 | ???? | ???? "???????????? useState ?????? useNewsStore (Zustand)" |
| L7 | ???? | ???? `useMemo, useState`?????? `useCallback, useEffect` |
| L13 | ???? | ???? `import { useNewsStore } from '@/store/newsStore'` |
| L14 | ???? | ???? `import type { NewsFilter } from './components/FilterPanel'` |
| L18-19 | ???? | ???? `BOOKMARK_STORAGE_KEY`??`PAGE_SIZE`??`PAGE_SIZE` ???????????? `BOOKMARK_STORAGE_KEY`?? |
| L33-49 | ???? | ???? `loadBookmarks()` / `saveBookmarks()` ???????? |
| L53-60 | ???? | ???? 7 ?? `useState()` ???? |
| L63-82 | ???? | ???? `filteredArticles` useMemo?????????????? NewsFeed?? |
| L34-39 | ???? | 1 ?? `useNewsStore()` ???????? 7 ?? useState |
| L52 | ???? | `append` ???????? `useNewsStore.getState().appendArticles(adapted)` |
| L70 | ???? | `loadData` deps ?????? `[]` ?????? `[setArticles, ...]` |
| L90 | ???? | `handleRefresh` deps ???? `setCurrentOffset` |
| L102-106 | ???? | `handleFilterChange` ???????? `setFilter(filter)` + `setCurrentOffset(0)` |
| L110-111 | ???? | `handleArticleClick` ???????? `selectArticle(article)` |
| L114-128 | ???? | ???? `handleBookmark` ?????????????? store?? |
| L140-147 | ???? | `<NewsFeed>` props ?? 10 ???????? 4 ?? |
| L153, L185 | ???? | `setSelectedArticle(null)` ?? `selectArticle(null)` |

### `src/components/organisms/analysis/news/NewsSentimentTrend.tsx`

**??????????**

| ???? | ???????? | ???? |
|:---|:---|:---|
| L4 | ???? | ???? "???????????? useState ?????? useNewsStore (Zustand)" |
| L7 | ???? | `useState, useRef` ?? `useMemo, useCallback` |
| L15 | ???? | ???? `import { useNewsStore } from '@/store/newsStore'` |
| L17-26 | ???? | `NewsFeedProps` ???? `articles`/`loading`/`hasMore`/`onBookmark`/`bookmarkedIds`??5 ?? props?? |
| L28-37 | ???? | ?????????????????????????? `useNewsStore()` ???? |
| L40-44 | ???? | ???? 4 ?? `useState` + 1 ?? `useRef` ???? |
| L57-66 | ???? | `handleSearchChange` ???? store actions?????????? ref |
| L68-76 | ???? | `handleFilterChange` ???? `setFilter` + `resetDisplay` |
| L78-86 | ???? | `handleLoadMore` ???? `setDisplayCount` |
| L88-92 | ???? | `handleRefresh` ???? `resetDisplay` |
| L189-190 | ???? | `onBookmark={onBookmark}` ?? `onBookmark={toggleBookmark}`??`bookmarkedIds?.has` ?? `bookmarkedIds.has` |

## 4. ????????????

### ???? 1??useState ?? Zustand state

```typescript
// ??????
const [articles, setArticles] = useState<V6NewsArticle[]>([])

// ??????
const { articles, setArticles } = useNewsStore()
```

### ???? 2??useState + ???? ?? Zustand action

```typescript
// ??????
const handleBookmark = useCallback((id: string) => {
  setBookmarkedIds((prev) => {
    const next = new Set(prev)
    next.has(id) ? next.delete(id) : next.add(id)
    saveBookmarks(next)
    return next
  })
}, [])

// ??????
const { toggleBookmark } = useNewsStore()
// toggleBookmark ?????????? localStorage ?????? + ????
```

### ???? 3??useCallback ?????? setState ?? ???????? store action

```typescript
// ??????
const handleFilterChange = useCallback((filter: NewsFilter) => {
  setActiveFilter(filter)     // ???? useState setter
  setCurrentOffset(0)         // ???? useState setter
  void loadData(0)           // ????????
}, [loadData])

// ??????
const handleFilterChange = useCallback((filter: NewsFilter) => {
  setFilter(filter)           // store action?????????? displayCount??
  setCurrentOffset(0)         // store action
  void loadData(0)           // ????????????????
}, [loadData, setFilter, setCurrentOffset])
```

### ???? 4??Props ???? ?? Store ????

```typescript
// ?????? ?? NewsPage ?? NewsFeed
<NewsFeed
  articles={filteredArticles}   // 10 ?? props
  loading={loading}
  hasMore={hasMore}
  onBookmark={handleBookmark}
  bookmarkedIds={bookmarkedIds}
  ...
/>

// ?????? ?? NewsPage ?? NewsFeed
<NewsFeed
  onLoadMore={loadMore}         // 4 ?? props??????????
  onRefresh={handleRefresh}
  onFilterChange={handleFilterChange}
  onArticleClick={handleArticleClick}
/>
// NewsFeed ???????? useNewsStore() ???????? articles/loading/hasMore/bookmarkedIds
```

## 5. ????????

| ?????? | ???? |
|:---|:---|
| `tsc --noEmit` | exit 0???????? |
| NewsPage ???????? | ??????filter ???? ?? store.setFilter ?? NewsFeed ?????? |
| NewsFeed ???????? | ??????toggleBookmark ?? store ?????? ?? localStorage?? |
| ???????? | ??????currentOffset ?? loadMore ?? appendArticles?? |
| ?????????????? | ??????NewsPage ?? NewsFeed ???????? store ?????? |

## 6. ????????????

- ?????? `src/store/analysisNewsStore.ts`?????? `useFilteredArticles()` / `useArticleStats()` ????????????
- ???????????????? IndexedDB ?????????? localStorage?????????????? store ????
- ?????? DataBridge ??????????P2-3??????????????????????????????