---
title: b批次高价值孤儿集成状态报�?2026-07-08
type: explanation
domain: project
phase: planning
tier: important
status: active
maintainer: V9 Architecture Team
summary: "B 批次原审计（07-07）曾误判'生产零引�?= �?= �?，要�?9 �?B2 组件整批删除。经用户反馈'死组件分析不全面''因子热力图应体现'，并依据新沉淀�?D11 标准推翻该误判："
tags: [project, plan, integration, explanation, governance, documentation, strategy]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-PROJ-035
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# B 批次高价值孤儿组件集成状态报�?
> **Date**�?026-07-08
> 依据：D11 孤儿组件分类与价值评估标准（已沉淀�?`code-quality-compliance` SKILL�?> 范围：将 B 批次判定的高价值孤儿（V1=1 规格契合�?*真正接入对应板块页面**，而非仅保�?> 说明：原 `./b批次高价值孤儿集成状态报�?2026-07-08.md` 在并行会话活动期间被清除，本报告为权威替代记�?
---

## 〇、背景与判据

B 批次原审计（07-07）曾误判"生产零引�?= �?= �?，要�?9 �?B2 组件整批删除。经用户反馈"死组件分析不全面""因子热力图应体现"，并依据新沉淀�?**D11 标准**推翻该误判：

- **V1=1（规格契合）OR V7=1（已�?registry 登记）→ 保留/集成，禁止删�?*
- 仅空壳且无规格无登记的符号才�?真死代码"，可考虑删（仍须用户确认�?
本次任务：把 3 个确认的高价值孤儿（V1=1）接入具体板块，落实"体现价�?的判断�?
---

## 一、集成落地清单（代码已交�?+ 门禁验证通过�?
| # | 组件 | 宿主页面（具体板块） | 数据�?/ 映射 | 集成方式 | 状�?|
|---|------|---------------------|--------------|----------|------|
| 1 | `FactorHeatmap` | `IntelligentScoreExplanation`（智能评分解释页�?| `result.dimensionScores` �?`buildFactorHeatmapData()` | import + `useMemo(heatmapData)` + 渲染段（`heatmapTitle`�?| �?已集成（恢复此前被回滚的改动�?|
| 2 | `ScoreFactorWaterfall` | `IntelligentScoreExplanation`（智能评分解释页�?| `result.dimensionScores` �?新增 `buildWaterfallFromDimensions()` 派生 | import + 渲染段（`waterfallTitle`），组件扩展 `dimensionScores` 属�?| �?已集�?|
| 3 | `SectorRotationHeatmap` | `SectorAnalysisPage`（板块分析页�?| `sectorHeatmap` store（页面已持有�?| `<SectorRotationHeatmap />` | �?已集�?|
| 4 | `CandlestickChart` | `StockAnalysisPage`（个股行情页�?| `quotes.history: KlineBar[]` �?`candleData`（`date→time` 映射�?| `<CandlestickChart data={candleData} height={400} />` | �?已集�?|

> 注：`FactorHeatmap` �?`ScoreFactorWaterfall` 同宿主（评分解释页），形�?雷达 + 关键因子 + 因子热力�?+ 因子贡献瀑布�?+ 思维�?的完整解释视图�?
---

## 二、配套代码改�?
- **`src/constants/score.constants.ts`**
  `INTELLIGENT_SCORE_EXPLANATION_LABELS` 新增 `heatmapTitle: '因子热力�?`、`waterfallTitle: '因子贡献瀑布�?`�?- **`src/components/organisms/analysis/score/ScoreFactorWaterfall.tsx`**
  - 扩展 `ScoreFactorWaterfallProps.dimensionScores?: DimensionScore[]`（`audit` 优先级更高，缺失时由 `dimensionScores` 兜底派生瀑布图）�?  - 新增 `buildWaterfallFromDimensions(dimensions)` 纯函数�?  - tooltip 改用 `twBg('neutral', 50)` / `twText(...)` 令牌，消除一�?`bg-white` 硬编码（满足 `lint:colors` error 级）�?  - 保留�?`audit` 路径与既有单测不变�?- **`src/components/organisms/analysis/score/IntelligentScoreExplanation.tsx`**
  恢复 `FactorHeatmap` 集成（import + 数据构建 + 渲染段），新�?`ScoreFactorWaterfall` 集成（含 `waterfallTitle`）�?- **`src/pages/analysis/SectorAnalysisPage.tsx`** / **`src/pages/analysis/StockAnalysisPage.tsx`**
  分别接入 `SectorRotationHeatmap` �?`CandlestickChart`，并完成各自数据源映射�?
---

## 三、门禁验证结�?
| 检查项 | 命令 / 范围 | 结果 |
|--------|------------|------|
| 类型安全 | `tsc:prod`（筛选本�?5 个集成文件） | **0 类型错误**（组件全部类型安全） |
| 单元测试 | `IntelligentScoreExplanation.test.tsx`(5) + `ScoreFactorWaterfall.test.tsx`(5) | **10 / 10 通过** |
| 颜色令牌 | `lint:colors`（error 级，本次 5 文件�?| **0 违规**�?2 �?warning 均为预存风格项，非硬编码色） |

> 说明：`tsc:prod` 整体退出码�?2，但**所有报错均位于 `permissionRevocationService.ts`**（并行会�?rbac 工作引入�?`readonly` 数组 `.push()` 类型错误），与本次集成无关，由该并行会话负责，未擅自改动以免冲突�?
---

## 四、D11 价值闭环验�?
| 组件 | V1 规格证据 | V4 展示价�?| V7 登记 | D11 分类 | 处置 |
|------|-----------|------------|---------|----------|------|
| `FactorHeatmap` | `../reference/04-ui-ux-specs.md` 图表库成�?| 高（直观热力�?| �?registry 关联 | 高价值孤�?| �?已集成体�?|
| `ScoreFactorWaterfall` | `./design/analysis-screening-module-dev-plan.md` DA-009 计划交付 | 高（贡献瀑布�?| �?| 高价值孤�?| �?已集成体�?|
| `SectorRotationHeatmap` | `./design/analysis-screening-module-dev-plan.md` DA-004 计划交付 | 高（板块轮动�?| �?| 高价值孤�?| �?已集成体�?|
| `CandlestickChart` | `04-ui-ux-specs.md:188` 图表�?| 高（K线） | �?| 高价值孤�?| �?已集成体�?|

新标准成功拦截了�?零引用即�?的误判：4 个组件均�?V1=1 判为禁删，并已通过实际集成证明其价值�?
---

## 五、未决项（非本次范围，待用户决策�?
1. **`cabin×4` 遗留组件**（`ScoreHistoryTable` / `ScoreItem` / `ScoreSnapshot` / `ScoreSummary`�?   legacy v0.9.11 残留，已被新�?`ScoreHistoryPanel` / `IntelligentScorePage` 覆盖（V6=1），判定�?保留非删、不集成"�?2. **5 个真死候�?*（B1×3 UseCase + B3×2 Service）�?�?**已于 2026-07-08 经用户授权删�?*
   - 删除文件：`submitOrder.useCase.ts` / `executePlan.useCase.ts` / `strategySnapshotSave.useCase.ts` / `sectorScoreService.ts` / `mockHoldingsData.ts`
   - 方式：`git rm`（可�?git 历史恢复，符合删除铁律）
   - 验证：精�?grep 确认零外部引�?�?删除�?`tsc:prod` 无断链错误�? 集成测试套件 21/21 仍全过。详�?`./b批次组件集成测试报告-b6-2026-07-08.md`�?3. **`permissionRevocationService.ts` �?3 �?tsc 错误**
   并行会话 rbac 工作引入，由该会话负责，本批次不触碰�?
---

## 六、执行环境说�?
执行期间并行会话在同一工作树进�?`git stash` / `rebase` / `merge`，曾导致 `IntelligentScoreExplanation.tsx` 出现整段重复副本�?`>>>>>>> Stashed changes` 冲突标记。当前磁盘状态已恢复�?*干净单副本（189 行）**，全部集成编辑完整保留；`git status` 显示相关文件为已暂存修改（`M`）。所有结论均�?信任但验�?核对真实文件系统，非依赖缓存�?