---
title: 业务能力补充报告：自选股异动 Widget
type: explanation
domain: frontend
phase: design
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "日期�?026-07-09 能力方向：业务功能能力（ cockpit / 盯盘�?Widget �? 状�?*：已完成，门禁全�?"
tags: [frontend, widget, screening, report, design]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-FRONT-031
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# 业务能力补充报告：自选股异动 Widget

**日期**�?026-07-09  
**能力方向**：业务功能能力（ cockpit / 盯盘�?Widget �? 
**状�?*：已完成，门禁全�?
---

## 一、补充能力概�?
在既有「自选股」Widget 基础上，新增 **「自选股异动」Widget**，实时展示观察池中：
- **涨幅�?*：按涨跌幅降�?- **跌幅�?*：按涨跌幅升�?- **振幅�?*：按绝对涨跌幅降�?
该能力复用现�?`watchlist` 数据源与 `useMarketData()` 框架，无需新增 IndexedDB store，属于低风险、高价值的实时盯盘能力补充�?
---

## 二、新�?修改文件清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/services/trading/watchlistMoversService.ts` | 新增 | 纯函数计算异动榜，支�?`topN` 与脏数据过滤 |
| `src/services/trading/watchlistMoversService.test.ts` | 新增 | 服务层单�?5 用例 |
| `src/cockpit/widgets/WatchlistMoversWidget.tsx` | 新增 | Widget UI，含 loading/error/�?数据四�?|
| `src/cockpit/widgets/WatchlistMoversWidget.test.tsx` | 新增 | Widget 层单�?4 用例 |
| `src/cockpit/core/widgetRegistry.ts` | 修改 | 注册 `watchlistMovers` |
| `src/constants/cockpit.constants.ts` | 修改 | 增加 `DEFAULT_WIDGET_CONFIG.watchlistMovers` �?`WIDGET_DEFAULT_DATA_SOURCE.watchlistMovers` |

---

## 三、验证结�?
| 门禁 | 命令 | 结果 |
|------|------|------|
| 类型检�?| `tsc -p tsconfig.json --noEmit` | 0 错误 |
| 测试审计 | `audit:tests` | 0 错误 |
| 颜色硬编�?| `lint:colors` | 0 错误 |
| 代码硬编�?| `audit:hardcode` | 0 Critical/Major�?9 Warning（既有静默回退�?|
| 新增服务测试 | vitest `watchlistMoversService.test.ts` | 5/5 通过 |
| 新增 Widget 测试 | vitest `WatchlistMoversWidget.test.tsx` | 4/4 通过 |
| 注册表回�?| vitest `widgetRegistry.test.ts` | 17/17 通过 |

---

## 四、设计要�?
1. **纯函数服�?*：`computeWatchlistMovers` 无副效应，便于单测与未来服务端移植�?2. **颜色合规**：全部使�?`twText/twBg/twBorder/DARK` 令牌；A股涨跌色通过 `getStockColorHex` 保持红涨绿跌�?3. **�?schema 变更**：不新增 IndexedDB store，因�?`validate-data-blueprint` store 数量保持 33 不变�?4. **可配�?*：组件通过 `DEFAULT_WIDGET_CONFIG` 统一配置标题、尺寸、分类；数据源走 `WIDGET_DEFAULT_DATA_SOURCE`�?
---

## 五、后续可扩展

- 支持用户自定义榜单长度（当前固定 top 5）�?- 接入 Toast 推送：当某只自选股涨幅/跌幅突破阈值时主动提醒�?- 增加「换手率榜」「成交量榜」等更多异动维度�?