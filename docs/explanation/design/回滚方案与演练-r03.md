---
title: R03 回滚方案与演练文�?�?V9 智能投研复盘系统
type: explanation
domain: project
phase: design
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "文档版本：v1.0 �?日期�?026-07-08 �?关联清单�?*：R03（回滚方案与演练�?> 配套文档：《发布计划与评审-r01.md》（�?R02..."
tags: [project, plan, system, review]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-PROJ-247
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# R03 回滚方案与演练文�?�?V9 智能投研复盘系统

> **文档版本**：v1.0 �?**日期**�?026-07-08 �?**关联清单�?*：R03（回滚方案与演练�?> 配套文档：《发布计划与评审-r01.md》（�?R02 构建门禁�?
---

## 1. 回滚定位与原�?
本系统为**个人级静态前端应�?*（Vite SPA，产�?`dist/`），无独立后端服务（交易接口�?dev 中间�?mock，生产不打包）。因此回滚以**静态资源回滚为主、数据兼容为�?*�?
1. **静态回滚优�?*：保留上一�?`dist/` 备份，出问题即重新部署旧包，最快、最稳�?2. **数据向后兼容**：IndexedDB 写入须保留旧 schema 可读能力，避免新版本写坏旧数据导致无法回滚�?3. **不破坏接口契�?*：前端调用的 `/api/v1/trade`（mock）与 LLM 代理须保持兼容或优雅降级，禁止发布破坏性变更�?4. **先决策后执行**：触发条件达成即升级决策�? 分钟内启�?SOP�?
---

## 2. 回滚触发条件（Triggers�?
满足**任一**即触发回滚评估：

| 等级 | 触发条件 | 示例 |
|---|---|---|
| 🔴 立即回滚 | P0 门禁在发布后失效 | 上线即白屏、主链路 500 |
| 🔴 立即回滚 | 数据损坏 / 丢失风险 | IndexedDB 写入异常、复盘数据错�?|
| 🟠 评估回滚 | 核心功能阻断�?> 5% | KAI 评分不可用、导出失�?|
| 🟠 评估回滚 | 性能劣化明显 | 首屏 > 5s、滚动严重掉�?|
| 🟡 观察修复 | 非阻断缺�?| 个别文案错误、低频样式问�?|

> 黄金观察期（R01 §7）内任一点命�?🔴 即执行回滚，不等待�?
---

## 3. 回滚类型与措�?
### 3.1 静态资源回滚（主手段）
- 保留发布�?`dist.backup.<timestamp>`（R01 §6 步骤⑥已生成）�?- 重新部署该备份目录即可，无需重新构建�?
### 3.2 数据兼容回滚
- **IndexedDB 版本�?*：数据层须使�?`dbVersion` 常量；新版本升级须提�?`onupgradeneeded` 迁移，且**保留旧数据可�?*�?- **withBroadcast 版本�?*：跨 Tab 广播消息�?schema 版本，旧 Tab 收到不识别消息时静默忽略而非崩溃�?- **禁止中途破坏�?schema 变更**：如确需改字段，先兼容读写旧结构一版本，下个迭代再清�?
### 3.3 接口 / 特性降�?- `/api/v1/trade` �?LLM 代理：前端须对失败做兜底（mock 数据 / 提示重试），不因接口异常整页崩�?- 若引入特性开关（feature flag），回滚=关开关，不重新部署�?
### 3.4 配置回滚
- 主题令牌、数据源开关等�?`src/constants` / `src/config`，变更可逆；保留上一版配置快照�?
---

## 4. 回滚决策与升级路�?
| 角色 | 决策�?| 联系方式 |
|---|---|---|
| 发布负责�?| 发起回滚 SOP | �?|
| 质量/架构 | 确认是否回滚（�?时） | �?|
| 业务 owner | 业务影响确认 | �?|

> 个人项目可合并；🟠 情形至少 1 人确认后再回滚，🔴 情形边回滚边通知�?
---

## 5. 回滚标准操作程序（SOP�?
```bash
# �?确认触发条件并记录时间（T0�?# �?定位上一版备�?ls -d dist.backup.* | sort | tail -1     # 取最新备�?
# �?重新部署备份（静态托管场景）
#   方案 A：直接把备份目录作为�?dist 发布
cp -r "dist.backup.<上一版时间戳>" dist_rollback
#   按托管方流程上传 dist_rollback（或切换符号链接 / 重启静态服务）

# �?版本标签回退（如通过 git 管理发布�?git tag -a "rollback.$(date +%Y%m%d_%H%M%S)" -m "回滚�?v9.rc.<上一�?"
#   如需代码级回退：git checkout <上一�?tag> 后重�?build

# �?重启 / 刷新静态服务（依托管方式）
#   CloudStudio / 静态服务器：重新指向备份目录并重启

# �?验证（见 §6�?```

---

## 6. 回滚后验证（Post-Rollback Verification�?
| 检查点 | 标准 | 命令 / 动作 |
|---|---|---|
| 首页可加�?| 无白�?| 打开预览地址 |
| 主链路可�?| 选股→KAI→导�?正常 | 手动 + `npm run test:e2e` |
| 数据可读 | IndexedDB 旧数据正常展�?| 打开已有复盘记录 |
| 接口降级 | LLM/交易接口异常时不�?| 断网测试 |
| 版本一�?| 当前版本 = 回滚目标�?| 检查页�?构建�?|

---

## 7. 回滚演练计划（Drill�?
> 评审�?*至少演练 1 �?*，验�?SOP 可执行、备份可用�?
| 步骤 | 演练内容 | 环境 | 通过标准 |
|---|---|---|---|
| D1 | 静态回�?| 预发布托�?| 5 分钟内恢复上一版、首页正�?|
| D2 | IndexedDB 兼容 | 本地 | 新写数据旧版可读 / 旧写新版可读 |
| D3 | 接口降级 | 本地 | 断网时页面不崩、有提示 |

**演练记录模板**�?
| 项目 | 结果 | 备注 |
|---|---|---|
| 演练日期 |  |  |
| 演练�?|  |  |
| D1 静态回�?| �?通过 �?失败 |  |
| D2 数据兼容 | �?通过 �?失败 |  |
| D3 接口降级 | �?通过 �?失败 |  |
| 问题与新发现 |  | 回填至体检清单 |

---

## 8. 职责矩阵（RACI�?
| 活动 | 发布负责�?| 质量/架构 | 业务 owner |
|---|---|---|---|
| 触发判定 | R | A | C |
| 执行回滚 SOP | R | C | I |
| 数据兼容确认 | C | R/A | I |
| 回滚后验�?| R | A | C |
| 演练组织 | R | C | I |

R=负责执行  A=最终责�? C=被咨�? I=被告�?