---
title: 2026-06-29-data-architecture-governance
type: explanation
domain: architecture
phase: design
tier: reference
status: active
maintainer: V9 Architecture Team
summary: "For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended)..."
tags: [architecture, governance, management, data, design, strategy, explanation]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-ARCH-017
referenced_by: [V9-DOC-PROJ-174, V9-DOC-META-000, V9-DOC-PROJ-176, V9-DOC-PROJ-182, docs/00-meta/deprecated-docs/old-versions/registry-index-v1.0.0-02-design.md, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# V9 ????????????????????????????????????

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ?????? V9 ????????????????????????????????????Store ???????????????????????????Python ???????????????IndexedDB ???JOIN???????????????????????????????????????
**Architecture:** ?????????????????????????????????P0 ?????????????????????????????????P1 ???DataBridge broadcast ????????????????????????????????????????????????????????????P2 ?????? DataSourceProvider ????????????Python ????????????????????????P3 ?????? queryBuilder ?????????Store ??????????????????????????????P4 ?????????????????????????????????????????????
**Tech Stack:** TypeScript, Zustand, Vitest (jsdom + fake-indexeddb)

---

## ????????????????????????

### ??????
- ??????Page ?????????????????? isLoading ??????- ??????engine ??????????????????????????????/?????????- L3/L4/L7/L8 ??????????????????L0/L1/L2/L5/L6 ???LLM ?????????- ?????????????????????????????????????????????????????? ???Store ???Builder ??????????????????- ??????????????????????????????????????????TDD Red-Green-Refactor???
### ????????????
- **hotSectorStore / valuePitStore / rotationSignalStore** ?????? Store ?????????`@deprecated`???*????????????????????????*?????????????????????????????????- **DataFlowEngine ChannelMeta ?????????`priority: 'high'|'normal'|'low'`** ????????????????????????
- **6 ???Store ?????? 3 ?????? isRefreshing ????????????**???signalStore/dualStrategyStore/disciplineStore??????orderStore/holdingsStore/poolStore ???positionStore ?????????- **DataSourceProvider ???????????????*????????????
- **???Store JOIN ???????????????dataFusionEngine.ts**??? Store ??????????????????????????????
- **???????????? 80+ ??????**?????????fetcher/scoring/trading/strategy ?????????????????????fetcherClient???dataFusionEngine???unifiedStockService

---

## Batch A ???Store ??????????????????P0?????????????????????
### Task 1: ?????? 3 ?????????Store ??????

**Files:**
- Delete: `src/store/hotSectorStore.ts`
- Delete: `src/store/valuePitStore.ts`
- Delete: `src/store/rotationSignalStore.ts`
- Delete: `tests/hotSectorStore.test.ts`
- Delete: `tests/valuePitStore.test.ts`
- Delete: `tests/rotationSignalStore.test.ts`
- Modify: `src/store/dualStrategyStore.ts` ??????????????????`@deprecated` ?????????????????????????????? Store???- Test: ???????????????????????????????????????
- [ ] **Step 1: ????????????????????????*

```bash
# ????????????????????????
grep -r "hotSectorStore" src/ --include="*.ts" --include="*.tsx" -l
grep -r "valuePitStore" src/ --include="*.ts" --include="*.tsx" -l
grep -r "rotationSignalStore" src/ --include="*.ts" --include="*.tsx" -l
```
Expected: ?????????src/ ?????????????????????????????????db.ts ???objectStore schema ?????????????????????????????????
- [ ] **Step 2: ?????? 6 ???????????????*

```bash
rm src/store/hotSectorStore.ts src/store/valuePitStore.ts src/store/rotationSignalStore.ts
rm tests/hotSectorStore.test.ts tests/valuePitStore.test.ts tests/rotationSignalStore.test.ts
```

- [ ] **Step 3: ?????????????????????????????????*

```bash
npx vitest run
```
Expected: ?????? PASS??????????????????

- [ ] **Step 4: ??????**

```bash
git add -A
git commit -m "chore: remove 3 deprecated stores (hotSector/valuePit/rotationSignal) and their tests"
```

---

## Batch B ????????????????????????P0?????????????????????
### Task 2: ???orderStore/holdingsStore/poolStore ?????? isRefreshing ????????????

**Files:**
- Modify: `src/store/orderStore.ts` ???initOrderStoreSubscriptions ???debounce callback ?????????isRefreshing ??????- Modify: `src/store/holdingsStore.ts` ?????????
- Modify: `src/store/poolStore.test.ts` ?????????
- Test: `tests/storeSubscriptions.test.ts`

- [ ] **Step 1: ?????????????????? ???orderStore ????????????????????????**

???`tests/storeSubscriptions.test.ts` ????????????

```typescript
describe('[Batch B] orderStore isRefreshing guard', () => {
  it('should skip refresh when isRefreshing is true', async () => {
    const { initOrderStoreSubscriptions } = await import('../src/store/orderStore')
    const cleanup = initOrderStoreSubscriptions()

    // ?????? isRefreshing = true
    useOrderStore.setState({ isRefreshing: true })

    // ?????? broadcast ?????? debounce callback
    // ...
    // ?????? refresh ??????????????????

    cleanup()
  })
})
```

- [ ] **Step 2: ????????????????????????**

```bash
npx vitest run tests/storeSubscriptions.test.ts
```
Expected: FAIL ???isRefreshing guard ????????????

- [ ] **Step 3: ???orderStore.ts ???debounce callback ?????????isRefreshing ??????*

```typescript
// ???debounce ?????????????????? refresh() ???????????????if (getState().isRefreshing) return
```

???????????? state ?????????`isRefreshing: boolean` ???????????????????????????????????? refresh ??????????????? true?????????????????? false???
- [ ] **Step 4: ???holdingsStore.ts ???poolStore.ts ?????? Step 3**

- [ ] **Step 5: ????????????????????????**

```bash
npx vitest run tests/storeSubscriptions.test.ts
```
Expected: PASS

- [ ] **Step 6: ??????????????????**

```bash
npx vitest run
```
Expected: ?????? PASS

- [ ] **Step 7: ??????**

```bash
git add -A
git commit -m "fix: add isRefreshing guard to orderStore/holdingsStore/poolStore debounce callbacks"
```

---

### Task 3: ???positionStore ???????????? + isRefreshing ??????

**Files:**
- Modify: `src/store/positionStore.ts` ???initPositionStoreSubscriptions ?????????200ms ?????? + isRefreshing ???- Test: `tests/storeSubscriptions.test.ts`

- [ ] **Step 1: ??????????????????**

```typescript
describe('[Batch B] positionStore debounce + isRefreshing guard', () => {
  it('should debounce recomputeFromOrders and skip when isRefreshing', async () => {
    // ???????????? orders ???????????????????????????recompute
    // ?????? isRefreshing=true ???????????? recompute
  })
})
```

- [ ] **Step 2: ????????????????????????**

- [ ] **Step 3: ?????? positionStore ?????? + isRefreshing**

```typescript
// initPositionStoreSubscriptions ??????
let _debounceTimer: ReturnType<typeof setTimeout> | null = null
const _isRefreshing = { value: false }

useOrderStore.subscribe(
  (state) => state.orders,
  (orders) => {
    if (_isRefreshing.value) return
    if (_debounceTimer) clearTimeout(_debounceTimer)
    _debounceTimer = setTimeout(() => {
      _isRefreshing.value = true
      usePositionStore.getState().recomputeFromOrders(orders)
        .finally(() => { _isRefreshing.value = false })
    }, 200)
  }
)
```

- [ ] **Step 4: ????????????????????????**

```bash
npx vitest run tests/storeSubscriptions.test.ts
```

- [ ] **Step 5: ??????**

```bash
git add -A
git commit -m "fix: add debounce + isRefreshing guard to positionStore subscriptions"
```

---

### Task 4: DataBridge broadcast ???????????????????????????
**Files:**
- Create: `src/lib/eventBus.ts`
- Modify: `src/core/databridge.ts` ???broadcast() ???????????????queueMicrotask ???????????????- Test: `tests/priorityBroadcastQueue.test.ts`
- Test: `tests/databridge.test.ts`

- [ ] **Step 1: ?????????????????? ???PriorityBroadcastQueue**

```typescript
// tests/priorityBroadcastQueue.test.ts
describe('PriorityBroadcastQueue', () => {
  it('should execute high priority callbacks before normal and low', async () => {
    const executionOrder: string[] = []
    const queue = new PriorityBroadcastQueue()

    queue.subscribe('orders', () => executionOrder.push('normal'), 'normal')
    queue.subscribe('orders', () => executionOrder.push('high'), 'high')
    queue.subscribe('orders', () => executionOrder.push('low'), 'low')

    queue.broadcast('orders', mockEnvelope)
    expect(executionOrder).toEqual(['high', 'normal', 'low'])
  })

  it('should maintain default behavior when no priority specified', () => {
    // ???priority ??????????????????????????????????????????
  })
})
```

- [ ] **Step 2: ????????????????????????**

```bash
npx vitest run tests/priorityBroadcastQueue.test.ts
```

- [ ] **Step 3: ?????? PriorityBroadcastQueue**

```typescript
// src/core/priorityBroadcastQueue.ts
export interface PrioritizedSubscriber {
  callback: (envelope: StandardEnvelope) => void
  priority: 'high' | 'normal' | 'low'
  order: number // ???????????????????????????????????????}

export class PriorityBroadcastQueue {
  private subscribers = new Map<string, PrioritizedSubscriber[]>()
  private counter = 0

  subscribe(channel: string, cb: (envelope: StandardEnvelope) => void, priority: 'high' | 'normal' | 'low' = 'normal'): () => void {
    // ?????? subscriber?????????priority ???order
    // ?????? unsubscribe ??????
  }

  broadcast(channel: string, envelope: StandardEnvelope): void {
    const subs = this.subscribers.get(channel)
    if (!subs) return
    // ???priority ??????: high > normal > low?????????????????? order
    const sorted = [...subs].sort((a, b) => {
      const p = { high: 0, normal: 1, low: 2 }
      return p[a.priority] !== p[b.priority] ? p[a.priority] - p[b.priority] : a.order - b.order
    })
    sorted.forEach(s => { try { s.callback(envelope) } catch(e) { /* log */ } })
  }
}
```

- [ ] **Step 4: ???databridge.ts ???broadcast() ?????????PriorityBroadcastQueue**

?????????????????????
1. ???????????? `subscribers` Map ????????????
2. ???`subscribers` ???callback ????????????????????????priority
3. ???broadcast forEach ?????? priority ??????
4. ?????????API ???????????????priority='normal'

- [ ] **Step 5: ??????????????????**

```bash
npx vitest run tests/priorityBroadcastQueue.test.ts tests/databridge.test.ts
```
Expected: PASS

- [ ] **Step 6: ??????**

```bash
git add -A
git commit -m "feat: add PriorityBroadcastQueue to DataBridge broadcast for priority-ordered subscriber execution"
```

---

## Batch C ???Python ?????????????????????P1?????????????????????
### Task 5: ?????? DataSourceProvider ?????????AkshareProvider ??????

**Files:**
- Create: `src/services/fetcher/types.ts` ???DataSourceProvider ????????????
- Create: `src/services/fetcher/akshareProvider.ts` ????????? fetcherClient ???AkShare ??????
- Create: `src/services/fetcher/mockProvider.ts` ????????? Mock ??????????????????????????????
- Create: `src/services/fetcher/dataSourceRegistry.ts` ?????????????????????
- Modify: `src/services/fetcher/fetcherService.ts` ????????? Provider ?????????????????? fetcherClient
- Test: `tests/fetcher/dataSourceProvider.test.ts`

- [ ] **Step 1: ??????????????????**

```typescript
// tests/fetcher/dataSourceProvider.test.ts
describe('DataSourceProvider interface contract', () => {
  it('AkshareProvider implements DataSourceProvider', async () => {
    const provider = new AkshareProvider({ baseURL: 'http://localhost:8000', timeout: 5000 })
    expect(provider.name).toBe('akshare')
    // ?????? healthCheck() ???????????????????????????unhealthy
    const health = await provider.healthCheck()
    // localhost:8000 ??????????????????
    expect(['healthy', 'unhealthy']).toContain(health.status)
  })

  it('MockProvider provides fallback data without network', async () => {
    const provider = new MockProvider()
    const health = await provider.healthCheck()
    expect(health.status).toBe('healthy')
    const data = await provider.fetchBasicData(['000001'])
    expect(data.length).toBeGreaterThan(0)
  })

  it('DataSourceRegistry supports fallback chain', async () => {
    const registry = new DataSourceRegistry()
    registry.register(new MockProvider())
    registry.register(new AkshareProvider({ baseURL: 'http://localhost:8000' }))
    // ?????????healthy ???provider ????????????
    const active = await registry.getActiveProvider()
    expect(active.name).toBe('mock') // akshare ?????????????????????mock
  })
})
```

- [ ] **Step 2: ????????????????????????**

```bash
npx vitest run tests/fetcher/dataSourceProvider.test.ts
```

- [ ] **Step 3: ?????? DataSourceProvider ??????**

```typescript
// src/services/fetcher/types.ts
export interface HealthStatus {
  status: 'healthy' | 'unhealthy' | 'unknown'
  latency?: number
  error?: string
}

export interface BasicDataResult {
  symbol: string
  name: string
  price: number
  changePercent: number
  volume: number
  // ... ???fetcherService ?????????????????????
}

export interface KlineDataResult {
  symbol: string
  date: string
  open: number
  high: number
  low: number
  close: number
  volume: number
}

export interface DataSourceProvider {
  readonly name: string
  healthCheck(): Promise<HealthStatus>
  fetchBasicData(symbols: string[]): Promise<BasicDataResult[]>
  fetchKlineData(symbol: string, period: string): Promise<KlineDataResult[]>
}
```

- [ ] **Step 4: ?????? AkshareProvider???????????????fetcherClient???*

???`fetcherClient` ???`collectBasic`/`collectKline`/`healthCheck` ?????????`DataSourceProvider` ?????????????????? fetcherClient ??????????????????????????????
- [ ] **Step 5: ?????? MockProvider?????????????????????????????????*

???????????????A ??????????????????0 ??????????????????????????????????????????????????????
- [ ] **Step 6: ?????? DataSourceRegistry?????????????????????+ ?????????*

```typescript
export class DataSourceRegistry {
  private providers: DataSourceProvider[] = []

  register(provider: DataSourceProvider): void
  async getActiveProvider(): Promise<DataSourceProvider>
  // ?????? providers??????????????????healthCheck() === 'healthy' ???  // ???????????? unhealthy????????????????????????????????????????????????
}
```

- [ ] **Step 7: ?????? fetcherService ?????? Provider**

???fetcherService ??????????????????????????? Registry ?????? active provider????????????????????? fetcherClient?????????fetcherClient ?????? AkshareProvider ??????????????????
- [ ] **Step 8: ????????????**

```bash
npx vitest run tests/fetcher/dataSourceProvider.test.ts
npx vitest run tests/fetcher/fetcherService.test.ts
```
Expected: ?????? PASS

- [ ] **Step 9: ??????**

```bash
git add -A
git commit -m "feat: abstract DataSourceProvider interface with Akshare/Mock providers and auto-fallback registry"
```

---

## Batch D ???IndexedDB ???Store JOIN ?????????P1?????????????????????
### Task 6: ?????? QueryBuilder ?????????Store ????????????

**Files:**
- Create: `src/data/queryBuilder.ts` ???QueryBuilder ?????????- Modify: `src/services/analysis` ????????? QueryBuilder ???????????? Promise.all
- Test: `tests/queryBuilder.test.ts`

- [ ] **Step 1: ??????????????????**

```typescript
// tests/queryBuilder.test.ts
describe('QueryBuilder', () => {
  it('should join stock with scores in single call', async () => {
    // ?????? QueryBuilder ???symbol ???????????? stocks + v6Scores + intelligentScores
    // ?????????????????? UnifiedStockData ??????
  })

  it('should handle missing data gracefully (stock exists but no score)', async () => {
    // ?????????????????????????????????throw??????????????? null/undefined
  })

  it('should batch multiple symbols efficiently', async () => {
    // ???????????????????????????Promise.all ??????????????????
  })
})
```

- [ ] **Step 2: ????????????????????????**

```bash
npx vitest run tests/queryBuilder.test.ts
```

- [ ] **Step 3: ?????? QueryBuilder**

```typescript
// src/data/queryBuilder.ts
export interface UnifiedStockQuery {
  symbol: string
  includeBasic?: boolean
  includeQuotes?: boolean
  includeV6Score?: boolean
  includeIntelligentScore?: boolean
  includeIndustryScore?: boolean
  includeSignals?: boolean
  includeNews?: boolean
}

export interface UnifiedStockData {
  stock?: Stock
  quotes?: DailyQuotes
  v6Score?: V6Score
  intelligentScore?: IntelligentScore
  industryScore?: IndustryScore
  signals?: Signal[]
  news?: NewsArticle[]
}

export class QueryBuilder {
  constructor(private dataLayer: DataLayer) {}

  async queryStock(params: UnifiedStockQuery): Promise<UnifiedStockData> {
    const tasks: Promise<any>[] = []
    if (params.includeBasic) tasks.push(this.dataLayer.stocks.get(params.symbol).catch(() => null))
    if (params.includeV6Score) tasks.push(this.dataLayer.v6Scores.get(params.symbol).catch(() => null))
    // ... ????????????

    const results = await Promise.all(tasks)
    // ?????????UnifiedStockData
  }

  async queryStocksBatch(symbols: string[], params: Omit<UnifiedStockQuery, 'symbol'>): Promise<Map<string, UnifiedStockData>> {
    // ??????????????????
  }
}
```

- [ ] **Step 4: ???dataFusionEngine.ts ?????????QueryBuilder**

???????????? 6 ???dataLayer ?????????`queryBuilder.queryStock({ symbol, includeBasic: true, includeV6Score: true, ... })`

- [ ] **Step 5: ????????????**

```bash
npx vitest run tests/queryBuilder.test.ts
npx vitest run
```

- [ ] **Step 6: ??????**

```bash
git add -A
git commit -m "feat: add QueryBuilder for cross-store aggregated queries, used in dataFusionEngine"
```

---

## Batch E ???????????????????????????P2??????????????????

### Task 7: ???????????? HEX ?????????chartColors ??????

**Files:**
- Modify: `src/config/chartColors.ts` ?????????????????????
- Test: `tests/chartColors.test.ts`

- [ ] **Step 1: ???????????? HEX ?????????*

```bash
grep -rn "#[0-9a-fA-F]\{6\}" src/components/ --include="*.tsx" --include="*.ts" | head -30
```

- [ ] **Step 2: ?????????????????? HEX ???????????????chartColors.ts**

?????????>=3 ?????? HEX ???????????????????????????????????????`CHART_UP_COLOR`, `CHART_DOWN_COLOR`, `CHART_NEUTRAL_COLOR`??????
- [ ] **Step 3: ???????????????????????????????????????*

```typescript
describe('chartColors', () => {
  it('should export all semantic color constants', () => {
    expect(CHART_UP_COLOR).toMatch(/^#[0-9a-fA-F]{6}$/)
    expect(CHART_DOWN_COLOR).toMatch(/^#[0-9a-fA-F]{6}$/)
  })
})
```

- [ ] **Step 4: ??????????????????**

```bash
npx vitest run
```

- [ ] **Step 5: ??????**

```bash
git add -A
git commit -m "refactor: extract repeated HEX colors to chartColors constants"
```

---

### Task 8: ???????????????????????????thresholds ??????

**Files:**
- Modify: `src/config/thresholds.ts` ??????????????????- Test: `tests/thresholds.test.ts`

- [ ] **Step 1: ????????????????????????**

```bash
# ?????????????????????????????????????????? 0, 1, -1, 100 ???????????????
grep -rn "\b[2-9][0-9]\{2,\}\b" src/components/ --include="*.tsx" | head -20
```

- [ ] **Step 2: ???????????????????????????????????????thresholds.ts**

- [ ] **Step 3: ??????????????????????????????*

- [ ] **Step 4: ??????????????????**

```bash
npx vitest run
```

- [ ] **Step 5: ??????**

```bash
git add -A
git commit -m "refactor: extract magic numbers to thresholds config"
```

---

## ???????????????????????????
| ?????? | Task | ?????? | ???????????? | ?????? |
|------|------|------|---------|------|
| **Batch A** | Task 1: ???????????? Store | ?????????| 10 min | ???|
| **Batch B** | Task 2: Store isRefreshing ???| ???| 20 min | ???|
| **Batch B** | Task 3: positionStore ?????? | ???| 15 min | ???|
| **Batch B** | Task 4: ?????????????????????| ???| 30 min | ???|
| **Batch C** | Task 5: DataSourceProvider | ???| 45 min | ???|
| **Batch D** | Task 6: QueryBuilder | ???| 30 min | ???|
| **Batch E** | Task 7: ?????????????????? | ???| 15 min | ???|
| **Batch E** | Task 8: ?????????????????? | ???| 15 min | ???|

**?????? Batch ?????????????????? `npx vitest run` ??????????????????????????????*

**???????????????**
- [ ] Store ???????????? 30+ ?????? ~27?????????3 ?????????Store???- [ ] ?????? 6 ?????????Store ?????? isRefreshing ????????????
- [ ] DataBridge broadcast ?????????????????????- [ ] fetcherService ?????? DataSourceProvider ?????????????????????????????? Mock ??????
- [ ] dataFusionEngine ?????? QueryBuilder ?????????Store ??????
- [ ] ???????????? 80+ ???????????? PASS
