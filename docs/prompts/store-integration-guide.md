---
title: Store ????????????
type: reference
domain: data
phase: development
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?????????????? `src/store/` ???????????????????????????????????????????????????????????? Zustand Store?????????? Store ????????????????????..."
tags: [data, integration, store]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-DATA-059
referenced_by: [V9-DOC-META-000, V9-DOC-PROJ-176, V9-DOC-DATA-061, V9-DOC-DATA-073, V9-DOC-PROJ-121, V9-DOC-AI-012, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# Store ????????????

> **????**?????????? `src/store/` ??????**????????????**?????????????????????????????????????????? Zustand Store?????????? Store ???????????????????? `audit:layers` / `audit:docs` ????????????
>
> **????????**??`../../AGENTS.md` ?????????????????????????????????????????????????????????????????????????????????????? `../../AGENTS.md` ?????????? `../../AGENTS.md` ??????
> **????**??`docs/README.md` ?? H ?? / `../how-to/how-to-add-store.md`(P1)

---

## ????

1. [Store ????????](#1-store-????????)
2. [????????](#2-????????)
3. [Zustand Store ????????](#3-zustand-store-????????)
4. [withBroadcast ?? Tab ????](#4-withbroadcast-??-tab-????)
5. [??????????.derived.ts??](#5-????????derivedts)
6. [?? Store ???? Checklist????????????](#6??-store-????-checklist????????)
7. [Store ??????????????](#7-store-??????????????)
8. [??????????](#8-??????????)
9. [????????](#9-????????)
10. [????????](#10-????????)

---

## 1. Store ????????

### 1.1 ??????????????????

```
src/config/       ?? ??????????????????????
src/core/         ?? ??????????DataBridge/ACL/EventBus/MemoryCache??
src/data/         ?? ????????IndexedDB/dataLayer/queryBuilder/types??
src/lib/          ?? ????????logger/format/errors/utils/derivedCache/withBroadcast??
src/services/     ?? ????????24 ??????
src/store/        ?? ?????? ?? **????????**
src/components/   ?? ??????
src/pages/        ?? ??????
src/portal/       ?? PortalShell ????????
src/apps/         ?? App ??????
```

### 1.2 Store ????????????

| ???? | ???? | ???????? |
|------|------|----------|
| **???????? `services/` ?? `core/`** | Store ???? Service ?????????????????????? `dataLayer` / `db` | `audit:layers` ???? |
| **???????? `lib/` ??????????????** | `logger`??`eventBus`??`withBroadcast`??`format`??`errors`??`utils`??`derivedCache`??`safeCoerce`??`perf` | ???????? `lib/` ???????? |
| **?????? `services/` / `data/` / `core/` ????** | Store ?????????????????????????????? | `audit:layers` ???? |
| **?????? Store ???? UI ????** | ?? JSX??DOM ?????????????? | ???????? |

> **??????????**??`???? API ?? fetcher ?? DataBridge.forward() ?? dataLayer ?? IndexedDB ?? services ?? store ?? components/pages`??Store ???????? services ????????**???????????? IndexedDB**??

---

## 2. ????????

### 2.1 ??????

| ???? | ???????? | ???? |
|------|----------|------|
| **Store ??????** | `kebab-case` + `Store.ts` | `analysisStore.ts` |
| **????????????** | ???????? + `.derived.ts` | `analysisStore.derived.ts` |
| **????????** | ???????? + `.test.ts` | `analysisStore.test.ts` |
| **Store ??????** | `kebab-case` | `withBroadcast.ts` |

### 2.2 ????????

| ???? | ???????? | ???? |
|------|----------|------|
| **Store Hook** | `use` + `PascalCase` + `Store` | `useAnalysisStore` |
| **State Interface** | `PascalCase` + `State` | `AnalysisState` |
| **????????** | `camelCase`?????????? | `scoreBySymbol`, `topStocks` |
| **Hook ????????** | `use` + `camelCase` | `useScoreLevelDistribution` |

> ???? `../../AGENTS.md` ??????"Store: camelCase + `Store` ???????? `analysisStore.ts`??"

---

## 3. Zustand Store ????????

### 3.1 ???????????? JSDoc??logger????????????

```typescript
/**
 * @module {cabinName}Store
 * @description ???????????? Store ??????
 * @lifecycle ??@Global / @Session / @Page??
 * @see @/pages/{cabin}/* - ??????
 * @see @/services/{domain}/{serviceName} - ????????????
 */

import { create } from 'zustand'
import { getLogger } from '@/lib/logger'
// import type { SomeType } from '@/data/types'  // ????1??????????
// import { someService } from '@/services/{domain}/{service}'  // ????3??Service ??

const logger = getLogger()

// ============================================================
// Store ??????State + Actions??
// ============================================================

interface {Cabin}State {
  /** ???????? */
  items: SomeType[]
  /** ???????? */
  loading: boolean
  /** ???????? */
  error: string | null

  // Actions
  /** ???????? */
  loadItems: () => Promise<void>
  /** ???????? */
  clearError: () => void
}

// ============================================================
// ????????
// ============================================================

const initialState = {
  items: [] as SomeType[],
  loading: false,
  error: null as string | null,
}

// ============================================================
// Store ????
// ============================================================

export const use{Cabin}Store = create<{Cabin}State>((set, get) => ({
  ...initialState,

  loadItems: async () => {
    logger.info('[{cabin}Store] loadItems ????')
    set({ loading: true, error: null })

    try {
      // TODO(????3??????????): const result = await someService()
      // if (result.success && result.data) {
      //   set({ items: result.data, loading: false })
      //   logger.info(`[{cabin}Store] loadItems ????: ${result.data.length} ??`)
      // } else {
      //   const message = result.error ?? '????????'
      //   logger.error(`[{cabin}Store] loadItems ????: ${message}`)
      //   set({ loading: false, error: message })
      // }

      // TODO: ??????????????3???????????????? Service ????
      set({ loading: false })
    } catch (err) {
      const message = err instanceof Error ? err.message : '????????'
      logger.error(`[{cabin}Store] loadItems ????: ${message}`)
      set({ loading: false, error: message })
    }
  },

  clearError: () => {
    set({ error: null })
  },
}))

// ============================================================
// ???????????????? .derived.ts ??????????
// ???????????????????????????????????? getState() ???????????????? State
// ============================================================
export * from './{cabin}Store.derived'
```

### 3.2 ????????????

| ???? | ???? | ???? |
|------|------|------|
| **JSDoc `@module`** | ?????????????????????????????? | ? |
| **JSDoc `@lifecycle`** | ??????????????`@Global`??????????????`@Session`????????????`@Page`?????????? | ???? |
| **`logger.info` ????** | ??????`[storeName] actionName ????`???? `[analysisStore] loadStocks ????` | ? |
| **?????????? context** | `logger.error('????????', { error: message })` ???????????? | ? |
| **`try/catch` ????** | ???? async Action ??????????set ?? `error` ???? | ? |
| **????????????** | ???? `initialState` ?????????????? | ???? |
| **????????** | ???????????? `as T[]` ?????????????? `never[]` | ? |

---

## 4. withBroadcast ?? Tab ????

### 4.1 ??????????????

V9 ?????? Tab ?????????????????????? Tab ?????????????????????????????????????? Tab ??????????????????????????

### 4.2 ????????

- **????????**??`@/lib/withBroadcast`???? `@/store/helpers/withBroadcast` ?????????????????????? `@/lib/withBroadcast`??
- **????**??`eventBus.emit()`?????? `EventBus` ?????????? `src/lib/eventBus.ts`??
- **????????**??`@/constants/store-channels.constants` ?? `EVENT_NAMES`

### 4.3 ????????

#### ???? A??????????????????????????

```typescript
import { withBroadcast } from '@/lib/withBroadcast'
import { EVENT_NAMES } from '@/constants/store-channels.constants'

// ?? Store ?? Action ????set ??????????
addItem: (item) => {
  set((s) => ({ items: [...s.items, item] }))
  withBroadcast(EVENT_NAMES.STOCK_POOL_CHANGED, { action: 'add', symbol: item.symbol })
}
```

#### ???? B??createBroadcaster????????????????

```typescript
import { createBroadcaster } from '@/lib/withBroadcast'
import { EVENT_NAMES } from '@/constants/store-channels.constants'

const broadcastPool = createBroadcaster(EVENT_NAMES.STOCK_POOL_CHANGED)

addItem: (item) => {
  set((s) => ({ items: [...s.items, item] }))
  broadcastPool({ action: 'add', symbol: item.symbol })
}
```

### 4.4 ????????????

```typescript
// ? ?? EVENT_NAMES ????????
EVENT_NAMES.STOCKS_CHANGED
EVENT_NAMES.SCORES_CHANGED
EVENT_NAMES.HOLDINGS_CHANGED

// ? ??????????????????????
'pool:changed'  // ????????????????
```

### 4.5 ?????????? Tab ??????????

```typescript
import { useEffect } from 'react'
import { eventBus } from '@/lib/eventBus'
import { EVENT_NAMES } from '@/constants/store-channels.constants'
import { useSomeStore } from '@/store/someStore'

function useCrossTabSync() {
  useEffect(() => {
    const handleChange = (payload: unknown) => {
      const { action } = payload as { action: string }
      logger.info('[CrossTab] ????????????', { action })
      // ???? Store ????
      useSomeStore.getState().loadItems()
    }

    eventBus.subscribe(EVENT_NAMES.STOCK_POOL_CHANGED, handleChange)
    return () => eventBus.unsubscribe(EVENT_NAMES.STOCK_POOL_CHANGED, handleChange)
  }, [])
}
```

> **????????**?????? `EventBus.subscribe()` ???????? `EventBus.unsubscribe()`???? `useEffect` cleanup ?????????????????????? `../../AGENTS.md` ??????????????????????????????

---

## 5. ??????????.derived.ts??

### 5.1 ????????

??????????Derived Queries????**??????**???? Store ??????????????????????????????????????????

1. **??????**?????? `useStore.getState()` ???????????????? `set()`
2. **????????**?????? `memoizeByRef` ???????????????????? `buildIndex` ???? O(n) ????
3. **??????????**???????????????????????????? `[]`??`0`??`null`??
4. **??????????????**???????????? Store ?? `lib/derivedCache`

### 5.2 ????????

```typescript
// {storeName}.derived.ts

import { use{Store} } from '@/store/{storeName}'
import { memoizeByRef, buildIndex, safeLength } from '@/lib/derivedCache'

// ============================================================
// ????????
// ============================================================
export interface SomeDistribution { /* ... */ }

// ============================================================
// ?????????????????????? memoizeByRef??
// ============================================================
export function isItemsEmpty(): boolean {
  return use{Store}.getState().items.length === 0
}

export function itemsCount(): number {
  return safeLength(use{Store}.getState().items)
}

// ============================================================
// ????????????buildIndex ??????
// ============================================================
export function itemById(id: string): SomeType | undefined {
  return use{Store}.getState().items.find(i => i.id === id)
}

// ============================================================
// ??????????memoizeByRef ??????
// ============================================================
export const someDistribution = memoizeByRef((items: readonly SomeType[]): SomeDistribution => {
  // ????????
}, 'someDistribution')

export function getSomeDistribution(): SomeDistribution {
  return someDistribution(use{Store}.getState().items)
}

// ============================================================
// React Hook ????????????????????????
// ============================================================
export function useSomeDistribution(): SomeDistribution {
  const items = use{Store}(state => state.items)
  return someDistribution(items)
}
```

### 5.3 ????????

?????????????? `src/store/derived.index.ts` ????????????UI ????????????????

```typescript
// src/store/derived.index.ts
export { useSomeDistribution, itemById } from '@/store/{storeName}.derived'
```

> **TODO**?????? Store ???????????????????? `derived.index.ts` ?????????? `src/store/derived.index.ts` ??????????

---

## 6. ?? Store ???? Checklist????????????

?? Store ???????????????????????????????????????????? `../../AGENTS.md` ??????

### ???? 1????????????`src/types/` ?? `src/data/types.ts`??

- [ ] ???? Store ??????**???????? Interface**???? `SomeData`??`SomeDataState`??
- [ ] ???? Action ??**????????**???? `LoadOptions`??`FilterParams`??
- [ ] ???????? Service ?????????????? `Result<T>` ?????????????? `ServiceResult<T>`
- [ ] ???? `npx tsc --noEmit` ????????????

### ???? 2??Store/??????`src/store/{storeName}.ts`??

- [ ] ???? ??3 ???????????? Store
- [ ] ??????????`use{PascalCase}Store`
- [ ] ?????????????? `initialState` ????
- [ ] ???? Action ???? `try/catch`?????????? `error` ????
- [ ] ?????????????????????????????????? `logger.info`
- [ ] ???????????? `withBroadcast` ?????????????????????? Tab ??????
- [ ] ?????????? `{storeName}.derived.ts` ????????????????5??
- [ ] ?? `derived.index.ts` ??????????
- [ ] ???? `npx tsc --noEmit` ????????????

### ???? 3??Service/????????`src/services/{domain}/`??

- [ ] ?????????? Service ????????
- [ ] Service ???? `DataBridge.forward()` ?? `dataBridge.query()` ????????????
- [ ] **???? Service ?????? `db` / `dataLayer`**?????????? `DataBridge`??
- [ ] Service ???????? `Result<T>` ???????? `success`/`data`/`error`??
- [ ] ???? `npx tsc --noEmit` ????????????

### ???? 4????????????`src/pages/` ?? `src/components/`??

- [ ] UI ????**?????? `use{Store}()` ?? `derived.index.ts` ????????**
- [ ] ???????????? `dataLayer` / `db` / `DataBridge`
- [ ] ???????????? Service?????????? Store Action??
- [ ] ?????????????? EventBus ???????????? `eventBus.subscribe`??
- [ ] ???? `npx tsc --noEmit` ????????????

### ??????????????

```powershell
# ????????
npx tsc --noEmit

# ????????????
npm run audit:layers
# ??????0 violations, 0 warnings

# ??????????????
npm run audit:hardcode
# ??????0 hardcoded colors
```

---

## 7. Store ??????????????

### 7.1 Store ??????????????????????

V9 ?? Store ????**????????**???????????????????????????????????? Store ??????????

| ?????? | ???? | ???????? |
|--------|------|----------|
| **????????** | `src/store/` ?????? | ???????? |
| **????????** | `kebab-case` + `Store.ts` | `audit:layers` ???????? |
| **????????** | `usePascalCaseStore` | ???????? |
| **derived ????** | ?????? `.derived.ts` | ?????????????? |
| **????????** | ?????? `.test.ts` | ?????????????? |
| **derived.index.ts ????** | ?? `derived.index.ts` ?????????? | ???????? |
| **????????** | ?????????????????? `EVENT_NAMES` | ???????????? |

### 7.2 ????????

???? Store ???????? `.test.ts` ????????????????????????

| ???????? | ???? | ???? |
|----------|------|------|
| **????????** | ???? `initialState` ???? | `expect(store.loading).toBe(false)` |
| **Action ????** | ???? Service ???????? | ???????????????? |
| **Action ????** | ???? Service ????/???? | ???? `error` ?????????? |
| **????????????** | `loading: true ?? false` | ???????????? |
| **????????** | ???? `.derived.ts` ???? | ?????????????????????? |
| **??????** | ?????? `isRefreshing` ?????? | ?????????????????? |
| **????????** | ???????????????? | ???????????????? |

### 7.3 ????????

```typescript
// {storeName}.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { use{Store} } from './{storeName}'

// ???? Store ????????????????????
beforeEach(() => {
  use{Store}.setState(use{Store}.getState(), true) // ?????? reset ????
})

describe('{storeName}', () => {
  it('????????????', () => {
    const state = use{Store}.getState()
    expect(state.items).toEqual([])
    expect(state.loading).toBe(false)
    expect(state.error).toBeNull()
  })

  it('loadItems ????????????', async () => {
    // TODO: mock Service ????
  })

  it('loadItems ???????? error', async () => {
    // TODO: mock Service ????
  })
})
```

> **TODO**???????????????????? mock Service ?????? `derivedCache` ??????????

---

## 8. ??????????

### 8.1 ??????????Critical??

| ?????? | ???????? | ???????? |
|--------|----------|----------|
| **Store ?????? db** | `import { db } from '@/data/db'; db.put(...)` | ???? Service ?? `DataBridge.forward()` |
| **Store ???? dataLayer** | `import { queryBuilder } from '@/data/queryBuilder'` | ???? Service ???????????? |
| **Store ???? UI ??** | `import { SomePage } from '@/pages/...'` | Store ?? UI ???? |
| **Store ???? JSX** | ?? Store ?????? `<Component />` | ?????????????? JSX |
| **???????????? DataBridge** | `useEffect(() => dataBridge.query(...), [])` | ???? ?? Store Action ?? Service ?? DataBridge |

### 8.2 ??????????????Major??

| ?????? | ???????? | ???????? |
|--------|----------|----------|
| **???????????? State** | `set({ derivedCount: items.length })` | ???? `.derived.ts` ?????????? |
| **State ????????** | `set({ computed: () => ... })` | ?????????? `.derived.ts` ?????? |
| **??????????????** | ?????????????? | ?????????? `setState(initialState)` |
| **??????????** | `catch` ???? `console.log` | ???? `error: string \| null` ???? |
| **??????????** | async ?????????? `loading` | ???? `loading` + `set({ loading: true/false })` |

### 8.3 ????????????????Major??

| ?????? | ???????? | ???????? |
|--------|----------|----------|
| **????????????** | `withBroadcast('myEvent', ...)` | `withBroadcast(EVENT_NAMES.SOME_CHANGED, ...)` |
| **??????????????????** | `withBroadcast` ???????????????????? | ?? `withBroadcast` ??????catch ?? log???????? |
| **??????????** | `eventBus.subscribe(...)` ?? `unsubscribe` | `useEffect` cleanup ???????????????? |
| **????????** | Store ?????????????????????????? Action | ?????????? `source`???? `signalStore.ts` ?????? |

### 8.4 ????????????????Minor??

| ?????? | ???????? | ???????? |
|--------|----------|----------|
| **??????????????** | `console.log('loaded')` | `logger.info('[storeName] actionName ????')` |
| **???????? context** | `logger.error('????')` | `logger.error('????', { error: message })` |
| **???? `console.log`** | `console.log(...)` | `import { getLogger } from '@/lib/logger'` |

---

## 9. ????????

?????????? Store ??????????????????????

```powershell
# 1. ????????
npx tsc --noEmit

# 2. ????????????????????Store ???????????? dataLayer / pages / components??
npm run audit:layers
# ??????0 violations, 0 warnings

# 3. ??????????Store ????
npm test -- --run src/store/{storeName}.test.ts
# ??????????????

# 4. ????????????????????????
npm test -- --run
# ??????????????

# 5. ????????????????????
npm run build
# ??????0 errors
```

---

## 10. ????????

| ???? | ???? | ???? |
|------|------|------|
| **AGENTS.md** | `../../AGENTS.md` | ?????????????????????????????????????? |
| **????????????** | `../explanation/overview.md` | ???????????????? |
| **????????** | `../explanation/cabins-overview.md` | 5 ???????????? |
| **????????** | `../reference/services-catalog.md` | 24 ???????????? |
| **????????** | `../reference/05-engine-specs.md` | L0-L8 ???????? |
| **????????** | `../reference/06-routing-specs.md` | ???????????? |
| **????????** | `../reference/coding-conventions.md` | ???????? |
| **JSDoc ????** | `../reference/jsdoc-convention.md` | JSDoc ???????? |
| **??????????** | `../reference/complexity-governance.md` | ????????/???????? |
| **how-to-add-store** | `../how-to/how-to-add-store.md` (P1) | ???????????????????? |
| **withBroadcast ????** | `../../src/lib/withBroadcast.ts` | ???????????? |
| **EVENT_NAMES ????** | `../../src/constants/store-channels.constants.ts` | ???????????? |
| **derived.index.ts** | `../../src/store/derived.index.ts` | ???????????????? |
| **analysisStore ????** | `../../src/store/analysisStore.ts` | ???? Store ???? |
| **signalStore ????** | `../../src/store/signalStore.ts` | ?? DataBridge ???????? |
| **analysisStore.derived** | `../../src/store/analysisStore.derived.ts` | ???????????????? |

---

## ???? A??Store ????????????

| ???? | ???? | ???? Store |
|------|------|-----------|
| `@Global` | ???????????????????????????? | `analysisStore`??`signalStore` |
| `@Session` | ????????????/?????????? | `portfolioStore`??`holdingsStore` |
| `@Page` | ?????????????????????? | `collectionWizardStore` |

> **TODO**???????????????????????????????????????????? `@Page` Store ??????????????????????

## ???? B??Store ?? IndexedDB ??????????

Store **??????**???? IndexedDB????????????????????????????

```
Store Action
  ?? Service???? analysisService.ts??
    ?? DataBridge.forward() / dataBridge.query()
      ?? dataLayer / queryBuilder
        ?? IndexedDB
```

???? Store ????????????????????????????

- [ ] `src/config/dbConfig.ts` ?? `STORE_NAME` ???????????? store ????
- [ ] `src/config/dbConfig.ts` ?? `ACL_MATRIX` ???????? read/write ??????
- [ ] `src/data/db-schema.ts` ?? `createSchema` ?????????? store?????????? Migration ??????????????
- [ ] `DB_VERSION` ???????????????????? store??

> ???? `../../AGENTS.md` ????????????????????????

---

_?????????? `../../AGENTS.md` v1.4.3 ???????? `../../AGENTS.md` ??????????????????????????????_
