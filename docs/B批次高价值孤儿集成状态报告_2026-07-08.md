# B 批次高价值孤儿组件集成状态报告

> 日期：2026-07-08
> 依据：D11 孤儿组件分类与价值评估标准（已沉淀进 `code-quality-compliance` SKILL）
> 范围：将 B 批次判定的高价值孤儿（V1=1 规格契合）**真正接入对应板块页面**，而非仅保留
> 说明：原 `B批次代码审查与改造报告_2026-07-08.md` 在并行会话活动期间被清除，本报告为权威替代记录

---

## 〇、背景与判据

B 批次原审计（07-07）曾误判"生产零引用 = 死 = 删"，要把 9 个 B2 组件整批删除。经用户反馈"死组件分析不全面""因子热力图应体现"，并依据新沉淀的 **D11 标准**推翻该误判：

- **V1=1（规格契合）OR V7=1（已在 registry 登记）→ 保留/集成，禁止删除**
- 仅空壳且无规格无登记的符号才属"真死代码"，可考虑删（仍须用户确认）

本次任务：把 3 个确认的高价值孤儿（V1=1）接入具体板块，落实"体现价值"的判断。

---

## 一、集成落地清单（代码已交付 + 门禁验证通过）

| # | 组件 | 宿主页面（具体板块） | 数据源 / 映射 | 集成方式 | 状态 |
|---|------|---------------------|--------------|----------|------|
| 1 | `FactorHeatmap` | `IntelligentScoreExplanation`（智能评分解释页） | `result.dimensionScores` → `buildFactorHeatmapData()` | import + `useMemo(heatmapData)` + 渲染段（`heatmapTitle`） | ✅ 已集成（恢复此前被回滚的改动） |
| 2 | `ScoreFactorWaterfall` | `IntelligentScoreExplanation`（智能评分解释页） | `result.dimensionScores` → 新增 `buildWaterfallFromDimensions()` 派生 | import + 渲染段（`waterfallTitle`），组件扩展 `dimensionScores` 属性 | ✅ 已集成 |
| 3 | `SectorRotationHeatmap` | `SectorAnalysisPage`（板块分析页） | `sectorHeatmap` store（页面已持有） | `<SectorRotationHeatmap />` | ✅ 已集成 |
| 4 | `CandlestickChart` | `StockAnalysisPage`（个股行情页） | `quotes.history: KlineBar[]` → `candleData`（`date→time` 映射） | `<CandlestickChart data={candleData} height={400} />` | ✅ 已集成 |

> 注：`FactorHeatmap` 与 `ScoreFactorWaterfall` 同宿主（评分解释页），形成"雷达 + 关键因子 + 因子热力图 + 因子贡献瀑布图 + 思维链"的完整解释视图。

---

## 二、配套代码改动

- **`src/constants/score.constants.ts`**
  `INTELLIGENT_SCORE_EXPLANATION_LABELS` 新增 `heatmapTitle: '因子热力图'`、`waterfallTitle: '因子贡献瀑布图'`。
- **`src/components/analysis/score/ScoreFactorWaterfall.tsx`**
  - 扩展 `ScoreFactorWaterfallProps.dimensionScores?: DimensionScore[]`（`audit` 优先级更高，缺失时由 `dimensionScores` 兜底派生瀑布图）。
  - 新增 `buildWaterfallFromDimensions(dimensions)` 纯函数。
  - tooltip 改用 `twBg('neutral', 50)` / `twText(...)` 令牌，消除一处 `bg-white` 硬编码（满足 `lint:colors` error 级）。
  - 保留原 `audit` 路径与既有单测不变。
- **`src/components/analysis/score/IntelligentScoreExplanation.tsx`**
  恢复 `FactorHeatmap` 集成（import + 数据构建 + 渲染段），新增 `ScoreFactorWaterfall` 集成（含 `waterfallTitle`）。
- **`src/pages/analysis/SectorAnalysisPage.tsx`** / **`src/pages/analysis/StockAnalysisPage.tsx`**
  分别接入 `SectorRotationHeatmap` 与 `CandlestickChart`，并完成各自数据源映射。

---

## 三、门禁验证结果

| 检查项 | 命令 / 范围 | 结果 |
|--------|------------|------|
| 类型安全 | `tsc:prod`（筛选本次 5 个集成文件） | **0 类型错误**（组件全部类型安全） |
| 单元测试 | `IntelligentScoreExplanation.test.tsx`(5) + `ScoreFactorWaterfall.test.tsx`(5) | **10 / 10 通过** |
| 颜色令牌 | `lint:colors`（error 级，本次 5 文件） | **0 违规**（22 个 warning 均为预存风格项，非硬编码色） |

> 说明：`tsc:prod` 整体退出码为 2，但**所有报错均位于 `permissionRevocationService.ts`**（并行会话 rbac 工作引入的 `readonly` 数组 `.push()` 类型错误），与本次集成无关，由该并行会话负责，未擅自改动以免冲突。

---

## 四、D11 价值闭环验证

| 组件 | V1 规格证据 | V4 展示价值 | V7 登记 | D11 分类 | 处置 |
|------|-----------|------------|---------|----------|------|
| `FactorHeatmap` | `04-ui-ux-specs.md` 图表库成员 | 高（直观热力） | 原 registry 关联 | 高价值孤儿 | ✅ 已集成体现 |
| `ScoreFactorWaterfall` | `analysis-screening-module-dev-plan.md` DA-009 计划交付 | 高（贡献瀑布） | 否 | 高价值孤儿 | ✅ 已集成体现 |
| `SectorRotationHeatmap` | `analysis-screening-module-dev-plan.md` DA-004 计划交付 | 高（板块轮动） | 否 | 高价值孤儿 | ✅ 已集成体现 |
| `CandlestickChart` | `04-ui-ux-specs.md:188` 图表库 | 高（K线） | 否 | 高价值孤儿 | ✅ 已集成体现 |

新标准成功拦截了原"零引用即删"的误判：4 个组件均因 V1=1 判为禁删，并已通过实际集成证明其价值。

---

## 五、未决项（非本次范围，待用户决策）

1. **`cabin×4` 遗留组件**（`ScoreHistoryTable` / `ScoreItem` / `ScoreSnapshot` / `ScoreSummary`）
   legacy v0.9.11 残留，已被新版 `ScoreHistoryPanel` / `IntelligentScorePage` 覆盖（V6=1），判定为"保留非删、不集成"。
2. **5 个真死候选**（B1×3 UseCase + B3×2 Service）
   逻辑已内联 Store / Mock 残留 / 冗余替代实现，符合 D11 真死定义；按"删除铁律"仍须用户明确授权后方可删除。
3. **`permissionRevocationService.ts` 的 3 个 tsc 错误**
   并行会话 rbac 工作引入，由该会话负责，本批次不触碰。

---

## 六、执行环境说明

执行期间并行会话在同一工作树进行 `git stash` / `rebase` / `merge`，曾导致 `IntelligentScoreExplanation.tsx` 出现整段重复副本与 `>>>>>>> Stashed changes` 冲突标记。当前磁盘状态已恢复为**干净单副本（189 行）**，全部集成编辑完整保留；`git status` 显示相关文件为已暂存修改（`M`）。所有结论均经"信任但验证"核对真实文件系统，非依赖缓存。
