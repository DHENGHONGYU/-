# V9 项目目录结构规范与使用指南

> **Version**: v2.0.0  
> **Last Updated**: 2026-07-13  
> **Maintainer**: 架构资产治理官  
> **Purpose**: 为初始 APP 开发团队建立规范的文件管理体系

---

## 一、目录结构总览

```
V9/                              # 项目根目录
├── .github/                     # GitHub 配置（CI/CD、PR模板等）
├── .husky/                      # Git Hooks 配置
├── archive/                     # 归档目录（过时资源、历史备份）
├── design-tokens/               # 设计令牌（主题变量定义）
├── docs/                        # 项目文档（按阶段分类）
├── e2e/                         # 端到端测试
├── packages/                    # 子包（独立发布模块）
├── plugins/                     # 插件系统（第三方服务集成）
├── prompts/                     # AI 提示词模板
├── public/                      # 静态资源（入口HTML、图标等）
├── scripts/                     # 开发脚本（审计、构建、工具等）
├── src/                         # 源代码（核心应用逻辑）
├── tests/                       # 单元测试与集成测试
└── [配置文件]                   # 根目录配置文件
```

---

## 二、目录分类标准

### 2.1 核心分类体系

| 分类 | 目录 | 职责 | 生命周期 |
|------|------|------|----------|
| **配置层** | `.github`, `.husky`, `设计令牌` | 项目配置与构建工具 | 永久 |
| **文档层** | `docs/` | 开发文档、设计规范、测试文档 | 持续更新 |
| **测试层** | `tests/`, `e2e/` | 单元测试、集成测试、端到端测试 | 持续更新 |
| **源代码** | `src/` | 核心应用逻辑 | 持续更新 |
| **工具层** | `scripts/`, `packages/` | 开发工具、独立模块 | 按需更新 |
| **扩展层** | `plugins/`, `prompts/` | 插件、AI 提示词 | 按需扩展 |
| **归档层** | `archive/` | 过时资源、历史备份 | 定期清理 |

### 2.2 `docs/` 目录细分

| 子目录 | 编号 | 内容 | 说明 |
|--------|------|------|------|
| `00-meta/` | 00 | 文档治理、项目总览、目录规范 | 文档系统的元数据 |
| `01-requirements/` | 01 | 需求文档、数据宪法、功能清单 | 产品需求定义 |
| `02-design/` | 02 | 架构设计、API 契约、技术规范 | 系统设计文档 |
| `03-development/` | 03 | 开发指南、代码规范、模板 | 开发参考文档 |
| `04-testing/` | 04 | 测试用例、测试计划、测试报告 | 测试相关文档 |
| `05-deployment/` | 05 | 部署方案、运维手册、发布计划 | 部署与运维文档 |
| `06-project-management/` | 06 | 项目管理、进度跟踪、风险评估 | 项目管理文档 |

### 2.3 `src/` 目录细分

| 子目录 | 职责 | 说明 |
|--------|------|------|
| `agents/` | AI Agent 管理 | Agent 注册、运行时、健康监控 |
| `apps/` | 应用模块 | 分析、命令、输入、输出、交易 |
| `cockpit/` | 驾驶舱 | Widget 引擎、Widget 注册表 |
| `components/` | UI 组件 | 原子组件、分子组件、有机体组件 |
| `config/` | 应用配置 | API 路径、数据源、阈值配置 |
| `constants/` | 常量定义 | 业务常量、颜色令牌、UI 文本 |
| `core/` | 核心框架 | DataBridge、ACL、路由守卫 |
| `data/` | 数据访问 | IndexedDB、Schema、Repository |
| `hooks/` | 自定义 Hook | 数据采集、防抖、页面守卫 |
| `i18n/` | 国际化 | 多语言支持 |
| `lib/` | 工具库 | 日志、验证、加密、性能监控 |
| `mcp/` | MCP 协议 | MCP Server、桥接、ACL |
| `pages/` | 页面组件 | 路由页面定义 |
| `services/` | 业务服务 | 回测、LLM、新闻、交易 |
| `store/` | 状态管理 | Zustand store 定义 |
| `types/` | 类型定义 | TypeScript 类型声明 |

---

## 三、命名规范

### 3.1 文件命名规则

| 文件类型 | 命名格式 | 示例 |
|----------|----------|------|
| 组件文件 | PascalCase | `Button.tsx`, `ScoreCard.tsx` |
| 工具函数 | camelCase | `utils.ts`, `format.ts` |
| 类型定义 | snake_case | `backtest.types.ts`, `trade.types.ts` |
| 配置文件 | snake_case | `api_paths.ts`, `llm_config.ts` |
| 测试文件 | `.test.ts/.test.tsx` | `utils.test.ts`, `Button.test.tsx` |
| 文档文件 | PascalCase / 中文 | `ARCHITECTURE.md`, `数据宪法.md` |
| 脚本文件 | snake_case | `audit-layer-calls.ts`, `generate-report.ts` |

### 3.2 目录命名规则

| 层级 | 命名格式 | 示例 |
|------|----------|------|
| 一级目录 | kebab-case | `data-collector`, `market-data` |
| 子目录 | kebab-case | `widget-engine`, `store-helpers` |
| 文档子目录 | 数字前缀 + 名称 | `00-meta`, `02-design` |

### 3.3 临时文件规则

- **禁止**在项目根目录或 `src/` 目录下创建临时文件
- 临时文件应放在 `.workbuddy/tmp/` 或 `scripts/` 目录下
- 临时文件命名以 `_` 前缀标识（如 `_debug_hash.py`）
- 临时文件在任务完成后应立即删除

---

## 四、版本控制策略

### 4.1 分支管理

| 分支类型 | 命名格式 | 用途 |
|----------|----------|------|
| 主分支 | `main` | 稳定版本，生产环境部署 |
| 开发分支 | `develop` | 日常开发，集成测试 |
| 功能分支 | `feature/xxx` | 新功能开发 |
| 修复分支 | `fix/xxx` | Bug 修复 |
| 发布分支 | `release/xxx` | 版本发布准备 |

### 4.2 提交规范

采用 Conventional Commits 规范：

```
<type>(<scope>): <description>

<body>

<footer>
```

**类型说明**：

| 类型 | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `refactor` | 重构（不改变功能） |
| `docs` | 文档更新 |
| `test` | 测试用例 |
| `style` | 代码风格 |
| `chore` | 构建/工具更新 |
| `perf` | 性能优化 |

### 4.3 文档版本管理

- 文档更新需同步更新文件头部的 `Last Updated` 日期
- 重大变更需更新版本号（如 v1.0.0 → v1.1.0）
- 历史版本归档到 `archive/docs/` 目录

---

## 五、文件归档流程

### 5.1 归档条件

以下情况的文件应归档到 `archive/` 目录：

1. **过时资源**：不再使用的组件、脚本、配置
2. **历史备份**：旧版本文档、废弃的设计方案
3. **临时产物**：架构雷达报告、审计报告输出
4. **重复文件**：已合并去重的冗余副本

### 5.2 归档目录结构

```
archive/
├── docs/                        # 文档归档
│   ├── architecture-radar/      # 架构雷达历史报告
│   ├── reports/                 # 审计报告历史
│   ├── playground/              # 实验性文档
│   └── 07-archive/              # 原归档目录迁移
├── scripts/                     # 脚本归档
│   └── doc-pipeline.ts          # 废弃的文档脚本
└── unused-components/           # 未使用组件归档
    └── wizard-steps/            # 向导步骤组件
```

### 5.3 归档操作流程

1. **评估**：确认文件不再被项目引用
2. **记录**：在 `docs/00-meta/REGISTRY_INDEX.md` 中记录归档信息
3. **迁移**：移动文件到 `archive/` 对应子目录
4. **验证**：运行 `npm run audit:doc-integrity` 确认无引用断裂

---

## 六、文件清理规则

### 6.1 定期清理

| 周期 | 清理内容 | 负责角色 |
|------|----------|----------|
| 每日 | `.workbuddy/tmp/` 临时文件 | 开发人员 |
| 每周 | 脚本目录下 `_` 前缀临时文件 | 开发人员 |
| 每月 | `docs/reports/` 审计报告 | 架构资产治理官 |
| 每季度 | `archive/` 中超过 6 个月的归档 | 架构资产治理官 |

### 6.2 禁止保留的文件

- `.bak`, `.bak2` 等备份文件
- `TEMP*` 临时文件
- 编辑器自动生成的 `.DS_Store`, `Thumbs.db`
- 构建产物目录（`.next`, `dist` 等，应在 `.gitignore` 中排除）

---

## 七、文档治理原则

### 7.1 单一事实源

- 每个数据定义、API 契约、业务规则应有**唯一的权威文档**
- 禁止在多个目录下维护相同内容的文档
- 文档之间通过链接引用，避免内容重复

### 7.2 文档同步

- 代码变更时应同步更新相关文档
- 定期运行 `npm run audit:doc-sync` 检查文档与代码一致性
- 文档更新需通过 Pull Request 审查

### 7.3 文档分级

| 级别 | 标识 | 要求 |
|------|------|------|
| **核心文档** | `[CORE]` | 必须与代码同步，定期审查 |
| **参考文档** | `[REF]` | 保持更新，按需审查 |
| **历史文档** | `[ARCHIVE]` | 已归档，不再更新 |

---

## 八、团队协作规范

### 8.1 职责分离

| 层级 | 职责 | 负责人 |
|------|------|--------|
| **UI 层** | 组件开发、样式维护 | 前端开发 |
| **业务逻辑层** | 服务实现、状态管理 | 全栈开发 |
| **数据访问层** | Schema 定义、Repository | 后端开发 |
| **文档层** | 文档编写、同步更新 | 全员负责 |

### 8.2 代码审查流程

1. 开发完成后创建 Pull Request
2. 至少 1 位团队成员审查
3. 通过 `npm run lint`, `npm test`, `npm run audit:layers` 检查
4. 审查通过后合并到 `develop` 分支

### 8.3 资源支持

- **组件库**：`src/components/` 提供统一的 UI 组件
- **工具函数**：`src/lib/` 提供通用工具函数
- **开发环境**：参考 `docs/03-development/getting-started.md`
- **文档系统**：`docs/` 目录提供完整的技术文档

---

## 九、违规处理

### 9.1 违规类型

| 违规 | 严重程度 | 处理方式 |
|------|----------|----------|
| 跨层调用 | 严重 | 立即修复 |
| 硬编码 | 中等 | 限期修复 |
| 文档缺失 | 中等 | 补充文档 |
| 文件杂乱 | 轻微 | 定期清理 |

### 9.2 审计工具

| 工具 | 命令 | 用途 |
|------|------|------|
| 分层调用审计 | `npm run audit:layers` | 检查跨层调用违规 |
| 硬编码审计 | `npm run audit:hardcode` | 检查硬编码常量 |
| 死代码审计 | `npm run audit:deadcode` | 检查未使用代码 |
| 文档同步审计 | `npm run audit:doc-sync` | 检查文档与代码一致性 |

---

## 十、附录

### A. 目录大小控制目标

| 目录 | 当前大小 | 目标大小 | 优化策略 |
|------|----------|----------|----------|
| `docs/` | ~67MB | ~15MB | 归档报告、去重、清理字体 |
| `src/` | ~25MB | ~25MB | 保持稳定 |
| `scripts/` | ~5MB | ~3MB | 清理临时脚本 |
| `archive/` | ~10MB | ~50MB | 长期归档（不限制） |

### B. 文档数量控制目标

| 目录 | 当前数量 | 目标数量 |
|------|----------|----------|
| `docs/` | ~1090 | ~500 |
| `scripts/` | ~80 | ~50 |

### C. 常用命令

```bash
# 类型检查
npm run typecheck

# 运行测试
npm test

# 运行所有审计
npm run audit:all

# 构建项目
npm run build

# 开发模式
npm run dev
```

---

> **文档状态**: 生效中  
> **生效日期**: 2026-07-13  
> **审批**: 架构资产治理