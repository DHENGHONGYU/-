---
title: ??????????????????Widget???WidgetShell + ???????????????
type: how-to
domain: frontend
phase: development
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?????????v1.0.0 ?????????026-07-12 ???????????? 20 ?????????????????????Widget ?????????????????????ErrorBoundary..."
tags: [frontend, widget, guide]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-FRONT-026
referenced_by: [V9-DOC-META-000, V9-DOC-PROJ-331, V9-DOC-PROJ-122, V9-DOC-PROJ-176, V9-DOC-FRONT-034, V9-DOC-PROJ-157, V9-DOC-PROJ-121, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---w-to
domain: frontend
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "??????????????????Widget???WidgetShell + ???????????????type: how-to document"
tags: [frontend, widget, guide]
phase: development
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
---

# ??????????????????Widget???WidgetShell + ???????????????
> **??????**???v1.0.0  
> **??????**???026-07-12  
> **??????**?????? 20 ?????????????????????Widget ?????????????????????ErrorBoundary ??????????????????????????????????????????????????????
---

## ????????????
- [ ] ?????????`../tutorials/getting-started.md`??????????????????????????????
- [ ] ?????????`../reference/widget-integration-checklist.md`???Widget ?????????????????????
- [ ] ?????????`../reference/atomic-component-system.md`????????????????????????
- [ ] ?????????Widget ??????????????????Store ???Service???- [ ] ?????????Widget ???????????????????????????`/cockpit` ??????????????????

---

## ?????? 1?????????Widget ??????????????????3 ?????????
???`src/types/widget.ts`???????????? `src/types/widget.ts`???????????? Widget ?????????
```typescript
// ???????????????SectorHeatmapWidget ?????????export interface SectorHeatmapWidgetConfig extends WidgetConfig {
  settings?: {
    title?: string
    showTrend?: boolean
    colorScheme?: 'red-green' | 'blue-purple'
  }
}
```

> **??????**?????????Widget ???????????? `WidgetConfig`?????????`src/types/widget.ts`??????
> ```typescript
> export interface WidgetConfig {
>   id: string
>   widgetId: string
>   position: { x: number; y: number }
>   size: { cols: number; rows: number }
>   settings?: Record<string, unknown>
> }
> ```

---

## ?????? 2?????????Widget ????????? ?????????
### 2.1 ????????????

```
src/components/widgets/
????????? WidgetShell.tsx          # ???????????????????????????????????? SectorHeatmapWidget/
???  ????????? index.tsx            # ?????????????????? Widget ????????????  ????????? SectorHeatmapView.tsx # ??????????????????????????????
???  ????????? useSectorHeatmap.ts   # ?????????Hook?????????????????????  ????????? types.ts             # ????????????????????????
```

### 2.2 ????????????

```tsx
/**
 * @module SectorHeatmapWidget
 * @description ???????????????Widget????????????????????????????????????????????????????????? *
 * @see @/components/widgets/WidgetShell.tsx - ??????????????? * @see @/store/sectorAnalysisStore.ts - ????????????
 */

import React, { useEffect, useMemo } from 'react'
import { WidgetShell } from '../WidgetShell'
import { useSectorAnalysisStore } from '@/store/sectorAnalysisStore'
import { THEME_TOKENS } from '@/constants/theme.tokens'
import { getStockColorClass } from '@/constants/theme.tokens'
import type { SectorHeatmapWidgetConfig } from '@/types/widget'

// ============================================================
// ?????????????????????????????????????????????// ============================================================

interface SectorHeatmapViewProps {
  sectors: Array<{
    sectorId: string
    sectorName: string
    score: number
    trend: 'up' | 'down' | 'flat'
  }>
  loading: boolean
  error: string | null
  onRetry: () => void
}

function SectorHeatmapView({ sectors, loading, error, onRetry }: SectorHeatmapViewProps) {
  if (loading) {
    return <div className={THEME_TOKENS.color.muted}>?????????..</div>
  }
  if (error) {
    return (
      <div className={THEME_TOKENS.color.destructive}>
        <p>????????????: {error}</p>
        <button onClick={onRetry} className={THEME_TOKENS.controlSizes.md}>??????</button>
      </div>
    )
  }
  if (sectors.length === 0) {
    return <div className={THEME_TOKENS.color.muted}>??????????????????</div>
  }

  return (
    <div className="grid grid-cols-4 gap-2">
      {sectors.map((sector) => (
        <div
          key={sector.sectorId}
          className={`p-3 rounded-lg ${getStockColorClass(sector.score > 3 ? 1 : -1)}`}
          title={`${sector.sectorName}: ${sector.score.toFixed(1)}`}
        >
          <div className="text-sm font-medium">{sector.sectorName}</div>
          <div className="text-xs opacity-75">{sector.score.toFixed(1)}</div>
          {sector.trend === 'up' && <span>???/span>}
          {sector.trend === 'down' && <span>???/span>}
          {sector.trend === 'flat' && <span>???/span>}
        </div>
      ))}
    </div>
  )
}

// ============================================================
// ?????????Hook????????????????????????// ============================================================

function useSectorHeatmapData() {
  const { sectors, loading, error, loadSectors, clearError } = useSectorAnalysisStore()

  useEffect(() => {
    loadSectors()
  }, [loadSectors])

  return {
    sectors,
    loading,
    error,
    onRetry: () => {
      clearError()
      loadSectors()
    },
  }
}

// ============================================================
// Widget ????????????WidgetShell ?????????// ============================================================

export interface SectorHeatmapWidgetProps {
  widgetId: string
  config: SectorHeatmapWidgetConfig
}

export function SectorHeatmapWidget({ widgetId, config }: SectorHeatmapWidgetProps) {
  const { sectors, loading, error, onRetry } = useSectorHeatmapData()

  // ??????????????????  const visualState = useMemo(() => {
    if (loading) return 'loading' as const
    if (error) return 'error' as const
    if (sectors.length === 0) return 'empty' as const
    return 'ready' as const
  }, [loading, error, sectors.length])

  return (
    <WidgetShell
      widgetId={widgetId}
      config={config}
      state={visualState}
      stateConfig={{
        loadingLabel: '??????????????????...',
        emptyTitle: '??????????????????',
        emptyDescription: '?????????????????????????????????,
        errorTitle: '????????????',
        errorDescription: error ?? '????????????',
        onRetry,
      }}
    >
      <SectorHeatmapView
        sectors={sectors}
        loading={loading}
        error={error}
        onRetry={onRetry}
      />
    </WidgetShell>
  )
}

export default SectorHeatmapWidget
```

### 2.3 ????????????????????????
| ?????? | ???????????? | ?????? | ???????????? |
|------|----------|------|----------|
| `WidgetShell` ?????? | ????????? | ?????? ErrorBoundary??????????????????????????? | ?????? WidgetShell?????????????????????????????? |
| ???????????????| ????????? | ???????????????????????????????????? | ???????????????????????????????????? |
| ?????????Hook | ???? ?????? | ?????????????????????`useXxxData` | ?????????????????????????????? Store |
| `visualState` | ????????? | `ready`/`loading`/`empty`/`error` | ???????????? WidgetShell ???state ??????|
| `useEffect` ?????? | ????????? | ??????/???????????? cleanup ?????????| ???????????? |
| ???????????? | ????????? | ?????? `THEME_TOKENS` / `COLOR_TOKENS` | ?????????HEX ???Tailwind ???|

---

## ?????? 3?????????Widget??? ?????????
### 3.1 ???Widget ??????????????????

???????????????????????? Widget ???????????????????????????`src/cockpit/core/widgetRegistry.ts`??????

```typescript
import { SectorHeatmapWidget } from '@/components/widgets/SectorHeatmapWidget'

export const WIDGET_REGISTRY = {
  // ... ?????? Widget ...
  sectorHeatmap: {
    component: SectorHeatmapWidget,
    title: '???????????????,
    defaultSize: { cols: 4, rows: 3 },
    category: 'analysis',
  },
} as const
```

### 3.2 ???????????????????????????????????????????????????
```typescript
// src/cockpit/defaultLayout.ts
export const DEFAULT_COCKPIT_LAYOUT = [
  // ... ?????? Widget ...
  {
    widgetId: 'sectorHeatmap-1',
    type: 'sectorHeatmap',
    position: { x: 0, y: 6 },
    size: { cols: 4, rows: 3 },
    settings: { title: '???????????????, showTrend: true },
  },
]
```

---

## ?????? 4????????????5 ?????????
### 4.1 ????????????
```bash
npx tsc --noEmit
```

### 4.2 ????????????

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { SectorHeatmapView } from './SectorHeatmapView'

describe('SectorHeatmapView', () => {
  it('renders sectors', () => {
    const sectors = [
      { sectorId: '1', sectorName: '?????????, score: 4.5, trend: 'up' as const },
      { sectorId: '2', sectorName: '?????????, score: 3.2, trend: 'flat' as const },
    ]
    render(<SectorHeatmapView sectors={sectors} loading={false} error={null} onRetry={() => {}} />)
    expect(screen.getByText('?????????)).toBeInTheDocument()
    expect(screen.getByText('4.5')).toBeInTheDocument()
  })

  it('shows loading state', () => {
    render(<SectorHeatmapView sectors={[]} loading={true} error={null} onRetry={() => {}} />)
    expect(screen.getByText('?????????..')).toBeInTheDocument()
  })

  it('shows error state', () => {
    render(<SectorHeatmapView sectors={[]} loading={false} error="Failed" onRetry={() => {}} />)
    expect(screen.getByText('????????????: Failed')).toBeInTheDocument()
  })
})
```

### 4.3 ????????????

```bash
npm run audit:layers
npm run audit:hardcode
```

### 4.4 ????????????

1. ????????????????????????`npm run dev`
2. ?????????????????? `/cockpit`
3. ?????? Widget??????????????????????????????4. ??????????????????????????????????????????????????????????????????????????????
---

## ?????????????????????
### ???1???Widget ???????????? dataLayer??????????????????

```tsx
// ????????????Widget ???????????? dataLayer???Widget ???????????? Store ???Service???import { dataLayer } from '@/data/dataLayer'
const data = await dataLayer.sectors.list()

// ?????????????????? Store ????????????
const { sectors } = useSectorAnalysisStore()
```

### ???2?????????WidgetShell ???ErrorBoundary

```tsx
// ??????????????? WidgetShell ?????????????????????????????????????????????export function MyWidget() {
  return <div>{data.map(...)}</div> // ?????? data ??????????????????????????????
}

// ????????????WidgetShell ?????? ErrorBoundary?????????????????????<WidgetShell widgetId={id} config={config}>
  <MyWidgetContent />
</WidgetShell>
```

### ???3??????????????????

```tsx
// ????????????????????????????????????????????????Widget ??????
widgetEventBus.publish('update', data)

// ??????????????????widget:{widgetId}:{event} ????????????
widgetEventBus.publish(`widget:${widgetId}:update`, data)
// ?????????WidgetShell ??????????????????
```

### ???4???useEffect ?????????Store ??????

```tsx
// ????????????Store ???????????????????????????????????????????????????useEffect(() => {
  useSectorAnalysisStore.subscribe((state) => {
    setLocalData(state.sectors)
  })
}, [])

// ????????????subscribe ?????? unsubscribe?????? cleanup ?????????useEffect(() => {
  const unsubscribe = useSectorAnalysisStore.subscribe((state) => {
    setLocalData(state.sectors)
  })
  return unsubscribe
}, [])

// ?????????????????????????????? useSectorAnalysisStore()???Zustand ??????????????????
const { sectors } = useSectorAnalysisStore()
```

### ???5???Widget ??????????????????Store

```tsx
// ????????????Widget ???????????? useState ???????????????????????????Widget ?????????????????????const [data, setData] = useState([])

// ???????????????????????????Store ????????????Widget ???????????????const { data } = useMyStore()
```

> **??????**???Widget ???????????????????????????????????????Store?????????UI ?????????????????????????????????????????????Widget ?????????
---

## ?????? 5???????????????????????????

### ??????????????????

```tsx
import { useMemo, useCallback } from 'react'

function useOptimizedData() {
  const { sectors } = useSectorAnalysisStore()

  // ?????? useMemo ??????????????????
  const sortedSectors = useMemo(() => {
    return [...sectors].sort((a, b) => b.score - a.score)
  }, [sectors])

  // ?????? useCallback ??????????????????
  const handleSelect = useCallback((id: string) => {
    // ...
  }, [])

  return { sortedSectors, handleSelect }
}
```

### ??????????????????????????????

```tsx
import { FixedSizeGrid } from 'react-window'

// ?????? react-window ???@tanstack/react-virtual ??????????????????
```

---

## ????????????

| ?????? | ?????? | ?????? |
|------|------|------|
| ?????? Store | `./how-to-add-store.md` | Widget ???????????????|
| ?????? Service | `./how-to-add-service.md` | Store ???????????????|
| Widget ??????????????????| `../reference/widget-integration-checklist.md` | ???????????? |
| Widget ???????????? | `../reference/widget-error-handling.md` | ?????????????????????|
| ?????????????????? | `../reference/atomic-component-system.md` | ???????????? |
| WidgetShell ?????? | `src/components/widgets/WidgetShell.tsx` | ???????????? |

---

> **???????????????*?????????`docs/README.md` ???C ?????????????????????Widget ????????????????????