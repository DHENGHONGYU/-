---
title: ??????????????????Service???DataBridge + Envelope ?????????
type: how-to
domain: backend
phase: development
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?????????v1.0.0 ?????????026-07-12 ???????????? 15 ?????????????????????Service ?????????????????????ACL ?????????Envelope ?????????????????????????????????"
tags: [backend, databridge, data]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-BACK-027
referenced_by: [V9-DOC-META-000, V9-DOC-BACK-026, V9-DOC-PROJ-331, V9-DOC-PROJ-122, V9-DOC-PROJ-176, V9-DOC-PROJ-157, V9-DOC-PROJ-121, V9-DOC-BACK-033, V9-DOC-BACK-045, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# ??????????????????Service???DataBridge + Envelope ?????????
> **??????**???v1.0.0  
> **??????**???026-07-12  
> **??????**?????? 15 ?????????????????????Service ?????????????????????ACL ?????????Envelope ?????????????????????????????????
---

## ????????????
- [ ] ?????????`../tutorials/getting-started.md`??????????????????????????????
- [ ] ?????????Service ?????????????????????`src/types/` ???`src/data/types.ts` ????????????
- [ ] ?????????????????? IndexedDB store?????? `src/config/dbConfig.ts` ???`STORE_NAME` ????????????
- [ ] ?????????ACL ???????????? `src/config/dbConfig.ts` ???`ACL_MATRIX` ???????????????????????????- [ ] ?????????ENVELOPE_ACTION?????? `src/core/envelope.ts` ???????????????????????????
---

## ?????? 1????????????????????????3 ?????????
???`src/data/types.ts` ?????????Service ??????/????????????????????????
```typescript
// ???????????????sectorAnalysisService ?????????export interface SectorAnalysisInput {
  sectorId: string
  dateRange?: { start: string; end: string }
}

export interface SectorAnalysisResult {
  sectorId: string
  sectorName: string
  score: number
  trend: 'up' | 'down' | 'flat'
  updatedAt: number
}
```

> **??????**???Service ??????????????????????????? `DataLayerResult<T>`???> ```typescript
> interface DataLayerResult<T> {
>   success: boolean
>   data?: T
>   error?: string
> }
> ```

---

## ?????? 2?????????Service??? ?????????
???`src/services/` ?????????????????????????????????????????????`{domain}/{action}Service.ts`???
### 2.1 ????????????

```typescript
/**
 * @module sectorAnalysisService
 * @description ????????????????????????????????????????????????????????????????????? *
 * @see @/store/sectorAnalysisStore.ts - ????????? * @see @/data/dataLayer.ts - ??????????????????
 */

import { dataBridge } from '@/core/databridge'
import { EnvelopeFactory } from '@/core/envelope'
import {
  MODULE_ID,
  ENVELOPE_TARGET,
  ENVELOPE_ACTION,
} from '@/config/dbConfig'
import { dataLayer } from '@/data/dataLayer'
import type { DataLayerResult, SectorAnalysisResult } from '@/data/types'
import { getLogger } from '@/lib/logger'
import { nanoid } from 'nanoid'

const logger = getLogger()

// ============================================================
// ?????????????????????????????????????????????dataLayer???// ============================================================

/**
 * ??????????????????????????????
 */
export async function loadSectorAnalysis(): Promise<DataLayerResult<SectorAnalysisResult[]>> {
  logger.info('[sectorAnalysisService] loadSectorAnalysis ??????)
  try {
    const list = await dataLayer.sectorScores.list()
    logger.info(`[sectorAnalysisService] loadSectorAnalysis ??????: ${list.length} ???`)
    return { success: true, data: list }
  } catch (err) {
    const message = err instanceof Error ? err.message : '????????????????????????'
    logger.error(`[sectorAnalysisService] loadSectorAnalysis ??????: ${message}`)
    return { success: false, error: message }
  }
}

/**
 * ????????????????????????
 */
export async function loadSectorTrend(
  sectorId: string,
): Promise<DataLayerResult<{ date: string; score: number }[]>> {
  logger.info(`[sectorAnalysisService] loadSectorTrend ?????? ${sectorId}`)
  try {
    const result = await dataLayer.sectorScores.getTrend(sectorId)
    return { success: true, data: result }
  } catch (err) {
    const message = err instanceof Error ? err.message : '????????????????????????'
    logger.error(`[sectorAnalysisService] loadSectorTrend ??????: ${message}`)
    return { success: false, error: message }
  }
}

// ============================================================
// ????????????????????? DataBridge ???????????????ACL ??????????????????
// ============================================================

/**
 * ????????????????????????
 *
 * ?????? DataBridge.forward() ???????????????ACL ???????????????????????? */
export async function saveSectorAnalysis(
  data: SectorAnalysisResult,
): Promise<DataLayerResult<void>> {
  logger.info(`[sectorAnalysisService] saveSectorAnalysis ?????? ${data.sectorId}`)

  const envelope = EnvelopeFactory.create(
    {
      source: MODULE_ID.analysis,      // ????????????
      target: ENVELOPE_TARGET.db,      // ??????????????????
      action: ENVELOPE_ACTION.insertSectorScore, // ????????????
      traceId: `sector-${nanoid(8)}-${data.sectorId}`, // ?????? ID
    },
    data,
  )

  try {
    await dataBridge.forward(envelope)
    logger.info(`[sectorAnalysisService] saveSectorAnalysis ??????: ${data.sectorId}`)
    return { success: true }
  } catch (err) {
    const message = err instanceof Error ? err.message : '????????????????????????'
    logger.error(`[sectorAnalysisService] saveSectorAnalysis ??????: ${message}`)
    return { success: false, error: message }
  }
}

/**
 * ??????????????????????????????
 */
export async function batchSaveSectorAnalysis(
  items: SectorAnalysisResult[],
): Promise<DataLayerResult<{ saved: number; failed: number }>> {
  logger.info(`[sectorAnalysisService] batchSaveSectorAnalysis ?????? ${items.length} ???`)
  let saved = 0
  let failed = 0

  for (const item of items) {
    const result = await saveSectorAnalysis(item)
    if (result.success) {
      saved++
    } else {
      failed++
      logger.error(`[sectorAnalysisService] ????????????: ${item.sectorId}, ${result.error}`)
    }
  }

  logger.info(`[sectorAnalysisService] batchSaveSectorAnalysis ??????: saved=${saved}, failed=${failed}`)
  return { success: true, data: { saved, failed } }
}
```

### 2.2 ????????????????????????
| ?????? | ???????????? | ?????? | ???????????? |
|------|----------|------|----------|
| `DataLayerResult<T>` | ????????? | ?????????????????? | ???????????????????????????????????????|
| `DataBridge.forward()` | ????????????????????? | ?????????????????????ACL | ???????????? `dataLayer.*.insert()`?????????ACL |
| `EnvelopeFactory.create()` | ????????????????????? | ?????? source/target/action/traceId | ?????? traceId???????????????|
| `MODULE_ID` | ????????? | ?????????????????? | ???????????????????????? |
| `ENVELOPE_ACTION` | ????????? | ???????????? | ???????????????????????? |
| `logger.info/error` | ????????? | ?????????????????? | ?????????????????????????????? |
| `try/catch` | ????????? | ??????async ???????????? | ???????????????????????????????????????|
| `nanoid` traceId | ???? ?????? | ?????????????????? | ???traceId???????????????????????????|

---

## ?????? 3?????????Envelope Action??? ?????????
?????????????????????`ENVELOPE_ACTION`???????????? `src/config/dbConfig.ts` ????????????

```typescript
export enum ENVELOPE_ACTION {
  // ... ???????????? ...
  insertSectorScore = 'insertSectorScore',
}
```

?????? `src/core/databridge.ts`?????? `DataBridge.routeToDB()`?????????????????? case???
```typescript
case ENVELOPE_ACTION.insertSectorScore:
  return await dataLayer.sectorScores.insert(payload)
```

---

## ?????? 4?????????ACL ????????? ?????????
???`src/config/dbConfig.ts` ???`ACL_MATRIX` ???????????? store ????????????????????????

```typescript
export const ACL_MATRIX = {
  // ... ?????? store ...
  sectorScores: {
    read: [MODULE_ID.analysis, MODULE_ID.tradinghub, MODULE_ID.scoring],
    write: [MODULE_ID.analysis, MODULE_ID.scoring],
  },
} as const
```

> **??????**???read/write ?????????????????????????????? `MODULE_ID`???
---

## ?????? 5????????????3 ?????????
### 5.1 ????????????
```bash
npx tsc --noEmit
```

### 5.2 ??????????????????????????????

```typescript
import { describe, it, expect, vi } from 'vitest'
import { loadSectorAnalysis, saveSectorAnalysis } from './sectorAnalysisService'
import { dataLayer } from '@/data/dataLayer'

vi.mock('@/data/dataLayer', () => ({
  dataLayer: {
    sectorScores: {
      list: vi.fn(),
      insert: vi.fn(),
    },
  },
}))

describe('sectorAnalysisService', () => {
  it('loadSectorAnalysis should return data', async () => {
    vi.mocked(dataLayer.sectorScores.list).mockResolvedValue([
      { sectorId: '1', sectorName: '?????????, score: 4.5, trend: 'up', updatedAt: Date.now() },
    ])
    const result = await loadSectorAnalysis()
    expect(result.success).toBe(true)
    expect(result.data).toHaveLength(1)
  })

  it('saveSectorAnalysis should use DataBridge', async () => {
    // ?????? Envelope ????????????..
  })
})
```

### 5.3 ????????????

```bash
npm run audit:layers
# ?????????sectorAnalysisService.ts ?????????????????????```

---

## ?????????????????????
### ???1???Service ???????????? dataLayer?????????ACL???
```typescript
// ????????????????????????????????? ACL ???????????????await dataLayer.orders.insert(order)

// ?????????????????? DataBridge ??????
const envelope = EnvelopeFactory.create({ source, target, action, traceId }, order)
await dataBridge.forward(envelope)
```

### ???2?????????traceId

```typescript
// ??????????????? traceId???????????????????????????EnvelopeFactory.create({ source, target, action }, data)

// ??????????????????traceId
EnvelopeFactory.create(
  { source, target, action, traceId: `sector-${nanoid(8)}-${data.sectorId}` },
  data
)
```

### ???3?????????????????????DataBridge??????????????????
```typescript
// ??????????????????????????? DataBridge????????????????????????????????????
const envelope = EnvelopeFactory.create({ source, target, action: 'query' }, params)
const result = await dataBridge.forward(envelope)

// ????????????????????????????????????dataLayer?????????????????? ACL???const result = await dataLayer.sectorScores.list()
```

> **??????**???????????????????????? `dataLayer`???????????????????????? `DataBridge.forward()`???
### ???4???Service ?????? Store????????????????????????

```typescript
// ????????????Service ?????? Store???Store ?????????pages/components ?????????import { useAnalysisStore } from '@/store/analysisStore'
const { scores } = useAnalysisStore.getState() // ?????????
// ????????????Service ???????????????????????????????????????Store
export async function computeScore(input: ScoreInput): Promise<ScoreResult> {
  // ???????????????Store ??????
}
```

### ???5?????????ACL_MATRIX ???????????????
```typescript
// ?????? ACL_MATRIX ?????????sectorScores ?????? write ?????????// DataBridge.forward() ?????????ACL ???????????????// ???????????????store ???????????????ACL_MATRIX???```

---

## ?????? 6?????? Store ?????????????????????

Service ?????????????????????Store ?????????
```typescript
import { loadSectorAnalysis } from '@/services/analysis/sectorAnalysisService'

export const useSectorAnalysisStore = create<SectorAnalysisState>((set, get) => ({
  // ...
  loadSectors: async () => {
    const result = await loadSectorAnalysis()
    if (result.success && result.data) {
      set({ sectors: result.data })
    }
  },
}))
```

---

## ????????????

| ?????? | ?????? | ?????? |
|------|------|------|
| ?????? Store | `./how-to-add-store.md` | Service ?????????????????? |
| ?????? Widget | `./how-to-add-widget.md` | Service ?????????????????? |
| DataBridge ?????? | `../../AGENTS.md` ?????| L4 ?????????????????????|
| ACL ?????? | `src/config/dbConfig.ts` | ???????????? |
| Envelope ?????? | `src/core/envelope.ts` | ???????????? |

---

> **???????????????*?????????`docs/README.md` ???C ?????????????????????Service ????????????????????