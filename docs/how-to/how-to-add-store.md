---
title: ??????????????????Store???Zustand + withBroadcast???
type: how-to
domain: data
phase: development
tier: standard
status: active
maintainer: V9 Architecture Team
summary: "?????????v1.0.0 ?????????026-07-12 ???????????? 15 ?????????????????????Store ???????????????????????????????????????????????????????????????????????????"
tags: [data, store, guide]
version: v1.0.0
last_updated: 2026-07-17
code_version: 2.0.0
doc_id: V9-DOC-DATA-055
referenced_by: [V9-DOC-META-000, V9-DOC-BACK-026, V9-DOC-PROJ-331, V9-DOC-PROJ-122, V9-DOC-PROJ-176, V9-DOC-DATA-061, V9-DOC-PROJ-157, V9-DOC-DATA-073, V9-DOC-PROJ-121, V9-DOC-PROJ-149]
change_log:
  - version: v1.0.0
changes: Initial version established
date: 2026-07-17
---

# ??????????????????Store???Zustand + withBroadcast???
> **??????**???v1.0.0  
> **??????**???026-07-12  
> **??????**?????? 15 ?????????????????????Store ???????????????????????????????????????????????????????????????????????????
---

## ????????????
???????????????????????????????????????
- [ ] ?????????`../tutorials/getting-started.md`??????????????????????????????
- [ ] ?????????Store ?????????????????????`src/types/` ???`src/data/types.ts` ?????????Interface???- [ ] ?????????????????? Service ????????????Store ???????????? `services/` ???`core/`???- [ ] ?????????`src/constants/store-channels.constants.ts` ????????????????????????????????????????????????
---

## ?????? 1??????????????????3 ?????????
???`src/types/modules/` ???`src/data/types.ts` ?????????Store ??????????????????
```typescript
// ???????????????sectorAnalysisStore ?????????export interface SectorAnalysisData {
  sectorId: string
  sectorName: string
  score: number
  trend: 'up' | 'down' | 'flat'
  updatedAt: number
}

export interface SectorAnalysisTrendPoint {
  date: string
  score: number
}
```

> **??????**???????????????????????? `Expect<Equals>` ?????????????????????`tests/__tests__/types/`??????
---

## ?????? 2?????????Store??? ?????????
???`src/store/` ?????????????????????????????????`{domain}Store.ts`???camelCase + `Store` ????????????
### 2.1 ????????????

```typescript
/**
 * @module sectorAnalysisStore
 * @description ????????????????????????????????????????????????????????????????????? *
 * @see @/services/analysis/sectorAnalysisService.ts - ??????????????????
 * @see @/pages/analysis/SectorAnalysisPage.tsx - ????????? */

import { create } from 'zustand'
import { getLogger } from '@/lib/logger'
import type { SectorAnalysisData, SectorAnalysisTrendPoint } from '@/data/types'
import { loadSectorAnalysis, loadSectorTrend } from '@/services/analysis/sectorAnalysisService'
import { EVENT_NAMES } from '@/constants/store-channels.constants'
import { withBroadcast } from '@/lib/withBroadcast'

const logger = getLogger()

// ============================================================
// Store ??????
// ============================================================

interface SectorAnalysisState {
  /** ???????????? */
  sectors: SectorAnalysisData[]
  /** ???????????? */
  trendData: SectorAnalysisTrendPoint[]
  /** ????????????*/
  loading: boolean
  /** ???????????? */
  error: string | null

  // Actions
  /** ?????????????????? */
  loadSectors: () => Promise<void>
  /** ???????????????????????? */
  loadTrend: (sectorId: string) => Promise<void>
  /** ???????????? */
  clearError: () => void
}

// ============================================================
// ????????????// ============================================================

const initialState = {
  sectors: [] as SectorAnalysisData[],
  trendData: [] as SectorAnalysisTrendPoint[],
  loading: false,
  error: null as string | null,
}

// ============================================================
// Store
// ============================================================

export const useSectorAnalysisStore = create<SectorAnalysisState>((set, get) => ({
  ...initialState,

  loadSectors: async () => {
    logger.info('[sectorAnalysisStore] loadSectors ??????)
    set({ loading: true, error: null })

    try {
      const result = await loadSectorAnalysis()
      if (result.success && result.data) {
        set({ sectors: result.data, loading: false })
        logger.info(`[sectorAnalysisStore] loadSectors ??????: ${result.data.length} ?????????`)
        // D-3: ??????????????????
        withBroadcast(EVENT_NAMES.SECTOR_ANALYSIS_CHANGED, { action: 'load', count: result.data.length })
      } else {
        const message = result.error ?? '????????????????????????'
        logger.error(`[sectorAnalysisStore] loadSectors ??????: ${message}`)
        set({ loading: false, error: message })
      }
    } catch (err) {
      const message = err instanceof Error ? err.message : '????????????????????????'
      logger.error(`[sectorAnalysisStore] loadSectors ??????: ${message}`)
      set({ loading: false, error: message })
    }
  },

  loadTrend: async (sectorId: string) => {
    logger.info(`[sectorAnalysisStore] loadTrend ?????? ${sectorId}`)
    set({ loading: true, error: null })

    try {
      const result = await loadSectorTrend(sectorId)
      if (result.success && result.data) {
        set({ trendData: result.data, loading: false })
        logger.info(`[sectorAnalysisStore] loadTrend ??????: ${result.data.length} ????????????`)
      } else {
        const message = result.error ?? '????????????????????????'
        logger.error(`[sectorAnalysisStore] loadTrend ??????: ${message}`)
        set({ loading: false, error: message })
      }
    } catch (err) {
      const message = err instanceof Error ? err.message : '????????????????????????'
      logger.error(`[sectorAnalysisStore] loadTrend ??????: ${message}`)
      set({ loading: false, error: message })
    }
  },

  clearError: () => {
    set({ error: null })
  },
}))

// ============================================================
// ????????????????????????
// ?????????????????????????????????????????????????????? getState() ????????????????????????State
// ============================================================
// export * from './sectorAnalysisStore.derived'
```

### 2.2 ????????????????????????
| ?????? | ???????????? | ?????? | ???????????? |
|------|----------|------|----------|
| `interface *State` | ????????? | ???????????? State ?????? | ?????? Action ?????? |
| `initialState` | ????????? | ?????????????????????| ???create ?????????????????????????????? |
| `withBroadcast()` | ????????? | ?????????????????????Tab ?????? | ?????????????????????Tab ???????????????|
| `logger.info/error` | ????????? | ???????????????????????? | ?????????????????????????????? |
| ???????????? `try/catch` | ????????? | ??????async Action ?????? | ???????????????????????? UI ???loading |
| `set({ loading: false })` | ????????? | ?????????????????????????????? loading | ?????????????????? loading |
| ???????????? `.derived.ts` | ???? ?????? | ???????????????memoizeByRef ?????? | ???State ???????????????????????????????????????|

---

## ?????? 3????????????????????????2 ?????????
?????????????????????????????????????????? `src/constants/store-channels.constants.ts` ???`EVENT_NAMES` ????????????

```typescript
export const EVENT_NAMES = {
  // ... ???????????? ...
  /** ???????????????????????????sectorAnalysisStore ?????????????????? */
  SECTOR_ANALYSIS_CHANGED: 'sector_analysis:changed',
} as const
```

> **????????????**???`{domain}_{subdomain}:changed`?????????????????????????????????
---

## ?????? 4????????????3 ?????????
### 4.1 ????????????
```bash
npx tsc --noEmit
```

### 4.2 ??????????????????????????????

???`src/store/sectorAnalysisStore.test.ts` ????????????

```typescript
import { describe, it, expect, vi } from 'vitest'
import { useSectorAnalysisStore } from './sectorAnalysisStore'

describe('sectorAnalysisStore', () => {
  it('should have initial state', () => {
    const state = useSectorAnalysisStore.getState()
    expect(state.sectors).toEqual([])
    expect(state.loading).toBe(false)
    expect(state.error).toBeNull()
  })

  it('should clear error', () => {
    useSectorAnalysisStore.setState({ error: 'test error' })
    useSectorAnalysisStore.getState().clearError()
    expect(useSectorAnalysisStore.getState().error).toBeNull()
  })
})
```

### 4.3 ????????????

```bash
npm run audit:layers
# ?????????sectorAnalysisStore.ts ?????????????????????```

---

## ?????? 5????????????????????????2 ?????????
```typescript
import { useSectorAnalysisStore } from '@/store/sectorAnalysisStore'

export function SectorAnalysisPage() {
  const { sectors, loading, error, loadSectors, clearError } = useSectorAnalysisStore()

  useEffect(() => {
    loadSectors()
  }, [loadSectors])

  if (loading) return <Loading />
  if (error) return <ErrorState message={error} onRetry={clearError} />

  return (
    <div>
      {sectors.map((sector) => (
        <SectorCard key={sector.sectorId} data={sector} />
      ))}
    </div>
  )
}
```

---

## ?????????????????????
### ???1???Store ???????????? dataLayer??????????????????

```typescript
// ????????????Store ???????????? dataLayer
import { dataLayer } from '@/data/dataLayer'
const data = await dataLayer.sectors.list() // ?????????Store ???????????? services/

// ?????????????????? Service ???????????????import { loadSectorAnalysis } from '@/services/analysis/sectorAnalysisService'
const result = await loadSectorAnalysis()
```

### ???2?????????????????? Tab ??????

```typescript
// ????????????set ????????????
set({ sectors: result.data })

// ????????????set ?????????set({ sectors: result.data })
withBroadcast(EVENT_NAMES.SECTOR_ANALYSIS_CHANGED, { action: 'load' })
```

### ???3?????? create ?????????????????????????????????????????????
```typescript
// ????????????????????????????????????????????????export const useStore = create<State>((set) => ({
  loadData: async () => { /* ... */ }, // ???????????????????????????}))

// ????????????????????? State ??????
interface State {
  loadData: () => Promise<void>
}
export const useStore = create<State>((set) => ({
  loadData: async () => { /* ... */ },
}))
```

### ???4?????????????????????State

```typescript
// ??????????????? State ??????????????????
interface State {
  sectors: SectorData[]
  topSector: SectorData | null // ???????????????????????? State
}

// ????????????????????????????????????export function getTopSector(): SectorData | null {
  const sectors = useSectorAnalysisStore.getState().sectors
  return sectors.length > 0 ? sectors.reduce((a, b) => a.score > b.score ? a : b) : null
}
```

---

## ????????????

| ?????? | ?????? | ?????? |
|------|------|------|
| ?????? Service | `./how-to-add-service.md` | Store ???????????????|
| ?????? Widget | `./how-to-add-widget.md` | Store ?????????????????? |
| Store ???????????? | `src/constants/store-channels.constants.ts` | ?????????????????? |
| withBroadcast ?????? | `src/lib/withBroadcast.ts` | ?????????????????? |
| ../../AGENTS.md ?????? | `../../AGENTS.md` | ???????????????????????????|

---

> **???????????????*?????????`docs/README.md` ???C ?????????????????????Store ????????????????????